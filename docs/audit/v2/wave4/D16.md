# D16 Audit: edit-channel, edit-profile, eid-cards, enable-tips, end-screen-editor

Auditor: Claude Opus 4.6 (1M context)
Date: 2026-03-30
Checklist: 46 checks (A1-A9, B10-B20, C21-C30, D31-D36, E37-E44, F45-F46)

---

### Screen: `edit-channel.tsx` (356 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | H | B19-Keyboard | 189 | No `KeyboardAvoidingView` wrapping the ScrollView. On iOS, the keyboard covers the description TextInput and save button. | Users cannot see what they type in the description field or reach the save button when keyboard is open on iOS. |
| 2 | M | A1-Light | 275,304,306,318,326 | Stylesheet hardcodes `colors.dark.bg`, `colors.dark.border`, `colors.dark.surface`, `colors.text.primary`, `colors.text.secondary` in static styles. While runtime JSX overrides some via `tc.*`, the static stylesheet values bleed through on unmounted states or fallback. | Light mode will show dark-themed base colors in edge cases (e.g., style composition order). |
| 3 | M | C23-Error | 153-158 | Error state shows "Back" button as the only action (`actionLabel={t('common.back')}`). No retry option. User must navigate away and come back to retry. | Poor error recovery; user cannot retry loading the channel data without leaving the screen. |
| 4 | M | C30-Concurrent | 126,135 | `handleSave` calls `updateMutation.mutate()` with no double-tap guard on the save button beyond `disabled={updateMutation.isPending}`. The `GradientButton` does not show a loading spinner while the mutation is pending -- only label text changes to "Saving...". If the mutation resolves quickly and the user taps fast, the mutation could be fired twice before `isPending` flips. | Potential duplicate API calls on rapid taps. |
| 5 | L | B13-DoubleTap | 197 | Avatar `Pressable` for `pickImage` has no debounce or `disabled` guard. Rapid taps could open multiple image pickers. | Multiple picker dialogs may stack on Android. |
| 6 | L | C28-BackState | 76-89 | `handleBack` uses `Alert.alert` for unsaved changes confirmation. This is acceptable per the code comment, but the error/loading early returns use `router.back()` directly without going through `handleBack`, so navigating back from error/loading state bypasses the dirty check (though not reachable if no data loaded). | Minor: inconsistent back handling across states. |
| 7 | L | A2-RTL | 228-230 | `CharCountRing` wrapper uses `paddingStart` (RTL-safe), but the `ringWrapBottom` uses `end` positioning -- this is correct for RTL. No issue. | N/A -- false alarm on review; RTL is handled. |
| 8 | I | D36-Micro | 260 | Save button has no loading indicator animation beyond text change. A spinner or pulse animation would improve perceived responsiveness. | Minor UX polish gap. |
| 9 | I | F46-Reachable | 33-42 | If `params.handle` is undefined and `channels` returns empty array, `handle` is `undefined`, the second query is disabled, and the screen shows loading skeleton forever (infinite loading). | Edge case: user with no channels sees perpetual skeleton. |

---

### Screen: `edit-profile.tsx` (815 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 10 | H | B19-Keyboard | 4,230 | `KeyboardAvoidingView` is imported but NEVER used in the JSX. The ScrollView has no keyboard avoidance. On iOS, lower form fields (location, profile links) are hidden behind the keyboard with no way to scroll to them. | Users cannot see or interact with lower form fields when keyboard is open on iOS. |
| 11 | H | E44-Haptic | -- | `useContextualHaptic` is never imported or used. Save, delete link, add link, toggle, and image picker actions have zero haptic feedback. | Screen feels unresponsive compared to other screens that use haptics consistently. |
| 12 | H | B11-Press | 241,282 | Cover photo `Pressable` (line 241) and avatar `Pressable` (line 282) have no `accessibilityRole`, no `accessibilityLabel`, and no visual press feedback (no `android_ripple`, no opacity change). | Inaccessible to screen readers; no visual indication these areas are tappable. |
| 13 | M | A1-Light | 593,700-713 | Stylesheet hardcodes `colors.dark.bg`, `colors.dark.border`, `colors.dark.bgCard`, `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` throughout static styles. While some JSX uses `tc.*`, many style objects like `input`, `label`, `usernameText`, `coverPlaceholderText`, `coverEditText`, `addLinkInput`, `addLinkSaveText` use hardcoded dark colors. | Broken light mode appearance across the entire form. |
| 14 | M | B14-Destructive | 455-458 | Private account toggle uses `accessibilityRole="button"` instead of `"switch"`. Missing `accessibilityState={{ checked: isPrivate }}` and `accessibilityLabel`. Screen readers announce this as a button, not a toggle. | Accessibility violation: toggle state not communicated to assistive technology. |
| 15 | M | C30-Concurrent | 224 | Save button calls `saveMutation.mutate()` directly. While `disabled={saveMutation.isPending || uploading}` prevents some double-taps, there is a race window between the tap and `isPending` becoming true (React state update is async). | Potential duplicate profile save + image upload on rapid double-tap. |
| 16 | M | B13-DoubleTap | 516 | Delete link button has `disabled={deleteLinkMutation.isPending && deleteLinkMutation.variables === link.id}` which only disables for the SPECIFIC link being deleted. A user can rapid-tap a different link's delete and trigger multiple simultaneous deletes. | Multiple concurrent delete mutations possible. |
| 17 | L | L-Import | 4 | `Switch` and `KeyboardAvoidingView` are imported from react-native but never used in JSX. Dead imports. | Code cleanliness; unused imports increase bundle marginally. |
| 18 | L | A5-Radius | 642 | `avatarEdit` badge uses `borderColor: colors.dark.bg` (line 642) hardcoded instead of `tc.bg`. When bg changes in light mode, the badge border won't match. | Visual artifact: badge border color mismatch in light mode. |
| 19 | L | C28-BackState | 217 | Back navigation via GlassHeader `leftAction` calls `router.back()` directly with no unsaved-changes guard. User can lose all edits by tapping back. | Data loss: users lose all unsaved profile edits on accidental back navigation. |
| 20 | I | D33-ProgressiveImage | 244 | `ProgressiveImage` is used for cover photo (good), but avatar uses `Avatar` component. If Avatar internally uses raw Image, this may miss progressive loading. | Minor: avatar may not have blur-up loading. |
| 21 | I | A8-Responsive | 603 | Cover photo height is hardcoded at 160px across all styles. On tablets or large screens, this looks disproportionately small. | Cover photo does not scale with screen size. |

---

### Screen: `eid-cards.tsx` (228 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 22 | H | E44-Haptic | -- | `useContextualHaptic` is never imported or used. Card selection, share actions, and bottom sheet interactions have zero haptic feedback. | Inconsistent UX: other screens provide haptic feedback on similar interactions. |
| 23 | M | A3-SafeArea | 82,156 | `SafeAreaView` has `edges={['top']}` only. Bottom safe area is not handled. ScrollView `contentContainerStyle` has no bottom padding. On devices with home indicator (iPhone X+), bottom content and share button in bottom sheet may be cut off. | Content may be hidden behind home indicator on modern iPhones. |
| 24 | M | A1-Light | 154,186-193,224 | Stylesheet hardcodes `colors.dark.bg` (line 154), card text uses `'#FFFFFF'` (line 186) and `'rgba(255,255,255,0.75)'` (line 193), `shareDirectText` uses `colors.gold` (line 224). Most colors are not theme-aware. | Completely broken in light mode -- white text on white background. |
| 25 | M | A8-Responsive | 165 | Card width is `'47%'` which doesn't account for the gap. With `gap: spacing.md` (12px) on a narrow screen, cards may overflow or have uneven spacing. Should use `flex: 1` with a fixed gap or calculate width dynamically. | Cards may overflow or layout unevenly on narrow screens (e.g., iPhone SE). |
| 26 | M | B11-Press | 94-98 | Card `Pressable` has no visual press feedback -- no `android_ripple`, no opacity/scale animation on press. Cards look static when tapped. | No visual feedback that cards are interactive; feels broken on Android. |
| 27 | L | C21-RealAPI | 89 | `BrandedRefreshControl` is present but `onRefresh` is a no-op (`() => {}`). Pull-to-refresh does nothing. Since this is a static list, the refresh control is misleading. | Users pull to refresh expecting something to happen; nothing changes. |
| 28 | L | B13-DoubleTap | 53-56 | `handleOccasionPress` sets state and opens bottom sheet with no debounce. Rapid taps could cause state flickering. | Minor: rapid taps may cause visual stutter. |
| 29 | I | D31-Entrance | 93 | `FadeInUp.delay(Math.min(index, 15) * 80)` caps at index 15, but there are only 6 items. The `Math.min(index, 15)` guard is unnecessary but harmless. | Dead code guard for a scenario that cannot occur. |
| 30 | I | A7-Typography | 109,110 | Arabic text `cardNameAr` uses `fonts.arabicBold` (good), but English text `cardName` has no explicit `fontFamily`, falling back to system default instead of `fonts.body`. | Inconsistent typography with rest of app. |

---

### Screen: `enable-tips.tsx` (681 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 31 | H | C22-Skeleton | 85-86,151 | `loading` state (line 85) is tracked but NEVER renders a skeleton or loading UI. The screen immediately renders the full form even while `fetchData` is still in-flight. `Skeleton` and `EmptyState` are imported (lines 17-18) but never used. | User sees the form before data loads; form state may be stale or default values shown as if real. |
| 32 | H | C23-Error | 86,112 | `error` state (line 86) is set on fetch failure (line 112) but NEVER rendered in the JSX. The error is silently swallowed -- user sees the form with default values and has no idea the fetch failed. | User unknowingly operates on default/stale data after a failed fetch. Silent error swallowing. |
| 33 | M | A3-SafeArea | 153,434 | `SafeAreaView` wraps the screen with default edges (all), but bottom spacing uses `<View style={{ height: spacing.xxl }} />` (line 434) which is a fixed 32px. On devices with large home indicators, content may still be partially obscured. | Inconsistent bottom padding across device types. |
| 34 | M | A1-Light | 444,474-497,508-510,554-557,569-578 | Extensive hardcoded dark-mode colors in stylesheet: `colors.dark.bg`, `colors.dark.border`, `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary`, `colors.warning`. Many text styles and borders are not theme-aware. | Broken light mode across the entire screen. |
| 35 | M | B19-Keyboard | 151 | No `KeyboardAvoidingView`. The custom amount input (line 271) and thank-you message TextInput (line 345) are in a ScrollView without keyboard avoidance. On iOS, these fields may be hidden behind the keyboard. | Users cannot see custom amount or thank-you message fields when keyboard is open on iOS. |
| 36 | M | C30-Concurrent | 422-429 | Save button `Pressable` has no `disabled` prop. `handleSave` (line 132) has no guard against double-tap or concurrent execution. User can tap save multiple times rapidly. | Multiple save operations could be triggered simultaneously. |
| 37 | M | A8-Responsive | 30 | `const { width } = Dimensions.get('window')` is called at module scope but the `width` variable is never used anywhere in the component or styles. Dead code that also doesn't respond to orientation changes. | Unused import/variable; if ever used, it would be stale after rotation. |
| 38 | L | B13-DoubleTap | 237-241 | Preset amount buttons have no debounce. Rapid tapping different amounts causes state thrashing. | Minor visual flicker on rapid taps. |
| 39 | L | A1-Light | 508-510 | Toggle thumb `backgroundColor: colors.text.primary` is hardcoded white-ish for dark mode. In light mode, the thumb would be dark text color -- visually wrong for a toggle thumb. | Toggle looks broken in light mode. |
| 40 | I | L-Import | 17-18 | `Skeleton` and `EmptyState` are imported but never used in JSX. Dead imports. | Code cleanliness. |
| 41 | I | B12-Disabled | 422 | Save button has no disabled state for when settings haven't changed. It's always tappable regardless of whether any changes were made. | User can "save" unchanged settings, triggering unnecessary toast feedback. |

---

### Screen: `end-screen-editor.tsx` (626 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 42 | C | C21-RealAPI | 104-110 | `setState` called inside `useQuery`'s `select` callback. `select` runs during render. Calling `setItems` and `setInitialized` inside `select` causes a state update during render, which triggers React's "Cannot update a component while rendering a different component" warning. This can cause infinite re-render loops in strict mode. | Potential infinite re-render loop or React warning in development; unpredictable behavior in production. |
| 43 | H | C23-Error | -- | `useQuery` result destructures only `isLoading` and `refetch`. `isError` is never captured or handled. If the API call fails, the screen shows nothing (loading skeleton disappears, empty state shows). No error message, no retry button for fetch failures. | Users see empty state with "Add" button when the API fails, with no indication of error or way to retry. |
| 44 | H | E44-Haptic | -- | `useContextualHaptic` is never imported or used. Add item, remove item, type selection, position selection, timing controls, and save have zero haptic feedback. | Screen feels unresponsive; inconsistent with app-wide haptic pattern. |
| 45 | M | A3-SafeArea | 167,462 | `SafeAreaView` has `edges={['top']}` only. `scrollContent` has `paddingBottom: 40` (line 464) which is hardcoded. On devices with home indicator, the save button at the bottom may be partially hidden. | Save button may be cut off on iPhone X+ devices. |
| 46 | M | A1-Light | 459,487-489,520-522,544-549,564-566,588,594-596 | Extensive hardcoded dark colors: `colors.dark.bg`, `colors.dark.border`, `colors.dark.bgCard`, `colors.dark.surface`, `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary`. Nearly every style uses hardcoded dark values. | Completely broken light mode. |
| 47 | M | B14-Destructive | 222-229 | Delete item button (trash icon) has no confirmation dialog. Tapping it immediately removes the end screen item with no way to undo. Items may have been carefully configured. | Accidental deletion of configured end screen items with no undo. |
| 48 | M | A8-Responsive | 462 | `paddingTop: 100` is hardcoded instead of using `insets.top + headerHeight`. Different devices have different safe area insets, so content may overlap with the header or have excessive gap. | Content may clip under header on devices with large notches, or have excessive padding on devices without notches. |
| 49 | M | C28-BackState | 170-171 | Back button calls `router.back()` directly with no unsaved-changes guard. User can lose all configured end screen items by accidentally navigating back. | Data loss: users lose all unsaved end screen configurations. |
| 50 | L | B13-DoubleTap | 131-133 | `handleAddItem` is debounced only by the `items.length >= MAX_ITEMS` guard. Rapid taps before state updates could add items beyond the limit due to stale closure. | Minor: could temporarily exceed MAX_ITEMS limit. |
| 51 | L | D34-LayoutStability | 61-71,208 | `createDraft` uses `Date.now().toString()` as `id`. If two items are added in the same millisecond (unlikely but possible on fast devices), they get duplicate keys, causing React key collision warnings and rendering bugs. | Potential React key collision on rapid additions. |
| 52 | L | A2-RTL | 479-484 | `infoText` does not set `textAlign` or `writingDirection`. For RTL languages, the info text layout may not flow correctly alongside the icon. | RTL text alignment may be incorrect for info card. |
| 53 | I | L-Import | 17,94 | `BottomSheet` and `BottomSheetItem` are imported and used for position picker (lines 433-450), but the position picker is also implemented inline with a grid of `Pressable` elements (lines 313-335). The bottom sheet version is dead code -- `positionSheetIndex` is never set to a non-null value by any user action. | Dead code: bottom sheet position picker exists but is unreachable. |

---

## Summary

| Screen | C | H | M | L | I | Total |
|--------|---|---|---|---|---|-------|
| edit-channel.tsx | 0 | 1 | 3 | 3 | 2 | 9 |
| edit-profile.tsx | 0 | 3 | 4 | 3 | 2 | 12 |
| eid-cards.tsx | 0 | 1 | 3 | 2 | 2 | 8 |
| enable-tips.tsx | 0 | 2 | 4 | 2 | 2 | 10 |
| end-screen-editor.tsx | 1 | 2 | 5 | 3 | 1 | 12 |
| **Total** | **1** | **9** | **19** | **13** | **9** | **51** |

**C:1 H:9 M:19 L:13 I:9 Total: 51**

### Top Issues by Severity

**CRASH (1):**
- `end-screen-editor.tsx`: `setState` inside `useQuery.select` callback causes render-during-render violation

**HIGH (9):**
- 3 screens missing `KeyboardAvoidingView` (edit-channel, edit-profile, enable-tips)
- 3 screens missing haptic feedback entirely (edit-profile, eid-cards, end-screen-editor)
- `enable-tips.tsx`: loading state tracked but never rendered; error state silently swallowed
- `end-screen-editor.tsx`: no error state handling for API failures
- `edit-profile.tsx`: cover/avatar Pressables inaccessible to screen readers

**Recurring patterns across all 5 screens:**
1. Hardcoded dark-mode colors in stylesheets (all 5 screens)
2. Missing or incomplete `KeyboardAvoidingView` for form screens (3/5)
3. Missing haptic feedback (3/5)
4. No unsaved-changes guard on back navigation (3/5)
5. No double-tap prevention on destructive/mutation actions (4/5)
