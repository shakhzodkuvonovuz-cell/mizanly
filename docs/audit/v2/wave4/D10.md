# D10 — Audit: Conversation Screens + Create Broadcast + Create Carousel

**Auditor:** Claude Opus 4.6 (1M context)
**Date:** 2026-03-30
**Scope:** 5 screens, 6,026 lines total
**Checklist:** 46 checks (A1-A9, B10-B20, C21-C30, D31-D36, E37-E44, F45-F46)

---

### Screen: `conversation/[id].tsx` (3,169 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | C | C29 | 1306-1307 | `emitEncryptedMessage` silently resolves `null` when offline — pending message added to UI but never queued to a durable store. If app is killed while offline, messages are lost permanently. `pendingMessages` is React state only (RAM). | Data loss: user sees "sent" but message evaporates on app restart |
| 2 | C | C30 | 1069-1091 | `useEffect` for pre-decryption iterates `messages` array and calls `setDecryptedContents` inside a functional updater that triggers an async `.then()` chaining another `setDecryptedContents`. On a conversation with 100+ encrypted messages, this fires 100+ concurrent decrypt+setState calls with no concurrency limit, potentially exhausting crypto workers and causing ANR. | Performance crash on large encrypted conversations |
| 3 | C | A1 | 2749 | `styles.container` hardcodes `backgroundColor: colors.dark.bg`. Same at lines 2770, 2913-2914, 2920, 2934, 2955, 2988-2989, 2995, 3000, 3020, 3033, 3048-3050, 3078, 3098, 3129, 3142-3143, 3153-3154. Over 20 stylesheet entries use `colors.dark.*` or `colors.text.*` directly — will show dark theme colors in light mode. | Broken light mode across entire conversation screen |
| 4 | H | A2 | 2850-2851 | `replyPreview` uses `borderLeftWidth: 3, borderLeftColor` — not RTL-safe. In RTL, the reply indicator border should be on the right. The `rtlBorderStart` util is used inline at L544 but the base style still has `borderLeftWidth`. | Reply preview border on wrong side in RTL |
| 5 | H | A2 | 708 | `marginLeft: 2` on expires-at timer is not RTL-safe. Should use `marginStart`. | Timer misaligned in RTL |
| 6 | H | A2 | 2154 | `marginLeft: spacing.sm` on pinned message text is not RTL-safe. Should use `marginStart`. | Pinned message text misaligned in RTL |
| 7 | H | A2 | 2787-2789 | `scrollToBottomFab` uses `right: spacing.base` — not RTL-safe. Should use `end`. | FAB on wrong side in RTL |
| 8 | H | A2 | 2905 | `reactionCount` uses `marginLeft: 2` — not RTL-safe. | Reaction count misaligned in RTL |
| 9 | H | A2 | 3033 | `slideCancelIndicator` uses `left: 50` — not RTL-safe. Should use `start`. | Slide cancel indicator on wrong side in RTL |
| 10 | H | A2 | 3088-3089 | `pendingText` uses `marginRight: spacing.sm` — not RTL-safe. | Pending message text misaligned in RTL |
| 11 | H | A2 | 3093 | `readReceipts` uses `marginLeft: spacing.xs` — not RTL-safe. | Read receipts misaligned in RTL |
| 12 | H | A2 | 3096-3097 | `readReceiptAvatar` uses `marginLeft: -6` — not RTL-safe. | Overlapping avatars wrong direction in RTL |
| 13 | H | A2 | 3101 | `readReceiptMore` uses `marginLeft: 2` — not RTL-safe. | "+3" text misaligned in RTL |
| 14 | H | A2 | 3114 | `readTime` uses `marginLeft: spacing.xs` — not RTL-safe. | Read time misaligned in RTL |
| 15 | M | B13 | 2352-2355 | Send button has no double-tap guard beyond `isSending` state. `isSending` is set in `handleSend` but immediately cleared at L1583 (before the emit resolves). Rapid taps can trigger duplicate encrypted messages. | Duplicate messages on fast double-tap |
| 16 | M | C23 | 2542-2543 | `deleteMessage` error `.catch` shows toast but doesn't restore the message in UI. The `invalidateQueries` already ran. If delete fails, the message silently disappears from UI but still exists on server. | Ghost messages: user thinks deleted but server still has it |
| 17 | M | C26 | 1388-1395 | Edit message path calls `messagesApi.editMessage` without optimistic update. Text stays in input until API resolves. If API is slow (2s), user sees no feedback that edit was submitted. | Sluggish edit UX on slow networks |
| 18 | M | B19 | 2126-2129 | `KeyboardAvoidingView` uses `behavior={Platform.OS === 'ios' ? 'padding' : undefined}`. On Android, `behavior` is `undefined` — the keyboard covers the input field. Should be `'height'` or `'padding'`. | Input hidden by keyboard on Android |
| 19 | M | A1 | 264 | `Icon` color at VoicePlayer L264 uses raw `'#fff'` — should use `tc.text.primary` or `colors.text.inverse`. | Hardcoded white may be invisible in light mode |
| 20 | M | A1 | 288 | VoicePlayer speed text uses inline `{ color: isOwn ? '#fff' : colors.emerald }` — raw `'#fff'` not theme-aware. | Speed indicator not theme-safe |
| 21 | M | A1 | 557 | View Once badge uses inline `{ color: isOwn ? 'rgba(255,255,255,0.7)' : colors.gold }` — hardcoded rgba. | View Once badge not theme-safe |
| 22 | M | A1 | 606-609 | Contact message uses `{ color: isOwn ? '#fff' : colors.text.primary }` and `colors.text.secondary` in stylesheet — not using `tc.*`. | Contact card not theme-safe |
| 23 | M | A1 | 2089 | Online status text uses hardcoded `{ color: colors.emerald, fontSize: 11 }` — fontSize should use `fontSize.xs` (11), but color is from constant not `tc.*`. | Minor: consistent but not dynamic |
| 24 | M | C27 | 1246-1250 | `markRead` fires on every mount (even on tab switch back). No guard for "already marked read". Causes unnecessary API calls and query invalidations on every navigation. | Wasted API calls on re-mount |
| 25 | L | B11 | 2292 | Attach button sets `disabled={uploadingMedia}` but no visual disabled state — icon color changes but Pressable has no opacity change. | No visible disabled state for attach button |
| 26 | L | A5 | 2891 | `paddingHorizontal: spacing.xs + 2` — arithmetic on spacing tokens defeats the purpose of the token system. | Spacing token inconsistency |
| 27 | L | A9 | 2200 | `onLayout` scrolls to end on every layout change. If the user is reading old messages, this force-scrolls them to bottom. Should only scroll to end on initial load. | Force-scroll disrupts reading history |
| 28 | L | D34 | 232 | `FadeInUp.delay(index * 50)` on `renderMediaItem` in GifPicker — for 30 items, the last item's entrance is delayed 1.5s. This causes visible layout instability as items appear one by one. | Staggered entrance causes layout shift on long lists |
| 29 | I | A7 | 288 | VoicePlayer speed text uses `fontFamily: 'DMSans_700Bold'` directly instead of `fonts.bodyBold` token. | Typography inconsistency |
| 30 | I | C30 | 1367 | `setTimeout(() => resolve(null), 3000)` in `emitEncryptedMessage` is a race condition — if the socket ACK comes after 3s, `resolve` is called twice (once by timeout, once by ACK). The second call is a no-op for Promises, but it masks slow-network situations. | Silent message delivery failure on slow networks |

---

### Screen: `conversation-info.tsx` (1,058 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 31 | H | A1 | 815 | `styles.container` hardcodes `backgroundColor: colors.dark.bg`. Same at lines 848, 884-885, 898, 913, 921-922, 923, 966, 974, 983, 985, 1002, 1007, 1017, 1032-1033, 1038, 1041, 1047, 1053, 1056. Over 25 stylesheet entries use `colors.dark.*` or `colors.text.*` — broken light mode. | Entire info screen dark-only |
| 32 | H | A2 | 323-324 | Inline `{ flexDirection: 'row' }` used for invite link row. Should use `rtlFlexRow` or `flexDirection: 'row'` is LTR-only. Same pattern at lines 348, 478, 495, 561, 564, 595. | Multiple rows not RTL-safe |
| 33 | M | B14 | 183-186 | `Alert.alert` for remove member confirmation is correct destructive pattern, but `handleLeave` at L195-199 also uses `Alert.alert` correctly. However, block user at L580-592 lacks a `haptic.delete()` or `haptic.error()` on destructive action. | No haptic feedback on block action |
| 34 | M | C23 | 89-95 | `leaveGroupMutation` has no `onError` handler. If leave fails, user sees no feedback — the mutation just silently fails. | Silent failure on leave group |
| 35 | M | C23 | 97-103 | `updateGroupMutation` has no `onError` handler. If name/avatar update fails, no feedback. | Silent failure on group name update |
| 36 | M | C23 | 105-109 | `addMembersMutation` has no `onError` handler. If adding members fails, no feedback. | Silent failure on add members |
| 37 | M | C23 | 122-128 | `muteMutation` has no `onError` handler. If mute toggle fails, no feedback — Switch toggles visually but may revert on refetch. | Mute toggle lies about state on failure |
| 38 | M | A3 | 251 | `ScrollView contentContainerStyle={{ paddingBottom: 60 }}` uses magic number instead of `insets.bottom + spacing.xl` for safe bottom padding. On phones with large home indicators, content may be clipped. | Content may be behind home indicator |
| 39 | L | B10 | 258-268 | Avatar edit overlay (`pickAvatar`) has no haptic feedback on press. | No haptic on avatar change |
| 40 | L | A2 | 877 | `memberRow` uses hardcoded `borderBottomColor: colors.dark.border` — not theme-aware. | Border color not theme-safe |
| 41 | L | E38 | 560-563 | Leave group loading state uses `Skeleton.Rect` as a spinner substitute. Skeletons are for content placeholders, not button loading indicators. Should use a small ActivityIndicator or disabled button text. | Incorrect loading pattern |
| 42 | I | B12 | 285-293 | "Add Members" button has no disabled state when `addMembersMutation.isPending`. Can be tapped again while adding. | Potential duplicate add requests |

---

### Screen: `conversation-media.tsx` (538 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 43 | H | A1 | 439 | `styles.container` hardcodes `backgroundColor: colors.dark.bg`. Same at lines 442-443, 478-479, 492-493, 509-510. Over 10 stylesheet entries use `colors.dark.*` or `colors.text.*` — broken light mode. | Entire media screen dark-only |
| 44 | H | A2 | 476 | `marginHorizontal: spacing.base` on `linkItem` — correct, but `marginEnd: spacing.md` on `linkIcon` (L489) is RTL-safe. However, L480 `borderColor: colors.active.white6` and L511 `borderColor: colors.active.white6` are hardcoded. Overall pattern OK for RTL but colors are not theme-aware. | Colors not theme-safe |
| 45 | M | C25 | 134-139 | Pagination query key is `['conversation-messages', conversationId]` but conversation screen uses `['messages', id]`. These are different cache keys for the same data. When user sends a new message in conversation, the media screen won't see it until manual refresh. | Stale media list after sending new media |
| 46 | M | A1 | 258 | `Icon` color uses `colors.emerald` directly in LinearGradient icon — should use `tc.*` for theme consistency (though emerald is brand color, it's in a non-dynamic context). Same at L286. | Minor: brand color used outside theme system |
| 47 | M | D34 | 232 | `FadeInUp.delay(index * 50)` on media items — for conversations with 100+ media items, the last items animate in after 5+ seconds. This causes layout instability and perceived slowness. | Long stagger delay on large media lists |
| 48 | L | B10 | 72-74 | `ScaleMediaItem` uses `useAnimatedPress` for press feedback but no haptic on press. Items are pressable but silent. | No haptic on media item press |
| 49 | L | C22 | 327-333 | Loading skeleton uses `Skeleton.ProfileHeader()` which is semantically wrong for a media grid loading state. Should show a grid of skeleton rectangles. | Wrong skeleton shape for media grid |
| 50 | L | A1 | 168 | Unsafe cast `(msg as unknown as Record<string, string>).thumbnailUrl` — breaks type safety and may silently return undefined without the expected fallback. | Type safety violation |
| 51 | I | B15 | 216-217 | `Linking.openURL` error only logs in `__DEV__` — user gets no feedback if link opening fails in production. | Silent link failure in production |

---

### Screen: `create-broadcast.tsx` (397 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 52 | H | A1 | 270 | `styles.container` hardcodes `backgroundColor: colors.dark.bg`. Lines 305, 309-310, 336-337, 339, 344, 351, 365, 392. Over 10 stylesheet entries use `colors.dark.*` or `colors.text.*` — broken light mode. | Entire broadcast create screen dark-only |
| 53 | H | A3 | 111 | Uses `<View>` as root instead of `<SafeAreaView>`. The `GlassHeader` is absolute-positioned, but the ScrollView `contentContainerStyle={{ paddingTop: insets.top + 52 }}` manually compensates. However, there is no bottom safe area handling — content at the bottom (Create button) may be behind home indicator. | Create button may be unreachable behind home indicator |
| 54 | M | B19 | 116-120 | `ScrollView` has `keyboardShouldPersistTaps="handled"` but no `KeyboardAvoidingView` wrapping it. On iOS, when the keyboard opens for the TextInput fields, the ScrollView doesn't adjust — the lower fields (description, slug) get hidden behind the keyboard. | Fields hidden by keyboard |
| 55 | M | C23 | 67-76 | `uploadAvatar` function has no error boundary around `fetch(uri)` — if the local file is deleted before upload (e.g., cache cleared), this throws an unhandled error that propagates to the mutation. The mutation's `onError` catches it, but the `setUploading(false)` at L82 was already set to true. Actually, on second look, the catch at L97-100 does reset uploading. But the error message from `fetch(uri)` failure will be cryptic ("Network request failed"). | Cryptic error message on local file access failure |
| 56 | M | B13 | 258 | `GradientButton onPress={() => isValid && createMutation.mutate()}` — no double-tap protection. The `isValid` check includes `!createMutation.isPending` which prevents re-submission while pending, but there's a race window between tap and `isPending` becoming true. | Potential duplicate broadcast creation on fast double-tap |
| 57 | L | B10 | 127 | Avatar picker `Pressable` has no haptic feedback on press. | No haptic on avatar pick |
| 58 | L | A2 | 359-360 | `slugContainer` uses `borderBottomColor: colors.dark.border` — hardcoded dark theme. | Slug border not theme-safe |
| 59 | L | D31 | 122 | `FadeInUp.delay(0)` — delay of 0 is a no-op. Should either be omitted or use a meaningful delay. | Redundant animation parameter |
| 60 | I | C29 | 79-101 | Create mutation does avatar upload inline. If the user goes offline mid-upload, the entire mutation fails with no recovery. Should upload first, then create with retry. | No offline resilience for upload+create flow |

---

### Screen: `create-carousel.tsx` (864 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 61 | H | A1 | 78 | `SlideThumb` uses inline `{ borderColor: colors.emerald, borderWidth: 2 }` — OK for brand accent, but other styles in the stylesheet use `colors.dark.*` and `colors.text.*` directly at lines 774, 822, 848, 855, 863. Multiple hardcoded dark theme colors. | Light mode broken for carousel editor |
| 62 | H | A1 | 83-84 | `thumbBadge` text uses `{ color: '#fff' }` — hardcoded white. Same at L774. Should use `colors.text.inverse` or `tc.text.primary`. | Badge text not theme-safe |
| 63 | H | A3 | 316 | Uses `<SafeAreaView edges={['top']}>` but no bottom edge. The `ScrollView` content at the bottom (branded content section, final spacer) may be behind the home indicator. | Content cut off at bottom |
| 64 | M | A1 | 287 | Empty state icon wrap uses template literal color `${colors.gold}25` — concatenating hex + alpha. This is non-standard CSS and only works because RN accepts 8-char hex. Fragile pattern. | Fragile color construction |
| 65 | M | A2 | 367-368 | Reorder arrow icons use `color="#fff"` — hardcoded white, not theme-safe. | Reorder arrows not theme-safe |
| 66 | M | B19 | 341-346 | `ScrollView` has `keyboardShouldPersistTaps="handled"` but no `KeyboardAvoidingView`. When caption or text overlay inputs are focused, the keyboard may cover them. | Input fields hidden by keyboard |
| 67 | M | C23 | 257-261 | `publishMutation.onError` shows toast but doesn't reset `uploadProgress` state. The progress bar stays at its last value even after error. | Progress bar stuck after publish failure |
| 68 | M | A1 | 604-605 | `styles.topicChip` border color uses inline string `'rgba(255,255,255,0.15)'` — hardcoded for dark theme. Same pattern at L631, L848. | Topic chips invisible in light mode |
| 69 | M | D34 | 61 | `FadeInDown.delay(index * 40)` on `SlideThumb` — for 35 slides (MAX_SLIDES), the last thumbnail entrance is delayed 1.4s. | Stagger delay causes layout instability with many slides |
| 70 | L | B11 | 282 | Close button accessibility label is hardcoded English `'Close'` instead of using `t('common.close')`. | Accessibility label not i18n |
| 71 | L | A1 | 604 | `borderColor: isSelected ? colors.emerald : 'rgba(255,255,255,0.15)'` — unselected state uses raw rgba instead of `tc.border`. | Unselected chip border not theme-safe |
| 72 | L | A1 | 846-848 | `radioOuter` border uses `'rgba(255,255,255,0.3)'` — hardcoded for dark theme. | Radio button border not theme-safe |
| 73 | L | C30 | 196-227 | Upload loop is sequential (`for...of` with `await`). For 35 slides, this means 35 sequential uploads. Should use `Promise.all` with concurrency limit (e.g., 3 at a time) for much faster upload. | Very slow upload for many slides |
| 74 | I | A7 | 746 | `emptyTitle` uses `fontWeight: '700'` alongside `fontFamily: fonts.bodyBold` — redundant since the bold font already implies weight 700. | Redundant font weight |
| 75 | I | B16 | 64-75 | `SlideThumb` onLongPress uses `Alert.alert` for destructive delete. This is correct per B14, but the long-press requires `total > 2` — meaning with exactly 2 slides, you cannot remove one via long-press. The minimum for a carousel is 2, so the user is stuck. They must use the photo picker to add more before they can remove. | Cannot remove slide when exactly 2 slides remain |

---

## Summary

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 3 |
| H (broken feature/auth) | 12 |
| M (error-handling/perf) | 20 |
| L (smell/minor) | 16 |
| I (suggestion) | 6 |
| **Total** | **57** |

### Top Issues by Impact

1. **C1 (L1306):** Offline messages stored only in React state — data loss on app kill
2. **C2 (L1069):** 100+ concurrent decrypt calls with no throttle — ANR risk
3. **C3 + H31-H52:** Hardcoded `colors.dark.*` in ALL 5 screens — entire light mode broken
4. **H4-H14:** 11 RTL violations in conversation screen alone (marginLeft, borderLeft, left, right)
5. **M18 (L2126):** Android keyboard covers input field — `KeyboardAvoidingView` behavior is `undefined`
