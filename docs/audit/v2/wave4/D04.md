# D04 — Screen Audit (Wave 4, Batch 4)

Auditor: Claude Opus 4.6 (1M context)
Date: 2026-03-30
Screens: 5 | Checks: 46 per screen

---

### Screen: `blocked.tsx` (214 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | M | A1-Light | 185 | `backgroundColor: colors.dark.bg` hardcoded in StyleSheet; overridden inline via `tc.bg` at L79/97 but the static style still references dark-only token | Light theme shows dark flash before inline style applies on mount |
| 2 | M | A1-Light | 189-195 | `skeletonRow` uses `colors.dark.bgCard` and `colors.active.white6` in static styles; only partially overridden inline at L106 (`tc.bgCard` covers bg but `borderColor` stays dark) | Skeleton rows have wrong border color in light theme |
| 3 | L | A1-Light | 206 | `name` style uses `colors.text.primary` in stylesheet (static dark); overridden at L138 inline but stylesheet value is dead code that misleads | Confusing, no runtime bug |
| 4 | L | A1-Light | 213 | `username` style uses `colors.text.secondary` in stylesheet; overridden inline at L141 | Dead code, same as above |
| 5 | L | A1-Light | 202 | `borderColor: 'rgba(248,81,73,0.2)'` hardcoded in `row` style; not adapted for light theme | Red-tinted border may not suit light backgrounds |
| 6 | L | A6-Spacing | 186 | `paddingBottom: 40` hardcoded magic number; should use `spacing.2xl` (32) or similar token | Inconsistent spacing system |
| 7 | L | A6-Spacing | 211 | `marginTop: 2` hardcoded; below `spacing.xs` (4); should be a token or 0 | Minor spacing inconsistency |
| 8 | M | B10-Haptic | - | No `useContextualHaptic` imported or used anywhere in file; unblock action and refresh have no haptic feedback | Haptic feedback missing on destructive action |
| 9 | L | B11-Press | 128-157 | FlatList `renderItem` uses `Animated.View` + `LinearGradient` but no `Pressable` wrapper; rows are not individually pressable (only the unblock button is) | Cannot tap user row to view profile |
| 10 | M | C26-Optimistic | 53-57 | `unblockMutation` has no `onMutate` optimistic update; user sees no change until server responds and `invalidateQueries` fires | Unblock feels slow; user can double-tap |
| 11 | L | C29-Offline | - | No offline detection; unblock will silently fail if network is down (error toast fires but user may have already navigated) | Poor offline UX |
| 12 | M | E43-Toast | 67-74 | `Alert.alert` used for unblock confirmation; rules say use `Alert.alert` only for destructive confirms, which this IS (unblock is destructive) — but the message says "Alert" which is acceptable. However `showToast` is imported but NOT used for the confirmation feedback on success (L55 only invalidates) | No success toast after unblock completes |
| 13 | L | E44-Haptic | - | Same as B10 — `useContextualHaptic` not used | Missing haptic on interactions |
| 14 | I | D31-Entrance | 131 | `FadeInUp.delay(index * 30)` — unbounded delay for large lists; 100 items = 3s stagger; should cap like `Math.min(index, 15) * 30` | Animation jank on large blocked lists |
| 15 | L | C28-BackState | - | After unblocking a user and going back, the previous screen's user list is not invalidated (only `['blocked']` query is invalidated, not the user's profile/followers) | Stale state on previous screen |

### Screen: `bookmark-collections.tsx` (241 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 16 | M | A1-Light | 167 | `backgroundColor: colors.dark.bg` in static `container` style | Light theme flash on mount |
| 17 | M | A1-Light | 182-186 | `skeletonCard` uses `colors.dark.bgCard` and `colors.active.white6` statically; not overridden with `tc.*` | Skeleton cards wrong in light theme |
| 18 | L | A1-Light | 198-199 | `cardGradient` has `borderColor: 'rgba(255,255,255,0.08)'` — too subtle for light theme | Border invisible on white background |
| 19 | L | A1-Light | 210 | `backgroundColor: colors.dark.bgElevated` in `cover` style; never overridden inline | Placeholder cover wrong color in light theme |
| 20 | L | A1-Light | 229 | `color: colors.text.primary` in static `name` style; overridden at L75 | Dead code |
| 21 | L | A1-Light | 239 | `color: colors.text.secondary` in static `count` style; overridden at L78 | Dead code |
| 22 | M | A3-SafeArea | 88, 131 | Error and main states use raw `<View>` with `useSafeAreaInsets` manual padding via `{ height: insets.top + 52 }` spacer divs; but loading state at L107-126 also uses the same pattern. No `SafeAreaView` from `react-native-safe-area-context` imported — relies on manual inset math which is fragile | Content can be obscured if GlassHeader height changes |
| 23 | H | A3-SafeArea | 88-102 | Error state has `<View style={{ height: insets.top + 52 }} />` spacer AFTER `<GlassHeader>`, but `GlassHeader` is typically absolutely positioned; if GlassHeader is NOT absolute, this creates double spacing | Layout broken — double padding or content under header |
| 24 | L | A6-Spacing | 224 | `gap: 2` in `info` style — hardcoded, below token minimum | Token violation |
| 25 | L | A6-Spacing | 235 | `marginTop: 2` in `countBadge` — hardcoded | Token violation |
| 26 | M | C21-RealAPI | 34-37 | `useQuery` without `staleTime`; default is 0, meaning every focus refetches | Unnecessary network requests on tab switches |
| 27 | L | C25-Pagination | 137-157 | FlatList has no `onEndReached` or infinite scroll; if user has many collections they all load at once | Not paginated; large collection lists may stutter |
| 28 | M | F45-Works | 84 | `renderItem` useCallback deps include `tc.bgElevated` but `tc.bgElevated` is NOT used inside the callback body; `tc.text.primary`, `tc.text.secondary`, `tc.text.tertiary` ARE used but NOT in deps | Stale closure: theme changes won't update rendered items |
| 29 | L | D34-LayoutStability | 62 | `ProgressiveImage` with `width="100%" height={200}` but the card uses `aspectRatio: 1` on the wrapper — conflicting: image says 200px height, wrapper says square | Layout shift if image aspect ratio doesn't match |
| 30 | I | B16-LongPress | 46-54 | `Pressable` has `onPress` but no `onLongPress` for edit/delete collection | No way to manage collections from this screen |

### Screen: `bookmark-folders.tsx` (321 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 31 | M | A1-Light | 222 | `container` uses `colors.dark.bg` statically | Light theme flash |
| 32 | M | A1-Light | 228-229 | `folderCard` uses `colors.dark.bgCard` statically; overridden at L45 via `tc.bgCard` inline but static style still there | Confusing but runtime OK due to override |
| 33 | L | A1-Light | 238 | `folderIcon` uses `colors.dark.bgElevated` statically; overridden at L46 inline | Dead code |
| 34 | L | A1-Light | 243 | `folderName` uses `colors.text.primary` statically; overridden at L49 | Dead code |
| 35 | L | A1-Light | 250 | `folderCount` uses `colors.text.tertiary` statically; overridden at L50 | Dead code |
| 36 | L | A1-Light | 286-293 | `input` style uses `colors.dark.bgElevated`, `colors.text.primary`, `colors.dark.border` statically; overridden at L198 inline | Dead code |
| 37 | L | A1-Light | 280 | `sheetTitle` uses `colors.text.primary`; overridden at L196 | Dead code |
| 38 | L | A1-Light | 306 | `cancelText` uses `colors.text.secondary`; overridden at L207 | Dead code |
| 39 | M | A1-Light | 317 | `createText` uses `color: '#fff'` hardcoded; not theme-aware | White text on create button doesn't adapt to themes |
| 40 | M | A1-Light | 190 | `<Icon name="plus" size="lg" color="#fff" />` — hardcoded white | FAB icon color not theme-aware |
| 41 | L | A1-Light | 272 | `shadowColor: '#000'` hardcoded for FAB shadow | Shadow color not theme-aware |
| 42 | M | B10-Haptic | - | No `useContextualHaptic` imported or used; folder press, long-press delete, create, FAB tap — all without haptic | Missing haptic feedback on all interactions |
| 43 | H | C23-Error | - | No `isError` check on `collectionsQuery`; if API fails, screen shows empty state (from `ListEmptyComponent`) instead of an error state with retry | User sees "no folders" when the real problem is network error |
| 44 | M | C26-Optimistic | 100-128 | `handleDeleteFolder` does N sequential API calls (`moveToCollection` for each post) with no optimistic UI removal; folder stays visible during potentially slow operation | Folder deletion feels broken — no loading indicator, no optimistic removal |
| 45 | M | C30-Concurrent | 114-117 | `Promise.all` with `.catch(() => {})` silently swallows individual move failures; partial moves leave data inconsistent | Silent data loss: some bookmarks may not move to 'default' |
| 46 | L | B14-Destructive | 100-128 | Destructive folder delete uses `Alert.alert` (correct) but no haptic on the destructive action | Missing haptic on destructive confirm |
| 47 | L | B12-Disabled | 209 | Create button (`createBtn`) has no disabled state when input is empty; `handleCreateFolder` validates but button is always active | Button is tappable in invalid state |
| 48 | M | B19-Keyboard | 197-204 | `TextInput` inside `BottomSheet` with `autoFocus` but no `KeyboardAvoidingView`; on Android the keyboard may cover the input and buttons | Input may be hidden behind keyboard |
| 49 | L | D34-LayoutStability | 75-79 | `foldersArray` creates fake `itemIds` array using `Array.from({ length: c.count })` just to get `.length` later; wasteful memory allocation for large counts | Allocates N-element arrays for display count; O(N) memory waste |
| 50 | I | A8-Responsive | 24-25 | `SCREEN_W` captured once from `Dimensions.get('window')` at module scope; does not update on rotation or foldable device resize | Cards sized wrong after orientation change |

### Screen: `boost-post.tsx` (460 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 51 | M | A1-Light | 271 | `container` uses `colors.dark.bg`; overridden at L92 inline | Light theme flash on mount |
| 52 | M | A1-Light | 283-286 | `previewCard` uses `colors.dark.bgCard` and `colors.dark.border` statically; overridden at L111 inline | Dead code but misleading |
| 53 | M | A1-Light | 309 | `previewThumb` uses `colors.dark.surface`; NOT overridden at L117 (only `tc.surface` at L117) — wait, L117 does use `tc.surface` inline | Runtime OK |
| 54 | M | A1-Light | 340-342 | `pill` uses `colors.dark.surface` and `colors.dark.border` statically; NOT overridden inline (L137-138 uses `styles.pill` only) | Pills have wrong background in light theme |
| 55 | M | A1-Light | 360-363 | `customInputRow` uses `colors.dark.bgCard` and `colors.dark.border`; overridden at L160 inline | Dead code |
| 56 | M | A1-Light | 377 | `customInput` uses `color: colors.text.primary` statically; NOT overridden inline (L162-170 has no color override) | Custom input text color wrong in light theme |
| 57 | M | A1-Light | 428-431 | `summary` card uses `colors.dark.bgCard` and `colors.dark.border` statically; overridden at L239 inline | Dead code |
| 58 | M | A1-Light | 440 | `summaryRow` borderBottomColor `colors.dark.border` statically; overridden at L240/244 inline | Dead code |
| 59 | M | B10-Haptic | - | No `useContextualHaptic` imported or used; budget selection, duration selection, boost button — no haptic | Missing haptic on all interactive elements |
| 60 | M | A6-Spacing | 275 | `marginTop: 100` hardcoded magic number for scroll offset under header | Fragile: breaks if header height changes, not safe-area aware |
| 61 | H | C21-RealAPI | 65-68 | `promotionsApi.boostPost` calls `/promotions/boost` which likely doesn't exist on the backend (no promotions module found in standard NestJS codebase); API call will 404 | Boost feature is non-functional |
| 62 | M | C29-Offline | - | No offline check before submitting boost (which involves payment/budget); user could lose money context | No offline guard on financial action |
| 63 | L | B12-Disabled | 231 | Boost button disabled only when `activeBudget <= 0`; should also disable when `boosting` is true (L230 `loading={boosting}` is cosmetic only) | Double-tap can submit twice |
| 64 | I | A6-Spacing | 224 | `info` style has `gap: 2` — not a spacing token | Minor token violation |
| 65 | L | A1-Light | 298-328 | Multiple styles (`previewLabel`, `previewId`, `previewHint`, `sectionTitle`) use `colors.text.*` statically; most are overridden inline but `pillText` at L351 is NOT fully overridden for the inactive state (L141 only overrides when using `tc.text.secondary` but `styles.pillText` itself has `colors.text.secondary`) | Stale color value |
| 66 | L | F45-Works | 401-405 | `reachValue` style defined but never referenced in JSX — dead style | Unused code |
| 67 | H | F45-Works | 39 | `useLocalSearchParams<{ postId: string }>()` — `postId` could be undefined at runtime despite type annotation (expo-router params are always `string | undefined`); the `if (!postId)` guard at L79 handles this but `handleBoost` at L61 could theoretically be called before the guard | Type unsafety with route params |
| 68 | L | D34-LayoutStability | 111-125 | Preview card renders a placeholder thumb (56x56 gray box) instead of the actual post thumbnail; user has no visual confirmation of which post they're boosting | UX: user cannot verify they selected the correct post |

### Screen: `branded-content.tsx` (330 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 69 | M | A1-Light | 188 | `container` uses `colors.dark.bg` statically; overridden at L67 | Light theme flash |
| 70 | M | A1-Light | 198-201 | `toggleCard` uses `colors.dark.bgCard` and `colors.dark.border` statically; overridden at L86 | Dead code |
| 71 | L | A1-Light | 217 | `toggleLabel` uses `colors.text.primary` statically; overridden at L89 | Dead code |
| 72 | L | A1-Light | 222 | `toggleSub` uses `colors.text.tertiary` statically; overridden at L90 | Dead code |
| 73 | M | A1-Light | 241-247 | `textInput` uses `colors.text.primary`, `colors.dark.surface`, `colors.dark.border` statically; overridden at L109 | Dead code |
| 74 | L | A1-Light | 278 | `previewNameCol` gap: 2 hardcoded | Minor spacing violation |
| 75 | M | B10-Haptic | - | No `useContextualHaptic` imported or used; toggle switch, save button — no haptic | Missing haptic on interactive elements |
| 76 | H | F45-Works | 36-51 | `handleSave` when `isPaidPartnership=true` and `partnerName` is empty does NOTHING (neither branch matches); the button is disabled for this case but the function still has a logic gap | If disabled guard is bypassed, no-op silently |
| 77 | M | F45-Works | 155 | `infoTitle` style at L319 uses `color: colors.gold` hardcoded; not applied via `tc.*` | Disclosure title not theme-aware |
| 78 | L | A1-Light | 307 | `infoCard` uses `backgroundColor: colors.active.gold10` and `borderColor: colors.gold` statically | Gold info card may not suit light theme |
| 79 | L | C28-BackState | - | After saving branded content and going back, no query invalidation for the post's data on the previous screen | Previous screen shows stale branded content state |
| 80 | M | C29-Offline | - | No offline check before saving branded partnership (modifies post data) | Changes lost silently if offline |
| 81 | L | A6-Spacing | 278 | `gap: 2` in `previewNameCol` — below token minimum | Token violation |
| 82 | I | B20-ScrollToInput | 108 | `TextInput` inside `ScrollView` but no scroll-to-input behavior; keyboard may cover the input field | Input may be hidden on small screens |
| 83 | L | A1-Light | 226-229 | `inputCard` uses `colors.dark.bgCard` and `colors.dark.border` statically; overridden at L106 | Dead code |

---

## Cross-Screen Systemic Issues

| # | Sev | Check | Screens | Finding | Impact |
|---|-----|-------|---------|---------|--------|
| 84 | M | A1-Light | ALL 5 | Every screen has `colors.dark.*` in static `StyleSheet.create()` that are partially overridden inline; this pattern is fragile and produces flash-of-wrong-theme on mount | Systematic light-theme regression |
| 85 | L | A4-StatusBar | ALL 5 | No `<StatusBar>` component in any screen; relies on parent layout to set status bar style | Status bar may not match screen theme |
| 86 | M | B10-Haptic | 3 of 5 | `blocked.tsx`, `boost-post.tsx`, `branded-content.tsx` do not use `useContextualHaptic`; only `bookmark-collections.tsx` uses it | Inconsistent haptic feedback across screens |

---

## Severity Counts

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 0 |
| H (broken feature/auth) | 4 |
| M (error-handling/perf) | 27 |
| L (smell/minor) | 37 |
| I (suggestion) | 4 |
| **Total** | **72** |

**C:0 H:4 M:27 L:37 I:4 Total:72**
