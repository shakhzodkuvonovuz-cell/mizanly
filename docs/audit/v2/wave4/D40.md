# D40 Audit: wind-down, xp-history, zakat-calculator, _layout

Auditor: Claude Opus 4.6 (1M context)
Date: 2026-03-30
Scope: 4 screens in `apps/mobile/app/(screens)/`

---

### Screen: `_layout.tsx` (15 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | L | A4-StatusBar | - | No `<StatusBar>` component rendered in this layout. Relies on root `_layout.tsx` which does set it, but screens in this stack group that use custom dark gradient backgrounds (e.g., `wind-down.tsx`) may have StatusBar content color mismatched against their bespoke gradient. | StatusBar text could be invisible on light gradient backgrounds if root theme is "light". |
| 2 | I | A2-RTL | 11 | `animation: 'slide_from_right'` does not flip for RTL locales. On Arabic/Urdu, back navigation visually moves the wrong direction. Expo Router supports `'slide_from_left'` but requires runtime detection. | Confusing navigation direction for RTL users. |
| 3 | I | D32-Transition | 11 | Only `slide_from_right` animation is set. No per-screen `presentation` overrides. Screens like `wind-down` (full-screen immersive) would benefit from `presentation: 'modal'` or `animation: 'fade'`. | All screens animate identically regardless of context. |

---

### Screen: `wind-down.tsx` (224 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 4 | H | A1-Light | 87-88 | `LinearGradient colors` hardcodes dark colors `['#0A1628', '#061118']` plus `tc.bg`. In light mode, `tc.bg` is a light color blended with two dark hex values producing an ugly dark-to-light gradient. All text uses `colors.text.primary` (light text) which will be invisible on the light portion. | Screen unusable in light mode -- text invisible, gradient broken. |
| 5 | M | A1-Light | 155-224 | `createStyles` uses `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary`, `colors.emerald` directly instead of `tc.text.*`. These are dark-theme-only tokens. In light mode, light-on-light text. | Most text invisible in light theme. |
| 6 | M | A2-RTL | 93-96, 199-204 | No RTL handling anywhere. `flexDirection: 'row'` on `messageContainer` (line 199) does not use `rtlFlexRow()`. Text alignment not flipped. In Arabic/Urdu, icon-then-text layout reads wrong direction. | RTL layout broken. |
| 7 | L | A4-StatusBar | - | No StatusBar override. The gradient background is always dark (hardcoded), but StatusBar style depends on root layout theme. If user is in light mode, StatusBar text will be dark on a dark gradient. | StatusBar text invisible on the dark gradient in light mode. |
| 8 | M | B13-DoubleTap | 121-143 | Neither `GradientButton` nor the `Pressable` for "Continue Scrolling" have double-tap protection. Rapid tapping "Close App" on Android calls `BackHandler.exitApp()` multiple times (harmless but wasteful). Rapid tapping "Continue Scrolling" calls `router.back()` multiple times, potentially popping more than one screen. | Double-back navigation could pop too many screens. |
| 9 | L | B11-Press | 133-143 | `Pressable` for "Continue Scrolling" has no visual press feedback (`style` is not a function of `pressed` state). User gets no tactile or visual indication the button was pressed. | Feels unresponsive. |
| 10 | M | C29-Offline | - | Screen is purely local (no API calls), which is fine. But BackHandler.exitApp() on Android and router.back() assume navigation stack exists. If deep-linked directly to this screen, `router.back()` may navigate to nothing. | Edge case: dead navigation on deep link. |
| 11 | L | D36-MicroInteraction | 121-143 | Button presses call `haptic.navigate()` but have no animated scale/opacity feedback. GradientButton may have built-in feedback, but the Pressable at line 133 definitely does not. | Inconsistent feedback between the two action buttons. |
| 12 | L | E44-Haptic | 124, 138 | Both buttons use `haptic.navigate()`. The "Close App" button is a destructive/exit action -- should arguably use a different haptic type (e.g., `haptic.warning()` or `haptic.delete()`). | Haptic doesn't match action severity. |
| 13 | I | D34-LayoutStability | 67-69 | `setInterval` toggles `phase` state every 4s, causing re-render with new text ("Breathe in" / "Breathe out"). The text length difference causes the circle content to reflow slightly. | Minor visual jitter on phase text change. |
| 14 | I | F45-Works | 125-128 | On iOS, `BackHandler.exitApp()` is a no-op -- the code correctly falls through to `router.back()`. However, the button label says "Close for now" which on iOS merely navigates back, not closing. Misleading label. | iOS: button says "close" but only goes back. |
| 15 | M | E38-Skeleton | - | No loading state at all. This screen has no API calls so this is acceptable, but there is also no entrance transition/skeleton for the breathing animation. The circles appear immediately at mid-scale. | Animation starts mid-cycle on mount, no graceful entrance. |

---

### Screen: `xp-history.tsx` (423 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 16 | M | A1-Light | 285-423 | `createStyles` uses `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` directly (lines 336, 340, 360, 389, etc.) instead of `tc.text.*`. These are dark-theme hardcoded values. In light mode, text will be wrong color. | Text colors incorrect in light mode. |
| 17 | M | A2-RTL | 307, 368 | `levelCard` uses `flexDirection: 'row'` (line 307) without `rtlFlexRow()`. The level badge layout (circle + info) will be wrong direction in RTL. Similarly, `eventRow` at line 368 uses `flexDirection: 'row'` hardcoded, though `XPEventRow` (line 130) does apply `rtlFlexRow(isRTL)` inline, overriding the style. Only `levelCard` is broken for RTL. | Level badge layout reversed for RTL users. |
| 18 | M | M-Perf | 70, 124, 159, 179 | `createStyles(tc)` is called on every render of `LevelBadge`, `XPEventRow`, `LoadingSkeleton`, and `XPHistoryScreen` -- that's 4 components each calling `StyleSheet.create()` on every render. `XPEventRow` is called per list item, so for 50 items = 50 `StyleSheet.create()` calls. Not memoized with `useMemo`. | Significant wasted work on re-renders; StyleSheet.create in render loop. |
| 19 | M | C23-Error | - | No error state handling. If `xpQuery` or `historyQuery` fail, `isLoading` becomes false, `events` is empty, and `EmptyState` is shown with the "start earning" message. The user sees "Start earning XP" when the real problem is a network error. | Error silently shown as empty state; user doesn't know to retry. |
| 20 | L | C28-BackState | - | After navigating away and returning, both queries use default `staleTime` (0), so they will refetch. This is acceptable but means every back-navigation triggers a flash. No `staleTime` configured on either query. | Unnecessary refetch on back navigation. |
| 21 | L | D31-Entrance | 129 | `FadeInUp.delay(Math.min(index * 60, 600))` -- good cap at 600ms, but this animation re-triggers on every re-render of the FlatList item (e.g., when scrolling back up to previously rendered items). Reanimated `entering` only fires on first mount, so this is fine. No issue. | N/A -- false alarm on investigation. |
| 22 | L | B13-DoubleTap | 239 | `GlassHeader` back button: `onPress: () => router.back()`. If `GlassHeader` doesn't internally debounce, rapid taps could pop multiple screens. | Possible double-back navigation. |
| 23 | H | A3-SafeArea | 290-293 | `content` style has `paddingTop: 100` (line 293) -- a hardcoded magic number presumably to account for the `GlassHeader` overlay. This does not adapt to different header heights, dynamic type sizes, or devices with taller status bars (e.g., Dynamic Island). It also doesn't use `insets` despite importing `useSafeAreaInsets`. | Content may overlap or have excessive gap on different devices/accessibility settings. |
| 24 | I | C25-Pagination | 262-263 | `onEndReachedThreshold={0.5}` is fine. But there's no visual "load more" indicator at the bottom of the list when `isFetchingNextPage` is false and `hasNextPage` is true. The user has no affordance that more content exists until they scroll to trigger fetch. | No visual hint that more data is available. |
| 25 | L | E44-Haptic | - | No haptic feedback on any interaction. The back button in `GlassHeader` and pull-to-refresh have no haptics. The `useContextualHaptic` hook is not imported or used. | No tactile feedback on this screen. |
| 26 | I | D33-ProgressiveImage | - | Not applicable -- no images on this screen. | N/A |

---

### Screen: `zakat-calculator.tsx` (950 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 27 | C | C21-RealAPI | 270-271 | `t('screens.zakatCalculator.shareMessage', ...)` -- the i18n key `screens.zakatCalculator.*` does NOT EXIST in `en.json` (verified via grep). The entire screen references ~20 keys under `screens.zakatCalculator.*` (lines 63-66, 270, 289, 313, 325, 329, 339, 349, 359, 369, 417, 427, 452, 464, 472, 476, 481, 488, 492, 501, 510, 516, 530, 544, 554). All will render as raw key strings like "screens.zakatCalculator.stepAssets". | Screen is completely broken -- all text shows i18n key paths instead of human-readable text. Every label, button, banner is a raw key string. |
| 28 | H | A1-Light | 571-950 | Same pattern as other screens: `createStyles` uses `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary`, `colors.emerald`, `colors.gold` directly. In light mode, text colors will be wrong. Multiple hardcoded `rgba(45,53,72,...)` and `rgba(28,35,51,...)` gradient colors assume dark background. | Entire screen broken in light mode. |
| 29 | H | A2-RTL | 584, 607-612, 679, 699, 778-779, etc. | Zero RTL handling. Multiple `flexDirection: 'row'` without `rtlFlexRow()`. Text alignment not flipped. Input fields with prefix "$" would need to be on the right side for RTL. Step indicator connector at line 649-656 uses `end: -50` which is start/end aware but the overall layout is not RTL-adapted. | Layout completely wrong for Arabic/Urdu users. |
| 30 | M | A8-Responsive | 32 | `const { width } = Dimensions.get('window')` at module scope -- called once on module load. The `width` variable is never actually used in the file (all `width:` references in styles are literal numbers). Dead code. Also, module-level `Dimensions.get()` doesn't update on device rotation or multitasking split-screen. | Dead import. If ever used, would show stale dimensions. |
| 31 | M | B13-DoubleTap | 376, 437, 447, 538, 548 | Five `Pressable` buttons have no double-tap protection. `goNext`/`goBack` could be called twice rapidly, though the state clamps prevent issues. But `handleShare` opens the share sheet -- double-tap could open it twice. `reset` called twice is idempotent but fires two haptics. | Share sheet could be double-opened. |
| 32 | M | B11-Press | 376, 437, 447, 538, 548 | All five `Pressable` buttons wrap a `LinearGradient` but provide no visual press feedback (no `style` function with `pressed` state, no opacity change). | Buttons feel dead -- no visual response to touch. |
| 33 | M | C23-Error | 193-198 | `useQuery` for metal prices has `retry: 2` but no error state display. If the query fails after retries, `priceData` is undefined and fallback prices are used silently. The user doesn't know they're seeing outdated prices from hardcoded constants. | Incorrect zakat calculation without user knowledge. Metal prices could be significantly outdated. |
| 34 | M | M-Perf | 60, 127, 171 | `createStyles(tc)` called on every render of `StepIndicator`, `InputCard`, and the main `ZakatCalculatorScreen` without memoization. `InputCard` is rendered 5-7 times per step, creating 5-7 `StyleSheet.create()` calls per render. | Wasted work on every keystroke (each keypress triggers state change and re-render). |
| 35 | L | B19-Keyboard | 292-299 | `KeyboardAvoidingView` with `behavior='padding'` on iOS is correct. But `keyboardVerticalOffset` is not set, and the `GlassHeader` is outside the `KeyboardAvoidingView`. The content has `paddingTop: 100` (line 580) to compensate for the header overlay. When keyboard opens, the offset calculation may be wrong because it doesn't account for the header. | Input fields may be partially hidden behind keyboard, especially on smaller devices. |
| 36 | L | B20-ScrollToInput | 296-299 | `ScrollView` with `keyboardShouldPersistTaps="handled"` is correct. But there's no `scrollTo` when an input focuses. On step 1 with 5 input cards, the bottom inputs (inventory, property) may be below the fold, and when tapped, the keyboard rises but the ScrollView doesn't auto-scroll to show the focused input. | Bottom inputs may be hidden behind keyboard. |
| 37 | H | C29-Offline | 206-213 | `onRefresh` resets ALL user input (lines 209-210: clears assets and deductions, sets step to 1) before refetching prices. If the user has carefully entered all their financial data and pulls to refresh, everything is wiped. This is data-destructive on a simple refresh. | Pull-to-refresh destroys all user input without confirmation. Devastating UX for users who spent minutes entering data. |
| 38 | M | C30-Concurrent | 266-278 | `handleShare` captures `netWealth` and `zakatDue` in its closure, but `formatCurrency` is defined AFTER `handleShare` (line 280 vs 266). Although JavaScript closures allow this (both are const in the same scope, and `handleShare` is only called later), `formatCurrency` is NOT in the useCallback dependency array. If `formatCurrency` were ever changed to depend on state, stale closure. | Fragile code ordering. formatCurrency missing from deps array. |
| 39 | L | D34-LayoutStability | 323-386, 390-457, 461-558 | Step transitions use conditional rendering (`currentStep === 1/2/3`). When switching steps, the entire step content unmounts and the new step mounts with `FadeInUp` animation. This causes a layout jump: old content disappears instantly, new content fades in from below. No exit animation. | Jarring step transitions -- content disappears then fades in from below. |
| 40 | L | B14-Destructive | 259-264, 538 | `reset` button (line 538) uses `haptic.delete()` which is good, but there's no confirmation dialog before wiping all entered data. On step 3 (results), tapping "Recalculate" immediately clears everything. | No confirmation before destroying user input. |
| 41 | M | A5-Radius | 355-356, 848-849 | `borderRadius: 3` hardcoded (line 355 for progress bar, and by reference at 356). Theme minimum is `radius.sm = 6`. Should use theme tokens. Also `borderRadius: radius.full` on 32x32 dot (line 624) is overkill -- `radius.full = 9999` works but is semantically wrong for a circle (should be `16`). | Inconsistent with theme system. |
| 42 | I | F45-Works | 280-282 | `formatCurrency` hardcodes `$` USD prefix and `en-US` locale. For a global Muslim app with users in Turkey, Indonesia, Bangladesh, etc., the currency and formatting should match the user's locale or at minimum be configurable. | Zakat amounts shown in USD format for all users regardless of locale. |
| 43 | I | E41-Icon | 328 | `icon="circle"` for "Cash & Bank" is a poor semantic choice. A dollar sign or banknote icon would be more descriptive. Similarly, `icon="layers"` for "Gold & Silver" is not intuitive. | Icons don't clearly represent their categories. |
| 44 | L | A3-SafeArea | 580 | `paddingTop: 100` hardcoded magic number for GlassHeader offset. Same issue as xp-history. | Content may overlap or gap on different devices. |
| 45 | I | C27-StaleData | 197 | `staleTime: 1000 * 60 * 60` (1 hour) for metal prices. Gold prices can move significantly in an hour during volatile markets. For a financial calculation tool, 15 minutes would be more appropriate. | Potentially outdated metal prices for financial decisions. |
| 46 | M | B12-Disabled | 376 | "Next" button on step 1 is always enabled even when all fields are empty ($0 total assets). User can proceed through all 3 steps with zero input and get a "below nisab" result. Should disable "Next" or show validation message when total assets is 0. | Users can complete the entire flow without entering anything, getting a meaningless result. |
| 47 | L | C30-Concurrent | 237-239 | `netWealth = totalAssets - totalDeductions` can be negative if deductions exceed assets. `zakatDue` handles this correctly (negative < nisab = no zakat), but `formatCurrency(netWealth)` will show `$-1,234.56` which is valid but not great UX. | Negative wealth displayed with awkward formatting. |

---

## Summary

| Screen | C | H | M | L | I | Total |
|--------|---|---|---|---|---|-------|
| `_layout.tsx` | 0 | 0 | 0 | 1 | 2 | 3 |
| `wind-down.tsx` | 0 | 1 | 4 | 4 | 2 | 11 |
| `xp-history.tsx` | 0 | 1 | 3 | 3 | 2 | 9 |
| `zakat-calculator.tsx` | 1 | 3 | 7 | 5 | 4 | 20 |
| **TOTAL** | **1** | **5** | **14** | **13** | **10** | **43** |

### Critical Finding

**#27 (C):** The entire zakat-calculator screen has NO i18n keys defined in `en.json` (or any language file). Every single label, button, title, and message will render as a raw key string like `"screens.zakatCalculator.stepAssets"`. The screen is completely non-functional for any user.

### Top Priorities

1. **#27** -- Add all `screens.zakatCalculator.*` i18n keys to all 8 language files (C)
2. **#37** -- Pull-to-refresh destroys user input without confirmation (H)
3. **#4** -- wind-down screen broken in light mode (H)
4. **#28** -- zakat-calculator broken in light mode (H)
5. **#23** -- Hardcoded `paddingTop: 100` doesn't adapt to devices (H)

C:1 H:5 M:14 L:13 I:10 Total: 43
