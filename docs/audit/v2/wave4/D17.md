# D17 — Screen Audit: event-detail, fasting-tracker, fatwa-qa, flipside, follow-requests

---

### Screen: `event-detail.tsx` (827 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | H | F45 | 170-173, 199, 225 | `GlassHeader` uses `onBack` prop without `showBack` or `showBackButton`. The `onBack` prop is ignored unless `showBack={true}` — resolved left action remains `undefined`, so **no back button renders** on any of the 3 GlassHeader instances. | User is trapped on screen with no way to navigate back. |
| 2 | M | A2 | all | No RTL support. No import of `rtlFlexRow` or `rtlTextAlign`. `flexDirection: 'row'` hardcoded in styles (lines 559, 660, 791, 804). Host row, RSVP buttons, bottom bar, attendee avatars all break in Arabic/Urdu. | Broken layout for 3/8 supported languages (ar, ur, ms partial). |
| 3 | M | B10/E44 | all | No `useContextualHaptic` usage anywhere. RSVP tap, share, calendar add, directions — none provide haptic feedback. | Silent interactions feel unresponsive. |
| 4 | M | B15/E43 | 118-133 | RSVP mutation has no `onError` handler and no `showToast` on success or failure. User gets no feedback when RSVP fails (network error, 403, etc.). | User taps "Going" and sees no confirmation or error — silent failure. |
| 5 | M | B13 | 135-138, 469-476 | `handleRsvp` and share button Pressables have no double-tap guard. Rapid taps can fire `rsvpMutation.mutate()` multiple times before `isPending` flips true (React state batching delay). | Duplicate RSVP mutations sent to server. |
| 6 | M | C30 | 135-138 | RSVP status set optimistically via `setRsvpStatus(status)` before mutation completes (line 136), but on mutation failure status is never reverted. The `onMutate`/`onError` rollback pattern is not implemented. | UI shows stale RSVP status after server rejection. |
| 7 | M | A8 | 32 | `const { width } = Dimensions.get('window')` imported and computed but **never used** anywhere in the file. Dead code, and `Dimensions.get` is not responsive to screen rotation/resize. | Dead import; if used later, won't respond to dimension changes. |
| 8 | M | E37/D33 | 240-242 | Cover image area renders a static placeholder `<Icon name="image">` instead of using `<ProgressiveImage>`. Even when `event.coverImageUrl` exists (likely field on the model), it's never rendered. | No event cover images ever displayed. |
| 9 | L | A1 | 506-507, 519-520, 615-616, 625-628, 677-678, 759-760, 795-796 | Multiple hardcoded dark colors in StyleSheet: `colors.dark.bg`, `colors.dark.bgCard`, `colors.dark.surface`, `colors.dark.border`, `colors.dark.borderLight`. These are overridden by `tc.*` in JSX for some but not all styles. Styles like `addToCalendar` (line 615), `directionsButton` (line 627), `rsvpButtonNotGoing` (line 677), `moreAvatar` (line 759) use hardcoded dark colors that won't change in light mode. | Light theme partially broken — some elements stay dark. |
| 10 | L | A7 | 546-547, 569-572 | `badgeText` and `hostText`/`hostName` use hardcoded `colors.text.*` that won't adapt to light theme (already overridden via tc in JSX for some but not these). | Typography color inconsistency in light mode. |
| 11 | L | C25 | 101-113 | Attendees query only fetches first page (`cursor: undefined`). No pagination for attendees list — if event has >20 attendees, only first page is fetched. The "See All Attendees" link exists but the initial fetch is still limited. | Incomplete attendee count display for large events. |
| 12 | C | C21 | 295 | `calshow:` URL scheme is **iOS-only** and will crash/do nothing on Android. No platform check before calling `Linking.openURL`. | Crash or no-op on Android when user taps "Add to Calendar". |
| 13 | L | B11 | 290-301, 321-331, 345, 450-457, 469-476, 487-496 | Multiple `Pressable` components have no `onPressIn`/`onPressOut` visual feedback (no scale animation, no opacity change). User taps feel unresponsive. | No visual press feedback on interactive elements. |
| 14 | I | A3 | 222, 468 | SafeAreaView uses `edges={['top']}` but the bottom bar at line 468 is `position: absolute, bottom: 0`. On devices with home indicator (iPhone X+), the bar may overlap the safe area inset at the bottom. | Bottom bar buttons may be obscured by home indicator. |
| 15 | I | D31 | 238 | Only `FadeInUp` entrance animation used. Bottom bar has no entrance animation — it pops in abruptly. | Minor polish gap. |

---

### Screen: `fasting-tracker.tsx` (425 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 16 | H | A1 | 39 | `StatCard` color prop is **always overridden**. Style array is `[styles.statValue, color ? { color } : undefined, { color: tc.text.primary }]` — the last item `{ color: tc.text.primary }` always wins due to RN style merge order. The `color` prop (e.g., `colors.emerald` for streak) is silently ignored. | Streak stat always shows default text color instead of emerald. Color prop is dead code. |
| 17 | M | C23 | 85-103 | `logMutation` has no `onError` handler. If logging a fast fails (network error, server error), the user sees the success haptic on tap (line 99) even though the mutation hasn't completed yet — haptic fires in `onSuccess` but `handleLogToday` also calls `haptic.success()` at line 112 **before** the mutation resolves. Double haptic on success, zero feedback on failure. | Silent failure when logging fast fails; premature haptic feedback. |
| 18 | M | B15/E43 | 95-103 | No `showToast` import or usage. No user-facing feedback for fasting log success or failure. | User taps "Yes I'm fasting" and gets no visual confirmation. |
| 19 | M | M | 79, 155 | `today = new Date()` is called on every render (line 79). It's used as a dependency in `useMemo` (line 155: `[currentMonth, logs, today]`). Since `today` is a new object each render, the memo **recomputes every render**, defeating its purpose entirely. | Calendar grid recomputes on every render — performance waste. |
| 20 | M | B13 | 194-199, 204-209 | "Yes/No fasting" buttons have `disabled={logMutation.isPending}` but `handleLogToday` fires `haptic.success()` immediately (line 112) before the mutation even starts. No debounce or guard against rapid sequential taps before React updates `isPending`. | Possible duplicate mutation on fast double-tap. |
| 21 | L | A7 | 288-424 | All styles use `fontWeight: '600'`/`'700'` string values instead of `fontFamily: fonts.*`. The `fonts` import exists (line 14) but is never used in the StyleSheet. This bypasses the custom font family system (PlayfairDisplay, DMSans). | Text renders in system font weights rather than branded custom fonts. |
| 22 | L | A1 | 289, 294, 319, 336-337, 340, 354-355, 367-368, 397, 403-404, 410 | Hardcoded `colors.dark.*` and `colors.text.*` in StyleSheet that are not overridden by `tc.*` in JSX: `calDayText` (line 397), `todayStatusText` (line 324), `todayBtnText` (line 320). | Light theme broken for calendar day text and status text. |
| 23 | L | C23 | 85-88 | `statsQuery` has no error state handling in the render. If stats query errors, the `stats ? ... : null` branch renders null — no error indication. | Stats silently disappear on error with no retry option. |
| 24 | I | D36 | 262-264 | Calendar day cells have no tap handler. User cannot tap a day to view/edit that day's fasting log — only today's prompt is interactive. | Limited interactivity; can only log today, not past days. |
| 25 | I | A5 | 392 | `marginBottom: 2` hardcoded pixel value instead of using spacing tokens. | Inconsistent spacing token usage. |

---

### Screen: `fatwa-qa.tsx` (251 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 26 | H | A3 | 100 | Uses raw `<View>` as root container instead of `<SafeAreaView>`. Content will render behind the status bar notch on iOS. The `GlassHeader` component may handle its own top inset internally, but the FlatList and ask form content below it have no safe area protection. | Content overlaps status bar / notch area. |
| 27 | M | A2 | all | No RTL support. No import of `rtlFlexRow` or `rtlTextAlign`. `flexDirection: 'row'` hardcoded in styles (lines 218, 219, 223, 231, 235, 238, 248). Tabs, filter chips, question headers, answer cards all break in Arabic. | Broken layout for RTL languages. |
| 28 | M | B15/E43 | 51-59 | `askMutation` has no `onError` handler. If question submission fails, user gets no error feedback — question is cleared on success but failure is silent. | User's typed question is preserved (good) but they don't know it failed. |
| 29 | M | B19 | 162-198 | Ask form has a `TextInput` with `multiline` but no `KeyboardAvoidingView` wrapper. On iOS, the keyboard will cover the submit button and lower portion of the text input. | Submit button hidden behind keyboard when typing question. |
| 30 | M | A7 | 216-249 | All styles use `fontWeight` string values instead of `fontFamily: fonts.*`. `fonts` is not imported. | Text renders in system fonts, not branded custom fonts. |
| 31 | L | A1 | 217, 219, 224, 230, 243 | Hardcoded `colors.dark.*` in StyleSheet (e.g., `colors.dark.bg`, `colors.dark.bgCard`, `colors.dark.border`). Some are overridden in JSX via `tc.*` but not all — `answerCard` (line 238) uses `colors.emerald + '08'` hardcoded background. | Light theme inconsistencies. |
| 32 | L | B10/E44 | all | No haptic feedback on question card tap (if it were tappable). Haptics only on tab switch and madhab filter — no haptic on submit button press. | Submit action feels less responsive than filter chips. |
| 33 | L | C28 | 30-33 | Tab state (`activeTab`), question text, and selected madhab are all local state. Navigating away and back resets everything — typed question is lost. | User loses in-progress question on back navigation. |
| 34 | I | B16 | 63-96 | Question cards are not tappable (no `onPress` on the card). User cannot tap to see full question/answer detail view. | Questions are display-only with no detail navigation. |
| 35 | I | D31 | 67 | `FadeInUp.delay(index * 60)` on every question card causes large cumulative delay for long lists (20 items = 1.2s delay for last item). Should cap the delay. | Late items animate in long after scroll, feels sluggish. |

---

### Screen: `flipside.tsx` (623 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 36 | H | A3 | 183, 198, 220, 427 | Uses raw `<View>` as root container in all 4 render branches (loading, error, create, profile). No `<SafeAreaView>` anywhere. Content renders behind status bar/notch. | Content behind notch on all screen states. |
| 37 | H | B14 | 152-161 | `handleDelete` uses `Alert.alert` for destructive profile deletion. The rules say to use `showToast()` for feedback and discourage `Alert.alert` for non-destructive actions. More critically, `Alert.alert` is used for a destructive confirmation but the dialog message is `t('flipside.description')` which is the feature description, not a deletion warning. | Misleading deletion confirmation — shows feature description instead of "Are you sure you want to delete your alt profile?" |
| 38 | M | A2 | all | No RTL support. No imports of `rtlFlexRow` or `rtlTextAlign`. `flexDirection: 'row'` hardcoded in styles (lines 558, 565, 571, 589). Profile header, action row, access button all break in Arabic. | RTL layout broken. |
| 39 | M | B14 | 498-510 | Second `Alert.alert` for removing access uses the person's display name as the message body, with no actual explanation of what the action does. | Confusing removal confirmation — just shows a name with no context. |
| 40 | M | B19 | 237-289 | Create form has `TextInput` fields but no `KeyboardAvoidingView`. On iOS, keyboard covers the `GradientButton` submit. Same issue in the inline edit form (lines 344-382). | Keyboard covers submit/save buttons. |
| 41 | M | B13 | 140-143, 146-149 | `handleCreate` and `handleUpdate` fire `haptic.success()` immediately before the async mutation completes. If mutation fails, user already felt success haptic. | Premature success haptic before server confirms. |
| 42 | L | A1 | 522, 539, 545 | Hardcoded colors in StyleSheet: `backgroundColor: 'rgba(0,0,0,0.2)'` (line 545), `borderColor: 'rgba(255,255,255,0.06)'` (multiple), `borderColor: 'rgba(255,255,255,0.04)'` (line 612), `backgroundColor: 'rgba(248,81,73,0.1)'` (line 584). These won't adapt to light theme. | Light theme elements have dark-mode-only colors. |
| 43 | L | A7 | 521-622 | All styles use `fontWeight` string values instead of `fontFamily: fonts.*`. `fonts` is not imported. | System fonts instead of branded fonts. |
| 44 | L | C23 | 181-193 | Loading state renders `Skeleton.ProfileHeader()` offset by `HEADER_HEIGHT` using inline style `{ marginTop: HEADER_HEIGHT }`. If `GlassHeader` uses `position: 'absolute'`, content may be incorrectly positioned. | Potential layout overlap between header and skeleton. |
| 45 | I | D34 | 298-312 | Posts rendered with `FadeInUp.delay(index * 40)` — same uncapped delay issue as fatwa-qa. 50 posts = 2s delay for last post. | Sluggish animation for long post lists. |
| 46 | I | C29 | all | No offline handling. If network is unavailable, queries fail silently (error state shows for profile query but not for posts or access list). | Partial offline handling. |

---

### Screen: `follow-requests.tsx` (230 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 47 | H | A3 | 129, 144 | Uses raw `<View>` as root container in both error and main render paths. No `<SafeAreaView>` — content renders behind status bar/notch. | Content behind notch on all device types. |
| 48 | M | A2 | all | No RTL support. `flexDirection: 'row'` hardcoded in styles (lines 205, 199). Request rows, skeleton rows break in Arabic/Urdu. | RTL layout broken. |
| 49 | M | B10/E44 | all | No `useContextualHaptic` usage anywhere. Accept/decline actions have no haptic feedback. | Silent interactions on critical accept/decline actions. |
| 50 | M | C25 | 98-103 | Uses `useQuery` (not `useInfiniteQuery`). Follow requests are fetched as a single page. If user has >20 pending requests, remaining are invisible with no "load more" or pagination. | Users with many follow requests only see first page. |
| 51 | M | B13 | 175-176 | Accept and decline mutations have no double-tap guard. The `pendingId` tracking (line 125) only prevents UI from showing buttons while pending for that specific item, but rapid taps on different items can fire concurrent mutations. Also, rapid taps on the same item before `isPending` updates can duplicate. | Potential duplicate accept/decline API calls. |
| 52 | L | A7 | 213-227 | All styles use `fontWeight` string values instead of `fontFamily: fonts.*`. `fonts` is not imported. | System fonts instead of branded fonts. |
| 53 | L | A1 | 195 | Hardcoded `colors.dark.bg` in StyleSheet container. Overridden in JSX for main container but not for the error state container. | Error state always dark themed. |
| 54 | L | B11 | 68-75, 76-83 | Accept/decline buttons use `Pressable` with `LinearGradient` child but no press animation (`onPressIn`/`onPressOut`, no scale). | No visual feedback on button press. |
| 55 | L | C28 | 117-123 | `refreshing` state is manually managed with `useState` + `async/await` pattern instead of using `requestsQuery.isRefetching` directly (which BrandedRefreshControl already supports). Redundant state management. | Minor code smell — unnecessary state variable. |
| 56 | I | D36 | 44-89 | `RequestRow` uses `FadeInUp.delay(index * 50)` — same uncapped animation delay pattern. 40 requests = 2s delay. | Late items animate in slowly. |
| 57 | I | B15 | 107, 113 | Accept/decline success only invalidates queries but shows no toast for success (only error shows toast). User doesn't know their action succeeded until the list refreshes. | No positive confirmation on accept/decline. |

---

## Summary

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 1 |
| H (broken feature) | 5 |
| M (error-handling/perf) | 20 |
| L (smell) | 17 |
| I (suggestion) | 7 |

**C:1 H:5 M:20 L:17 I:7 Total: 50**
