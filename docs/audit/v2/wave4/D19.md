# D19 Audit — Wave 4

**Auditor:** Claude Opus 4.6 (1M context)
**Date:** 2026-03-30
**Scope:** 5 screens (green-screen-editor, hadith, hajj-companion, hajj-step, halal-finder)
**Checklist:** 46 checks (A1-A9, B10-B20, C21-C30, D31-D36, E37-E44, F45-F46)

---

### Screen: `green-screen-editor.tsx` (963 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | L | A1-Light | 611-614 | `styles.container` uses `colors.dark.bg` hardcoded. While `tc.bg` is applied inline at L406, the static style still defaults to dark. Multiple text styles (L669 `categoryButtonText`, L689 `gridTitle`, L720 `colorName`, L760 `gradientName`, L858 `uploadButtonText`, L862 `uploadButtonSubtext`, L870 `recentTitle`, L902 `sliderTitle`, L915 `sliderLabel`, L946 `cancelButtonText`, L959 `applyButtonText`) use `colors.text.*` directly instead of `tc.text.*`, breaking light theme. | Text invisible or wrong color in light mode. |
| 2 | L | A2-RTL | 486-516 | Horizontal `ScrollView` for category tabs has no RTL consideration. `contentContainerStyle` uses no `flexDirection: rtlFlexRow(isRTL)`. The screen doesn't import or use any RTL utilities. | Category tabs may appear in wrong order for Arabic/Urdu users. |
| 3 | M | A8-Responsive | 24 | `Dimensions.get('window')` called at module scope (L24). Value is captured once at import time and never updates on orientation change, split view, or window resize. Used for `screenHeight * 0.38` (L622) and grid item widths (L698-699, L729-730, L770, L796). | Layout breaks on rotation, iPad split view, or foldable devices. |
| 4 | L | A5-Radius | — | All radius values use `radius.*` from theme. Pass. | — |
| 5 | L | A6-Spacing | — | All spacing values use `spacing.*` from theme. Pass. | — |
| 6 | M | B13-DoubleTap | 125-148 | `handleRecord` has no debounce/guard. Rapid double-tap on record button calls `recordAsync` twice or calls `stopRecording` then immediately tries `recordAsync` again due to async state update. | Crash or corrupted recording. |
| 7 | L | B10-Haptic | 594 | "Apply and Record" button (L594) has no haptic feedback on press. Similarly, "Cancel" button (L591) has no haptic. These are primary actions. | Inconsistent tactile feedback. |
| 8 | M | C21-RealAPI | 80-98 | `IMAGE_BACKGROUNDS` and `VIDEO_BACKGROUNDS` are arrays of `{ name: string }` with NO actual image/video URIs. They render as placeholder icons (L290 icon, L319 play icon) with no real media. Selecting one sets `selectedBackground` to a name string like `'beach'` which is never resolved to an actual asset. | Feature is non-functional facade. Images/videos tabs show empty placeholder cards. |
| 9 | L | C27-StaleData | 112 | `audioPermission` state is set but never read anywhere in the component. Dead state variable. | Unused code, wasted memory. |
| 10 | L | C27-StaleData | 114 | `recordedUri` state is set (L140 `setRecordedUri(video.uri)`) but never read/used elsewhere. After recording, the URI goes nowhere - no preview, no save, no navigation. | User records video but nothing happens with it. |
| 11 | M | C23-Error | 143 | `catch (_err: unknown)` in `handleRecord` silently swallows all recording errors including permission denials, hardware failures, out-of-space. No toast or user feedback. | User sees recording stop with no explanation on failure. |
| 12 | M | B17-BrandedRefresh | 152-155 | `onRefresh` immediately sets `refreshing` to true then false synchronously on the same tick. This makes the refresh spinner flash for 0ms. There's no data to refresh, so pull-to-refresh is misleading. | Confusing UX - pull-to-refresh does nothing but exists. |
| 13 | L | D34-LayoutStability | 207-231 | Color grid items computed with `(screenWidth - spacing.base * 4 - spacing.md * 3) / 4` (L698) using static `screenWidth`. Grid item width is computed at stylesheet creation time, not reactively. | Grid breaks on dynamic screen width changes. |
| 14 | I | F45-Works | 594 | "Apply and Record" button navigates to `/(screens)/camera` (L594) but does NOT pass the selected background, gradient, blur intensity, or edge smoothing as params. All user selections are lost. | Entire screen's purpose (choosing a background) is discarded on navigation. |
| 15 | H | F45-Works | 1 | Comment on L1 says "Background segmentation requires expo-gl or react-native-vision-camera with frame processors -- not yet installed." The entire green screen feature is non-functional: camera records normally without any background replacement. Toast at L134 confirms this. | Core feature (green screen) doesn't work. Screen is a UI shell with no functionality. |
| 16 | L | A3-SafeArea | 406 | `SafeAreaView edges={['top']}` is used, but the bottom bar (L586-603) is `position: absolute, bottom: 0` with no bottom safe area padding. On newer iPhones with home indicator, the bottom bar overlaps the gesture area. `paddingBottom: spacing.lg` (L939) provides only 20px which may not be enough for the 34px home indicator. | Bottom buttons partially hidden behind home indicator on iPhone X+. |

### Screen: `hadith.tsx` (559 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 17 | H | C21-RealAPI | 166 | `islamicApi.listHadiths()` returns `PaginatedResponse<Hadith>` which is `{ data: Hadith[], meta: { cursor?, hasMore } }`. Line 166 checks `Array.isArray(listResp) ? listResp : []` which will always be `false` since `listResp` is an object with `data` and `meta`. The result is always `[]`, meaning the "Previous Hadith" list is permanently empty. Should be `Array.isArray(listResp) ? listResp : (listResp as any).data ?? []` or `(listResp as { data: ApiHadith[] }).data ?? []`. | Previous hadith list is always empty. Only daily hadith works. |
| 18 | L | A1-Light | 412-415 | `styles.container` uses `colors.dark.bg` hardcoded. While `tc.bg` is applied inline at L306, static stylesheet text styles use `colors.text.*` (L444, L449, L455, L460, L465, L489, L500, L502, L504, L529, L534, L555) instead of `tc.text.*`. Only some styles are overridden inline. | Text wrong color in light theme. |
| 19 | M | C25-Pagination | 155-156 | `islamicApi.listHadiths()` accepts a cursor parameter but the screen never passes it. `hadiths.slice(1)` is the only data shown. Even if bug #17 were fixed, there's no infinite scroll/load-more/onEndReached for the FlatList. API supports pagination but screen ignores it. | Only first page of hadiths ever loaded. |
| 20 | L | A2-RTL | 470-472 | `actionRow` uses `flexDirection: 'row'` hardcoded. For RTL languages, action buttons should be mirrored. No `rtlFlexRow` utility used. | Action buttons in wrong order for Arabic users. |
| 21 | M | B13-DoubleTap | 194-218 | `handleBookmark` has no debounce. Rapid double-tap toggles bookmark twice, causing two API calls that cancel each other out. The optimistic update and rollback logic works correctly for single failures but not for rapid consecutive calls. | Double-tap un-bookmarks immediately after bookmarking, confusing user. |
| 22 | L | C26-Optimistic | 211-215 | Bookmark rollback on API error (L211-214) toggles `isBookmarked` back, but does NOT update `bookmarkedIds` Set. The Set and the hadith object's `isBookmarked` field get out of sync. | `bookmarkedIds` Set becomes stale after failed bookmark. |
| 23 | L | D33-ProgressiveImage | — | No images in this screen. N/A. | — |
| 24 | M | C28-BackState | 240-242 | `selectHadith` sets `currentHadith` to the tapped item, but the hero card doesn't scroll back to top. User taps a previous hadith card at the bottom, hero card updates but user doesn't see it. | User selects a hadith but doesn't see the updated hero card. |
| 25 | I | B16-LongPress | 99 | `PreviousHadithCard` has `onPress` but no `onLongPress` for quick-save/share. The hero card has share/copy/bookmark actions, but previous cards have none. | Inconsistent interaction model between hero and list items. |
| 26 | L | A7-Typography | 34 | `Dimensions.get('window')` at module scope (L34), `width` used nowhere in JSX (only in skeleton L257). Minor - skeleton width calculation is static. | Layout issue on orientation change for skeleton. |

### Screen: `hajj-companion.tsx` (558 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 27 | L | A1-Light | 377-379 | `styles.container` uses `colors.dark.bg` hardcoded. Multiple stylesheet values use `colors.dark.*` (L452 `progressBarBg`, L487 `stepCircle`, L491 `stepCircle.borderColor`) and `colors.text.*` directly (L403-406 `startTitle`/`startSubtitle`, L440 `progressDay`, L444 `progressYear`, L462 `progressPercent`, L502 `stepNumber`, L520 `stepName`, L525 `stepDesc`, L548 `shareButtonText`, L553 `resetButtonText`). Some are overridden inline, some are not. | Incorrect colors in light theme across multiple elements. |
| 28 | M | B10-Haptic | 171-186 | "Start Tracker" button has no haptic feedback. Reset confirmation, share, and step navigation all lack haptic. None of `useContextualHaptic` is imported or used in this screen. | No tactile feedback on any interaction. |
| 29 | M | B14-Destructive | 341-353 | Reset progress BottomSheet has a destructive "Reset" action (destroys all hajj progress). It fires `resetMutation.mutate()` immediately on press with no confirmation dialog/second step. The BottomSheet itself acts as one level of confirmation, but for data-destroying actions a second confirmation (alert/modal) is standard. | Accidental tap on reset destroys all hajj progress with no undo. |
| 30 | M | C23-Error | 73-79 | `createMutation` has no `onError` handler. If creating hajj progress fails (network error, server error), user gets no feedback. `setShowYearPicker(false)` only runs `onSuccess`. | User taps year, sheet stays open forever on error. Silent failure. |
| 31 | M | C23-Error | 81-94 | `resetMutation` has no `onError` handler. Failed reset gives no user feedback. `setShowResetSheet(false)` only runs `onSuccess`. | Reset sheet stays open on error. No indication of failure. |
| 32 | L | A2-RTL | 478 | `timelineLine` uses `left: 23` (L478) hardcoded LTR position. Should use `start: 23` for RTL support. | Timeline line mispositioned for RTL users. |
| 33 | M | C29-Offline | 63-94 | No offline handling. Queries have no `staleTime` or `cacheTime` set, so they default to 0/5min. Mutations have no retry logic. Offline user sees loading forever. | Screen non-functional offline with no offline indicator. |
| 34 | L | B12-Disabled | 171-186 | "Start Tracker" button shows no loading/disabled state while `createMutation.isPending`. User can tap multiple times creating duplicate progress entries. | Duplicate hajj progress records on multiple taps. |
| 35 | I | D31-Entrance | 204 | `FadeInDown` on progress card is good. Timeline steps have staggered `FadeInUp`. Entrance animations present. | — |
| 36 | L | E41-Icon | 166 | `Icon name="map-pin"` used appropriately for Hajj/location theme. Pass. | — |

### Screen: `hajj-step.tsx` (471 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 37 | L | A1-Light | 307-309 | `styles.container` uses `colors.dark.bg`. Multiple stylesheet values use `colors.dark.*` (L332 `headerCircle.backgroundColor`, L336 `headerCircle.borderColor`, L348 `headerCircleText.color`, L389-393 `duaCard` bg/border, L404 `duaDivider`, L435 `checkbox.borderColor`) and `colors.text.*` directly (L358 `stepNameAr.color`, L375 `description.color`, L381 `sectionTitle.color`, L399 `duaArabic.color`, L416 `duaEnglish.color`, L446 `checkText.color`, L450 `checkTextDone.color`). Some overridden inline, many not. | Broken colors in light theme. |
| 38 | M | B10-Haptic | All | No `useContextualHaptic` import or usage. Toggling checklist items, expanding duas, marking step complete - none have haptic feedback. | No tactile feedback on any interaction. |
| 39 | M | C23-Error | 76-84 | `updateMutation` has no `onError` handler. Checklist toggle (L86-108) calls `updateMutation.mutate()` on every checkbox tap. Failed mutation silently loses checklist state — optimistic local state diverges from server with no rollback or error toast. | User checks items, they appear checked locally but server never saves. No error shown. |
| 40 | M | B13-DoubleTap | 110-118 | `handleMarkComplete` has no guard against double-tap. Two rapid taps call `updateMutation.mutate({ currentStep: nextStep })` twice. While the mutation is idempotent in value, the double navigation (`router.back()` at L116-117) could cause issues. | Double mutation + potential navigation stack corruption. |
| 41 | L | C28-BackState | 60-74 | `useEffect` initializes `checklistState` from progress. If user navigates back and returns, `checklistState` re-initializes from (potentially stale) query cache, potentially losing in-flight mutations not yet synced. | Checklist state may flash/reset on re-entry. |
| 42 | L | A2-RTL | 425-429 | `checkItem` uses `flexDirection: 'row'` hardcoded. Checkbox appears on left for RTL users when it should be on right. No `rtlFlexRow` import or usage. | Checklist layout wrong for Arabic/Urdu users. |
| 43 | M | B12-Disabled | 270-289 | "Mark Complete" button has no disabled/loading state during `updateMutation.isPending`. User can tap repeatedly. | Multiple completion mutations fired on impatient taps. |
| 44 | I | E43-Toast | 110-118 | `handleMarkComplete` doesn't show a success toast after marking a step complete. User gets no confirmation besides navigation back. | Missing success feedback. |

### Screen: `halal-finder.tsx` (356 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 45 | L | A1-Light | 248 | `styles.container` uses `colors.dark.bg`. Multiple static styles use `colors.dark.*` (L259 `chip.backgroundColor`, L261 `chip.borderColor`, L280 `card.backgroundColor`, L284 `card.borderColor`) and `colors.text.*` (L269 `chipText.color`, L300 `cardName.color`, L329 `cardAddress.color`, L339 `metaText.color`, L350 `ratingText.color`, L354 `distanceText.color`). Some overridden inline (card, chip), some not (ratingText L350). | Incorrect colors in light theme for several elements. |
| 46 | M | C25-Pagination | 133-138 | `halalApi.findNearby()` accepts `cursor` parameter for pagination. The FlatList has no `onEndReached`, no `onEndReachedThreshold`, no cursor tracking, no "load more" functionality. Only the first page of results is ever displayed. | Users near many halal restaurants only see first page. |
| 47 | H | C21-RealAPI | 141 | Data unwrapping is fragile. `rawData` is the result of `.then(r => r.data)` on L136. But `halalApi.findNearby` returns `PaginatedResponse<HalalRestaurant>` which after the API client's `TransformInterceptor` unwrap becomes `{ data: HalalRestaurant[], meta: {...} }`. Then `.then(r => r.data)` extracts the inner `data` array. Line 141 then does another conditional unwrap: `Array.isArray(rawData) ? rawData : (rawData as ...).data ?? []`. If the first `.then(r => r.data)` already extracted the array, the `Array.isArray` check is redundant but correct. However, if the API response structure changes or `TransformInterceptor` doesn't wrap (e.g., error fallback), this double-unwrap silently returns `[]`. | Fragile data extraction that silently produces empty results on response format changes. |
| 48 | M | C29-Offline | 118-138 | No offline handling. Location query and restaurant query have no error states rendered. If location permission is denied and the Mecca fallback coordinates return no restaurants, user sees generic empty state with no explanation about location permission. | User denied location permission sees unexplained empty results. |
| 49 | L | C23-Error | 118-128 | `locationQuery` has no error state handling. If `Location.requestForegroundPermissionsAsync()` throws (e.g., on web, or module not linked), the query errors silently. The fallback `currentLocation` at L131 covers the denied case but not the thrown case. | Unhandled exception in location query. |
| 50 | L | B13-DoubleTap | 167-171 | Cuisine filter chips have no debounce. Rapid tapping triggers multiple `setSelectedCuisine` state updates and query refetches. React Query handles this gracefully (cancels stale queries) but it's still wasteful. | Unnecessary network requests on rapid taps. |
| 51 | M | C30-Concurrent | 157-189 | `listHeader` useMemo depends on `[selectedCuisine, haptic]` but `haptic` is a hook return that changes identity every render (unless memoized internally). If `haptic` reference changes, the entire header including the FlatList re-renders unnecessarily. Also missing `tc` from deps despite using `tc.bgElevated` and `tc.border` at L173. | Stale closure: chip styles don't update on theme change. Potential extra re-renders. |
| 52 | L | A2-RTL | 157-189 | Cuisine filter chips FlatList is `horizontal` with no `inverted` prop for RTL. The `isRTL` is available but not applied to the horizontal FlatList. | Cuisine chips scroll direction wrong for RTL users. |
| 53 | H | F45-Works | 29-42 | Local `HalalRestaurant` interface defines `isVerified: boolean` and `averageRating: number` but the API type in `halalApi.ts` defines `rating?: number` (not `averageRating`) and has no `isVerified` field at all. `reviewCount` is optional in API type but required in local interface. `distanceKm` exists in local interface but not in API type. | Type mismatch between screen and API: `averageRating` will always be `undefined`, rating badge never shows. `isVerified` badge never shows. |
| 54 | I | D36-MicroInteraction | 219 | `FadeInUp` animation on cards is good. `Math.min(index, 15) * 40` caps animation delay nicely. | Good animation cap prevents excessive delay. |
| 55 | L | E42-BrandedRefresh | 229 | `BrandedRefreshControl` used correctly. `restaurantsQuery.isRefetching` used for refreshing state. Pass. | — |

---

## Summary

| Screen | C | H | M | L | I | Total |
|--------|---|---|---|---|---|-------|
| green-screen-editor.tsx | 0 | 1 | 4 | 6 | 1 | 12 |
| hadith.tsx | 0 | 1 | 3 | 4 | 1 | 9 |
| hajj-companion.tsx | 0 | 0 | 4 | 3 | 1 | 8 |
| hajj-step.tsx | 0 | 0 | 4 | 3 | 1 | 8 |
| halal-finder.tsx | 0 | 2 | 3 | 4 | 1 | 10 |
| **Totals** | **0** | **4** | **18** | **20** | **5** | **47** |

**Severity counts: C:0 H:4 M:18 L:20 I:5 Total: 47**

### Top Issues by Priority

1. **H #15 (green-screen):** Entire green screen feature is non-functional. Camera records normally without any background segmentation. Pure UI facade.
2. **H #17 (hadith):** `listHadiths` data unwrapping bug means "Previous Hadith" list is always empty. `Array.isArray(objectWithDataKey)` always returns false.
3. **H #47 (halal-finder):** Fragile double-unwrap of API response data that masks errors silently.
4. **H #53 (halal-finder):** Type mismatch between screen interface and API type (`averageRating` vs `rating`, missing `isVerified`, missing `distanceKm`). Rating and verification badges will never render.
5. **M #8 (green-screen):** Image/Video backgrounds are name-only arrays with no actual assets. Two entire tabs are non-functional.
6. **M #28, #38 (hajj):** Both hajj screens have zero haptic feedback on any interaction.
7. **M #30, #31, #39 (hajj):** All mutations across both hajj screens lack `onError` handlers - failures are completely silent.
