# D24 Audit: morning-briefing, mosque-finder, muted, mutual-followers, my-reports

---

### Screen: `morning-briefing.tsx` (763 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | C | C21-RealAPI | 187-190 | API `getDailyBriefing` returns `{ greeting, date, hijriDate, prayerTimes?, dailyVerse?, tasks[] }` but the screen casts it to `DailyBriefing` interface which expects `hadithOfTheDay`, `ayahOfTheDay`, `duaOfTheDay`, `dhikrChallenge`, `tasksCompleted`, `totalTasks`, `completedTasks` -- none of which exist in the API response type. Every `.hadithOfTheDay.arabic`, `.ayahOfTheDay.surah`, `.duaOfTheDay.arabic`, `.dhikrChallenge.target` etc. will throw "Cannot read property of undefined" at runtime. | Crash on load -- entire screen is broken against real API |
| 2 | C | C21-RealAPI | 281 | Division `briefing.tasksCompleted / briefing.totalTasks` -- if `totalTasks` is 0 or undefined (likely given API mismatch), this produces `NaN` or `Infinity`, rendered as `NaN%` width on the progress bar. | Visual corruption or crash |
| 3 | H | B13-DoubleTap | 77-86 | `DhikrCounter.handlePress` increments count every tap with no debounce. Rapid double-tap fires the `onComplete` mutation twice (one for count reaching target, another for the subsequent tap that `isComplete` hasn't yet caught due to React batching). | Double API mutation call |
| 4 | H | C27-StaleData | 71 | `DhikrCounter` initializes `count` from `initialCount` prop via `useState`. If parent re-renders with new `initialCount` (e.g. after refetch), the counter keeps stale local state and ignores the updated value. | Counter out of sync after pull-to-refresh |
| 5 | H | A1-Light | 513 | `container` style hardcodes `backgroundColor: colors.dark.bg`. While JSX overrides with `tc.bg`, the StyleSheet default is dark-only. Also `greeting` (L519), `hijriDate` (L526), `progressTitle` (L544), `cardTitle` (L578), `prayerName` (L600), `prayerTime` (L606), `arabicText` (L613), `contentText` (L628), `sourceText` (L634), `dhikrCount` (L662), `dhikrTarget` (L667), `dhikrPhrase` (L689) all hardcode `colors.text.primary`/`secondary`/`tertiary` in StyleSheet. Some are overridden in JSX, but `hijriDate` (L526 uses `colors.gold` -- not theme-aware), `bonusXPText` (L549), `reflectionInput` (L719 uses `colors.dark.border`), `surahRef` (L640 uses `colors.gold`) are NOT overridden. | Light mode colors wrong for ~8 text styles |
| 6 | H | B19-Keyboard | 459-468 | `TextInput` for reflection is inside a `ScrollView` but there is no `KeyboardAvoidingView` or `keyboardShouldPersistTaps`. When keyboard opens on the reflection field near the bottom, it will be obscured by the keyboard on iOS. | Text input hidden behind keyboard |
| 7 | M | A4-StatusBar | - | No `<StatusBar>` component rendered. Screen relies on global config which may not match the dark background. | Status bar icons invisible on dark bg |
| 8 | M | C23-Error | 196-199 | `completeMutation` has no `onError` handler. If `islamicApi.completeDailyTask` fails, the user gets no feedback -- the task appears uncompleted with no error message. | Silent mutation failure |
| 9 | M | A2-RTL | - | `isRTL` is never destructured from `useTranslation()`. Arabic text has `textAlign: 'right'` (L614) which is correct for Arabic content, but prayer time grid and card layouts use no RTL-aware `flexDirection` flipping. Header card layouts with `flexDirection: 'row'` won't flip in RTL locales. | RTL layout not adapted |
| 10 | M | C26-Optimistic | 202-205 | `handleCompleteTask` calls `completeMutation.mutate()` but the UI doesn't optimistically update `completedTasks`. The user must wait for the full refetch cycle after `invalidateQueries` before seeing the checkmark. | Laggy feedback on task completion |
| 11 | M | B12-Disabled | 392-397 | The Quran task `Pressable` calls `handleCompleteTask` on press, but if mutation is already pending (user tapped quickly), it fires again since there's no `disabled` prop checking `completeMutation.isPending`. | Multiple simultaneous mutations |
| 12 | M | C30-Concurrent | 123-158 | `playAyahAudio` uses `isPlayingAyah` state to detect toggle, but the state is stale inside the closure. If user taps play rapidly before state updates, multiple Sound instances can be created concurrently without the previous being cleaned up. | Audio overlap / resource leak |
| 13 | L | A7-Typography | 519 | `greeting` uses `fonts.headingBold` but `hijriDate` (L523) and `bonusXPText` (L549) use literal `colors.gold` without theme-aware override in JSX. | Inconsistent theming |
| 14 | L | D31-Entrance | 256-501 | Every card has `FadeInUp` with staggered delays (0, 100, 200, ... 800ms). Total entrance animation is 1.2s (800ms delay + 400ms duration). This is excessive -- users see blank space for nearly a second before bottom content appears. | Sluggish perceived load |
| 15 | L | E44-Haptic | 160-168 | `handlePlayHadith` and `handlePlayDua` use `haptic.navigate()` which is semantically wrong -- these are playback actions, not navigation. Should use `haptic.tick()` or no haptic. | Wrong haptic feedback type |
| 16 | I | C29-Offline | - | No offline handling. If the device is offline, `briefingQuery` will fail and show the EmptyState error, but there's no cached briefing fallback or explicit offline message. | Poor offline experience |
| 17 | I | A6-Spacing | 460 | `reflectionInput` style has redundant `borderColor: colors.dark.border` (L726) that's overridden by the inline `{ borderColor: tc.border }` on L460 -- but the StyleSheet still has the hardcoded dark value. | Dead code / confusion |

---

### Screen: `mosque-finder.tsx` (685 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 18 | C | F46-Reachable | 149 | `MosqueCard` navigates to `'/(screens)/mosque-detail'` but no `mosque-detail.tsx` file exists in `app/(screens)/`. The route will crash with "No route named '/(screens)/mosque-detail'". | Crash on mosque card tap |
| 19 | H | A8-Responsive | 33 | `const { width } = Dimensions.get('window')` is computed once at module load. On tablet rotation or split-screen, `width` is stale. Should use `useWindowDimensions()` hook. | Stale width on rotation / split-screen |
| 20 | H | A3-SafeArea | 284, 300, 320, 340 | Loading/error/empty states use `<SafeAreaView>` but the main FlatList view (L340) also uses `<SafeAreaView>` without `edges` prop -- defaults to all edges, potentially double-insetting bottom on tab-bar screens. The loading/error/empty states also lack `edges` specification. | Inconsistent safe area handling |
| 21 | H | B13-DoubleTap | 197 | Directions button `onPress` calls `openDirections` → `Linking.openURL` with no debounce. Double-tap opens directions twice (two map app launches). | Double external app launch |
| 22 | H | A1-Light | 472-474 | `container` hardcodes `backgroundColor: colors.dark.bg`. While JSX overrides it, `distanceText` (L576), `nextPrayerText` (L592), `qiblaDirection` (L663) use hardcoded `colors.emerald`/`colors.gold` in StyleSheet without theme override. | Non-theme-aware colors |
| 23 | M | C30-Concurrent | 227-261 | `fetchData` calls `Location.requestForegroundPermissionsAsync()` every time (initial + every refresh). Permission request is idempotent but wasteful on refresh. Also, if fetchData is called while already running (double pull-to-refresh), two concurrent fetches race to set state. | Race condition on concurrent refresh |
| 24 | M | A2-RTL | - | Screen doesn't destructure or use `isRTL`. Search bar, mosque cards, and qibla card all use `flexDirection: 'row'` which won't flip for RTL locales. The Qibla arrow direction indicator at L682 (`rotate: '45deg'`) is a fixed rotation that doesn't account for RTL text direction context. | RTL layout not adapted |
| 25 | M | E41-Icon | 108-116 | `FACILITY_ICONS` maps semantic facilities to non-semantic icons: `parking` -> `map-pin`, `wheelchair` -> `check-circle`, `wudu` -> `globe`, `cafe` -> `clock`. These icons don't represent the actual facility at all. | Misleading facility icons |
| 26 | M | D34-LayoutStability | 354-379 | Map height is fixed at 220px. When `userLocation` is null (permission denied initially, then granted on retry), the map suddenly appears, shifting the entire FlatList content down by 220px + margin. | Layout jump when map appears |
| 27 | M | C23-Error | 255-256 | `catch (err)` in `fetchData` catches ALL errors including location timeout, network failure, and API errors -- but shows the same generic `failedToLoadMosques` message for all. Location permission denied has its own handling (L231-235), but location timeout or GPS unavailable falls into the generic catch. | Indistinguishable error messages |
| 28 | L | M-Perf | 441 | `computeQiblaBearing` is called twice in the same render (degrees + direction on L441) for the same coordinates. Should compute once and destructure. | Redundant computation |
| 29 | L | B10-Haptic | 217 | `haptic` is imported and used in `MosqueCard` but only on the directions button. No haptic on the main card press (L149 navigates without haptic). | Missing haptic on navigation |
| 30 | L | A7-Typography | 555, 576, 592, 629 | Several text styles use `fonts.bodySemiBold` while others use inline `fontWeight`. Inconsistent typography approach across the file. | Typography inconsistency |
| 31 | I | E42-BrandedRefresh | 349 | `BrandedRefreshControl` is used but `refreshing` state is manually managed with `useState` instead of using `briefingQuery.isRefetching` from react-query. The manual state can go out of sync. | Manual refresh state management |
| 32 | I | B16-LongPress | 147-210 | `MosqueCard` has no long-press action. Common pattern would be to show quick actions (save mosque, share location, copy address). | Missing long-press affordance |

---

### Screen: `muted.tsx` (210 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 33 | H | B14-Destructive | 138-145 | Unmute uses `Alert.alert` instead of `showToast()` for the confirmation dialog. Per mobile-screens rules, `Alert.alert` should only be used for destructive confirmations, but unmute is NOT destructive -- it restores a relationship. Also, the style is `'destructive'` (L143) which shows red text on iOS for a positive action (unmuting). | Wrong UX pattern for non-destructive action |
| 34 | H | B10-Haptic | - | No `useContextualHaptic` import or usage anywhere. Unmute button, pull-to-refresh, and list interactions have zero haptic feedback. | No haptic feedback on any interaction |
| 35 | H | A1-Light | 181, 188, 202, 209 | StyleSheet hardcodes `colors.dark.bg` (L181), `colors.dark.bgCard` (L188), `colors.text.primary` (L202), `colors.text.secondary` (L209). The `container` bg is overridden in JSX but `name` and `username` styles are NOT overridden -- they use hardcoded dark-mode colors. | Light mode text colors wrong |
| 36 | M | A7-Typography | 202 | `name` style uses `fontWeight: '600'` instead of `fonts.bodySemiBold`. This is inconsistent with the theme's font family approach and may render differently on Android (which resolves weight to a different font file). | fontWeight instead of font family |
| 37 | M | C26-Optimistic | 60-63 | `unmuteMutation.onSuccess` just invalidates queries, causing a full refetch. No optimistic removal of the unmuted user from the list. User must wait for refetch to see the item disappear. | Laggy unmute feedback |
| 38 | M | A2-RTL | 39 | `isRTL` is destructured from `useTranslation()` but never used anywhere in the component. Row layout (`flexDirection: 'row'`) and badge layout won't flip in RTL. | Unused isRTL, RTL layout not adapted |
| 39 | M | B15-Toast | 63 | `unmuteMutation.onError` shows toast with `err.message` which may be a raw server error string (e.g. "Request failed with status code 500"). Should use a user-friendly translated message. | Raw error message shown to user |
| 40 | L | E38-Skeleton | 94-101 | Skeleton rows use `{ backgroundColor: tc.bgCard }` inline style while the `skeletonRow` style already has `backgroundColor: colors.dark.bgCard`. This creates a double-bg situation where the StyleSheet value is ignored but still declared. | Redundant/confusing style override |
| 41 | L | B15-Toast | 62 | `unmuteMutation.onSuccess` doesn't show a success toast. User unmutes someone but gets no confirmation feedback other than the item eventually disappearing after refetch. | No success feedback |
| 42 | I | D31-Entrance | 120 | `FadeInUp.delay(index * 30)` -- with 30ms per item, a list of 50 muted users means the last item animates in 1.5s after the first. Unbounded stagger. | Animation delay grows with list size |

---

### Screen: `mutual-followers.tsx` (312 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 43 | H | A3-SafeArea | 174, 198, 216 | Error, loading, and main views use raw `<View>` instead of `<SafeAreaView>`. While `useSafeAreaInsets` is used for `paddingTop: headerHeight` on the FlatList, the error state (L174) and loading state (L198) containers have no safe area bottom protection. On iPhones with home indicator, content can be cut off at the bottom. | Content behind home indicator |
| 44 | H | B10-Haptic | - | No `useContextualHaptic` imported or used. Follow/unfollow button taps, user row taps, and refresh all have zero haptic feedback. | No haptic on any interaction |
| 45 | H | A1-Light | 280 | `container` hardcodes `backgroundColor: colors.dark.bg`. JSX overrides it but `name` (L301 -- `colors.text.primary`, `fontWeight: '600'`), `handle` (L302 -- `colors.text.secondary`) use hardcoded dark colors in StyleSheet. JSX overrides `name` and `handle` with `tc.text.*` so these are dead-but-confusing. | Hardcoded dark colors in StyleSheet |
| 46 | M | C23-Error | 90-131 | `followMutation` and `unfollowMutation` have no `onError` handler. If follow/unfollow fails, the optimistic cache update persists (showing wrong state) and the user gets no error feedback. The button will show "Following" but the server didn't actually follow. | Silent follow failure + stale optimistic state |
| 47 | M | A7-Typography | 301 | `name` uses `fontWeight: '600'` instead of `fonts.bodySemiBold`. Same Android rendering issue as muted.tsx. | fontWeight instead of font family |
| 48 | M | A2-RTL | 66 | `isRTL` destructured but never used. `nameRow` uses `flexDirection: 'row'` (L300) which doesn't flip. | Unused isRTL, no RTL adaptation |
| 49 | M | B13-DoubleTap | 52-57 | `GradientButton` for follow/unfollow has no `disabled` state while mutation is pending. Rapid taps can fire multiple follow/unfollow mutations concurrently, causing server-side race conditions (follow then immediately unfollow). | Concurrent follow/unfollow mutations |
| 50 | M | C28-BackState | 90-131 | Optimistic updates only target `['mutual-followers', targetUsername]` cache key. Other query caches like `['followers', ...]` or `['following', ...]` or the user's profile won't reflect the follow/unfollow until refetched. | Stale follow state on back navigation |
| 51 | L | D31-Entrance | 37 | `FadeInUp.delay(index * 50)` with no upper bound. A mutual followers list of 100 items means 5s total stagger. | Unbounded animation stagger |
| 52 | L | B15-Toast | - | No toast feedback on follow/unfollow success or failure. User has no confirmation other than the button label changing. | No mutation feedback |
| 53 | I | A6-Spacing | 229 | FlatList `contentContainerStyle` uses `{ paddingTop: headerHeight }` but no horizontal padding, while `row` style has `paddingHorizontal: spacing.base`. This means the empty state and footer skeleton have no horizontal padding. | Inconsistent padding |

---

### Screen: `my-reports.tsx` (249 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 54 | H | A3-SafeArea | 113, 132, 149 | All three views (error, loading, main) use raw `<View>` with `useSafeAreaInsets` for top padding only. No bottom safe area protection. On iPhones with home indicator, the last report card can be partially obscured. | Content behind home indicator |
| 55 | H | A1-Light | 188 | `container` hardcodes `backgroundColor: colors.dark.bg`. StyleSheet `reason` (L223) uses `colors.text.primary`, `target` (L240) uses `colors.text.secondary`, `date` (L244) uses `colors.text.tertiary` -- but JSX overrides these with `tc.*`. However, `distanceText` equivalent styles are not all overridden. Checking: `reason` L92 has `{ color: tc.text.primary }` override, `target` L102 has override, `date` L103 has override. So the StyleSheet colors are dead code but the container bg is overridden. Lower severity than initially assessed but still confusing. | Hardcoded dark colors in StyleSheet (dead code) |
| 56 | M | A7-Typography | 223-234 | `reason` uses `fontWeight: '600'` and `badgeText` uses `fontWeight: '700'` instead of `fonts.bodySemiBold`/`fonts.bodyBold`. Inconsistent with theme font family approach. | fontWeight instead of font family |
| 57 | M | B10-Haptic | 50 | `haptic.tick()` is called on pull-to-refresh which is unconventional -- haptic should fire on user-initiated discrete actions, not on a continuous gesture like pull-to-refresh. And there's no haptic on any other interaction in the screen. | Misplaced haptic usage |
| 58 | M | A2-RTL | - | `isRTL` not destructured. `headerRow` uses `flexDirection: 'row'` (L208) which doesn't flip for RTL. Report reason text is LTR-only. | RTL layout not adapted |
| 59 | M | C23-Error | 87 | `statusColor + '33'` and `statusColor + '1A'` string concatenation for gradient colors. If `getStatusColor` returns a theme-aware color that's not a simple hex string (e.g. `rgb()` or `rgba()`), the concatenation produces an invalid color. Currently works because all returned values are hex, but fragile. | Fragile color concatenation |
| 60 | M | D34-LayoutStability | 118 | Error state renders `<View style={{ height: insets.top + 52 }} />` as a spacer, but loading state (L137) also renders the same spacer. The main FlatList state uses `paddingTop: insets.top + 52 + spacing.md`. The extra `spacing.md` in the main state means content position shifts by 12px when transitioning from loading to loaded. | 12px layout jump on load |
| 61 | L | B11-Press | 80-108 | Report cards are not pressable. Users can see their reports but can't tap to view details or track status updates. Expected: tap to navigate to a report detail screen. | Non-interactive report cards |
| 62 | L | A6-Spacing | 205 | `card` has both `gap: spacing.xs` (L204) and `marginBottom` on child elements (headerRow L210 has `marginBottom: 4`). Mixing gap and margin leads to inconsistent spacing. | Mixed spacing approaches |
| 63 | I | D31-Entrance | 80 | `FadeInUp.delay(index * 50)` -- same unbounded stagger issue. 50 reports = 2.5s total stagger. | Unbounded animation stagger |

---

## Summary

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 2 |
| H (broken feature) | 13 |
| M (error-handling/perf) | 19 |
| L (smell) | 12 |
| I (suggestion) | 5 |
| **Total** | **51** |

**C:2 H:13 M:19 L:12 I:5 Total: 51**

### Critical items requiring immediate attention:
1. **#1** -- `morning-briefing.tsx` API response shape mismatch. The `DailyBriefing` interface expects fields (`hadithOfTheDay`, `ayahOfTheDay`, `duaOfTheDay`, `dhikrChallenge`) that do not exist in the API client's return type. Every access to these nested properties will crash.
2. **#18** -- `mosque-finder.tsx` navigates to `mosque-detail` which doesn't exist as a screen file.
