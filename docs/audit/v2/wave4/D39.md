# D39 Audit: waqf, watch-history, watch-party, whats-new, why-showing

---

### Screen: `waqf.tsx` (292 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | C | C21-RealAPI | 56-67 | Payment flow creates a `PaymentIntent` but never confirms it with Stripe SDK (`confirmPayment`). The `clientSecret` returned by `createPaymentIntent` is discarded. Step 2 records the contribution on the backend regardless of whether payment was actually collected. Money is never charged; backend records phantom contributions. | Users appear to contribute without paying; fund amounts are fictional |
| 2 | H | A1-Light | 261-290 | Styles use `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` (hardcoded dark-mode palette) instead of `tc.text.*` for `infoText`, `fundTitle`, `creatorName`, `fundDesc`, `goalAmount`, `percentText`, `sheetTitle`, `sheetFundName`, `sheetAmountText`, `sheetCurrencyPrefix`, `sheetCustomInput`. These will be invisible or wrong in light mode. | Text unreadable in light theme |
| 3 | H | C23-Error | 81-93 | `fundsQuery` has no `isError` handling. If the query fails, `FlatList` renders `EmptyState` with "no funds" which is misleading -- user thinks there are no funds when the network request actually failed. | Error masquerades as empty data |
| 4 | M | B13-DoubleTap | 135 | Contribute button on each fund card (`<Pressable>`) has no double-tap protection. Rapid taps call `handleOpenContribute` multiple times. The sheet has `GradientButton` with `disabled={isProcessing}` but the card-level button does not guard. | Multiple bottom sheets could open or state race |
| 5 | M | C30-Concurrent | 49-78 | `handleContribute` does two sequential network calls (`createPaymentIntent` + `api.post`) without a transaction. If the second call fails after the first succeeds, a PaymentIntent is created but the contribution is not recorded. No rollback. | Orphaned PaymentIntent; money charged but not recorded |
| 6 | M | C29-Offline | 81 | No offline detection. `fundsQuery` will silently fail and show empty state. No retry button on the empty component. | User on bad network sees "no funds" with no way to retry |
| 7 | M | B19-Keyboard | 227-239 | Custom amount `TextInput` inside `BottomSheet` has no `KeyboardAvoidingView` or scroll accommodation. On smaller screens the input may be hidden behind the keyboard. | Input obscured by keyboard |
| 8 | L | E44-Haptic | 135 | Fund card contribute button uses no haptic feedback on press. Only `handleOpenContribute` triggers `haptic.navigate()` but the `Pressable` itself has no haptic on the press visual feedback. | Inconsistent haptic feel compared to other screens |
| 9 | L | A7-Typography | 267-279 | `fundTitle`, `contributeBtnText`, `raisedAmount` etc. use inline `fontWeight: '600'`/`'700'` instead of `fonts.bodySemiBold`/`fonts.bodyBold` family. Inconsistent with project font system. | Font may not match brand typeface |
| 10 | L | D34-LayoutStability | 102 | `FadeInUp.delay(index * 60)` on every fund card causes visible layout shift as cards animate in sequentially. No fixed-height placeholder before animation. | Cards pop in one by one causing content jump |
| 11 | I | C25-Pagination | 172 | `onEndReached` has no `onEndReachedThreshold` set. Default is very close to bottom, may cause delayed loading of next page. | Pagination may trigger late |

---

### Screen: `watch-history.tsx` (332 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 12 | H | A1-Light | 277-317 | `videoTitle`, `channelName`, `videoStats`, `durationText` use `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` (hardcoded dark palette) instead of `tc.text.*`. In light mode these text colors will be wrong. | Text invisible or wrong contrast in light theme |
| 13 | H | B14-Destructive | 138-158 | Clear history uses `Alert.alert` with destructive action. Project rule says `Alert.alert` is only for destructive confirmations (acceptable here), but the rule also says to use `showToast()` for mutation feedback. The success case after clearing has no toast -- user gets no confirmation that history was actually cleared. | No success feedback after destructive clear action |
| 14 | M | E44-Haptic | 46-107 | `VideoCard` component has no haptic feedback on press. `watch-history.tsx` does not import or use `useContextualHaptic` anywhere. Every pressable should have at minimum a tick haptic. | No tactile feedback on any interaction |
| 15 | M | B13-DoubleTap | 52-57 | `VideoCard` Pressable has no double-tap guard. Rapid taps on a video card will push the same route multiple times. | Duplicate navigation pushes |
| 16 | M | C28-BackState | 147-158 | After `clearWatchHistory()` succeeds, `refetch()` is called but its result is not awaited. If user navigates back quickly, the list may still show stale items. | Stale data briefly visible after clear |
| 17 | M | A8-Responsive | 247 | `headerSpacer: { height: 100 }` is a hardcoded pixel value that assumes GlassHeader height. If header height changes (e.g., large dynamic type), content will overlap or have wrong gap. | Content may overlap header on different devices/font sizes |
| 18 | L | D34-LayoutStability | 51 | `FadeInUp.delay(index * 50)` on each `VideoCard` causes sequential pop-in. Combined with a 100px header spacer, the first visible content animates in noticeably. | Layout instability during initial load |
| 19 | L | A2-RTL | 113 | `isRTL` is destructured from `useTranslation()` but never used anywhere in the component. Dead code. | Unused variable |
| 20 | I | A9-Scroll | 200 | `removeClippedSubviews={true}` is set but no `windowSize` or `maxToRenderPerBatch` optimization. For long history lists this could be improved. | Suboptimal FlatList performance for large lists |

---

### Screen: `watch-party.tsx` (386 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 21 | H | A1-Light | 352-365 | `partyTitle`, `hostName`, `viewerCount`, `createTitle`, `createInput` color use `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` instead of `tc.text.*`. Will be wrong in light mode. | Text colors broken in light theme |
| 22 | H | C25-Pagination | 47-51 | `partiesQuery` uses `useQuery` (not `useInfiniteQuery`) to fetch `/watch-parties`. Returns all parties in a single request with no cursor-based pagination. If there are hundreds of parties, this loads them all at once. | Unbounded response size; performance degrades with scale |
| 23 | H | C23-Error | 107-198 | No error state handling for `partiesQuery.isError`. If the query fails, `parties` is `[]` and `ListEmptyComponent` shows "No watch parties" -- identical to the genuine empty state. User cannot distinguish network error from no data. | Error state indistinguishable from empty state |
| 24 | M | B13-DoubleTap | 253-254 | Create button's `disabled` check prevents double-tap during pending, but the `onPress` does not check `createMutation.isPending` inline -- it relies solely on the `disabled` prop. If the button is re-enabled between renders, a race is possible. Additionally, the join button (line 149-153) and card press (line 119-122) have no double-tap guards. | Duplicate party joins or navigations |
| 25 | M | C29-Offline | 47 | No offline handling. Failed query silently shows empty list. | No indication of network failure |
| 26 | M | C30-Concurrent | 41-45 | Debounce timer uses `useRef<ReturnType<typeof setTimeout>>` but cleanup on unmount only clears the ref if it exists. If the component unmounts during a debounce window, `setDebouncedVideoSearch` fires on an unmounted component. React 18+ handles this gracefully with no-op setState, but it's still a code smell. | Potential setState-on-unmounted warning in older React versions |
| 27 | M | B19-Keyboard | 208-214 | `TextInput` for party name inside `BottomSheet` has no keyboard avoidance. Similarly, the video search input (line 271-279) inside the video picker sheet. | Inputs may be hidden behind keyboard |
| 28 | L | E44-Haptic | 119-122 | Card press uses `haptic.tick()` but join button on same card also uses `haptic.navigate()`. Two different haptics for the same destination is inconsistent. | Inconsistent haptic feedback |
| 29 | L | D34-LayoutStability | 114 | `FadeInUp.delay(index * 60)` on party cards causes sequential animation. | Content shift during load |
| 30 | L | A7-Typography | 348-368 | Multiple styles use inline `fontWeight` instead of `fonts.*` family references. | Inconsistent with brand font system |
| 31 | I | C27-StaleData | 86 | After creating a party, `invalidateQueries` is called but the new party won't appear until refetch completes. No optimistic update. | Brief delay before new party appears in list |

---

### Screen: `whats-new.tsx` (85 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 32 | H | F46-Reachable | 70 | This screen is an orphan. No other file in the codebase navigates to `/(screens)/whats-new`. The `settings.whatsNew` i18n key does not exist in `en.json`. The screen is completely unreachable by any user flow. | Dead screen; no user can ever see it |
| 33 | H | C21-RealAPI | 13-28 | `CHANGELOG` is entirely hardcoded as a static array. No API call, no remote config. When the app updates, the changelog must be manually edited in source code and a new binary deployed. There is no way to add changelog entries without a code change. | Changelog cannot be updated without app release |
| 34 | M | A1-Light | 54 | `backgroundColor: \`${colors.emerald}12\`` -- this appends the string `"12"` to the hex color, resulting in an invalid 8-char hex with `12` as alpha. This is not a valid CSS/RN opacity notation (needs `1F` for ~12% alpha in hex). The intended `12%` opacity actually produces `0x12 = 7%` opacity in hex notation, which may be unintentional, but more critically `colors.emerald` is `#0A7B4F` so `#0A7B4F12` is valid 8-digit hex with 7% alpha. However, this is not using `tc` for theme awareness. | Hardcoded color ignores theme; opacity may differ from intent |
| 35 | M | E39-EmptyState | 41-64 | No empty state handling. If `CHANGELOG` were somehow empty, the screen renders nothing except the header. | Edge case: blank screen with no explanation |
| 36 | L | A9-Scroll | 41 | `ScrollView` has no `showsVerticalScrollIndicator={false}` -- inconsistent with other screens that hide it. Minor visual polish. | Visible scroll indicator inconsistent with design |
| 37 | L | D31-Entrance | 42-63 | Changelog items have `FadeInUp` animations but the `SafeAreaView` container has no entrance animation. Abrupt container appearance followed by animated children. | Slightly jarring entrance |
| 38 | I | A7-Typography | 74-85 | Styles defined with `StyleSheet.create` (non-themed, static) rather than `createStyles(tc)` pattern used by every other audited screen. Color overrides happen inline via `style={[styles.x, { color: tc.text.y }]}`. This works but is inconsistent with the project pattern. | Style pattern inconsistency |

---

### Screen: `why-showing.tsx` (368 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 39 | H | A1-Light | 268-366 | Multiple styles in `createStyles` use `colors.text.primary` (lines 291, 297, 326), `colors.text.secondary` (lines 331, 366), `colors.glass.border` (line 268), `colors.error` (line 350), `colors.active.emerald10` (lines 277, 315), `colors.active.error10` (line 343) instead of theme-aware `tc.*` equivalents. The `postContent`, `sectionTitle`, `reasonLabel`, `actionButtonText`, `actionButtonSecondaryText` will all be wrong in light mode. | Major light-theme text/color breakage across the entire screen |
| 40 | M | E44-Haptic | 110-132 | No haptic feedback anywhere. `handleNotInterested` and `handleSeeLess` are mutation actions that should trigger `haptic.send()` or similar. Neither imports nor uses `useContextualHaptic`. | No tactile feedback on any action button |
| 41 | M | B13-DoubleTap | 216-236 | "Not interested" and "See less" buttons are plain `Pressable` with no disabled state during async operations. Rapid taps will fire multiple API calls. | Duplicate dismiss/report API calls |
| 42 | M | C28-BackState | 115-119 | `handleNotInterested` calls `router.back()` after the API call succeeds, but doesn't invalidate the feed query. The post the user marked as "not interested" will still appear in the feed when they navigate back. | Dismissed post still visible in feed after going back |
| 43 | M | C27-StaleData | 49-108 | Data fetching in `useEffect` with manual `useState` instead of `useQuery`. No cache, no refetch, no stale-while-revalidate. If user navigates away and back, the entire fetch runs again with full loading state. | No caching; unnecessary re-fetches; full loading flash on re-visit |
| 44 | L | D34-LayoutStability | 136-156 | Loading skeleton has `paddingTop: insets.top + 60` but the non-loading view has `paddingTop: insets.top + 60` in ScrollView `contentContainerStyle`. The GlassHeader is positioned absolutely (it uses `position: 'absolute'` internally), so the `60` is a magic number for header height. If header height changes, content overlaps. | Fragile header offset; magic number |
| 45 | L | A2-RTL | 38-47 | `reasonIconMap` is defined inside the component body, recreated on every render. Should be a module-level constant. | Unnecessary object allocation per render |
| 46 | I | C21-RealAPI | 53-55 | `postsApi.getById` is called alongside `feedApi.explainPost` in `Promise.all`, but if `params.postId` is undefined (no postId in URL), both calls are skipped via ternary. However there is no guard or error message for the missing `postId` case -- the screen just shows no post preview and fallback reasons. | Silent degradation when postId is missing |

---

## Summary

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 1 |
| H (broken feature) | 8 |
| M (error-handling/perf) | 14 |
| L (smell) | 10 |
| I (suggestion) | 4 |
| **Total** | **37** |

### Critical/High highlights:
- **C1 (waqf):** Payment never confirmed with Stripe SDK -- contributions are recorded without charging money.
- **H (all 5 screens):** `colors.text.*` used instead of `tc.text.*` -- light theme broken on every screen.
- **H32 (whats-new):** Completely unreachable orphan screen with no navigation path.
- **H22 (watch-party):** No pagination on party list (`useQuery` instead of `useInfiniteQuery`).
- **H23 (watch-party), H3 (waqf):** Error states indistinguishable from empty states.
- **H13 (watch-history):** No success feedback after destructive clear action.

C:1 H:8 M:14 L:10 I:4 Total: 37
