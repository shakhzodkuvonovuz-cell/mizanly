# D32 — Screens Audit: saved, saved-messages, schedule-live, schedule-post, scholar-verification

**Auditor:** Claude Opus 4.6 (1M context)
**Date:** 2026-03-30
**Checklist:** 46 checks (A1-A9, B10-B20, C21-C30, D31-D36, E37-E44, F45-F46)

---

### Screen: `saved.tsx` (550 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | H | A1-Light | 478 | `gridText` uses `colors.text.primary` (hardcoded dark theme) instead of `tc.text.primary`. Same at L495 (`videoTitle`), L496 (`videoChannel`), L497 (`videoDuration`). These 4 styles will be invisible/unreadable in light mode. | Text invisible on light backgrounds |
| 2 | M | A3-SafeArea | 263 | No `SafeAreaView` wrapper and no `useSafeAreaInsets`. The container is a raw `<View>` with `flex:1`. Content behind status bar/notch is only "protected" by `headerSpacer: { height: 100 }` (L471) which is a hardcoded pixel value, not inset-aware. | Content behind notch on devices with large insets |
| 3 | L | A2-RTL | 138 | `isRTL` is destructured from `useTranslation()` but never used anywhere in the component or styles. Dead variable. | Dead code |
| 4 | M | B10-Haptic | - | No `useContextualHaptic` import or usage anywhere. Tab changes, pull-to-refresh completion, navigation taps have zero haptic feedback. | No tactile feedback on any interaction |
| 5 | M | B13-DoubleTap | 303-305 | Grid items (`PostGrid`, `ReelGrid`, `VideoRow`) use `Pressable` with `onPress` but no double-tap guard. Rapid taps push duplicate routes (e.g., `router.push('/(screens)/post/${item.id}')` fires twice). | Duplicate screens pushed on fast tap |
| 6 | H | C27-StaleData | 149 | Query key `['saved-posts']` does not include `activeCollection` (L146). When user navigates with different `collection` params, the cached data from a previous collection is shown. `activeCollection` is computed but never passed to the API call or query key. | Wrong data shown when switching collections |
| 7 | M | C29-Offline | - | No offline handling. If network is unavailable, `useInfiniteQuery` fails silently and the error branch (L229) shows a generic `EmptyState` with icon "flag". No explicit offline detection or messaging. | Poor UX when offline |
| 8 | L | D31-Entrance | 303 | `FadeInUp.delay(Math.min(index, 15) * 40)` means items beyond index 15 all animate simultaneously. This is intentional capping, but the cap is at 600ms total which is fine. No finding here -- noting the pattern is acceptable. | N/A |
| 9 | M | D34-LayoutStability | 307-319 | `ListEmptyComponent` conditionally renders 9 skeleton items (grid) or the empty state. When data loads, the entire layout shifts from skeleton to grid with no smooth transition. | Layout jump when data arrives |
| 10 | L | E44-Haptic | - | Same as B10. No haptic feedback on any user action (tab switch, item tap, pull refresh). | Missing haptic feedback |
| 11 | H | F45-Works | 146 | `activeCollection` is computed from `collectionName` param (L146) but is never passed to any query function. The comment at L180 says "Collection filtering is handled via API query params" but `usersApi.getSavedPosts(pageParam)` only receives `pageParam`, not the collection. The feature is broken -- collection filtering does nothing. | Collection/folder filtering is non-functional |
| 12 | M | C23-Error | 248 | Error state uses inline style `{ flex: 1, justifyContent: 'center' }` instead of a stylesheet entry. Minor but inconsistent with the rest of the screen. | Style inconsistency |
| 13 | L | B17-BrandedRefresh | 299-301 | BrandedRefreshControl is correctly used on all 4 FlatLists. Pass. | N/A |

---

### Screen: `saved-messages.tsx` (270 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 14 | H | A1-Light | 250,252,260,264,265,267 | Multiple styles use `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` (hardcoded dark theme tokens) instead of `tc.text.*`. Affects `infoText`, `searchInput`, `forwardText`, `messageText`, `timeText`, `composeInput`. | Text unreadable in light mode |
| 15 | M | A3-SafeArea | 142 | No `SafeAreaView` and no `useSafeAreaInsets`. The container is a raw `<View style={styles.container}>`. The `GlassHeader` may have internal safe area handling, but the compose bar at the bottom (L205) has no bottom inset padding -- it will be behind the home indicator on notched devices. | Compose bar hidden behind home indicator |
| 16 | M | B13-DoubleTap | 218-219 | Send button has `disabled={!newMessage.trim() || saveMutation.isPending}` which guards against double-submit. Pass on send. However, `onLongPress` at L103 sets `menuItem` which opens a bottom sheet -- rapid long-press could set menuItem multiple times, though BottomSheet visibility toggle makes this safe. Pass. | N/A |
| 17 | C | B14-Destructive | 237-239 | Delete action in BottomSheet has `destructive` prop but no confirmation dialog. Single tap on "Delete" immediately calls `deleteMutation.mutate()`. Saved messages could contain important notes -- accidental deletion with no undo or confirm. | Data loss on accidental tap |
| 18 | M | C25-Pagination | 187 | `onEndReached` calls `messagesQuery.fetchNextPage()` without checking `!messagesQuery.isFetchingNextPage`. This can trigger redundant fetch calls when scrolling fast. | Redundant API calls on fast scroll |
| 19 | M | D31-Entrance | 99 | `FadeInUp.delay(index * 40)` has no cap on `index`. With 100+ messages, the last items have `delay(4000)` -- 4 seconds of delay before they animate in. This will cause items to appear invisible/blank for seconds. | Items invisible for seconds in long lists |
| 20 | L | A2-RTL | 38 | `isRTL` is destructured but only used in one place (L207, compose input `textAlign: 'right'`). The search input (L163) does not get RTL text alignment. | Search input not RTL-aligned |
| 21 | M | C30-Concurrent | 54-62 | `saveMutation` calls `queryClient.invalidateQueries` on success. If user sends multiple messages quickly (before mutation resolves), the `newMessage` state is cleared on each success, but if the second mutation was queued before the first completed, the `newMessage` for the second call captured the old closure value. The mutation uses a closure over `newMessage` (L55). This is safe because `mutationFn` captures `newMessage` at call time. Pass. | N/A |
| 22 | M | B19-Keyboard | 178 | FlatList does not set `keyboardShouldPersistTaps="handled"`. When keyboard is open (from compose bar), tapping a message card requires two taps -- first to dismiss keyboard, second to trigger longPress. | Double-tap needed when keyboard is open |
| 23 | L | E41-Icon | 145 | GlassHeader `leftAction` missing `accessibilityLabel`. Should have `accessibilityLabel: t('accessibility.goBack')`. Same for `rightAction` at L147. | Missing accessibility labels on header actions |
| 24 | M | D34-LayoutStability | 150-157 | The `infoBar` with `LinearGradient` renders immediately. If the info bar height changes between re-renders or is conditionally shown, the list below jumps. Currently always shown, so minimal impact. | Minor layout stability concern |
| 25 | L | C23-Error | - | No error handling UI for failed mutations. `saveMutation` has no `onError` callback -- if save fails, user sees no feedback. `deleteMutation` also has no `onError`. `pinMutation` has no `onError`. | Silent failure on mutation errors |

---

### Screen: `schedule-live.tsx` (502 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 26 | H | A1-Light | 406,410,447,450,469,474,487,493,501 | Extensive use of `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` in styles instead of `tc.text.*`. Affects `inputLabel`, `input`, `thumbnailPlaceholderText`, `thumbnailHint`, `sheetTitle`, `pickerLabel`, `pickerChipText`, `pickerPreviewText`, `uploadText`. | Text unreadable in light mode |
| 27 | M | B13-DoubleTap | 293-298 | `GradientButton` for scheduling has `disabled={!canSchedule}` which checks `scheduleMutation.isPending`. This guards against double-submit. Pass. | N/A |
| 28 | M | C30-Concurrent | 116-137 | `scheduleMutation.mutationFn` sets `uploading` state directly (`setUploading(true/false)`) inside the mutation function. If the mutation is retried or cancelled, `uploading` state can get stuck to `true` (the `onError` handler resets it, but if the component unmounts during upload, the state setter fires on unmounted component). | Potential stuck upload overlay or unmounted state update |
| 29 | M | B19-Keyboard | 186-189 | ScrollView has `keyboardShouldPersistTaps="handled"` which is correct. Pass. | N/A |
| 30 | L | B20-ScrollToInput | 198,221 | When TextInput fields are focused, no `ScrollView` scroll-to-input behavior. The description field (L221) with `multiline` could be behind the keyboard on smaller devices. No `KeyboardAvoidingView` wrapping. | Input hidden behind keyboard on small screens |
| 31 | M | C29-Offline | - | No offline detection. If user fills out the form and taps schedule while offline, the mutation fails with a generic error from `onError`. No pre-check or offline indicator. | Confusing error when offline |
| 32 | L | D36-MicroInteraction | 302-378 | Date picker bottom sheet uses chip-style selectors. Selected state changes color but no scale/bounce animation on chip selection. Functional but could benefit from micro-interaction. | Missing micro-interaction on date picker chips |
| 33 | M | F45-Works | 148-149 | `onSuccess` calls `router.back()` then immediately `router.push('/(screens)/live/${live.id}')`. Calling back() then push() in sequence is a race condition with expo-router -- the push may resolve relative to the current screen before back() completes, or the navigation stack state may be inconsistent. Should use `router.replace()` instead. | Navigation race condition after scheduling |
| 34 | H | C21-RealAPI | 138-143 | `liveApi.create()` is called with `scheduledAt` but the live API endpoint may not exist or may not accept `scheduledAt`. No verification that this endpoint actually works. The `liveApi` service must be checked. | Potentially calling non-existent API endpoint |
| 35 | L | A6-Spacing | 397 | `bodyContent` uses `gap: spacing.lg` for card spacing. Consistent with theme. Pass. | N/A |

---

### Screen: `schedule-post.tsx` (943 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 36 | H | A1-Light | 558,562,581,609,647,657,672,675,720,756,760,784,808,851,855,882,899,923 | Massive use of `colors.text.*` hardcoded dark theme tokens throughout all styles. Every text style in the entire 400+ line stylesheet uses hardcoded color tokens instead of `tc.text.*`. | Entire screen unreadable in light mode |
| 37 | L | A2-RTL | 26 | `isRTL` is destructured but never used anywhere in the component or styles. Dead variable. | Dead code |
| 38 | C | C21-RealAPI | 91-92 | Comment explicitly states "Backend auto-publisher not yet implemented" and "Backend create DTOs may not validate scheduledAt field". The screen sends `scheduledAt` to `postsApi.create()`, `threadsApi.create()`, `reelsApi.create()` but the backend may silently ignore it or reject it. The post gets "created" but never actually published at the scheduled time. | Scheduled posts never publish -- silent data loss |
| 39 | H | F45-Works | 128-133 | For Bakra (reels), `reelsApi.create()` is called with `videoUrl: mediaUrls[0] || ''` and `duration: 0`. An empty string videoUrl will either fail or create a broken reel. Duration of 0 is incorrect. The comment says "Will be set by the video processing pipeline" but there's no evidence of such pipeline. | Broken reel creation with empty URL and zero duration |
| 40 | M | F45-Works | 31 | `selectedDate` is initialized to `now.getDate() + 2`. If today is the 30th of a 31-day month, this becomes 32, which is not a valid date. When constructing `new Date(currentYear, currentMonth, 32, ...)` at L102, JavaScript wraps to the next month silently, but `selectedDate=32` won't match any calendar day visually, so no day appears selected in the grid. | Calendar shows no selection on month boundaries |
| 41 | M | F45-Works | 71-76 | `getNextWeekend()` returns `d.getDate()` after adding days. If the resulting date is in the next month, it returns a day number that doesn't exist in `currentMonth`. Tapping "This Weekend" sets `selectedDate` to a day in the wrong month (e.g., day 2 when the calendar shows March but the weekend is April 2). | Quick date buttons broken at month boundaries |
| 42 | M | F45-Works | 77-80 | Same issue with `getNextWeek()`. Returns `d.getDate()` without checking if the month changed. Day 7 in next month vs day 7 in current month. | Quick date "Next Week" broken at month boundaries |
| 43 | M | B10-Haptic | 95 | `haptic.send()` is called in `handleSchedule` but no haptic on calendar day selection, hour/minute selection, AM/PM toggle, quick date selection, or month navigation. | Inconsistent haptic usage |
| 44 | M | B19-Keyboard | 162 | ScrollView does not set `keyboardShouldPersistTaps="handled"`. The screen doesn't have text inputs, but tapping calendar items while the soft keyboard from a previous screen is still dismissing could cause issues. Minor. | N/A (no inputs on this screen) |
| 45 | L | D34-LayoutStability | 162-163 | ScrollView has no `contentContainerStyle` with bottom padding defined in the ScrollView itself. Bottom spacing is handled by a `<View style={styles.bottomSpacing} />` (L490) with `height: 100`. The absolute-positioned bottom bar (L494, `position: 'absolute', bottom: 0`) overlaps scroll content. The 100px spacer may not be enough on all devices. | Content may be hidden behind bottom bar |
| 46 | M | A8-Responsive | 665 | Calendar day width is `100/7%` which is correct for 7-column grid. But `aspectRatio: 1` makes each cell a square, and on very narrow screens (e.g., iPhone SE at 320pt), days could be 45px squares which is tight for tap targets (minimum 44pt recommended). Borderline pass. | Tight tap targets on narrow screens |
| 47 | L | B12-Disabled | 502 | Schedule button has `disabled={isScheduling}` but no visual disabled state beyond the gradient appearing normal. When scheduling, an `ActivityIndicator` replaces content which is good, but the button doesn't dim or change opacity when disabled. | No visual disabled state on schedule button |
| 48 | H | A4-StatusBar | 159 | No status bar style configuration. The screen uses dark gradients with light text, but the status bar text color is not explicitly set. On light mode, status bar text could be dark on dark gradient background. | Status bar text potentially invisible |
| 49 | L | C23-Error | 111 | `try { mediaUrls = params.mediaUrls ? JSON.parse(params.mediaUrls) : []; } catch { /* malformed param */ }` -- error is silently swallowed. If params are malformed, mediaUrls stays `[]` and the post is created without media, losing the user's attached media. | Silent media loss on malformed params |

---

### Screen: `scholar-verification.tsx` (650 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 50 | H | A1-Light | 464,473,490,493,508,546,599,612,617,642 | Styles use `colors.text.primary`, `colors.text.secondary`, `colors.text.tertiary` instead of `tc.text.*`. Affects `fieldLabel`, `textInput`, `pickerText`, `pickerPlaceholder`, `documentText`, `sectionTitle`, `statusLabel`, `infoLabel`, `infoValue`, `badgePreviewDesc`. | Text unreadable in light mode |
| 51 | L | A2-RTL | - | No `isRTL` usage. TextInput fields have no RTL text alignment. For Arabic/Urdu speakers filling in institution names, text direction could be incorrect. | RTL text input not handled |
| 52 | M | B13-DoubleTap | 375 | "Add Document" button (`Pressable` at L375) has no debounce. Currently it just shows a toast (placeholder), but when real document picker is implemented, rapid taps could open multiple pickers. | Potential double document picker |
| 53 | M | C23-Error | 153-161 | `fetchStatus` catch block (L156-158) silently swallows ALL errors with comment "no existing application". This assumes any error means no application exists, but network errors, auth errors, server 500s are all silently eaten. The user sees the application form when they might actually have an existing application that failed to load. | Existing application invisible on network error |
| 54 | M | C23-Error | 207 | `handleSubmit` catch block (L207) shows `t('scholar.alreadyApplied')` for ALL errors, not just duplicate application errors. A network error or validation error would incorrectly show "already applied" message. | Wrong error message for non-duplicate errors |
| 55 | L | B10-Haptic | 187-212 | `handleSubmit` calls `haptic.success()` on success but no haptic on error. `handleAddDocument` calls `haptic.tick()`. Document removal calls `haptic.tick()`. Bottom sheet selections call `haptic.tick()`. Reasonable coverage but no error haptic. | No error haptic feedback |
| 56 | M | D34-LayoutStability | 214-226 | Skeleton loading state (L214-226) shows 3 rects + 1 tall rect. When the actual form renders, it has completely different layout (fields, pickers, document list). The jump from skeleton to form is jarring. | Layout shift from skeleton to form |
| 57 | L | E39-EmptyState | - | No empty state for when verification is rejected. The `StatusTracker` shows "rejected" state visually, but there's no guidance text or "re-apply" action for the user. They see the rejection but can't do anything about it. | No actionable UX for rejected applications |
| 58 | M | B19-Keyboard | 300,313 | ScrollView wrapping the form (L300) does not set `keyboardShouldPersistTaps="handled"`. When typing in the institution field and the keyboard is open, tapping the specialization picker requires two taps. | Double-tap needed to dismiss keyboard and select |
| 59 | L | F46-Reachable | 34-47 | `SPECIALIZATIONS` and `MADHABS` arrays have hardcoded English labels (e.g., `'Fiqh'`, `'Hanafi'`) instead of using `t()` translation keys. These labels are not localized for the 8 supported languages. | Hardcoded English labels not translated |
| 60 | L | A6-Spacing | 453 | `contentContainer` uses `paddingBottom: spacing.xxl`. Need to verify `spacing.xxl` exists in theme -- standard spacing tokens are xs, sm, md, base, lg, xl, 2xl. If `xxl` is not defined, this is `undefined` which becomes 0, and form content may be cut off. | Potentially missing bottom padding |
| 61 | M | C29-Offline | - | No offline handling. The form can be filled out and submitted offline, which will fail with the generic "already applied" error message (see finding 54). No save-as-draft or offline indicator. | Misleading error when submitting offline |
| 62 | L | E41-Icon | - | No `accessibilityLabel` on any Pressable in the form (specialization picker L326, madhab picker L344, add document L375, remove document L369). Screen readers cannot identify these controls. | Missing accessibility labels |

---

## Summary

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 2 |
| H (broken feature) | 7 |
| M (error-handling/perf) | 18 |
| L (smell/minor) | 14 |
| I (suggestion) | 0 |
| **Total** | **41** |

### Critical/High Findings Requiring Immediate Attention

1. **C38 (schedule-post L91):** Backend auto-publisher not implemented. Scheduled posts are created but NEVER published. Silent data loss.
2. **C17 (saved-messages L237):** Delete with no confirmation dialog. One tap permanently deletes saved notes.
3. **H36 (schedule-post):** Entire screen uses hardcoded dark-only color tokens (18+ occurrences). Completely broken in light mode.
4. **H26 (schedule-live):** Same light-mode breakage (9+ occurrences).
5. **H50 (scholar-verification):** Same light-mode breakage (10+ occurrences).
6. **H14 (saved-messages):** Same light-mode breakage (6+ occurrences).
7. **H1 (saved):** Same light-mode breakage (4 occurrences).
8. **H6/H11 (saved):** Collection filtering is computed but never wired to API calls -- feature is non-functional.
9. **H39 (schedule-post L128):** Reel scheduling sends empty videoUrl and duration=0.

C:2 H:7 M:18 L:14 I:0 Total: 41
