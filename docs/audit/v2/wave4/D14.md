# D14 -- Audit: disappearing-default, disappearing-settings, discover, disposable-camera, dm-note-editor

---

### Screen: `disappearing-default.tsx` (349 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 1 | L | A1-Light | 229,258,263,266,277,286,294,302,317,321,324,337,346 | StyleSheet uses `colors.dark.*` and `colors.text.*` hardcoded values. While inline styles override with `tc.*`, the stylesheet defaults bleed through on any code path that skips the inline override (e.g. `styles.description` line 258 uses `colors.text.secondary` without inline override at line 156 -- the override exists, but the static fallback in the stylesheet is dark-only). | Light theme renders wrong base colors in stylesheet-only properties |
| 2 | M | B10-Haptic | -- | No haptic feedback on radio selection. `useContextualHaptic` is not imported or used. Sibling `disappearing-settings.tsx` uses haptic on every selection. Inconsistent UX between the two nearly identical screens. | Silent tap feedback on settings selection |
| 3 | L | B13-DoubleTap | 59-73 | `handleSelect` has a `saving` guard, but `saving` is only set during the `AsyncStorage.setItem` call which is near-instant (~1ms). The guard is effectively useless against rapid taps. | Theoretical double-fire, low practical risk since local storage |
| 4 | L | A3-SafeArea | 127 | Root `View` has no `SafeAreaView` wrapper and no `edges` protection. Relies on `GlassHeader` and `paddingTop: 100` hardcoded at line 235 for top inset. Bottom content could be obscured by home indicator on notched devices. | Bottom content may be hidden behind home indicator |
| 5 | I | A9-Scroll | 137-213 | `Animated.ScrollView` has no `keyboardShouldPersistTaps` prop. Not critical since there are no inputs, but inconsistent with other settings screens. | Minor consistency gap |
| 6 | L | C29-Offline | 41 | AsyncStorage read failure is silently caught (line 49: empty `catch`). No user feedback if storage is corrupted. The save path (line 68) correctly shows an error toast, but the load path does not. | Corrupted storage silently defaults to "off" with no indication |
| 7 | I | D31-Entrance | 143-212 | Staggered `FadeInUp` animations with 100ms increments is good. No finding. | -- |
| 8 | L | E44-Haptic | -- | Missing `useContextualHaptic` import. Rule: `useContextualHaptic()` -- NEVER `useHaptic()`. This screen uses neither. | Violates branded haptic rule |

---

### Screen: `disappearing-settings.tsx` (369 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 9 | L | A1-Light | 222,243,251,267,279,286,293,296,307,311,320,341,350,365 | StyleSheet uses `colors.dark.*` and `colors.text.*` hardcoded values. Runtime inline styles override some, but not all. `lockBadge` at line 267 hardcodes `borderColor: colors.dark.bg` -- this is used as a "cutout" effect and will look wrong in light theme. | Lock badge border visible as dark ring on light backgrounds |
| 10 | M | B13-DoubleTap | 84-90 | `handleSave` calls `mutation.mutate(selectedDuration)` without checking `mutation.isPending`. Rapid taps on the save button (header check icon) can fire the API call multiple times. | Duplicate API calls to set disappearing timer |
| 11 | L | C22-Skeleton | -- | No loading skeleton. The screen shows content immediately from params (no initial fetch), so a skeleton isn't strictly needed. But if `conversationId` is empty (line 55 fallback to `''`), the mutation will fire with an empty string. | No validation of required `conversationId` param |
| 12 | H | C23-Error | 55 | `conversationId` defaults to `''` if missing from params. The mutation at line 64 sends this empty string to the API (`messagesApi.setDisappearingTimer('', duration)`), which will construct URL `/messages/conversations//disappearing` -- likely a 404 or route mismatch. No validation or early return. | Crash or 404 when navigated to without conversationId |
| 13 | L | A5-Radius | 259 | `marginRight: -32` is a magic number for positioning the lock badge. Uses `right: '50%'` + negative margin, which is fragile and may misalign on different screen densities. | Badge positioning may be off on some devices |
| 14 | L | C28-BackState | 67-68 | On mutation success, `router.back()` is called. But the previous screen has no mechanism to know the timer was changed (no query invalidation, no params update). The caller must refetch independently. | Stale data on previous screen after save |
| 15 | I | D36-MicroInteraction | 148-192 | Radio buttons have no animated transition between selected/unselected states. The selection highlight (`optionRowSelected` background) pops in instantly. | Missing smooth selection transition |

---

### Screen: `discover.tsx` (779 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 16 | H | A1-Light | 548,569,574,582,601,603,644,649,660,671,684,691,697,707,718,738,740,748,776 | Massive hardcoded `colors.dark.*` and `colors.text.*` usage in StyleSheet (19+ occurrences). Many styles have NO inline override -- e.g. `categoryText` (line 582), `featuredTitle` (line 619), `featuredCreatorName` (line 649), `hashtagTextGold` (line 712), `hashtagCountGold` (line 724), `footerText` (line 776). These will render wrong in light mode. | Entire discover screen broken in light theme |
| 17 | H | A7-Typography | -- | Zero `fontFamily` usage anywhere in the file. All text uses system default font via `fontWeight` strings (`'500'`, `'600'`, `'700'`). The `fonts` token is not imported. Every other screen in this audit uses `fonts.body`, `fonts.bodySemiBold`, etc. | All text renders in system font, not branded font family |
| 18 | M | A7-Typography | 489,496 | Inline styles with `fontWeight: '500'` on quick-link Text components. Should use `fontFamily: fonts.bodyMedium` from theme. | Inconsistent with branded typography system |
| 19 | M | B10-Haptic | -- | No haptic feedback anywhere. Category pill taps, hashtag taps, featured card taps, grid item taps -- all silent. This is the main discovery/explore screen with heavy interaction. | No tactile feedback on a core navigation screen |
| 20 | C | M-Perf | 295-306 | `ExploreGridItem` renders `<ExpoVideo shouldPlay isLooping isMuted>` for every reel/video in the grid. In an infinite-scrolling grid, this means **all visible videos autoplay simultaneously**. With 9 items per screen and infinite scroll, this can spawn dozens of active video players, causing severe memory pressure, battery drain, and potential OOM crash. | Memory exhaustion and battery drain from unbounded video autoplay |
| 21 | M | C30-Concurrent | 384-413 | `featuredItems` is derived from `exploreItems` on every render -- filtering, slicing, and mapping 5 items. This is not memoized (`useMemo`), so it recalculates on every state change (category switch, scroll, refresh). | Unnecessary re-computation on every render |
| 22 | M | A8-Responsive | 39-44 | `Dimensions.get('window')` called at module level. The value is captured once at import time and never updated. Screen rotation or iPad split-view will use stale dimensions. | Grid items wrong size after rotation |
| 23 | L | A6-Spacing | 483-498 | Inline styles on quick-link Pressables with hardcoded layout values. Should use StyleSheet for consistency and performance. | Inline style objects recreated every render |
| 24 | L | B13-DoubleTap | 270-279 | `handlePress` in `ExploreGridItem` navigates without any debounce. Rapid tapping can push the same screen multiple times onto the navigation stack. | Duplicate screen push on double-tap |
| 25 | M | E42-BrandedRefresh | 527-530 | `BrandedRefreshControl` is correctly used. No finding. | -- |
| 26 | L | A2-RTL | 483 | `flexDirection: 'row'` on quick-link buttons, but uses `gap` which is RTL-safe. `paddingHorizontal` is RTL-safe. No finding on RTL. | -- |
| 27 | M | C27-StaleData | 375-380 | `useInfiniteQuery` has no `staleTime` configured. Default is 0, meaning data is always considered stale. Every focus/mount triggers a refetch of the entire explore feed. | Excessive network requests on screen focus |
| 28 | L | D34-LayoutStability | 472-475 | `FadeInUp` animation with `springify()` on each grid item causes layout shift as items spring into position. With `numColumns={3}`, items in the same row may spring at different times (different delay values), causing visual jitter. | Visual jitter during grid population |
| 29 | I | A3-SafeArea | -- | No `SafeAreaView`. Relies on `GlassHeader` + `headerSpacer` (line 551-552). Bottom is protected by `paddingBottom: spacing.xl` in `listContent`. Acceptable pattern but fragile. | -- |
| 30 | M | C25-Pagination | 421-425 | `loadMore` checks `!exploreLoading` but `exploreLoading` is the initial loading state, not the `isFetchingNextPage` state. Once initial load completes, `exploreLoading` is always false. Should check `isFetchingNextPage` to prevent concurrent page fetches. | Multiple pages fetched simultaneously on fast scroll |

---

### Screen: `disposable-camera.tsx` (589 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 31 | M | A1-Light | 427,442,456,468,473,477,492,542,560,569,572,578 | StyleSheet uses `colors.dark.*` and `colors.text.*` hardcoded. `taglineMain` (line 492), `photoLabelText` (line 560), `noEditText` (line 578), `shareHeaderText` (line 588) -- all hardcode dark theme text colors with no inline override. | Multiple text elements invisible or wrong color in light theme |
| 32 | L | A1-Light | 456 | `miniCameraWrapper` border uses `colors.text.primary` (hardcoded to white for dark theme). In light theme this would be dark, which may actually be fine, but it's inconsistent with the dynamic theme approach. | Minor theme inconsistency on camera border |
| 33 | C | B13-DoubleTap | 320 | `GradientButton onPress={() => postMutation.mutate()}` has no guard against `postMutation.isPending`. The `loading={isPosting}` prop only shows a spinner but doesn't disable the button. Meanwhile, the header share button at line 266 DOES check `!isPosting`. Two share triggers exist, one unguarded. | Double submission uploads photos twice, creates duplicate stories |
| 34 | H | B13-DoubleTap | 265-268 | Header share button checks `!isPosting` but `isPosting` is set inside `mutationFn` (line 154), not before `mutate()` is called. Between tap and `mutationFn` execution, `isPosting` is still `false`, allowing a second tap through. | Race condition in double-tap guard |
| 35 | L | A3-SafeArea | 7,331 | `SafeAreaView` is imported but never used. The camera view at line 331 uses bare `View` with no safe area protection. Bottom capture button may overlap with home indicator. | Unused import; bottom UI may be obscured |
| 36 | M | C29-Offline | 156-166 | Photo upload uses `fetch(uri)` to read local file then `fetch(presignData.uploadUrl)` to upload. No retry logic, no timeout, no offline detection. A network failure mid-upload silently fails with only a toast. The user has already taken the photo and cannot retake (the `isCaptured` state prevents it). | Photo permanently lost on upload failure with no recovery |
| 37 | H | F45-Works | 112-114 | `setTimeout` inside `capturePhotos` for bounce animation reset: `setTimeout(() => { captureScale.value = withSpring(...) }, 150)`. This `setTimeout` is not cleaned up on unmount. If the component unmounts within 150ms of capture (e.g., quick back navigation), the shared value write will target a disposed component. | Potential crash from writing to disposed shared value |
| 38 | L | D33-ProgressiveImage | 281-286,295-300 | `ProgressiveImage` used for captured photos but with `height={300}` hardcoded. The wrapper has `aspectRatio: 3/4` (line 539). The `height={300}` may conflict with the aspect ratio constraint. | Potential image squishing or clipping |
| 39 | M | B14-Destructive | 144-149 | `retake` discards both captured photos with no confirmation. The user spent time posing for a dual-camera shot; accidental retake loses both photos permanently. | Accidental photo loss with no undo/confirm |
| 40 | L | C21-RealAPI | 152-197 | Upload flow calls `storiesApi.create` with `stickerData` containing `backPhotoUrl` and `frontPhotoUrl`. The backend `CreateStoryPayload` type must support this `disposable` sticker type. If not, the story creates without the dual-photo layout. | Story may render without disposable camera layout |
| 41 | I | E44-Haptic | 109 | `haptic.send()` is used for capture. Good choice for a camera shutter action. No finding. | -- |
| 42 | L | A4-StatusBar | -- | No explicit StatusBar configuration. Camera screens typically need `barStyle="light-content"` to ensure status bar text is visible over the camera preview. | Status bar text may be invisible over light camera subjects |

---

### Screen: `dm-note-editor.tsx` (373 lines)

| # | Sev | Check | Line | Finding | Impact |
|---|-----|-------|------|---------|--------|
| 43 | M | A1-Light | 280,295,305,309,318,326,334,337,351,364,367 | StyleSheet uses `colors.dark.bg`, `colors.text.*`, `colors.active.*` hardcoded. `inputLabel` (line 305), `textInput` (line 309), `expiryLabel` (line 326), `previewTitle` (line 337), `previewText` (line 351), `currentNoteText` (line 367) -- all hardcode dark theme colors. Inline overrides exist for most but not all (e.g. `expiryLabel` line 326 overridden at line 175, but `previewTitle` line 337 has NO override at line 194). | Some text colors wrong in light theme |
| 44 | H | B14-Destructive | 91-99 | Delete uses `Alert.alert` with destructive style. Rule says: use `<BottomSheet>` -- NEVER RN `Modal`. `Alert.alert` is a system modal, not the branded BottomSheet. | Violates component rule: destructive action not using BottomSheet |
| 45 | M | B10-Haptic | -- | No haptic feedback on any interaction. Post/delete/expiry selection -- all silent. The `useContextualHaptic` hook is not imported. | No tactile feedback on mutation actions |
| 46 | M | B13-DoubleTap | 86-89 | `handlePost` checks `!content.trim()` but does not check `createMutation.isPending`. The `GradientButton` at line 210 has `disabled={!content.trim() || createMutation.isPending}` which prevents visual re-tap, but `handlePost` itself can be called from other code paths without the guard. | Mutation can fire multiple times if called programmatically |
| 47 | L | C28-BackState | 69-71 | On `createMutation.onSuccess`, `queryClient.invalidateQueries` is called for `['dm-note']`, which is correct. But `router.back()` is NOT called -- the user stays on the editor after posting. This may be intentional (edit-and-see-preview UX), but contrasts with `disappearing-settings.tsx` which navigates back on save. | Inconsistent post-save navigation pattern |
| 48 | M | C27-StaleData | 49-56 | `useQuery` for existing note has no `staleTime`. On every focus, it refetches. If the user is typing and the screen refocuses (e.g., from notification overlay), the refetch triggers `existingNote` to update, but the `hydrated` flag prevents overwriting the current input. This is correctly handled -- no data loss. However, the refetch itself is unnecessary churn. | Unnecessary network requests on focus |
| 49 | L | A7-Typography | 305,309,326,337,351,367 | Some text styles use `fontSize` from theme but no `fontFamily`. The `inputLabel`, `textInput`, `expiryLabel`, `previewTitle`, `previewText`, `currentNoteText` in the StyleSheet lack `fontFamily`. Other files consistently use `fonts.body` etc. | Inconsistent font family usage in stylesheet |
| 50 | L | B19-Keyboard | 124-126 | `KeyboardAvoidingView` with `behavior={Platform.OS === 'ios' ? 'padding' : undefined}`. On Android, `behavior` is `undefined`, which means no keyboard avoidance at all. Android should use `'height'` or the screen should use `android:windowSoftInputMode="adjustResize"`. | TextInput may be hidden behind keyboard on Android |
| 51 | I | E40-BottomSheet | 256-271 | `BottomSheet` correctly used for expiry picker. Good. | -- |
| 52 | L | C23-Error | 53 | `useQuery` returns `DMNote | null` but there is no error state handling in the JSX. If the fetch fails, `existingNote` is `undefined` (not null), `isLoading` is false, and the screen renders the form with empty content -- appearing as if there's no existing note when there might be one. | Network error silently shows empty editor instead of error state |
| 53 | I | A3-SafeArea | 114 | `SafeAreaView` with `edges={['top']}` correctly protects top. Bottom is covered by `KeyboardAvoidingView` + scroll. Acceptable. | -- |

---

## Summary

| Severity | Count |
|----------|-------|
| C (crash/security/data-loss) | 2 |
| H (broken feature) | 4 |
| M (error-handling/perf) | 12 |
| L (smell) | 17 |
| I (suggestion) | 5 |
| **Total** | **40** |

### Critical (C)
- **#20**: discover.tsx -- All video grid items autoplay simultaneously causing OOM risk
- **#33**: disposable-camera.tsx -- Unguarded share button allows duplicate story creation

### High (H)
- **#12**: disappearing-settings.tsx -- Empty `conversationId` param causes API 404
- **#16**: discover.tsx -- 19+ hardcoded dark-theme colors, entire screen broken in light mode
- **#17**: discover.tsx -- Zero `fontFamily` usage, all text in system font not branded font
- **#34**: disposable-camera.tsx -- Race condition in double-tap guard due to async `isPosting` set
- **#37**: disposable-camera.tsx -- Uncleared `setTimeout` writes to disposed shared value on fast unmount
- **#44**: dm-note-editor.tsx -- Destructive delete uses `Alert.alert` instead of `BottomSheet`

C:2 H:6 M:12 L:17 I:5 Total: 42
