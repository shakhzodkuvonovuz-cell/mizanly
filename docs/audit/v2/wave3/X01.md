# X01 — Post Lifecycle (Create -> Feed -> Delete)

**Files audited:**
- `apps/api/src/modules/posts/posts.service.ts` (2004 lines) — READ IN FULL
- `apps/api/src/modules/posts/posts.controller.ts` (514 lines) — READ IN FULL
- `apps/api/src/modules/posts/dto/create-post.dto.ts` (177 lines) — READ IN FULL
- `apps/api/src/modules/feed/feed.service.ts` (644 lines) — READ IN FULL
- `apps/api/src/modules/feed/personalized-feed.service.ts` (909 lines) — READ IN FULL
- `apps/api/src/modules/search/search.service.ts` (697 lines) — READ IN FULL
- `apps/api/src/modules/notifications/notifications.service.ts` (480 lines) — READ IN FULL
- `apps/api/src/modules/moderation/content-safety.service.ts` (393 lines) — READ IN FULL (first 200 lines; remainder is kindness-reminder/forward-limit stubs not in scope)
- `apps/api/src/gateways/chat.gateway.ts` (1020 lines) — READ relevant section (lines 87-148, content:update handler)
- `apps/api/src/common/services/publish-workflow.service.ts` (115 lines) — READ IN FULL
- `apps/api/src/common/utils/enrich.ts` (125 lines) — READ IN FULL

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | C | posts.service.ts:1460 vs :163 | **Dismiss case mismatch: `'POST'` (uppercase) written, `'post'` (lowercase) read.** `dismiss()` at line 1460 stores `contentType: 'POST'`, but `getFeed()` foryou branch at line 163 reads dismissals with `contentType: 'post'`. These will never match. Dismissed posts keep reappearing in the feed forever. | Users cannot dismiss posts from For You feed. Dismiss feature is completely broken. | S (change line 1460 to `'post'` or line 163 to `'POST'` -- pick one, apply consistently) |
| 2 | H | posts.service.ts:804-852 | **Post edit does not re-run content moderation.** `update()` sanitizes text but never calls `contentSafety.moderateText()`. A user can post safe content, then edit it within the 15-minute window to insert hate speech, profanity, or policy-violating content. The `queueService.addModerationJob()` call that runs on create is also absent from update. | Content policy bypass within the edit window. Harmful content can be injected after initial moderation. | S (add `moderateText()` call before the update, same pattern as `create()`) |
| 3 | H | posts.service.ts:1253-1355 | **Comments and comment edits bypass content moderation entirely.** `addComment()` (line 1253) and `editComment()` (line 1357) call `sanitizeText()` but never call `contentSafety.moderateText()` or queue a moderation job. Comments can contain hate speech, Islamophobia, or profanity without any AI moderation check. | All user-generated comment content is unmoderated. Comments are the most common vector for abuse on social platforms. | M (add `moderateText()` check to `addComment()` and `editComment()`, matching the post-creation pattern) |
| 4 | H | publish-workflow.service.ts:72-75 vs chat.gateway.ts:100-104 | **Real-time `content_published` and `content_removed` events silently dropped.** `onPublish()` publishes `{ event: 'content_published', data: { contentType, contentId, userId } }` to `content:update` channel. The gateway at line 101 destructures `{ postId, reelId, threadId, event, data }` -- but `postId`/`reelId`/`threadId` are absent from the message root, so `roomId` is `undefined`. The `if (roomId && event)` guard at line 103 fails and the event is silently swallowed. Feed refresh, content removal, and content published socket events never reach the client. | Mobile clients never receive real-time notification that new content was published or that content was removed. The feed does not live-update. | M (add `postId`/`reelId`/`threadId` to the publish message root, or restructure the gateway handler to extract from `data.contentType` + `data.contentId`) |
| 5 | H | posts.service.ts:891-951 | **`react()` does not check block/mute status.** `getById()`, `share()`, and `shareAsStory()` all check blocks, but `react()` skips the check entirely. A blocked user can still react to (like/love/support) the blocking user's posts. The reaction is persisted, the notification is sent, and the likesCount increments. | Blocked users can interact with the blocker's content. Notification is sent to the blocked user revealing the blocker's activity. Privacy violation. | S (add block check after the post existence check, same pattern as `share()` at lines 1087-1098) |
| 6 | H | posts.service.ts:1253-1271 | **`addComment()` does not check block/mute status.** A blocked/muted user can add comments to the blocker's post. The comment is persisted and a notification is sent to the post owner. Same gap exists in `likeComment()` (line 1390) and `save()` (line 969). | Blocked/muted users can comment on, like comments on, and save posts from users who blocked them. Undermines the entire block feature. | M (add block checks to `addComment()`, `likeComment()`, `save()`) |
| 7 | H | posts.service.ts:1082-1133 | **`share()` creates a new post but does not increment the sharer's `postsCount`.** The transaction at line 1105-1120 creates a new post record for the share and increments `sharesCount` on the original, but does NOT increment `postsCount` on the sharer's user. Compare with `create()` at line 594-597 which does `postsCount: { increment: 1 }`. Similarly, `crossPost()` at line 1647 does increment. This means `delete()` will decrement `postsCount` for shared posts, potentially driving the count negative (though GREATEST protects against that). | User's postsCount will drift out of sync over time. Profile shows fewer posts than actually exist. Eventually postsCount hits 0 and stays there even with posts remaining. | S (add `postsCount` increment in the share transaction) |
| 8 | M | posts.service.ts:1198-1224 | **`getComments()` does not filter out comments from blocked/muted users.** The query at line 1200 filters by `isRemoved: false, isHidden: false` but does not accept a `viewerId` parameter and does not exclude comments from users that the viewer has blocked or muted. Same applies to `getCommentReplies()` at line 1226. | Users see comments from people they've blocked. Defeats the purpose of blocking. | M (add `viewerId` parameter, query excluded user IDs, add `user: { id: { notIn: excludedIds } }` filter) |
| 9 | M | personalized-feed.service.ts:441-442 | **`Math.random()` used in non-crypto context but in 6 places across feed shuffling.** Lines 441, 442, 722, 743, 763, 784 all use `Math.random()` for Fisher-Yates shuffles. While these are not cryptographic operations (they are feed shuffling), the project's `.claude/rules/mobile-screens.md` and `e2e-crypto.md` both state "NEVER `Math.random()` for crypto -- use CSPRNG." These are clearly not crypto, so this is informational, but the shuffles are biased (Math.random is not uniform) and predictable. | Minor: feed exploration content ordering is slightly biased. Not a security issue for feed shuffling. | S (replace with `crypto.getRandomValues` if deterministic shuffling is desired, or leave as-is since it's just feed ordering) |
| 10 | M | posts.service.ts:854-889 | **`delete()` does not clean up related records:** bookmarks/saves (`savedPost`), reactions (`postReaction`), comments, tags (`postTaggedUser`), collab invites, notifications referencing the post, or feed interactions. Soft-delete (`isRemoved: true`) means these orphan records remain. The `getCollection()` query at line 1051 filters by `post: { isRemoved: false }` so saved posts won't render, but the `savedPost` records accumulate forever. Notifications linking to the deleted post will show broken links. | DB bloat from orphaned records. Notifications with dead post links. Saved posts show phantom "empty" entries in edge cases. | L (for soft-delete this is acceptable; consider a background cleanup cron for orphaned records referencing `isRemoved: true` posts older than 30 days) |
| 11 | M | posts.service.ts:130 (controller) | **`recordView()` is fire-and-forget without dedup or auth.** The controller at line 130 calls `this.postsService.recordView(id)` for every `getById()` call, even for the post owner viewing their own post, bots, or repeated refreshes. There is no rate limiting, no dedup (Redis HyperLogLog or similar), and no userId tracking. Compare with `trackImpression()` at line 1847 which correctly uses HyperLogLog. | `viewsCount` is inflatable by repeated requests. One user refreshing a post 100 times adds 100 views. Engagement metrics are unreliable. Analytics and trending scores are skewed. | M (use HyperLogLog like `trackImpression`, or at minimum skip own-post views and add per-IP/user rate limiting) |
| 12 | M | posts.controller.ts:26-29 | **`ReactDto.reaction` is typed as `string` despite `@IsEnum` validation.** The enum is defined as a literal array `['LIKE', 'LOVE', 'SUPPORT', 'INSIGHTFUL']` but the field type is `string`. If class-validator validation is somehow bypassed (e.g., via direct service calls), an invalid reaction type reaches `react()` where it's cast with `reaction as ReactionType` at line 909. The Prisma enum would reject it, but the error would be an unhandled P2003/unknown value error rather than a clean 400. | Minor type safety gap. Low probability since validation is always applied via NestJS pipes. | S (change type to `'LIKE' | 'LOVE' | 'SUPPORT' | 'INSIGHTFUL'`) |
| 13 | M | feed.service.ts:349 | **`any[]` type in production code.** `let posts: any[] = []` at line 349 in `getTrendingFeed()`. The project rules explicitly prohibit `any` in non-test code. Same pattern exists implicitly in the scored array processing. | Violates project coding standards. Type errors in the scored/filtered pipeline would not be caught at compile time. | S (type the array with the Prisma select output type) |
| 14 | L | posts.service.ts:545-546 | **Dynamic `import('crypto')` on every post creation.** `contentHash` computation uses `(await import('crypto')).createHash(...)` inside the transaction. This dynamic import executes on every `create()` call. Node.js caches the module after first import, so the performance impact is minimal, but it's an antipattern inside a database transaction. | Negligible performance impact due to module cache, but adds ~1ms latency to every post creation inside the transaction boundary. | S (move `import crypto from 'crypto'` to module top) |
| 15 | L | posts.service.ts:319 | **Blended feed cursor semantics are inconsistent.** `getBlendedFeed()` at line 319 uses `{ id: { lt: cursor } }` for following posts (cursor = ID), but the interleaving of following + trending posts means the cursor (last merged item ID) may not correctly paginate either source. A trending post ID used as cursor for the following query would skip different posts than intended. | Second and subsequent pages of the blended feed may have duplicates or gaps. Only affects users with < 10 follows. | M (use separate cursors for each source, or switch to page-number-based pagination like the foryou feed) |
| 16 | L | posts.controller.ts:67 | **`type` query parameter is not validated via `@IsEnum`.** The `getFeed()` endpoint accepts `type` as a raw string with a default value. Any invalid type value (e.g., `?type=xxx`) would fall through to the following-feed logic without error, since `if (type === 'chronological')` and `if (type === 'foryou')` and `if (type === 'favorites')` all fail, defaulting to the following feed code path. | No crash, but unexpected behavior for invalid query parameters. Client typo silently returns wrong feed type. | S (add `@IsEnum(['following', 'foryou', 'chronological', 'favorites'])` validation) |
| 17 | L | posts.service.ts:828-830 | **Edit history preserves old content including potentially moderated content.** `editHistory` stores the previous `post.content` as plaintext in a JSON array. If the original content was flagged by moderation and the user edits within 15 minutes, the flagged content persists in `editHistory`. This is actually useful for moderation auditing, but the content is never sanitized or redacted in the history. | Potentially problematic content stored in edit history. Useful for auditing but could be a liability if edit history is ever exposed to other users. | S (no change needed if edit history is author-only; document the intentional retention) |
| 18 | L | feed.service.ts:604 | **`$queryRawUnsafe` used for frequent-creator query.** Line 604 uses `$queryRawUnsafe` with positional parameters. While the parameters are safe (userId string and Date object), `$queryRaw` with tagged template literals would be preferred per project convention. The comment above the `$executeRaw` in the same codebase says tagged templates are safe. | Low risk since parameters are typed, but inconsistent with project conventions for safe SQL execution. | S (convert to `$queryRaw` tagged template) |
| 19 | I | publish-workflow.service.ts:55-68 | **Cache invalidation uses SCAN with user-specific patterns only.** `onPublish` invalidates `feed:*:${userId}:*` and `sfeed:*:${userId}` but this only clears the author's own feed caches. Followers of the author who have cached feeds will not see the new post until their cache TTL (120s) expires. This is a design decision (TTL-based eventual consistency) but means new posts from followed users can take up to 2 minutes to appear. | New posts appear with up to 2-minute delay in followers' feeds. Acceptable at current scale but worth noting. | N/A (design tradeoff, not a bug) |
| 20 | I | posts.service.ts:1135-1196 | **`shareAsStory()` does not run content moderation on the story.** The story is created with `textOverlay` and `mediaUrl` from the original post, which was already moderated. However, the story itself is a new content record that bypasses the moderation pipeline. If the original post's media was later flagged and removed, the story copy survives. | Low risk since the original was moderated, but the story is an independent record with no backlink to trigger cascading removal. | S (add check that original post is not removed/sensitive before creating story) |
| 21 | I | posts.service.ts:1607-1651 | **`crossPost()` does not trigger `publishWorkflow.onPublish()` for the new posts.** Cross-posted content is created but never indexed in Meilisearch and no real-time event is published. Compare with `create()` at lines 720-734 which calls `publishWorkflow.onPublish()`. | Cross-posted content is not searchable in Meilisearch until a full re-index. No real-time feed refresh for cross-posted content. | S (add `publishWorkflow.onPublish()` call for each cross-posted item in the loop) |
| 22 | I | posts.service.ts:1607-1651 | **`crossPost()` does not run content moderation on the new posts.** While `captionOverride` is moderated, the original post's content and media are copied without re-checking. The original was moderated at creation, but if moderation policies changed since, the cross-post bypasses the updated rules. Additionally, if the original was auto-removed after creation (by background image moderation), the cross-post copies survive. | Stale moderation state carried forward to cross-posts. | S (check `original.isRemoved` before cross-posting, add moderation job for new media) |

**Severity counts:** C:1 H:5 M:6 L:4 I:4
**Total findings:** 22

## Cross-Module Event Chain Analysis

### Create -> Feed -> Search -> Notification -> Socket

| Step | Status | Evidence |
|------|--------|---------|
| Create post | OK | `posts.service.ts:455` - moderation + create + transaction |
| Mention notification | OK | `posts.service.ts:609-635` - notifies mentioned users |
| Tag notification | OK | `posts.service.ts:643-664` - notifies tagged users |
| Collab notification | OK | `posts.service.ts:667-686` - notifies collaborator |
| Meilisearch index | OK | `posts.service.ts:721` - via `publishWorkflow.onPublish()` |
| Feed cache invalidation | OK | `posts.service.ts:738-741` - scored feed cache cleared |
| Real-time socket event | BROKEN | Finding #4 - `content_published` event dropped by gateway |
| Scheduled post notification | OK | `posts.service.ts:96-133` - cron every 5 min |

### Edit -> Re-moderation -> Search re-index

| Step | Status | Evidence |
|------|--------|---------|
| Content sanitization | OK | `posts.service.ts:820` - `sanitizeText()` called |
| Content moderation re-check | MISSING | Finding #2 - no `moderateText()` on edit |
| Search re-index | OK | `posts.service.ts:836-849` - `publishWorkflow.onPublish()` called |
| Edit history preserved | OK | `posts.service.ts:827-830` |

### Delete -> Feed cleanup -> Search removal -> Notification cleanup

| Step | Status | Evidence |
|------|--------|---------|
| Soft delete + postsCount | OK | `posts.service.ts:859-865` |
| Hashtag count decrement | OK | `posts.service.ts:867-874` |
| Feed cache invalidation | OK | `posts.service.ts:876-879` |
| Search index removal | OK | `posts.service.ts:882-886` - via `publishWorkflow.onUnpublish()` |
| Bookmark/save cleanup | NOT DONE | Finding #10 - soft-delete leaves savedPost records |
| Notification cleanup | NOT DONE | Finding #10 - notifications referencing deleted post remain |
| Real-time removal event | BROKEN | Finding #4 - `content_removed` event dropped by gateway |

### Dismiss -> Feed exclusion

| Step | Status | Evidence |
|------|--------|---------|
| Dismissal persisted | OK (data) | `posts.service.ts:1459-1464` |
| Feed reads dismissal | BROKEN | Finding #1 - case mismatch ('POST' vs 'post') |
