# B05 — Video, VideoComment, VideoReaction, VideoBookmark, VideoInteraction, VideoReply

**Files audited:**
- `apps/api/src/modules/videos/videos.service.ts` (1117 lines) — READ IN FULL
- `apps/api/src/modules/videos/videos.controller.ts` (357 lines) — READ IN FULL
- `apps/api/src/modules/video-replies/video-replies.service.ts` (175 lines) — READ IN FULL
- `apps/api/src/modules/video-replies/video-replies.controller.ts` (68 lines) — READ IN FULL
- `apps/api/src/modules/clips/clips.service.ts` (99 lines) — READ IN FULL
- `apps/api/src/modules/bookmarks/bookmarks.service.ts` (414 lines) — READ IN FULL
- `apps/api/prisma/schema.prisma` — Video, VideoComment, VideoReaction, VideoBookmark, VideoInteraction, VideoReply, VideoCommentLike, VideoPremiere, VideoClip, VideoChapter, EndScreen models — READ IN FULL
- `apps/api/src/modules/feed/personalized-feed.service.ts` — video queries only — READ RELEVANT SECTIONS
- `apps/api/src/modules/channels/channels.service.ts` — video queries only — READ RELEVANT SECTIONS

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | C | videos.service.ts:486 | Raw SQL uses `"Channel"` (Prisma model name) instead of `"channels"` (@@map table name). `UPDATE "Channel" SET "videosCount" = GREATEST(...)` will fail at runtime with "relation Channel does not exist". | Video delete silently fails to decrement channel videosCount; transaction aborts, video not soft-deleted | S — change `"Channel"` to `"channels"` |
| 2 | H | videos.service.ts:513-514 | `like()` checks `status !== PUBLISHED` but does NOT check `video.isRemoved`. Removed videos can still receive likes. | Likes accumulate on removed content, pollutes counters | S — add `\|\| video.isRemoved` to the guard |
| 3 | H | videos.service.ts:571-572 | `dislike()` same as #2 — no `isRemoved` check. | Dislikes on removed videos | S |
| 4 | H | videos.service.ts:635-636 | `comment()` same as #2 — no `isRemoved` check. | Comments on removed videos, commentsCount increments on invisible content | S |
| 5 | H | videos.service.ts:742-743 | `bookmark()` same as #2 — no `isRemoved` check. | Bookmarks on removed videos, savesCount increments | S |
| 6 | H | videos.service.ts:779-780 | `view()` same as #2 — no `isRemoved` check. | viewsCount inflates on removed videos | S |
| 7 | H | videos.service.ts:608-628 | `removeReaction()` TOCTOU race: `existingReaction.isLike` is read at line 609 outside the transaction. Between lines 609 and 614, a concurrent `like()` or `dislike()` flip can change the value. The transaction then decrements the wrong counter (likesCount vs dislikesCount). | Counter desync: one counter goes negative (GREATEST prevents <0, but the other stays too high). Net effect: permanent +1/-1 skew per race occurrence. | M — move the read inside an interactive transaction (`$transaction(async tx => { const reaction = await tx.videoReaction.findUnique(...); ... })`) |
| 8 | H | videos.service.ts:682-703 | `getComments()` does not filter out comments from banned/deactivated/deleted users. No `user: { isBanned: false, isDeactivated: false, isDeleted: false }` filter. | Comments from banned users visible to all, defeats moderation | S — add user filter to where clause |
| 9 | H | videos.service.ts:889-909 | `getCommentReplies()` same as #8 — no user ban/deactivation filter on replies. | Banned user replies visible | S |
| 10 | H | videos.service.ts:858-870 | `getRecommended()` checks `isRemoved: false` and `status: PUBLISHED` but does NOT filter `user: { isDeactivated: false, isBanned: false, isDeleted: false }`. Videos from banned users appear in recommendations. | Banned-user content promoted to other users | S — add user filter |
| 11 | H | videos.service.ts:746-759 | `bookmark()` has a check-then-act race: the `findUnique` at line 746 is outside the transaction. If two concurrent requests both pass the check, the second `create` inside the transaction throws P2002 — which is NOT caught. Results in unhandled 500. Compare with `bookmarks.service.ts:saveVideo()` which handles P2002. | 500 error on concurrent bookmark attempts | S — wrap in try/catch for P2002 or use upsert |
| 12 | M | videos.service.ts:1037-1053 | `setEndScreens()` deletes all existing end screens then creates new ones in separate operations (not a transaction). If any create fails, old end screens are already gone — data loss. | Partial failure = total end-screen loss | S — wrap in `$transaction` |
| 13 | M | videos.service.ts:1106-1114 | `parseChaptersFromDescription()` deletes existing chapters then creates new ones — not wrapped in a transaction. Same data-loss risk. | Partial failure = chapter loss | S — wrap in `$transaction` |
| 14 | M | videos.service.ts:936-949 | `createPremiere()` creates a premiere record then updates the video's `isPremiereEnabled` in separate operations — not a transaction. If the video update fails, premiere exists but video doesn't reflect it. | Orphaned premiere, inconsistent state | S — wrap in `$transaction` |
| 15 | M | video-replies.service.ts:110-118 | `getByComment()` fetches user data but does NOT filter out banned/deactivated/deleted users. Banned user video replies appear normally. | Banned-user video replies visible | S — add `isBanned: false, isDeactivated: false, isDeleted: false` to user where |
| 16 | M | clips.service.ts:15 | `create()` checks `video.status !== 'PUBLISHED'` but does NOT check `video.isRemoved`. Clips can be created from removed videos. | Clips reference removed content | S — add isRemoved check |
| 17 | M | clips.service.ts:44-64 | `getByVideo()` has no filtering: no `isRemoved` check on source video, no user ban filter. Clips from removed videos or by banned users are visible. | Moderated content leaks through clips | M — add source video and user filters |
| 18 | M | clips.service.ts:66-86 | `getByUser()` same as #17 — no source video or user ban filtering. | Same | M |
| 19 | M | clips.service.ts:46 | `getByVideo()` uses `id: { lt: cursor }` for pagination on CUIDs. CUIDs are not strictly lexicographically ordered by creation time. Should use Prisma cursor-based pagination (`{ cursor: { id: cursor }, skip: 1 }`). | Clips can be skipped or duplicated during pagination | S |
| 20 | M | bookmarks.service.ts:304-359 | `getSavedVideos()` does NOT filter `video.isRemoved` or `video.user.isBanned/isDeactivated/isDeleted`. Removed or banned-user videos appear in saved-videos list. | Users see removed/moderated content in their saved list | M — add nested where filters |
| 21 | M | channels.service.ts:385-402 | Channel analytics `topVideos` query checks `isRemoved: false` but does NOT check `status: 'PUBLISHED'`. Draft, processing, or unlisted videos appear in channel top-5. | Unpublished content leaks to channel analytics | S — add status filter |
| 22 | M | schema (VideoComment) | `VideoComment` has no `isDeleted` boolean. The `deleteComment()` method sets `content` to `'[deleted]'` as a pseudo-delete, but there's no way to filter these from queries. `getComments` returns them with `[deleted]` text, and `_count.replies` includes deleted comments in reply counts. | Deleted comments still counted, pseudo-delete is unfilterable | M — add `isDeleted Boolean @default(false)` to model, update queries |
| 23 | M | schema (VideoInteraction) | `VideoInteraction` model exists with fields (`viewed`, `liked`, `shared`, `saved`, `watchDurationMs`, `completionRate`) but NO service code writes to it. Only usage is `privacy.service.ts` delete. The model is dead — never populated. | Dead model wastes schema space, migration overhead, confusing for developers | M — remove model or wire it up |
| 24 | M | schema (VideoCommentLike) | `VideoCommentLike` model exists, `VideoComment.likesCount` counter exists, but there is NO endpoint or service method to like/unlike video comments. The only usage is privacy service deletion. | `likesCount` on VideoComment is always 0; dead feature | M — implement like/unlike endpoint or remove model+counter |
| 25 | L | schema (Video) | `Video` model has no `@@index([userId])`. Multiple services query videos by userId (channels, privacy, admin, feed personalization). | Full table scan when filtering by userId on large dataset | S — add `@@index([userId])` |
| 26 | L | schema (VideoBookmark) | `VideoBookmark` has `@@id([userId, videoId])` and `@@index([userId, createdAt])` but no `@@index([videoId])`. Any query to find "who bookmarked this video" requires a full scan. | Slow reverse lookups (currently no such query, but schema gap) | S — add `@@index([videoId])` |
| 27 | L | schema (VideoReply) | `VideoReply.commentId` is a polymorphic FK (string, no relation). When the parent Comment or ReelComment is hard-deleted, orphan VideoReply rows persist with dangling `commentId`. | Orphaned rows accumulate over time | M — add application-level cascade or periodic cleanup job |
| 28 | L | schema (VideoClip) | `VideoClip` has `likesCount`, `sharesCount`, `viewsCount` counters but no endpoints to like, share, or record views on clips. Dead counters. | Counters always 0, misleading schema | S — remove counters or implement endpoints |
| 29 | L | videos.service.ts:735 | `deleteComment()` sets content to `[deleted]` but does NOT decrement `likesCount` on the VideoComment or clean up related `VideoCommentLike` rows. If likes ever get implemented, deleted comments retain like artifacts. | Inconsistent state on pseudo-deleted comments | S |
| 30 | I | schema (VideoReply) | `VideoReply` has no `updatedAt` field. If replies are ever edited (feature extension), there's no timestamp to track modifications. | Minor schema completeness gap | S |
| 31 | I | videos.service.ts:974 | `setPremiereReminder()` raw SQL `UPDATE video_premieres SET "reminderCount" = "reminderCount" + 1` does not use `GREATEST(0, ...)`. While incrementing can't produce negatives, it's inconsistent with the decrement on line 993 which does use GREATEST. | Style inconsistency, no functional impact | S |

**Severity counts:** C:1 H:7 M:10 L:5 I:2
**Total findings:** 31
