---
# B03 — Reel, ReelComment, ReelReaction, ReelBookmark, ReelView

**Note:** No `ReelBookmark` or `ReelView` Prisma models exist. Bookmarks (saves) and views are tracked via the `ReelInteraction` model (fields `saved`, `viewed`). Audit scope expanded to include `ReelInteraction`, `ReelCommentReaction`, and `ReelTemplate`.

**Files audited:**
- `apps/api/prisma/schema.prisma` lines 1153-1291, 2710-2727, 2939-2953 — READ IN FULL (Reel, ReelTaggedUser, ReelReaction, ReelComment, ReelCommentReaction, ReelInteraction, ReelTemplate)
- `apps/api/src/modules/reels/reels.service.ts` (1352 lines) — READ IN FULL
- `apps/api/src/modules/reels/reels.controller.ts` (341 lines) — READ IN FULL
- `apps/api/src/modules/reels/dto/create-reel.dto.ts` (168 lines) — READ IN FULL
- `apps/api/src/modules/reels/dto/create-comment.dto.ts` (14 lines) — READ IN FULL
- `apps/api/src/modules/reel-templates/reel-templates.service.ts` (116 lines) — READ IN FULL
- `apps/api/src/modules/reel-templates/reel-templates.controller.ts` (95 lines) — READ IN FULL
- `apps/api/src/common/utils/enrich.ts` (125 lines) — READ IN FULL
- `apps/api/src/modules/users/users.service.ts` lines 461-501 (getSavedReels) — READ relevant section
- `apps/api/src/modules/privacy/privacy.service.ts` lines 240-637 (GDPR export/erasure) — READ relevant sections
- `apps/api/src/config/prisma.service.ts` lines 45-69 (CHECK constraints) — READ relevant section

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | H | `reels.service.ts:757-762` | `comment()` increments `commentsCount` with raw `+ 1` instead of `GREATEST(0, "commentsCount" + 1)`. While incrementing cannot go negative on its own, the asymmetry with `deleteComment` (which uses `GREATEST`) suggests the pattern was intended to be consistent. More critically, the raw increment lacks the CHECK constraint that exists for `likesCount` and `viewsCount` but is **missing** for `commentsCount` on the `reels` table (see finding #2). | Counter inconsistency if concurrent race conditions produce unexpected state. | S |
| 2 | H | `prisma.service.ts:53-54` | DB CHECK constraints only cover `likesCount` and `viewsCount` on the `reels` table. Missing constraints for `commentsCount`, `sharesCount`, `savesCount`, and `loopsCount`. These counters can theoretically go negative under race conditions despite `GREATEST(0, ...)` in application code, because the constraint is the true DB-level safety net. | Negative counter values could appear in the DB for 4 of 6 counter fields if application-level GREATEST is bypassed (e.g., raw admin query, migration, or concurrent bug). | S |
| 3 | H | `reels.service.ts:557-562` | `recordView()` is called fire-and-forget from controller line 118 (`this.reelsService.recordView(id)` — no `await`). It increments `viewsCount` unconditionally without checking if the reel exists, is removed, or is in PROCESSING state. Any request to `GET /reels/:id` with a non-existent ID will throw `NotFoundException` from `getById` but will also call `recordView` which silently `.catch(() => {})` ignores errors — but for **valid** reels, it double-counts views: once from `recordView` (fired from GET) and again if the user also hits `POST /reels/:id/view`. | Inflated view counts. Every `GET /reels/:id` adds a view regardless of auth, plus `POST /reels/:id/view` adds another — double counting. | M |
| 4 | H | `reels.service.ts:832-879` | `getComments()` does not filter out comments from banned (`isBanned`), deactivated (`isDeactivated`), or deleted (`isDeleted`) users. The feed queries (lines 359, 443) correctly filter `user: { isDeactivated: false, isBanned: false, isDeleted: false }` but comments are returned with no such check. | Comments from banned/deactivated users remain visible, undermining moderation. | S |
| 5 | H | `reels.service.ts:789` | `deleteComment()` soft-deletes by setting `content: '[deleted]'` but `getComments()` (line 850) has no filter to exclude these soft-deleted comments. They appear in the comment list with content `[deleted]` and the original user's profile data (username, avatar). This is a design choice on some platforms (Reddit-style), but the `commentsCount` is decremented (line 790), creating a mismatch: visible comment count in list != `commentsCount` field. | Data inconsistency: UI shows N comments but `commentsCount` says N-K. Users see ghost `[deleted]` comments with author info leaked. | M |
| 6 | H | `reels.service.ts:990-992` | `getUserReels()` does not check if the target user is banned, deactivated, or deleted. It only checks `user.findUnique({ where: { username } })` and throws `NotFoundException` if not found, but a banned/deactivated user would still be "found" and their reels served. Feed queries (lines 359, 443) filter these states, but profile-based reel listing does not. | Reels from banned/deactivated users visible via `/reels/user/:username`. | M |
| 7 | M | `reels.service.ts:1065-1072` | `getByAudioTrack()` does not filter by `user: { isBanned: false, isDeactivated: false, isDeleted: false }`, unlike `getFeed` and `getTrendingReels`. Same issue in `getDuets()` (line 1116) and `getStitches()` (line 1166). | Reels from banned/deactivated users appear in audio track, duet, and stitch listings. | S |
| 8 | M | `schema.prisma:1261-1278` | `ReelComment` model lacks a `@@index([userId])` index. The GDPR data export (`privacy.service.ts:266`) queries `reelComment.findMany({ where: { userId } })` and the GDPR erasure (`privacy.service.ts:508`) does `reelComment.deleteMany({ where: { userId } })`. Without an index on `userId`, these operations require a full table scan on `reel_comments`. `Post`'s `Comment` model has `@@index([userId, createdAt(sort: Desc)])` (schema line 1607) but `ReelComment` does not. | Slow GDPR export/erasure operations at scale. | S |
| 9 | M | `reels.service.ts:800-808` | `likeComment()` increments `likesCount` using Prisma's `{ increment: 1 }` instead of raw `GREATEST(0, ...)`. While the increment itself won't go negative, the asymmetric pattern (unlike uses `GREATEST` at line 827) means the code path lacks consistency. More importantly, `likeComment` silently swallows `P2002` duplicate errors (line 810-812) by returning `{ liked: true }` — this hides the fact that the user already liked the comment, and the UI gets a false success. | Silent double-like swallowing; inconsistent counter pattern. | S |
| 10 | M | `reels.service.ts:480-484` | `updateReel()` fetches the reel with `findUnique({ where: { id } })` which returns ALL fields, but the check on line 483 compares `reel.userId !== userId`. Since `Reel.userId` is nullable (`String?`, schema line 1155), if a user is deleted (onDelete: SetNull), `reel.userId` becomes `null` and `null !== userId` is always `true`, throwing `ForbiddenException`. This is correct behavior, but similar `null`-userId comparisons at lines 574, 614, 1213, 1225, 1310 all share this pattern — the reel becomes unmanageable by anyone. No admin override path exists. | Orphaned reels (userId = null) become permanently unmanageable: can't delete, archive, update, or publish. | M |
| 11 | M | `reel-templates.service.ts:55-76` | `browse()` returns templates with no filter on `sourceReel.isRemoved`, `sourceReel.status`, or the source reel user's ban/deletion status. A template referencing a removed or banned user's reel will still appear in browse results. Also no filter for the template creator's ban status. | Templates referencing removed/banned content visible to all users. | S |
| 12 | M | `reel-templates.service.ts:88-99` | `markUsed()` increments `useCount` unconditionally without checking if the source reel is removed or if the user is banned. Any user can inflate the use count repeatedly. No deduplication (same user can call `POST /reel-templates/:id/use` unlimited times). | Unbounded `useCount` inflation; no per-user deduplication. | S |
| 13 | M | `reels.controller.ts:118` | `recordView()` is called without `await` in the controller's `getById()`. This means the response is sent before the view count update completes. If the DB connection pool is exhausted, these fire-and-forget updates queue up unboundedly. The same pattern exists for `recordLoop` (line 242). | Under load, unbounded fire-and-forget DB writes can exhaust connection pool, causing cascading failures for all queries. | M |
| 14 | M | `reels.service.ts:898-902` | `share()` increments `sharesCount` without `GREATEST(0, ...)`. While the check-then-increment pattern prevents double-counting, if the `shared` flag is somehow corrupted (e.g., direct DB edit), the increment has no floor guard. Inconsistent with `unbookmark` (line 951-955) which does use `GREATEST`. | Minor inconsistency; no floor guard on `sharesCount` increment. | S |
| 15 | L | `reels.service.ts:832` | `getComments()` only filters `blockerId: userId` (line 837) — it excludes users that the requesting user blocked. But it does **not** filter the reverse: users who blocked the requesting user. The `getById` method (line 527-535) checks both directions. A user who blocked you can still see their comments appear in reel comment threads you view, because the reverse block is not filtered. Compare with `getFeed` which excludes both directions (line 342). | Inconsistent block enforcement between feed and comments. | S |
| 16 | L | `reels.service.ts:1042-1063` | `report()` uses `description: 'reel:${reelId}'` as a string in the `Report` model to identify the reported content. This is a stringly-typed foreign key rather than using the `Report` model's dedicated `reelId` or similar field. If the Report model has proper relation fields, they are not being used. Duplicate check also relies on this string match rather than a proper unique constraint. | Fragile report deduplication; not using proper relational fields. | S |
| 17 | L | `schema.prisma:1248-1259` | `ReelReaction` uses `@@id([userId, reelId])` as a composite primary key, which means a user can only have one reaction type per reel. The `reaction` field defaults to `LIKE` but the `like()` method (line 651) always sets `reaction: ReactionType.LIKE`. If the app ever needs multiple reaction types (heart, laugh, etc.), the schema supports it but the service hardcodes LIKE only. | No actual bug; future feature limitation. | S |
| 18 | L | `reels.service.ts:119-155` | `create()` wraps reel creation and user counter increment in `$transaction`, but the subsequent hashtag upserts (lines 166-174), tagged user creation (lines 192-195), and notification dispatch (lines 215-247) are **outside** the transaction. If the transaction succeeds but hashtag upserts fail, the reel exists but hashtag counts are wrong. | Partial failure leaves hashtag counts inconsistent with actual reel data. | M |
| 19 | L | `reels.service.ts:571-607` | `delete()` soft-deletes (sets `isRemoved: true`) and decrements `reelsCount` in a transaction, but does NOT decrement `audioTrack.reelsCount` when the deleted reel had an `audioTrackId`. Compare with the audio-tracks service which has explicit `incrementReelsCount`/`decrementReelsCount` methods. | Audio track reel counts inflate over time as reels are soft-deleted without decrementing. | S |
| 20 | I | `schema.prisma:1199-1204` | `Reel` model has 6 counter fields (`likesCount`, `commentsCount`, `sharesCount`, `savesCount`, `viewsCount`, `loopsCount`) but `REEL_SELECT` (service line 26-75) does not include `savesCount` or `loopsCount`. These fields are maintained in the DB but never returned to clients. | Dead data: `savesCount` and `loopsCount` are written but never read by the API. | S |
| 21 | I | `reels.service.ts:381` | Feed response strips the internal `score` field with `({ score, ...reel }) => reel` but TypeScript does not enforce that `score` is actually present on all items. If the scored feed cache returns items without `score`, this destructuring silently creates an `undefined` variable. | No runtime impact; minor type safety gap. | S |

**Severity counts:** C:0 H:6 M:8 L:5 I:2
**Total findings:** 21
---
