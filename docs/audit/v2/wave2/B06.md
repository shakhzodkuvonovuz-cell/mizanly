# B06 — Conversation, Message, MessageReaction, MessageMedia, ConversationParticipant

**Note:** The Prisma schema uses `ConversationMember` (not `ConversationParticipant`) and has no `MessageMedia` model (media fields are inline on `Message`). Audit covers: `Conversation`, `Message`, `MessageReaction`, `ConversationMember` (the actual model names).

**Files audited:**
- `apps/api/prisma/schema.prisma` (5037 lines) — READ IN FULL for Conversation (L1656-1695), ConversationMember (L1697-1719), Message (L1721-1783), MessageReaction (L2304-2316)
- `apps/api/src/modules/messages/messages.service.ts` (1719 lines) — READ IN FULL
- `apps/api/src/modules/messages/messages.controller.ts` (756 lines) — READ IN FULL
- `apps/api/src/gateways/chat.gateway.ts` (1020 lines) — READ IN FULL
- `apps/api/src/modules/messages/internal-e2e.controller.ts` (112 lines) — READ IN FULL
- `apps/api/src/modules/chat-export/chat-export.service.ts` (222 lines) — READ IN FULL
- `apps/api/src/common/services/counter-reconciliation.service.ts` (610 lines) — READ lines 250-292 (unread reconciliation)
- `apps/api/src/modules/blocks/blocks.service.ts` — READ lines 100-172 (post-block conversation cleanup)

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | C | messages.service.ts:1395 | `promoteToAdmin` checks `member.role !== 'owner'` but `createGroup` (L694-704) never sets creator's role to `'owner'` — all members get default `'member'` role. `promoteToAdmin`, `demoteFromAdmin`, and the `role === 'owner'` branch in `banMember` are all dead code. No one can ever promote, demote, or have owner-level ban privileges through these endpoints. | Group admin system completely broken. No admin hierarchy can be established via promote/demote endpoints. | S — Add `role: 'owner'` for creator in `createGroup` member creation. |
| 2 | C | messages.service.ts:1702 | `setMessageExpiry` checks `member.role !== 'ADMIN'` (uppercase) but roles are stored as lowercase `'admin'` / `'member'`. Same bug at L1640 in `createGroupTopic` (`member.role !== 'ADMIN'`). These permission checks never match, meaning admin role is ignored for expiry/topic creation — only `createdById` fallback works. | Admin role checks silently fail. Admins promoted via `changeGroupRole` cannot create topics or set expiry. | S — Change `'ADMIN'` to `'admin'` at L1702 and L1640. |
| 3 | C | messages.service.ts:1714 | `setMessageExpiry` stores `disappearingDuration = expiryDays * 24 * 60` (minutes). But `sendMessage` (L424) interprets `disappearingDuration` as seconds: `Date.now() + convo.disappearingDuration * 1000`. For `expiryDays=1`: stores 1440, `sendMessage` computes 1440 * 1000ms = 24 minutes expiry instead of 24 hours. `setDisappearingTimer` (L1161) stores duration directly in seconds, creating unit inconsistency. | Messages expire 60x too fast when set via `setMessageExpiry`. 1-day setting expires in 24 minutes, 90-day setting expires in 36 hours. | S — Change L1714 to `expiryDays * 24 * 60 * 60` (seconds). |
| 4 | H | messages.service.ts:1352-1370 | `sendViewOnceMessage` is a separate code path that bypasses all safeguards: does NOT update `conversation.lastMessageAt`/`lastMessageText`/`lastMessageById`, does NOT increment `unreadCount` for other members, does NOT check blocks, does NOT check E2E enforcement (`isE2E` flag), does NOT use a transaction, does NOT send push notifications. | View-once messages are invisible in conversation list (no lastMessage update), recipients get no unread badge or push, and view-once messages can be sent as plaintext into E2E-enforced conversations. | M — Route through `sendMessage` with `isViewOnce: true` instead of duplicating logic. |
| 5 | H | messages.service.ts:1596-1608 | `processExpiredMessages` cron clears plaintext fields on expired messages but does NOT clear E2E crypto fields: `encryptedContent`, `e2eSenderRatchetKey`, `encNonce`, `e2eVersion`, `e2eSenderDeviceId`, `e2eCounter`, `e2ePreviousCounter`, `e2eSenderKeyId`. Compare to `deleteMessage` (L557-576) which correctly clears all E2E fields. Same gap in view-once cleanup at L1611-1622. | Expired/disappearing encrypted messages retain ciphertext and ratchet keys in DB indefinitely. Ratchet keys + compromised identity key = message decryption. | S — Add all E2E fields to both `updateMany` calls in `processExpiredMessages`. |
| 6 | H | messages.service.ts:842 | `joinViaInviteLink` Redis TTL re-seed: `Math.max(1, Math.floor((new Date().getTime() - Date.now()) / 1000))`. Both operands are current time, so result is always 0, Math.max returns 1. Redis TTL is always 1 second, making re-seeding useless. Should be `inviteExpiresAt.getTime() - Date.now()`. | Redis invite cache re-seeding after fallback to DB always expires in 1 second. Every subsequent join hits the database directly instead of Redis. | S — Fix to `convo.inviteExpiresAt.getTime() - Date.now()` (need to select `inviteExpiresAt` at L837). |
| 7 | H | messages.service.ts:125-143 | `getConversations` does not filter `isArchived: false`. Archived conversations appear in the main conversation list. The separate `getArchivedConversations` correctly filters `isArchived: true`, but the main list returns everything. | Users see archived conversations in their main inbox, defeating the archive feature. Double-display with archived list. | S — Add `isArchived: false` to the `where` clause at L128. |
| 8 | H | messages.service.ts:1098-1117 | `forwardMessage` transaction creates messages and updates `conversation.lastMessageAt/lastMessageText/lastMessageById` but does NOT increment `unreadCount` for other members in target conversations. | Forwarded messages don't generate unread badges for recipients. Users miss forwarded messages until they manually open the conversation. | S — Add `conversationMember.updateMany` with `unreadCount: { increment: 1 }` inside the transaction for each target conversation. |
| 9 | H | counter-reconciliation.service.ts:263 | Raw SQL uses `"Message"` (Prisma model name) as table name, but schema has `@@map("messages")`. Actual PostgreSQL table is `messages`, not `Message`. | `reconcileUnreadCounts` cron job throws SQL error on every run — table `Message` does not exist. Unread counts are never reconciled. | S — Change `"Message"` to `"messages"` at L263. |
| 10 | M | messages.service.ts:422 | `e2eSenderKeyId` uses truthy check (`data.e2eSenderKeyId ? ...`) instead of `!== undefined`. If sender key ID is `0` (valid Int value), the field is silently dropped. Other Int fields (`e2eCounter`, `e2ePreviousCounter`, `e2eSenderDeviceId`) correctly use `!== undefined`. | Sender key ID `0` for the first group session key is lost, breaking group message decryption for that chain. | S — Change to `data.e2eSenderKeyId !== undefined`. |
| 11 | M | messages.service.ts:155-185 | `getMessages` filters `isDeleted: false` but does NOT filter `isScheduled: false`. Scheduled messages (not yet sent) are visible to all conversation members. | Users see each other's unsent scheduled messages before they're meant to be delivered, breaking the scheduling feature's purpose. | S — Add `isScheduled: false` to the `where` clause at L172. |
| 12 | M | messages.service.ts:1483-1505 | `getDMNotesForContacts` does not filter out blocked users. A user who has blocked caller (or been blocked by caller) still has their DM note returned. | Blocked users' DM notes are visible, leaking presence/status info through the block boundary. | S — Add block check similar to `getMessages` block filter. |
| 13 | M | schema.prisma:1656-1695 | `Conversation` model has `createdById` with `onDelete: SetNull` but NO `@@index([createdById])`. All permission checks (`updateGroup`, `addGroupMembers`, `removeGroupMember`, `changeGroupRole`) do `findUnique` + compare `createdById`, but listing conversations by creator has no index support. | Query performance degrades for admin queries filtering by creator. Not a correctness bug but a scaling concern with 100K+ conversations. | S — Add `@@index([createdById])` to Conversation model. |
| 14 | M | schema.prisma:1721-1783 | `Message` model has `pinnedById` FK to User with `onDelete: SetNull` but NO `@@index([pinnedById])`. Also missing index on `clientMessageId` for dedup lookups (has `@unique` which creates an index, so this is fine). However, `isPinned` has no index — `getPinnedMessages` queries `isPinned: true` per conversation which is covered by the composite `[conversationId, createdAt]` index but not optimally. | Minor: `pinnedById` has no index. The `isPinned` filter relies on sequential scan within conversation partition. | S — Add `@@index([conversationId, isPinned])` on Message. |
| 15 | M | messages.service.ts:694-704 | `createGroup` does NOT use a `$transaction`. Between `findMany` (user validation) and `conversation.create`, a race condition can create duplicate groups if two requests arrive simultaneously with the same parameters. `createDM` (L636) correctly uses `$transaction` for the same pattern. | Potential duplicate group creation under concurrent requests. | M — Wrap in `$transaction` like `createDM`. |
| 16 | M | messages.service.ts:549-578 | `deleteMessage` does not verify conversation membership. Any user who knows a `messageId` can probe whether they are the sender (gets `ForbiddenException` vs `NotFoundException`). Also, sealed sender messages have `senderId=null` — the `message.senderId !== userId` check would throw `ForbiddenException`, making sealed sender messages undeletable by the actual sender. | Sealed sender messages cannot be deleted by their sender. Minor info leak via error differentiation. | M — Add membership check; for sealed sender, verify via `clientMessageId` or conversation membership. |
| 17 | M | messages.service.ts:806-826 | `generateGroupInviteLink` does not check if caller is admin/owner — only requires membership. Any group member can generate invite links, which may not be intended for private groups with `genderRestriction` or `lockCode`. | Any member can share group invite links, bypassing admin control over group access. | S — Add admin/owner role check. |
| 18 | L | schema.prisma:1656-1695 | `Conversation` has `genderRestriction` field but it is never enforced in any service code. `addGroupMembers`, `joinViaInviteLink`, and `createGroup` all skip gender checks. | The gender restriction feature is a no-op — the field exists but is never read. | M — Enforce in join/add member paths, or remove the field. |
| 19 | L | messages.service.ts:708-722 | `updateGroup` only allows group creator (`convo.createdById !== userId`). Admins (promoted via `changeGroupRole`) cannot update group name/avatar/description. This is inconsistent with `banMember` which allows admins to act. | Admins cannot perform basic group management tasks. | S — Also allow `role === 'admin'` in the permission check. |
| 20 | L | messages.service.ts:125 | `getConversations` does not filter `isBanned: true` memberships. Banned users see the conversation in their list (though `requireMembership` blocks them from sending messages or reading messages). | Banned users see phantom conversations they cannot interact with. | S — Add `isBanned: false` to the `where` clause. |
| 21 | L | messages.service.ts:807-826 | `generateGroupInviteLink` stores invite code in both Redis (7-day TTL) and DB. But the DB write is wrapped in `.catch(() => {})` — if DB persistence fails silently, the invite works for 7 days via Redis but is lost on Redis flush. No retry, no logging. | Invite codes may silently fail to persist to DB, making them unrecoverable after Redis eviction. | S — Log the error in the catch handler. |
| 22 | L | messages.service.ts:1507-1588 | `publishScheduledMessages` cron processes up to 50 messages per tick. If more than 50 scheduled messages are overdue (e.g., after a long outage), the remaining ones are delayed by additional minutes. No catch-up mechanism or adaptive batch size. | Scheduled messages can be significantly delayed after service outages proportional to backlog size. | M — Add loop until no more overdue messages, or increase batch size. |
| 23 | I | schema.prisma:1746 | `starredBy String[] @default([])` is marked `@deprecated` with comment "use StarredMessage join table instead." The field still exists in the schema and is not removed, adding dead weight to every Message row. | Dead schema field. Not a bug but technical debt. | S — Remove field in next migration. |
| 24 | I | messages.service.ts:493 | `_skipRedisPublish` is cast via `as any` to avoid type errors. This pattern is used in both `handleMessage` (L499) and `handleSealedMessage` (L626). | Type safety bypass. Minor, but violates the no-`any` rule for non-test code. | S — Add `_skipRedisPublish?: boolean` to the sendMessage data type. |
| 25 | I | messages.service.ts:99 | `MESSAGE_SELECT` always returns `messageType: 'SYSTEM'` messages which have `senderId: null`. The sender join returns `null`, which is handled gracefully. However, SYSTEM messages from `internal-e2e.controller.ts` (L99) cast `'SYSTEM' as any` because `MessageType` enum may not include SYSTEM. | If `MessageType` enum doesn't include `SYSTEM`, these messages have an invalid enum value in the DB. | S — Add `SYSTEM` to `MessageType` enum if not present. |

**Severity counts:** C:3 H:6 M:6 L:4 I:3
**Total findings:** 22
