# T03 — Test Coverage Audit: reels, reel-templates, clips

**Auditor:** Claude Opus 4.6 (1M context)
**Date:** 2026-03-30
**Source lines:** 2,077 (reels 1,693 | reel-templates 211 | clips 173)
**Test lines:** 3,137 (reels 2,607 | reel-templates 196 | clips 334)
**Test-to-source ratio:** 1.51:1

---

## Module 1: reels (service: 1,352 LOC | controller: 341 LOC)

### Spec files (9 total)
- `reels.service.spec.ts` (996 lines)
- `reels.controller.spec.ts` (264 lines)
- `reels.carousel.spec.ts` (210 lines)
- `reels.carousel-validation.spec.ts` (158 lines)
- `reels.publish-fields.spec.ts` (287 lines)
- `reels.comment-permission.spec.ts` (146 lines)
- `reels.service.concurrency.spec.ts` (123 lines)
- `reels.service.auth.spec.ts` (119 lines)
- `reels.service.edge.spec.ts` (304 lines)

### Service methods (26 public)

| # | Method | Test Status | Notes |
|---|--------|-------------|-------|
| 1 | `create()` | Tested | Happy path, carousel, publish fields, hashtag upsert, edge cases (Arabic caption, empty caption, hashtag-only caption). Content moderation check tested indirectly. |
| 2 | `getFeed()` | Tested | Engagement scoring order, block/mute exclusion, anonymous feed, edge (limit=0). |
| 3 | `getTrendingReels()` | Tested | Engagement scoring, empty result, ordering. |
| 4 | `updateReel()` | **NOT TESTED** | 30-line method: finds reel, checks ownership, checks isRemoved, updates caption/hashtags, re-indexes. Zero tests. |
| 5 | `getById()` | Tested | Happy path, isLiked/isBookmarked, not-found, removed, PROCESSING status, without user context. |
| 6 | `recordView()` | **NOT TESTED** | Fire-and-forget view increment called from controller `getById`. No direct test. |
| 7 | `recordLoop()` | **NOT TESTED** | Fire-and-forget loop increment. No test. |
| 8 | `delete()` | Tested | Ownership, not-found, forbidden. Hashtag decrement and Stream cleanup tested via mock assertions. |
| 9 | `publishTrial()` | **NOT TESTED** | Converts trial reel to published. Checks ownership, isTrial flag, triggers publish workflow. Zero tests. |
| 10 | `like()` | Tested | Happy path, self-like prevention, duplicate (P2002), not-READY, notification. |
| 11 | `unlike()` | Tested | Happy path, not-found (P2025). |
| 12 | `comment()` | Tested | Happy path, not-found, not-READY, comment permission (EVERYONE/NOBODY/FOLLOWERS), parentId validation. |
| 13 | `deleteComment()` | Tested | Own comment, not-found, forbidden (non-author non-owner), reel-owner can delete others' comments. |
| 14 | `likeComment()` | **NOT TESTED** | Transaction-based comment like with P2002 idempotency. Zero tests in any spec file. |
| 15 | `unlikeComment()` | **NOT TESTED** | Transaction-based comment unlike. Zero tests. |
| 16 | `getComments()` | Tested | Pagination, empty, block/mute exclusion. |
| 17 | `share()` | Tested | Happy path (interactive tx), not-found. Idempotency (already shared) tested via mock. |
| 18 | `bookmark()` | Tested | Happy path, not-found, already-bookmarked (ConflictException). |
| 19 | `unbookmark()` | Tested | Happy path, not-bookmarked (NotFoundException). |
| 20 | `view()` | Tested | Happy path (interactive tx), already-viewed skip, not-found implied. |
| 21 | `getUserReels()` | Tested | By username, user-not-found. |
| 22 | `report()` | Tested | Happy path, unknown reason maps to OTHER. |
| 23 | `getByAudioTrack()` | **NOT TESTED** | Paginated query by audioTrackId with like/bookmark enrichment. Zero tests. |
| 24 | `getDuets()` | **NOT TESTED** | Paginated query by duetOfId. Controller test delegates but no service test. |
| 25 | `getStitches()` | **NOT TESTED** | Paginated query by stitchOfId. Controller test delegates but no service test. |
| 26 | `archive()` | Tested | Owner, non-owner, not-found. |
| 27 | `unarchive()` | Tested | Owner, non-owner. |
| 28 | `getShareLink()` | Tested | Happy path (via controller spec). |
| 29 | `saveDraft()` | **NOT TESTED** | Creates reel with DRAFT status. Zero tests. |
| 30 | `getDrafts()` | **NOT TESTED** | Fetches user's draft reels. Zero tests. |
| 31 | `deleteDraft()` | **NOT TESTED** | Deletes draft with ownership + status check. Zero tests. |
| 32 | `getDownloadUrl()` | **NOT TESTED** | Returns download URL with watermark text. Zero tests. |
| 33 | `getAccessibilityReport()` | **NOT TESTED** | Pure function returning accessibility score. Zero tests. |
| 34 | `moderateReelThumbnail()` (private) | **NOT TESTED** | AI moderation with BLOCK/WARNING paths. No test. |

### Controller endpoints (30)

| # | Endpoint | Method | Test Status | Notes |
|---|----------|--------|-------------|-------|
| 1 | `POST /reels` | create | Tested | Delegation verified. |
| 2 | `POST /reels/drafts` | saveDraft | **NOT TESTED** | Missing from controller mock + test. |
| 3 | `GET /reels/drafts` | getDrafts | **NOT TESTED** | Missing from controller mock + test. |
| 4 | `GET /reels/feed` | getFeed | Tested | |
| 5 | `GET /reels/trending` | getTrending | **NOT TESTED** | Missing from controller mock. `getTrendingReels` not in mockService. |
| 6 | `GET /reels/user/:username` | getUserReels | Tested | |
| 7 | `GET /reels/audio/:audioTrackId` | getByAudioTrack | **NOT TESTED** | Missing from controller mock + test. |
| 8 | `PATCH /reels/:id` | updateReel | **NOT TESTED** | Missing from controller mock + test. `updateReel` not in mockService. |
| 9 | `GET /reels/:id` | getById | Tested | Also calls recordView. |
| 10 | `DELETE /reels/:id` | delete | Tested | |
| 11 | `POST /reels/:id/like` | like | Tested | |
| 12 | `DELETE /reels/:id/like` | unlike | Tested | |
| 13 | `POST /reels/:id/comment` | comment | Tested | With and without parentId. |
| 14 | `GET /reels/:id/comments` | getComments | Tested | |
| 15 | `POST /reels/:id/comments/:commentId/like` | likeComment | Tested | Controller delegation tested. |
| 16 | `DELETE /reels/:id/comments/:commentId/like` | unlikeComment | Tested | Controller delegation tested. |
| 17 | `DELETE /reels/:id/comments/:commentId` | deleteComment | Tested | |
| 18 | `POST /reels/:id/share` | share | Tested | |
| 19 | `POST /reels/:id/bookmark` | bookmark | Tested | |
| 20 | `DELETE /reels/:id/bookmark` | unbookmark | Tested | |
| 21 | `POST /reels/:id/view` | view | Tested | Auth + anon paths. |
| 22 | `POST /reels/:id/loop` | loop | **NOT TESTED** | Missing from controller mock. `recordLoop` not in mockService. |
| 23 | `POST /reels/:id/report` | report | Tested | |
| 24 | `GET /reels/:id/duets` | getDuets | Tested | Controller delegation only. |
| 25 | `GET /reels/:id/stitches` | getStitches | Tested | Controller delegation only. |
| 26 | `PATCH /reels/:id/archive` | archive | Tested | |
| 27 | `PATCH /reels/:id/publish-trial` | publishTrial | **NOT TESTED** | Missing from controller mock. `publishTrial` not in mockService. |
| 28 | `PATCH /reels/:id/unarchive` | unarchive | Tested | |
| 29 | `GET /reels/:id/share-link` | getShareLink | Tested | |
| 30 | `GET /reels/:id/download` | getDownloadUrl | **NOT TESTED** | Missing from controller mock. `getDownloadUrl` not in mockService. |
| 31 | `DELETE /reels/drafts/:id` | deleteDraft | **NOT TESTED** | Missing from controller mock. `deleteDraft` not in mockService. |

---

## Module 2: reel-templates (service: 116 LOC | controller: 95 LOC)

### Service methods (5)

| # | Method | Test Status | Notes |
|---|--------|-------------|-------|
| 1 | `create()` | Tested | Happy path, empty name, no segments, invalid segment times. |
| 2 | `browse()` | Tested | Pagination, hasMore. |
| 3 | `getById()` | Tested | Happy path, not-found. |
| 4 | `markUsed()` | Tested | Increment useCount. |
| 5 | `delete()` | Tested | Owner, non-owner (ForbiddenException). |

### Controller endpoints (5)

| # | Endpoint | Method | Test Status | Notes |
|---|----------|--------|-------------|-------|
| 1 | `GET /reel-templates` | browse | Tested | cursor + trending flag. |
| 2 | `GET /reel-templates/:id` | getById | Tested | |
| 3 | `POST /reel-templates` | create | Tested | |
| 4 | `POST /reel-templates/:id/use` | markUsed | Tested | |
| 5 | `DELETE /reel-templates/:id` | delete | Tested | |

### Missing error-path tests

| # | Method | Missing path |
|---|--------|-------------|
| 1 | `markUsed()` | NotFoundException when template not found |
| 2 | `delete()` | NotFoundException when template not found |
| 3 | `browse()` | Trending sort order verification (trending=true returns useCount desc) |
| 4 | `browse()` | Limit clamping (limit > 50, limit < 1) |
| 5 | `create()` | Negative segment times (startMs < 0 or endMs < 0) |

---

## Module 3: clips (service: 99 LOC | controller: 74 LOC)

### Service methods (5)

| # | Method | Test Status | Notes |
|---|--------|-------------|-------|
| 1 | `create()` | Tested | Happy path, video-not-found, not-published, endTime<=startTime, >60s, exceeds duration, default title. |
| 2 | `getByVideo()` | Tested | Pagination, hasMore, cursor. |
| 3 | `getByUser()` | Tested | Pagination, cursor, hasMore. |
| 4 | `delete()` | Tested | Owner, not-found/not-owned. |
| 5 | `getShareLink()` | Tested | Happy path, not-found. |

### Controller endpoints (5)

| # | Endpoint | Method | Test Status | Notes |
|---|----------|--------|-------------|-------|
| 1 | `POST /clips/video/:videoId` | create | Tested | |
| 2 | `GET /clips/video/:videoId` | getByVideo | Tested | With limit parsing. |
| 3 | `GET /clips/me` | getByUser | Tested | |
| 4 | `DELETE /clips/:id` | delete | Tested | Including ForbiddenException propagation. |
| 5 | `GET /clips/:id/share` | getShareLink | Tested | |

### Missing coverage

| # | Method | Missing path |
|---|--------|-------------|
| 1 | `create()` | Video with `hlsUrl: null` (clipUrl becomes null) |
| 2 | `create()` | Video with `duration: null` (endTime > duration check skipped) |
| 3 | `getByVideo()` | Empty result (returns empty data with null cursor) |
| 4 | `getByUser()` | Empty result same concern |

---

## Findings Table

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 1 | C | reels | `updateReel()` / `PATCH /reels/:id` | UNTESTED | 30-line method with ownership check, isRemoved guard, Prisma update, and publish workflow re-index. Zero tests at service or controller level. Public mutation endpoint. |
| 2 | C | reels | `publishTrial()` / `PATCH /reels/:id/publish-trial` | UNTESTED | Converts trial reel to published (changes visibility). Ownership check, isTrial guard, publish workflow. Zero tests. |
| 3 | C | reels | `saveDraft()` / `POST /reels/drafts` | UNTESTED | Creates DRAFT-status reel. Zero tests at service or controller level. |
| 4 | C | reels | `getDrafts()` / `GET /reels/drafts` | UNTESTED | Fetches user's drafts. Zero tests. |
| 5 | C | reels | `deleteDraft()` / `DELETE /reels/drafts/:id` | UNTESTED | Deletes draft with ownership + status check. Zero tests. |
| 6 | H | reels | `getDownloadUrl()` / `GET /reels/:id/download` | UNTESTED | Returns original video URL + watermark text. Ownership-independent. Zero tests. |
| 7 | H | reels | `getByAudioTrack()` / `GET /reels/audio/:audioTrackId` | UNTESTED | Public browsing endpoint. Paginated query. Zero tests at service or controller level. |
| 8 | H | reels | `getDuets()` | UNTESTED (service) | Controller test delegates call but service method (parent not-found, pagination, like/bookmark enrichment) has zero direct tests. |
| 9 | H | reels | `getStitches()` | UNTESTED (service) | Same as getDuets -- controller delegates but no service-level assertion on query logic or NotFoundException. |
| 10 | H | reels | `GET /reels/trending` (controller) | UNTESTED | Controller `getTrending` not in mockService, no controller-level test. Service tested separately. |
| 11 | M | reels | `likeComment()` | UNTESTED (service) | Transaction with P2002 idempotency path. Controller delegation tested but service logic (NotFoundException for wrong reelId, duplicate handling) untested. |
| 12 | M | reels | `unlikeComment()` | UNTESTED (service) | Transaction-based unlike. Controller delegation tested but service logic untested. |
| 13 | M | reels | `recordView()` | UNTESTED | Fire-and-forget increment. Called from `getById` controller. Silently swallows errors via `.catch(() => {})`. |
| 14 | M | reels | `recordLoop()` / `POST /reels/:id/loop` | UNTESTED | Fire-and-forget loop increment. Both controller delegation and service logic untested. |
| 15 | M | reels | `getAccessibilityReport()` | UNTESTED | Pure function returning score + issues. Easy to test, zero tests. |
| 16 | M | reels | `moderateReelThumbnail()` (private) | UNTESTED | AI moderation: BLOCK removes reel + notifies, WARNING marks sensitive. No coverage of either branch. |
| 17 | M | reels | `updateReel()` — isRemoved guard | UNTESTED | BadRequestException('Reel has been removed') path untested. |
| 18 | M | reels | `getById()` — block check | UNTESTED | Block check between viewer and reel owner (NotFoundException when blocked) untested. |
| 19 | M | reels | `getById()` — scheduled content hiding | UNTESTED | Future-scheduled reels hidden from non-owners. No test covers scheduledAt future + non-owner path. |
| 20 | M | reels | `like()` — scheduled reel guard | UNTESTED | `scheduledAt > now && userId !== owner` throws NotFoundException. No test. |
| 21 | M | reels | `comment()` — parentId cross-reel | UNTESTED | Parent comment belonging to a different reel should throw NotFoundException. No test. |
| 22 | M | reels | `report()` — duplicate report | UNTESTED | `findFirst` returns existing report, returns `{ reported: true }` without creating new. No test. |
| 23 | M | reels | `report()` — reel not found | UNTESTED | NotFoundException when reel doesn't exist. No test. |
| 24 | M | reels | `delete()` — hashtag decrement | UNTESTED | Hashtag counter decrement for removed reel (reel with hashtags[] populated). No specific test. |
| 25 | M | reel-templates | `markUsed()` — not found | UNTESTED | NotFoundException when template doesn't exist. No test. |
| 26 | M | reel-templates | `delete()` — not found | UNTESTED | NotFoundException when template doesn't exist. No test. |
| 27 | M | reel-templates | `create()` — negative segment times | UNTESTED | `startMs < 0` or `endMs < 0` should throw BadRequestException. No test. |
| 28 | L | reels | `controller.spec` — mockService incomplete | WEAK MOCK | mockService missing 10 methods: `getTrendingReels`, `updateReel`, `publishTrial`, `getDownloadUrl`, `getByAudioTrack`, `saveDraft`, `getDrafts`, `deleteDraft`, `recordLoop`, `getAccessibilityReport`. Tests cannot catch routing regressions for these endpoints. |
| 29 | L | reels | `getUserReels()` — own vs other | WEAK ASSERTION | Test doesn't verify that owner sees trial/scheduled reels while others don't. Only tests basic username lookup. |
| 30 | L | reels | `getFeed()` — anonymous path | WEAK ASSERTION | Tests userId=string but never userId=undefined (anonymous browsing). `getFeed(undefined)` not tested despite `OptionalClerkAuthGuard`. |
| 31 | L | reels | `bookmark()` — scheduled reel guard | WEAK | Service checks `scheduledAt > now` before allowing bookmark. Not tested. |
| 32 | L | clips | `create()` — null hlsUrl | WEAK | When video has `hlsUrl: null`, clipUrl is set to `null`. Not tested. |
| 33 | L | clips | `create()` — null duration | WEAK | When video has `duration: null`, endTime check is skipped. Not tested. |
| 34 | L | reel-templates | `browse()` — trending sort | WEAK ASSERTION | Test doesn't verify that `trending=true` changes sort order to `useCount: desc`. |
| 35 | L | reel-templates | `browse()` — limit clamping | WEAK | `Math.min(Math.max(limit, 1), 50)` clamping logic not tested with boundary values. |
| 36 | I | reels | All spec files | SUGGESTION | 9 separate spec files for one service is excessive fragmentation. Consolidating into 2-3 (core, edge, auth) would reduce boilerplate (each file re-creates the identical TestingModule). |
| 37 | I | reels | `REEL_SELECT` constant in spec | SUGGESTION | Test file duplicates REEL_SELECT from service.ts but with fewer fields (missing `streamId`, `hlsUrl`, `dashUrl`, `qualities`, `isLooping`, `normalizeAudio`, `isPhotoCarousel`, `carouselUrls`, `carouselTexts`, `altText`, `locationName`, `locationLat`, `locationLng`, `commentPermission`, `brandedContent`, `brandPartner`, `remixAllowed`, `topics`, `isTrial`, `scheduledAt`). This means mock assertions against SELECT shape will not catch field drift. |
| 38 | I | clips | `getByVideo()` — empty result cursor | SUGGESTION | When `clips` is empty, `clips[clips.length - 1]?.id` is `undefined` but returned as cursor. Should be `null` for consistency. |

---

## Summary

### reels
- **34 public methods/endpoints** total (service + controller)
- **20 tested**, **14 untested** (41% gap)
- Strong: like/unlike, comment permissions, carousel validation, concurrency, auth matrix
- Weak: Entire draft CRUD (3 endpoints), updateReel, publishTrial, download, audio track browsing, comment like/unlike at service level

### reel-templates
- **5 service methods, 5 endpoints** -- all tested at both layers
- Missing: 3 error-path tests (not-found on markUsed/delete, negative segment times)

### clips
- **5 service methods, 5 endpoints** -- all tested at both layers
- Coverage is thorough including edge cases (default title, cursor pagination, boundary conditions)
- Missing: 2 minor null-value edge cases

### Severity Counts

**C:5 H:4 M:13 L:7 I:3 Total: 32**
