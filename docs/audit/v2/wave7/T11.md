# T11 — Test Coverage Audit: islamic, mosques, halal, scholar-qa, live, audio-rooms, broadcast

## Module Summary

| Module | Source LOC | Test LOC | Ratio | Spec Files |
|--------|-----------|----------|-------|------------|
| islamic | 3,725 (service 2317, controller 821, notifications 282, prayer-calc 305) | 3,123 (6 spec files) | 0.84 | 6 |
| mosques | 295 (service 176, controller 119) | 290 (2 spec files) | 0.98 | 2 |
| halal | 316 (service 190, controller 126) | 305 (2 spec files) | 0.97 | 2 |
| scholar-qa | 251 (service 151, controller 100) | 318 (2 spec files) | 1.27 | 2 |
| live | 693 (service 462, controller 231) | 680 (4 spec files) | 0.98 | 4 |
| audio-rooms | 736 (service 540, controller 196) | 816 (2 spec files) | 1.11 | 2 |
| broadcast | 503 (service 317, controller 186) | 636 (3 spec files) | 1.26 | 3 |

---

## 1. ISLAMIC MODULE

### 1.1 islamic.controller.ts (55 endpoints)

| # | Sev | Method/Endpoint | Test Status | Issue |
|---|-----|----------------|-------------|-------|
| 1 | H | `GET /islamic/hadith` (getHadiths) | controller: NOT tested | Controller test mock lists method but no test block exercises it |
| 2 | H | `POST /islamic/hadiths/:id/bookmark` (bookmarkHadith) | controller: NOT tested | Mock for `toggleHadithBookmark` missing from controller spec; endpoint completely untested at controller layer |
| 3 | H | `POST /islamic/mosques/follow` (followMosque) | controller: NOT tested | `followMosque` not mocked or tested in controller spec |
| 4 | H | `GET /islamic/mosques/my-mosque` (getMyMosqueTimes) | controller: NOT tested | `getFollowedMosqueTimes` not mocked or tested in controller spec |
| 5 | M | `GET /islamic/prayer-notifications/settings` | controller: NOT tested | Mock present but no test block |
| 6 | M | `PATCH /islamic/prayer-notifications/settings` | controller: NOT tested | Mock present but no test block |
| 7 | M | `GET /islamic/quran-plans/active` | controller: NOT tested | Mock present but no test block |
| 8 | M | `GET /islamic/quran-plans/history` | controller: NOT tested | Mock present but no test block |
| 9 | M | `PATCH /islamic/quran-plans/:id` | controller: NOT tested | Mock present but no test block |
| 10 | H | `GET /islamic/quran/chapters/:surahNumber/verses` | controller: NOT tested | No controller test |
| 11 | H | `GET /islamic/quran/chapters/:surahNumber/verses/:ayahNumber` | controller: NOT tested | No controller test |
| 12 | H | `GET /islamic/quran/juz/:juzNumber` | controller: NOT tested | No controller test |
| 13 | H | `GET /islamic/quran/random-ayah` | controller: NOT tested | No controller test |
| 14 | M | `POST /islamic/charity/campaigns` | controller: NOT tested | No controller test for campaign creation |
| 15 | M | `GET /islamic/charity/campaigns` | controller: NOT tested | No controller test for campaign listing |
| 16 | M | `GET /islamic/charity/campaigns/:id` | controller: NOT tested | No controller test |
| 17 | M | `GET /islamic/charity/my-donations` | controller: NOT tested | No controller test |
| 18 | M | `GET /islamic/hajj/progress` | controller: NOT tested | Mock present but no test block |
| 19 | M | `POST /islamic/hajj/progress` | controller: NOT tested | Mock present but no test block |
| 20 | M | `PATCH /islamic/hajj/progress/:id` | controller: NOT tested | Mock present but no test block |
| 21 | M | `GET /islamic/tafsir/sources` | controller: NOT tested | Mock present but no test block |
| 22 | M | `GET /islamic/scholar-verification/status` | controller: NOT tested | Mock present but no test block |
| 23 | M | `GET /islamic/content-filter/settings` | controller: NOT tested | No controller test |
| 24 | M | `PATCH /islamic/content-filter/settings` | controller: NOT tested | No controller test |
| 25 | M | `POST /islamic/dhikr/sessions` | controller: NOT tested | Mock present but no test block |
| 26 | H | `GET /islamic/dhikr/community` | controller: NOT tested | `getCommunityDhikrTotal` not mocked in controller spec |
| 27 | M | `GET /islamic/dhikr/stats` | controller: NOT tested | Mock present but no test block |
| 28 | M | `GET /islamic/dhikr/leaderboard` | controller: NOT tested | Mock present but no test block |
| 29 | M | `POST /islamic/dhikr/challenges` | controller: NOT tested | Mock present but no test block |
| 30 | M | `GET /islamic/dhikr/challenges` | controller: NOT tested | Mock present but no test block |
| 31 | M | `GET /islamic/dhikr/challenges/:id` | controller: NOT tested | Mock present but no test block |
| 32 | M | `POST /islamic/dhikr/challenges/:id/join` | controller: NOT tested | Mock present but no test block |
| 33 | H | `GET /islamic/prayer-times/current-window` | controller: NOT tested | `getCurrentPrayerWindow` mock present but no test block |
| 34 | M | `GET /islamic/fasting/log` | controller: NOT tested | No controller test |
| 35 | M | `GET /islamic/fasting/stats` | controller: NOT tested | No controller test |
| 36 | M | `GET /islamic/duas` | controller: NOT tested | No controller test |
| 37 | M | `GET /islamic/duas/daily` | controller: NOT tested | No controller test |
| 38 | M | `GET /islamic/duas/categories` | controller: NOT tested | No controller test |
| 39 | M | `GET /islamic/duas/bookmarked` | controller: NOT tested | No controller test |
| 40 | M | `DELETE /islamic/duas/:id/bookmark` | controller: NOT tested | No controller test |
| 41 | M | `GET /islamic/names-of-allah` | controller: NOT tested | No controller test |
| 42 | M | `GET /islamic/names-of-allah/daily` | controller: NOT tested | No controller test |
| 43 | M | `GET /islamic/hifz/progress` | controller: NOT tested | No controller test |
| 44 | M | `GET /islamic/hifz/stats` | controller: NOT tested | No controller test |
| 45 | M | `GET /islamic/hifz/review-schedule` | controller: NOT tested | No controller test |
| 46 | M | `GET /islamic/daily-tasks/today` | controller: NOT tested | No controller test |
| 47 | H | `GET /islamic/glossary` | controller: NOT tested | `getGlossary` not mocked in controller spec |
| 48 | H | `POST /islamic/classify-content` | controller: NOT tested | `classifyIslamicContent` not mocked in controller spec |
| 49 | H | `POST /islamic/detect-hadith-grade` | controller: NOT tested | `detectHadithGrade` not mocked in controller spec |

### 1.2 islamic.service.ts — Untested Methods

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 50 | C | `toggleHadithBookmark` | NOT tested | Service method exists (line 323-336) but no test in any spec file. Uses `prisma.hadithBookmark` which is not mocked. |
| 51 | C | `followMosque` | NOT tested | Controller calls it but no service test exists. Method not visible in read portion but called by controller. |
| 52 | C | `getFollowedMosqueTimes` | NOT tested | Controller calls it but no service test exists. |
| 53 | M | `getCurrentPrayerWindow` | Partially tested | Called internally by getRamadanInfo but no direct service-level test for the public endpoint version |
| 54 | M | `getAdhanReciters` | NOT tested | Pure data method, no test |
| 55 | M | `getCalculationMethods` | NOT tested | Returns 8 methods, no test (different from `getPrayerMethods` which returns 5) |
| 56 | M | `getQuranReciters` | NOT tested | Pure data method, no test |
| 57 | M | `getQuranAudioUrl` | NOT tested | URL construction logic untested |
| 58 | L | `getAudioAyahNumber` | NOT tested | Private helper with offset table, no test verifying correctness |

### 1.3 islamic-notifications.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 59 | L | `isInPrayerDND` — fallback from mosque coordinates | Tested for null case | No test for the `hgetall` fallback path where mosque lat/lng is used to compute prayer times |
| 60 | L | `shouldShowPrayFirstNudge` — full path | Weakly tested | Returns `{show: boolean}` but no test exercises the path where `isInPrayerDND` returns true AND prayer name is returned |
| 61 | L | `getJummahReminder` | Weakly tested | Assertion only checks property existence, not behavior on a Friday vs non-Friday (time-dependent) |
| 62 | L | `getRamadanStatus` | Tested but non-deterministic | Test checks `isRamadan` is boolean, but cannot verify correctness since Hijri date is computed from current date |

### 1.4 prayer-calculator.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 63 | L | `calculatePrayerTimes` | Well tested | 7 tests covering locations, methods, ordering. Good. |
| 64 | L | `getRamadanDatesForYear` | Well tested | 3 tests. Good. |

---

## 2. MOSQUES MODULE

### 2.1 mosques.controller.ts (8 endpoints)

All 8 endpoints have controller tests. Coverage is good.

### 2.2 mosques.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 65 | M | `join` — ConflictException path | NOT tested | Service test mocks `findUnique` to return null (no existing member), but never tests the `ConflictException` path when user is already a member |
| 66 | L | `findNearby` — distance sort correctness | Weakly tested | Test returns empty array; no test verifies Haversine distance calculation or sort order with multiple mosques |
| 67 | L | `createPost` — with mediaUrls | NOT tested | Only tested without mediaUrls parameter |
| 68 | L | `getMembers` — pagination hasMore | NOT tested | Only tests single-page result; never verifies `hasMore: true` scenario |

---

## 3. HALAL MODULE

### 3.1 halal.controller.ts (6 endpoints)

All 6 endpoints have controller tests. Coverage is good.

### 3.2 halal.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 69 | M | `addReview` — ConflictException for duplicate review | NOT tested | The `ConflictException` path when `findUnique` returns an existing review is never exercised |
| 70 | M | `verifyHalal` — ConflictException for already-verified | NOT tested | The `existingReview` check on `verifyHalal` is never tested for the duplicate-vote path |
| 71 | L | `findNearby` — pagination hasMore | NOT tested | Never tests with limit+1 results to verify `hasMore: true` |
| 72 | L | `getReviews` — pagination hasMore | NOT tested | Never tests `hasMore: true` scenario |

---

## 4. SCHOLAR-QA MODULE

### 4.1 scholar-qa.controller.ts (9 endpoints)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 73 | M | `PUT :id/questions/:qid/answered` (markAnswered) | NOT tested at controller layer | Controller test has no `markAnswered` describe block |

### 4.2 scholar-qa.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 74 | M | `schedule` — ForbiddenException when not verified scholar | NOT tested | Mock returns `{ status: 'approved' }` always. But service code checks for `'VERIFICATION_APPROVED'` status. The mock value `'approved'` never matches, meaning the test passes for the wrong reason — the `findFirst` returns the object but the status comparison `status: 'VERIFICATION_APPROVED'` in the `where` clause means Prisma filters it. Since it's mocked, the mock ALWAYS returns the verification object regardless of the `where` status filter, so the auth check is silently bypassed. |
| 75 | M | `voteQuestion` — self-vote prevention | NOT tested | `BadRequestException` for `question.userId === userId` is not tested |
| 76 | M | `voteQuestion` — ConflictException for double vote | NOT tested | `existingVote` path via `scholarQuestionVote.findUnique` not tested |
| 77 | M | `voteQuestion` — P2002 race condition handler | NOT tested | The catch block for concurrent P2002 duplicates is not exercised |

---

## 5. LIVE MODULE

### 5.1 live.controller.ts (22 endpoints)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 78 | H | `POST :id/guests/invite` (inviteGuest) | NOT tested at controller layer | Mock missing from controller spec |
| 79 | H | `POST :id/guests/accept` (acceptGuest) | NOT tested at controller layer | Mock missing from controller spec |
| 80 | H | `DELETE :id/guests/:userId` (removeGuest) | NOT tested at controller layer | Mock missing from controller spec |
| 81 | H | `GET :id/guests` (listGuests) | NOT tested at controller layer | Mock missing from controller spec |
| 82 | H | `POST /live/rehearse` (startRehearsal) | NOT tested at controller layer | Mock missing from controller spec |
| 83 | H | `PATCH :id/go-live` (goLive) | NOT tested at controller layer | Mock missing from controller spec |
| 84 | H | `PATCH :id/end-rehearsal` (endRehearsal) | NOT tested at controller layer | Mock missing from controller spec |
| 85 | H | `PATCH :id/subscribers-only` (setSubscribersOnly) | NOT tested at controller layer | Mock missing from controller spec |

### 5.2 live.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 86 | C | `inviteGuest` — happy path (host invites, max 4 check) | NOT tested | Only auth tests (non-host rejection) exist; no test for successful invite or max-4 limit enforcement |
| 87 | C | `acceptGuestInvite` | NOT tested | No test in any spec file. Race condition re-check for >4 accepted guests also untested |
| 88 | C | `removeGuest` — happy path | NOT tested | Only auth test (non-host rejection); no test for successful removal |
| 89 | C | `listGuests` | NOT tested | No test in any spec file |
| 90 | C | `startRehearsal` | NOT tested | No test in any spec file |
| 91 | C | `goLiveFromRehearsal` | NOT tested | No test in any spec file |
| 92 | C | `endRehearsal` | NOT tested | No test in any spec file |
| 93 | M | `setSubscribersOnly` — happy path | NOT tested | Only auth test (non-host rejection) exists; no test for successful toggle |
| 94 | M | `join` — subscribers-only enforcement | NOT tested | `isSubscribersOnly` path with `follow.findUnique` never tested |
| 95 | M | `join` — re-join after leaving | NOT tested | The `existing && existing.leftAt` branch is never exercised |
| 96 | M | `join` — host short-circuit | NOT tested | `hostId === userId` early return path untested |
| 97 | M | `startLive` — ENDED/CANCELLED rejection | Tested for ENDED only | CANCELLED rejection path not tested |
| 98 | M | `cancelLive` — already-CANCELLED rejection | Tested | OK |
| 99 | L | `startLive` — Cloudflare Stream integration failure fallback | NOT tested | The `catch` block when `stream.createLiveInput` fails is not tested |

---

## 6. AUDIO-ROOMS MODULE

### 6.1 audio-rooms.controller.ts (16 endpoints)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 100 | H | `GET /audio-rooms/active` (getActiveRooms) | NOT tested at controller layer | Mock missing from controller spec |
| 101 | H | `GET /audio-rooms/upcoming` (getUpcomingRooms) | NOT tested at controller layer | Mock missing from controller spec |
| 102 | H | `GET /audio-rooms/recordings` (listRecordings) | NOT tested at controller layer | Mock missing from controller spec |
| 103 | H | `GET :id/recording` (getRecording) | NOT tested at controller layer | Mock missing from controller spec |
| 104 | H | `POST :id/recording/start` (startRecording) | NOT tested at controller layer | Mock missing from controller spec |
| 105 | H | `POST :id/recording/stop` (stopRecording) | NOT tested at controller layer | Mock missing from controller spec |

### 6.2 audio-rooms.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 106 | C | `startRecording` | NOT tested | No test in any spec file. Host check, room-not-found, room-not-live paths all untested |
| 107 | C | `stopRecording` | NOT tested | No test in any spec file. Duration calculation untested |
| 108 | C | `getRecording` | NOT tested | No test in any spec file. NotFoundException for missing recording untested |
| 109 | C | `listRecordings` | NOT tested | No test in any spec file |
| 110 | C | `getActiveRooms` | NOT tested | No test in any spec file |
| 111 | C | `getUpcomingRooms` | NOT tested | No test in any spec file |
| 112 | C | `createPersistentRoom` | NOT tested | No test in any spec file |
| 113 | M | `changeRole` — maxSpeakers enforcement | Tested | OK — test verifies speaker count check |
| 114 | M | `join` — P2002 race condition handler | NOT tested | The Prisma P2002 catch block for duplicate join is not exercised |

---

## 7. BROADCAST MODULE

### 7.1 broadcast.controller.ts (19 endpoints)

All 19 endpoints have controller tests. Coverage is good.

### 7.2 broadcast.service.ts

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 115 | M | `subscribe` — P2002 race condition handler | NOT tested | Catch block for duplicate subscription never exercised |
| 116 | M | `sendMessage` — notification fan-out to subscribers | NOT tested | The background `.then()` chain that creates notifications is completely untested |
| 117 | M | `sendMessage` — Redis publish for real-time | NOT tested | `redis.publish('content:update', ...)` not verified |
| 118 | L | `getSubscribers` — authorization check when no userId | NOT tested | When `userId` is undefined, `requireRole` is skipped — this open-access path not explicitly tested |
| 119 | L | `discover` — cursor pagination | Weakly tested | Only tests empty result; never tests hasMore=true |

---

## Cross-Cutting Issues

| # | Sev | Module | Issue |
|---|-----|--------|-------|
| 120 | I | islamic | Controller spec mocks 53 service methods but only tests ~18 of them. 35 endpoints have mocks ready but zero test blocks. This is deceptive — the mock setup suggests coverage that doesn't exist. |
| 121 | I | live | Controller spec only mocks 13 of 22 service methods. 9 endpoints (guests, rehearsal, subscribers-only) have zero controller tests AND zero mocks. |
| 122 | I | audio-rooms | Controller spec mocks 10 of 16 service methods. 6 endpoints (active, upcoming, recordings, recording start/stop, getRecording) completely absent from controller tests. |
| 123 | I | all | No integration-style test verifies that Prisma `$executeRaw` SQL (used in live viewer counts, broadcast subscriber counts, dhikr challenge counters) produces correct SQL. These are validated only at deploy time per CLAUDE.md. |
| 124 | I | islamic | `createDonation` has a `throw BadRequestException` on line 675 BEFORE the validation logic on lines 677-685. The dead code after the throw is unreachable and untestable. Tests correctly verify the throw but the validation logic below it can never execute. |

---

## Severity Counts

| Severity | Count | Description |
|----------|-------|-------------|
| C (critical-path-untested) | 14 | Core features with zero test coverage: toggleHadithBookmark, followMosque, getFollowedMosqueTimes, live guest management (invite/accept/remove/list), rehearsal (start/go-live/end), audio recording (start/stop/get/list), active/upcoming rooms, persistent rooms |
| H (public-endpoint-untested) | 19 | Public endpoints missing controller-layer tests: 11 islamic controller endpoints missing tests entirely (glossary, classify, hadith-grade, Quran verse endpoints, community dhikr, prayer window), 8 live controller endpoints, 6 audio-rooms controller endpoints |
| M (error-path-untested) | 38 | Error/edge paths not tested: ConflictException paths (mosques join, halal review, halal verify, scholar vote, broadcast subscribe P2002), ForbiddenException paths, validation branches, notification fan-out, re-join logic |
| L (weak-assertion) | 11 | Tests that check existence but not behavior: non-deterministic date tests, pagination hasMore never verified, distance sort not verified |
| I (suggestion) | 5 | Deceptive mock setups, unreachable dead code, raw SQL untested |

**C:14 H:19 M:38 L:11 I:5 Total: 87**
