# T13 — Test Coverage Audit: Queue Processors

**Scope:** `apps/api/src/common/queue/processors/*.ts`, `queue.service.ts`, `with-correlation.ts`, all `*.processor.ts`

## Inventory

### Processors (6 files, 6 workers)

| Processor | Queue Name | Job Handlers | Test File Exists |
|-----------|-----------|-------------|-----------------|
| `notification.processor.ts` | `notifications` | `push-trigger`, `bulk-push` | Yes |
| `analytics.processor.ts` | `analytics` | `award-xp`, `update-streak`, `track-engagement` | Yes |
| `search-indexing.processor.ts` | `search-indexing` | `index`, `update`, `delete` | Yes |
| `ai-tasks.processor.ts` | `ai-tasks` | `moderate`, `generate-caption` | Yes |
| `webhook.processor.ts` | `webhooks` | `deliver` (via `deliverWebhook`) | Yes |
| `media.processor.ts` | `media-processing` | `image-resize`, `blurhash`, `video-transcode` | Yes |

### Supporting Files

| File | Test File |
|------|----------|
| `queue.service.ts` | **NONE** (no `queue.service.spec.ts`) |
| `with-correlation.ts` | **NONE** (no test file anywhere) |
| `queue.module.ts` | **NONE** |

---

## Findings

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 1 | C | `queue.service.ts` | Entire class (7 public methods) | **NO TEST FILE** | `QueueService` has zero unit tests. `addPushNotificationJob`, `addGamificationJob`, `addWebhookDeliveryJob`, `addSearchIndexJob`, `addModerationJob`, `moveToDlq`, `getStats` -- none tested directly. `moveToDlq` has complex dual-storage logic (Redis + Prisma FailedJob) with fallback to Sentry; zero assertions on any of it. |
| 2 | C | `queue.service.ts` | `moveToDlq()` | **UNTESTED** | 50-line method with: final-attempt guard, Redis LPUSH+LTRIM, Prisma FailedJob.create, Promise.allSettled dual-write, Sentry capture on double-failure. Zero test coverage. All processor specs mock it as a no-op. |
| 3 | C | `queue.service.ts` | `getStats()` | **UNTESTED** | Iterates 6 queues, calls 5 BullMQ count methods each, catches per-queue errors returning zeroed stats. Zero test assertions. Health controller spec mocks it; never tests the real implementation. |
| 4 | C | `queue.service.ts` | `withCorrelation()` | **UNTESTED** | Private method that reads from AsyncLocalStorage via `getCorrelationId()` and merges into job data. Never verified that correlation IDs actually propagate into enqueued jobs. |
| 5 | C | `with-correlation.ts` | `attachCorrelationId()` | **NO TEST FILE** | Exported function used by all 6 processors. Extracts correlationId from job.data, sets Sentry scope tag, logs. Zero test coverage. |
| 6 | C | `notification.processor.ts` | `bulk-push` handler | **DEAD CODE** | Processor handles `bulk-push` jobs, but QueueService has NO `addBulkPushJob()` method. No producer anywhere in the codebase calls `notificationsQueue.add('bulk-push', ...)`. The processBulkPush logic (DB persist + push send) is unreachable in production. Test exists but tests dead code. |
| 7 | C | `media.processor.ts` | All 3 handlers (`image-resize`, `blurhash`, `video-transcode`) | **DEAD CODE** | QueueService has NO method to enqueue media jobs. `mediaQueue` is injected but only used in `close()` and `getStats()`. No producer calls `.add()` on it. All 3 handlers are unreachable. |
| 8 | C | `analytics.processor.ts` | `track-engagement` handler | **DEAD CODE** | QueueService has `addGamificationJob()` (for `award-xp`/`update-streak`) but NO method for `track-engagement`. The handler is a no-op stub anyway (logs only, no storage). Unreachable from any producer. |
| 9 | C | `ai-tasks.processor.ts` | `generate-caption` handler | **DEAD CODE + UNTESTED** | Handler throws `Error('Caption generation not implemented')`. No QueueService method enqueues this job type. Handler is unreachable AND unimplemented. No test covers it. |
| 10 | H | `webhook.processor.ts` | `deliverWebhook()` happy path | **UNTESTED** | Test covers URL validation (HTTP/localhost/private IP rejection) and HMAC signature headers. But: successful delivery updating `prisma.webhook.update({ lastUsedAt })` is NOT tested. Non-2xx response throwing `Error('Webhook delivery failed')` is NOT tested. Only the HMAC signature test actually calls `deliverWebhook` to completion. |
| 11 | H | `webhook.processor.ts` | `onModuleInit` / `onModuleDestroy` | **UNTESTED** | Unlike notification/analytics/media processors, webhook processor spec has zero lifecycle tests. No test for worker creation with REDIS_URL, no test for graceful shutdown. |
| 12 | H | `ai-tasks.processor.ts` | `onModuleInit` / `onModuleDestroy` | **UNTESTED** | No lifecycle tests. No test for worker creation with REDIS_URL or shutdown. |
| 13 | M | `notification.processor.ts` | `processBulkPush` error path | **WEAK** | Test verifies `createMany` is called but does NOT test the `.catch()` branch where DB persist fails but push still sends. The catch only logs -- push delivery continues regardless of DB failure. |
| 14 | M | `media.processor.ts` | `processImageResize()` happy path | **UNTESTED** | Test only verifies SSRF rejection (internal URL blocked). The actual sharp image processing, EXIF stripping, resize variants, R2 upload -- none tested. (Mocking sharp/S3 is feasible.) |
| 15 | M | `media.processor.ts` | `processBlurHash()` happy path | **UNTESTED** | Test only verifies SSRF rejection. The actual blurhash encode + Prisma write to reel/post/story/video -- none tested. |
| 16 | M | `media.processor.ts` | `processBlurHash()` content types | **UNTESTED** | Handler writes blurhash to 4 different Prisma models (reel, post, story, video) based on contentType. Zero tests for any of these branches. |
| 17 | M | `media.processor.ts` | `processImageResize()` fetch failure | **UNTESTED** | `response.ok` check throws on fetch failure. Not tested. |
| 18 | M | `media.processor.ts` | `processImageResize()` missing R2 credentials | **UNTESTED** | Code has an if/else for missing R2 credentials (skips upload, logs debug). Not tested. |
| 19 | M | `ai-tasks.processor.ts` | `processModeration()` thread/reel/message content types | **UNTESTED** | Test covers `post` and `comment` content types. `thread`, `reel`, and `message` content type branches (lines 117-124) for looking up reportedUserId have zero test assertions. |
| 20 | M | `search-indexing.processor.ts` | `update` action | **UNTESTED** | Test covers `index`, `delete`, and missing-document edge cases. The `update` action (which shares code path with `index`) has no explicit test. |
| 21 | M | `search-indexing.processor.ts` | Unknown action `default` throw | **UNTESTED** | `throw new Error('Unknown search index action: ...')` at line 96. No test verifies this error path. |
| 22 | M | `notification.processor.ts` | Unknown job name `default` throw | **UNTESTED** | `throw new Error('Unknown notification job type: ...')` at line 50. No test. |
| 23 | M | `analytics.processor.ts` | Unknown job name `default` throw | **UNTESTED** | `throw new Error('Unknown analytics job type: ...')` at line 62. No test. |
| 24 | M | `ai-tasks.processor.ts` | Unknown job name `default` throw | **UNTESTED** | `throw new Error('Unknown AI task job type: ...')` at line 59. No test. |
| 25 | M | `media.processor.ts` | Unknown job name `default` throw | **UNTESTED** | `throw new Error('Unknown media job type: ...')` at line 79. No test. |
| 26 | M | `webhook.processor.ts` | `AbortSignal.timeout(15000)` | **UNTESTED** | Webhook delivery has a 15s timeout via `AbortSignal.timeout()`. Not tested (fetch timeout behavior). |
| 27 | M | `queue.service.ts` | `onModuleDestroy()` | **UNTESTED** | Closes all 6 queues via `Promise.allSettled`. Not tested. |
| 28 | M | `queue.service.ts` | Circuit breaker integration | **UNTESTED** | All `add*Job` methods wrap queue.add() in `this.circuitBreaker.exec('redis', ...)`. Circuit breaker failure path (Redis down) never tested. |
| 29 | L | `analytics.processor.spec.ts` | `processEngagementTracking` | **WEAK ASSERTION** | Test only checks `resolves.not.toThrow()`. The handler is a no-op (just logs). Test doesn't verify anything meaningful -- it's testing that a log statement doesn't crash. |
| 30 | L | `search-indexing.processor.spec.ts` | `onModuleInit` | **WEAK ASSERTION** | Tests `expect(processor).toBeInstanceOf(SearchIndexingProcessor)` and `not.toThrow()`. These are tautological -- proving the class is itself and that a no-op doesn't throw. |
| 31 | L | `notification.processor.spec.ts` | Worker event handlers | **UNTESTED** | `on('completed')` (duplicate! registered twice) and `on('failed')` event handlers are not tested. The mock Worker's `.on()` is never invoked with callbacks in tests. |
| 32 | L | All processor specs | Worker `on('failed')` -> DLQ | **NEVER VERIFIED** | All 6 processors register `worker.on('failed', ...)` that calls `this.queueService.moveToDlq(job, err, queueName)`. Since BullMQ Worker is fully mocked, these event handlers are never invoked in any test. The DLQ path is completely untested. |
| 33 | L | `queue.module.ts` | No-op queue stub | **UNTESTED** | When REDIS_URL is not set in dev, a no-op stub is created that silently drops jobs. The stub's behavior (returning noop IDs, logging) is never tested. |
| 34 | L | `queue.module.ts` | Production REDIS_URL guard | **UNTESTED** | In production/staging, missing REDIS_URL throws with a detailed error message. Not tested. |
| 35 | I | `notification.processor.ts` | Duplicate `on('completed')` handler | Not a test issue | Lines 66-68 and 76-79 both register `worker.on('completed', ...)`. The first logs "completed", the second checks for slow jobs. Both fire on every completion. Consider consolidating into one handler. |
| 36 | I | All processors | Identical boilerplate | N/A | All 6 processors share identical patterns: REDIS_URL check, Worker creation, 3 event handlers (completed, failed, slow-job completed), onModuleDestroy. Consider extracting a base class or factory to reduce 600+ lines of duplication. |
| 37 | I | `queue.service.ts` | `moveToDlq` double-await | Correctness issue | Lines 191 and 194 both `await Promise.allSettled([redisDone, dbDone])`. The first await resolves both promises; the second re-awaits already-resolved promises. The `.catch()` handlers on lines 173 and 186 swallow errors, so `redisDone`/`dbDone` always resolve to `undefined`. The `status === 'rejected'` check on line 195 will NEVER be true -- caught errors return fulfilled promises. The Sentry fallback is dead code. |

---

## Dead Code Summary (handlers with no producer)

| Processor | Handler | QueueService Method | Producer Calls | Verdict |
|-----------|---------|-------------------|----------------|---------|
| notification | `bulk-push` | **NONE** | 0 | DEAD |
| analytics | `track-engagement` | **NONE** | 0 | DEAD |
| ai-tasks | `generate-caption` | **NONE** | 0 | DEAD |
| media | `image-resize` | **NONE** | 0 | DEAD |
| media | `blurhash` | **NONE** | 0 | DEAD |
| media | `video-transcode` | **NONE** | 0 | DEAD |

6 of 13 total job handlers (46%) are dead code with no way to enqueue them.

## Active Producers (handlers that ARE reachable)

| QueueService Method | Producer Locations | Job Name in Processor |
|--------------------|-------------------|----------------------|
| `addPushNotificationJob` | islamic.service, notifications.service, users.service | `push-trigger` |
| `addGamificationJob` | posts, threads, reels, videos, stories, channels, scheduling (20+ call sites) | `award-xp`, `update-streak` |
| `addWebhookDeliveryJob` | **NONE found** (only in mock-providers.ts) | `deliver` |
| `addSearchIndexJob` | auth, privacy, reports, publish-workflow, search-reconciliation (15+ call sites) | `index`, `update`, `delete` |
| `addModerationJob` | posts.service, reels.service, reports.service | `moderate` |

Note: `addWebhookDeliveryJob` exists in QueueService but has zero real callers. The webhook processor handles delivery, but nothing ever enqueues a webhook job.

---

## Severity Counts

**C: 9** | **H: 3** | **M: 16** | **L: 6** | **I: 3** | **Total: 37**
