# T12 — Test Coverage Audit: Admin, Waitlist, Common Services

**Scope:** admin module, waitlist module, and common services: circuit-breaker, feature-flags, payment-reconciliation, counter-reconciliation, meilisearch-sync, ab-testing, analytics, email, publish-workflow, query-diagnostics, search-reconciliation

## Summary

| Module/Service | Source Lines | Test Lines | Ratio | Spec File Exists |
|---|---|---|---|---|
| admin.service.ts | 349 | 515 | 1.48 | Yes |
| admin.controller.ts | 139 | 192 | 1.38 | Yes |
| waitlist.service.ts | 178 | 0 | 0.00 | **NO** (tested via controller.spec) |
| waitlist.controller.ts | 37 | 264 | 7.14 | Yes |
| circuit-breaker.service.ts | 190 | 348 | 1.83 | Yes |
| feature-flags.service.ts | 147 | 0 | 0.00 | **NO** |
| ab-testing.service.ts | 278 | 191 | 0.69 | Yes |
| payment-reconciliation.service.ts | 632 | 830 | 1.31 | Yes |
| counter-reconciliation.service.ts | 610 | 0 | 0.00 | **NO** |
| meilisearch-sync.service.ts | 281 | 0 | 0.00 | **NO** |
| analytics.service.ts | 119 | 0 | 0.00 | **NO** |
| email.service.ts | 203 | 583 | 2.87 | Yes |
| publish-workflow.service.ts | 115 | 0 | 0.00 | **NO** |
| query-diagnostics.service.ts | 99 | 0 | 0.00 | **NO** |
| search-reconciliation.service.ts | 228 | 0 | 0.00 | **NO** |

**8 of 15 files have ZERO test coverage.**

---

## Findings

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|---|---|---|---|---|
| 1 | C | feature-flags | `isEnabled()` | **NO TESTS** | Core feature flag read path untested. Returns boolean based on flag value ("true"/"false"/percentage). No spec file exists. Used across entire app to gate features. |
| 2 | C | feature-flags | `isEnabledForUser()` | **NO TESTS** | Percentage rollout per-user (hash-based bucketing) completely untested. Deterministic hash could be wrong, bucket distribution could be skewed, nobody would know. |
| 3 | C | feature-flags | `setFlag()` | **NO TESTS** | Dual-write path (Redis + DB) untested. DB failure logging path untested. Cache invalidation (`this.localCache = null`) untested. |
| 4 | C | feature-flags | `deleteFlag()` | **NO TESTS** | Delete from Redis + DB untested. DB failure path untested. |
| 5 | C | feature-flags | `getAllFlags()` | **NO TESTS** | Redis -> DB fallback chain untested. Both-down returns empty object untested. |
| 6 | C | feature-flags | `getFlagValue()` (private) | **NO TESTS** | 3-tier fallback (local cache -> Redis -> DB) untested. Cache TTL (30s) behavior untested. Redis failure -> stale cache reuse untested. Both Redis+DB down returns null untested. |
| 7 | C | counter-reconciliation | `reconcileUserFollowCounts()` | **NO TESTS** | Cron job reconciling followersCount/followingCount uses raw SQL. 610 lines of SQL-heavy service with zero test coverage. Financial and user-visible counter integrity at stake. |
| 8 | C | counter-reconciliation | `reconcilePostCounts()` | **NO TESTS** | likesCount/commentsCount reconciliation untested. |
| 9 | C | counter-reconciliation | `reconcileUserPostCounts()` | **NO TESTS** | User postsCount reconciliation untested. |
| 10 | C | counter-reconciliation | `reconcilePostSavesCounts()` | **NO TESTS** | savesCount reconciliation untested. |
| 11 | C | counter-reconciliation | `reconcilePostSharesCounts()` | **NO TESTS** | sharesCount reconciliation untested. |
| 12 | C | counter-reconciliation | `reconcileUnreadCounts()` | **NO TESTS** | Unread message count reconciliation untested. Directly affects chat UX (badge counts). |
| 13 | C | counter-reconciliation | `reconcileCoinBalances()` | **NO TESTS** | Negative coin/diamond balance detection and reset untested. Financial data. |
| 14 | C | counter-reconciliation | `reconcileUserContentCounts()` | **NO TESTS** | threadsCount/reelsCount reconciliation untested. |
| 15 | C | counter-reconciliation | `reconcileReelCounts()` | **NO TESTS** | Reel likesCount/commentsCount reconciliation untested. |
| 16 | C | counter-reconciliation | `reconcileThreadCounts()` | **NO TESTS** | Thread likesCount/repliesCount reconciliation untested. |
| 17 | C | counter-reconciliation | `reconcileVideoCounts()` | **NO TESTS** | Video likesCount/commentsCount reconciliation untested. |
| 18 | C | counter-reconciliation | `reconcileHashtagCounts()` | **NO TESTS** | Hashtag postsCount reconciliation untested. |
| 19 | C | counter-reconciliation | `reconcileAll()` | **NO TESTS** | Admin-triggered full reconciliation (calls all 12 sub-methods) untested. Exposed via admin controller. |
| 20 | C | meilisearch-sync | `syncAll()` | **NO TESTS** | Full Meilisearch reindex triggered by admin. 281 lines, 6 sync methods (users, posts, threads, reels, videos, hashtags). Zero test coverage. Batch cursor pagination logic untested. |
| 21 | C | meilisearch-sync | `syncUsers()` | **NO TESTS** | User sync with batch cursor pagination untested. |
| 22 | C | meilisearch-sync | `syncPosts()` | **NO TESTS** | Post sync untested. |
| 23 | C | meilisearch-sync | `syncThreads()` | **NO TESTS** | Thread sync untested. |
| 24 | C | meilisearch-sync | `syncReels()` | **NO TESTS** | Reel sync untested. |
| 25 | C | meilisearch-sync | `syncVideos()` | **NO TESTS** | Video sync untested. |
| 26 | C | meilisearch-sync | `syncHashtags()` | **NO TESTS** | Hashtag sync untested. |
| 27 | C | search-reconciliation | `reconcileSearchIndex()` | **NO TESTS** | Weekly cron re-indexes 7 content types (posts, threads, reels, videos, users, hashtags) + removes deleted content. 228 lines, zero tests. |
| 28 | C | analytics | `track()` | **NO TESTS** | Core analytics event tracking. Buffered writes, auto-flush at 100 events, MAX_BUFFER_SIZE cap. Zero tests. |
| 29 | C | analytics | `increment()` | **NO TESTS** | Redis counter increment with pipeline + 24h TTL untested. |
| 30 | C | analytics | `getCounter()` | **NO TESTS** | Counter read untested. |
| 31 | C | analytics | `getCounters()` | **NO TESTS** | Bulk counter read (mget) untested. |
| 32 | C | analytics | `flush()` (private) | **NO TESTS** | Buffer-to-Redis pipeline flush untested. Error retry (put events back in buffer) untested. ltrim to 100K untested. |
| 33 | C | analytics | `onModuleDestroy()` | **NO TESTS** | Cleanup (clearInterval + final flush) untested. |
| 34 | C | publish-workflow | `onPublish()` | **NO TESTS** | Publication workflow: search indexing + cache invalidation (SCAN-based) + Redis pub/sub. 115 lines, zero tests. Called by admin ban/unban and all content creation paths. |
| 35 | C | publish-workflow | `onUnpublish()` | **NO TESTS** | Unpublish workflow: search deletion + Redis pub/sub untested. |
| 36 | C | query-diagnostics | `diagnoseHotPaths()` | **NO TESTS** | EXPLAIN ANALYZE runner for 6 critical queries. Plan parsing, cost extraction, scan type detection all untested. |
| 37 | H | admin.controller | `POST /admin/search/sync` | **NO TESTS** | Admin endpoint to trigger full Meilisearch sync. Not tested in controller spec. Verifies admin, calls meilisearchSync.syncAll(). |
| 38 | H | admin.controller | `POST /admin/counters/reconcile` | **NO TESTS** | Admin endpoint to trigger counter reconciliation. Not tested in controller spec. Verifies admin, calls counterReconciliation.reconcileAll(). |
| 39 | H | admin.controller | `PATCH /admin/flags/:name` | setFlag tested but **validation untested** | setFlag endpoint is tested for delegation, but the BadRequestException for invalid flag values (regex validation: must be "true"/"false"/number 0-100) is NOT tested. |
| 40 | M | admin.service | `resolveReport()` TEMP_BAN path | **NO TESTS** | Service handles action='TEMP_BAN' (72-hour ban via banUser), but no test covers this path. |
| 41 | M | admin.service | `resolveReport()` MUTE path | **NO TESTS** | Service handles action='MUTE' (increment warningsCount on user), but no test covers this path. |
| 42 | M | admin.service | `resolveReport()` comment removal | **NO TESTS** | When REMOVE_CONTENT and reportedCommentId is set, should mark comment as isRemoved. Only post removal is tested. |
| 43 | M | admin.service | `resolveReport()` message removal | **NO TESTS** | When REMOVE_CONTENT and reportedMessageId is set, should mark message as isDeleted. Not tested. |
| 44 | M | admin.service | `resolveReport()` WARNING notification | **NO TESTS** | When action=WARN and reportedUserId exists, sends notification. Not tested (notificationsService.create mock not asserted). |
| 45 | M | admin.service | `resolveReport()` moderationLog creation | **WEAK** | moderationLog.create mock exists but test does not assert it was called for any action. |
| 46 | M | admin.service | `resolveReport()` adminAuditLog creation | **WEAK** | adminAuditLog.create mock exists but never asserted. |
| 47 | M | admin.service | `banUser()` adminAuditLog creation | **NO TESTS** | Audit log for ban action not verified in any test. |
| 48 | M | admin.service | `banUser()` publishWorkflow.onUnpublish | **NO TESTS** | Banned user removal from search index not verified. |
| 49 | M | admin.service | `unbanUser()` publishWorkflow.onPublish | **NO TESTS** | Unbanned user re-indexing not verified. |
| 50 | M | admin.service | `banUser()` permanent ban (no duration) | **WEAK** | Test passes duration=24 and duration=undefined but does not assert banExpiresAt is null when duration is omitted. |
| 51 | M | ab-testing | `onModuleInit()` | **NO TESTS** | Hydration from DB to Redis on startup completely untested. DB experiments not in Redis path untested. |
| 52 | M | ab-testing | `getExperiment()` DB fallback | **PARTIAL** | Returns null for missing experiment from Redis, but DB fallback path (Redis null -> DB lookup -> re-populate Redis) never tested. |
| 53 | M | ab-testing | `getExperiments()` DB fallback | **NO TESTS** | When Redis SCAN returns empty keys, DB fallback for experiments list untested. |
| 54 | M | ab-testing | `getVariant()` empty variants | **NO TESTS** | Returns 'control' when experiment has empty variants array, but not explicitly tested. |
| 55 | M | ab-testing | `deleteExperiment()` DB deletion | **WEAK** | Asserts Redis del called but does not verify prisma.experiment.delete was called. |
| 56 | M | ab-testing | `createExperiment()` DB mirror | **WEAK** | Verifies Redis set but does not assert prisma.experiment.upsert was called (it's fire-and-forget). |
| 57 | M | ab-testing | `getMetrics()` | **WEAK** | Test mocks produce incomplete data. Does not verify actual event name extraction from Redis key suffix. |
| 58 | M | waitlist | WaitlistService (standalone) | **NO SPEC FILE** | WaitlistService has no dedicated spec. All tests go through WaitlistController spec, which is fine for integration, but means service-level error paths (Prisma failures, Redis failures) are not isolated. |
| 59 | M | waitlist | `getPositionByEmail()` null entry | **NO TESTS** | Private method uses `!` non-null assertion on findUnique result. If email doesn't exist, this throws an unhandled TypeError, not a user-friendly error. |
| 60 | M | waitlist | `getCachedCount()` Redis set failure | **NO TESTS** | Redis.set failure in cache-write path not tested. The `.catch(() => {})` silently swallows. |
| 61 | M | email | `sendRawHtml()` | **NO TESTS** | Public method that delegates to private send(). Used by WaitlistService. No direct test for sendRawHtml itself. |
| 62 | L | admin.controller | `deleteFlag()` non-admin rejection | **NO TESTS** | Test only checks happy path. verifyAdmin rejection not tested for this endpoint. |
| 63 | L | admin.controller | `setFlag()` non-admin rejection | **NO TESTS** | Test only checks happy path. verifyAdmin rejection not tested for this endpoint. |
| 64 | L | admin.controller | `getFlags()` non-admin rejection | **NO TESTS** | verifyAdmin rejection not tested. Only happy path tested. |
| 65 | L | circuit-breaker | timeout behavior | **NO TESTS** | Default timeout configs (3s for Redis, 10s for Stripe) never tested. Only `timeout: false` used in tests. |
| 66 | L | payment-reconciliation | `reconcileTips()` diamond calc edge case | **WEAK** | Only tests net=9, diamonds=1285. Does not test zero net amount (amount equals platformFee), or null receiverId. |
| 67 | L | payment-reconciliation | membership `unpaid` status | **WEAK** | Only tests 'canceled', not 'unpaid' which is also handled on line 458. |
| 68 | L | admin.service | `resolveReport()` default action mapping | **NO TESTS** | When action string doesn't match any known value (falls through all if/else), status='RESOLVED' and actionTaken='NONE'. Not tested. |
| 69 | L | ab-testing | `scanKeys()` multi-page cursor | **NO TESTS** | SCAN-based key collection only tested with single-page response (cursor='0' immediately). Multi-page pagination untested. |
| 70 | I | counter-reconciliation | SQL correctness | n/a | All 12 reconciliation methods use raw SQL ($queryRaw, $executeRaw). Table names mix quoted-lowercase ("posts") and PascalCase ("Post"). This inconsistency (e.g., UPDATE "posts" vs "Post") cannot be caught without integration tests. |
| 71 | I | meilisearch-sync | `isAvailable()` guard | n/a | syncAll() early-returns `{ error: -1 }` when Meilisearch unavailable. Callers may not handle negative numbers in a Record<string, number>. |
| 72 | I | publish-workflow | cache invalidation SCAN pattern | n/a | Uses `feed:*:${userId}:*` pattern in SCAN. If userId contains special characters, the MATCH pattern could behave unexpectedly. |
| 73 | I | feature-flags | `simpleHash()` | n/a | Module-level function (not a method). Deterministic hash for percentage rollout. If the hash distribution is poor, A/B tests and feature rollouts will be skewed. Should have unit tests for distribution uniformity. |

---

## Module-by-Module Detail

### 1. Admin Module

**admin.service.ts (349 lines) / admin.service.spec.ts (515 lines) -- ratio 1.48:1**

Public methods:
- `verifyAdmin()` -- Tested (3 cases: admin, non-admin, missing user)
- `getReports()` -- Tested (5 cases: basic, pagination, status filter, cursor, non-admin rejection)
- `getReport()` -- Tested (3 cases: found, not-found, non-admin)
- `resolveReport()` -- Partially tested (WARN, DISMISS, REMOVE_CONTENT for post, BAN_USER, not-found, non-admin). **Missing: TEMP_BAN, MUTE, comment removal, message removal, WARNING notification, moderationLog assertion, adminAuditLog assertion**
- `getStats()` -- Tested (2 cases: success, non-admin)
- `banUser()` -- Well tested (7 cases: basic+clerk, clerk failure, admin target, missing target, duration, non-admin, no clerkId). **Missing: adminAuditLog, publishWorkflow.onUnpublish assertions**
- `unbanUser()` -- Tested (3 cases: success+clerk, clerk failure, non-admin). **Missing: publishWorkflow.onPublish assertion**

**admin.controller.ts (139 lines) / admin.controller.spec.ts (192 lines) -- ratio 1.38:1**

Endpoints:
- `GET /admin/reports` -- Tested (2 cases)
- `GET /admin/reports/:id` -- Tested (2 cases)
- `PATCH /admin/reports/:id` -- Tested (1 case)
- `GET /admin/stats` -- Tested (1 case)
- `POST /admin/users/:id/ban` -- Tested (2 cases)
- `POST /admin/users/:id/unban` -- Tested (1 case)
- `GET /admin/flags` -- Tested (1 case)
- `PATCH /admin/flags/:name` -- Tested (1 case, **no validation test**)
- `DELETE /admin/flags/:name` -- Tested (1 case)
- `POST /admin/search/sync` -- **NOT TESTED**
- `POST /admin/counters/reconcile` -- **NOT TESTED**

### 2. Waitlist Module

**waitlist.service.ts (178 lines) / NO dedicated spec -- tested through controller spec (264 lines)**

Public methods:
- `join()` -- Tested via controller (7 cases: new join, normalize email, already joined, valid referral, invalid referral, name+source, email send+failure)
- `getStats()` -- Tested via controller (3 cases: Redis cache hit, cache miss, Redis error)
- `getPosition()` -- Tested via controller (2 cases: valid code, invalid code)

Private methods:
- `getPositionByEmail()` -- Used internally, tested indirectly. **Non-null assertion on line 106 is a crash risk if email not found.**
- `getCachedCount()` -- Tested indirectly (cache hit, miss, error paths)
- `sendConfirmationEmail()` -- Tested indirectly (send success, send failure)

### 3. Circuit Breaker Service

**circuit-breaker.service.ts (190 lines) / circuit-breaker.spec.ts (348 lines) -- ratio 1.83:1**

Public methods:
- `getBreaker()` -- Tested (5 cases: create, idempotent, separate names, default configs, custom options)
- `exec()` -- Tested (7 cases: success, value return, error propagation, circuit open, fail-fast, fallback-open, fallback-closed, auto-create)
- `getStatus()` -- Tested (6 cases: empty, all registered, closed state, open state, stats fields, success tracking)
- `onModuleDestroy()` -- Tested (2 cases: shutdown all, error during shutdown)
- Half-open recovery -- Tested (1 case with short resetTimeout)
- Sentry integration -- Tested (1 case)

**Good coverage. Missing: timeout behavior with real timeout configs.**

### 4. Feature Flags Service -- **COMPLETELY UNTESTED**

**feature-flags.service.ts (147 lines) / NO spec file -- ratio 0.00**

Public methods (ALL untested):
- `isEnabled()` -- Untested
- `isEnabledForUser()` -- Untested (percentage rollout with hash bucketing)
- `setFlag()` -- Untested (dual-write Redis+DB)
- `deleteFlag()` -- Untested (dual-delete)
- `getAllFlags()` -- Untested (Redis->DB fallback)
- Private `getFlagValue()` -- Untested (3-tier fallback: local cache -> Redis -> DB)
- Module-level `simpleHash()` -- Untested

### 5. A/B Testing Service

**ab-testing.service.ts (278 lines) / ab-testing.spec.ts (191 lines) -- ratio 0.69:1**

Public methods:
- `onModuleInit()` -- **NOT TESTED** (DB->Redis hydration)
- `createExperiment()` -- Tested (1 case: Redis set verified). **DB upsert not asserted.**
- `getExperiment()` -- Tested (2 cases: found, not found from Redis). **DB fallback not tested.**
- `getExperiments()` -- Tested (2 cases: found, empty). **DB fallback not tested.**
- `getVariant()` -- Tested (5 cases: disabled, missing, existing assignment, new assignment, consistency). **Empty variants not tested.**
- `getUserAssignments()` -- Tested (1 case)
- `trackConversion()` -- Tested (1 case)
- `getMetrics()` -- Tested (2 cases: found, missing). **Event name parsing weak.**
- `deleteExperiment()` -- Tested (1 case). **DB delete not asserted.**
- Private `scanKeys()` -- Only tested with single-page cursor. Multi-page untested.

### 6. Payment Reconciliation Service

**payment-reconciliation.service.ts (632 lines) / payment-reconciliation.spec.ts (830 lines) -- ratio 1.31:1**

**Well covered.** All 5 reconciliation methods tested. Key gaps:
- `reconcileTips()` zero net amount (amount == platformFee) not tested
- `reconcileTips()` null receiverId not tested
- `reconcileMembershipSubscriptions()` 'unpaid' Stripe status not explicitly tested (same code path as 'canceled')
- `reconcileAll()` tested for normal and partial-failure scenarios

### 7. Counter Reconciliation Service -- **COMPLETELY UNTESTED**

**counter-reconciliation.service.ts (610 lines) / NO spec file -- ratio 0.00**

12 public methods (ALL untested):
- `reconcileUserFollowCounts()` -- followersCount + followingCount
- `reconcilePostCounts()` -- likesCount + commentsCount on posts
- `reconcileUserPostCounts()` -- postsCount on users
- `reconcilePostSavesCounts()` -- savesCount on posts
- `reconcilePostSharesCounts()` -- sharesCount on posts
- `reconcileUnreadCounts()` -- unreadCount on conversation members
- `reconcileCoinBalances()` -- negative balance detection + reset
- `reconcileUserContentCounts()` -- threadsCount + reelsCount
- `reconcileReelCounts()` -- reel engagement counters
- `reconcileThreadCounts()` -- thread engagement counters
- `reconcileVideoCounts()` -- video engagement counters
- `reconcileHashtagCounts()` -- hashtag postsCount
- `reconcileAll()` -- orchestrator for admin trigger

### 8. Meilisearch Sync Service -- **COMPLETELY UNTESTED**

**meilisearch-sync.service.ts (281 lines) / NO spec file -- ratio 0.00**

7 methods (ALL untested):
- `syncAll()` -- orchestrator
- `syncUsers()`, `syncPosts()`, `syncThreads()`, `syncReels()`, `syncVideos()`, `syncHashtags()` -- batch cursor pagination + document mapping

### 9. Analytics Service -- **COMPLETELY UNTESTED**

**analytics.service.ts (119 lines) / NO spec file -- ratio 0.00**

Public methods (ALL untested):
- `track()` -- buffer cap, auto-flush at 100
- `increment()` -- Redis pipeline + TTL
- `getCounter()` -- single counter read
- `getCounters()` -- bulk mget read
- `onModuleDestroy()` -- cleanup
- Private `flush()` -- buffer->Redis pipeline, error retry

### 10. Email Service

**email.service.ts (203 lines) / email.service.spec.ts (583 lines) -- ratio 2.87:1**

**Well covered.** All public methods tested including XSS escaping, template wrapping, Resend initialization, send success/failure. One gap: `sendRawHtml()` not directly tested (only indirectly via WaitlistService).

### 11. Publish Workflow Service -- **COMPLETELY UNTESTED**

**publish-workflow.service.ts (115 lines) / NO spec file -- ratio 0.00**

Public methods (ALL untested):
- `onPublish()` -- search indexing + SCAN-based cache invalidation + Redis pub/sub
- `onUnpublish()` -- search deletion + Redis pub/sub

### 12. Query Diagnostics Service -- **COMPLETELY UNTESTED**

**query-diagnostics.service.ts (99 lines) / NO spec file -- ratio 0.00**

Public methods (ALL untested):
- `diagnoseHotPaths()` -- EXPLAIN ANALYZE on 6 queries, plan parsing, cost extraction

### 13. Search Reconciliation Service -- **COMPLETELY UNTESTED**

**search-reconciliation.service.ts (228 lines) / NO spec file -- ratio 0.00**

Public methods (ALL untested):
- `reconcileSearchIndex()` -- weekly cron, 7 content types (posts, threads, reels, videos, users, hashtags), index + delete paths

---

## Severity Counts

**C: 36** (critical-path-untested -- entire services with zero tests)
**H: 2** (public endpoints untested in admin controller)
**M: 16** (error paths and side-effect assertions missing)
**L: 8** (weak assertions, edge cases missing)
**I: 4** (suggestions/observations)

**Total: 66**

## Priority Recommendations

1. **feature-flags.service.ts** (C, 6 findings) -- Used across the entire app. Zero tests. Needs spec immediately.
2. **counter-reconciliation.service.ts** (C, 13 findings) -- 610 lines of raw SQL, financial data (coin balances), user-visible counters. Zero tests.
3. **analytics.service.ts** (C, 6 findings) -- Core event tracking with buffer/flush logic. Zero tests.
4. **meilisearch-sync.service.ts** (C, 7 findings) -- Admin-triggered full reindex. Zero tests.
5. **publish-workflow.service.ts** (C, 2 findings) -- Called on every content publish/unpublish + admin ban/unban. Zero tests.
6. **search-reconciliation.service.ts** (C, 1 finding) -- Weekly cron, 228 lines. Zero tests.
7. **query-diagnostics.service.ts** (C, 1 finding) -- Plan parsing logic. Zero tests.
8. **admin.service resolveReport()** (M, 5 findings) -- Multiple action paths (TEMP_BAN, MUTE, comment/message removal, WARNING notification) untested.
9. **ab-testing onModuleInit() + DB fallbacks** (M, 4 findings) -- Startup hydration and resilience paths untested.
