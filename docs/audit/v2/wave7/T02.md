# T02 — Test Coverage Audit: Posts, Bookmarks, Collabs, Polls

**Auditor:** Claude Opus 4.6 (1M context)
**Date:** 2026-03-30
**Scope:** `apps/api/src/modules/{posts,bookmarks,collabs,polls}/`

## Source vs Test Line Counts

| Module | Source LOC | Test LOC | Ratio (test:src) |
|--------|-----------|----------|-----------------|
| posts | 2,518 (service 2,004 + controller 514) | 4,020 (11 spec files) | 1.60:1 |
| bookmarks | 568 (service 414 + controller 154) | 545 (4 spec files) | 0.96:1 |
| collabs | 171 (service 110 + controller 61) | 203 (2 spec files) | 1.19:1 |
| polls | 337 (service 247 + controller 90) | 481 (3 spec files) | 1.43:1 |
| **TOTAL** | **3,594** | **5,249** | **1.46:1** |

---

## POSTS MODULE

### PostsController (514 lines, 36 endpoints)

| # | Sev | Method/Endpoint | Test Status | Issue |
|---|-----|----------------|-------------|-------|
| 1 | C | `GET /posts/feed` (getFeed) | Tested | Controller delegates, 1 test |
| 2 | C | `POST /posts` (create) | Tested | Controller delegates, 1 test |
| 3 | C | `GET /posts/:id` (getById) | Tested | Controller delegates, 1 test |
| 4 | C | `PATCH /posts/:id` (update) | Tested | Controller delegates, 1 test |
| 5 | C | `DELETE /posts/:id` (delete) | Tested | Controller delegates, 1 test |
| 6 | C | `POST /posts/:id/react` (react) | Tested | Controller delegates, 1 test |
| 7 | C | `DELETE /posts/:id/react` (unreact) | Tested | Controller delegates, 1 test |
| 8 | C | `POST /posts/:id/save` (save) | Tested | Controller delegates, 1 test |
| 9 | C | `DELETE /posts/:id/save` (unsave) | Tested | Controller delegates, 1 test |
| 10 | C | `POST /posts/:id/share` (share) | Tested | Controller delegates, 1 test |
| 11 | C | `GET /posts/:id/comments` (getComments) | Tested | Controller delegates, 1 test |
| 12 | C | `POST /posts/:id/comments` (addComment) | Tested | Controller delegates, 1 test |
| 13 | H | `GET /posts/:id/comments/hidden` (getHiddenComments) | **NO controller test** | Controller endpoint untested — only service tested |
| 14 | C | `GET /posts/:id/comments/:commentId/replies` (getCommentReplies) | Tested | Controller delegates, 1 test |
| 15 | C | `PATCH /posts/:id/comments/:commentId` (editComment) | Tested | Controller delegates, 1 test |
| 16 | C | `DELETE /posts/:id/comments/:commentId` (deleteComment) | Tested | Controller delegates, 1 test |
| 17 | C | `POST /posts/:id/comments/:commentId/like` (likeComment) | Tested | Controller delegates, 1 test |
| 18 | C | `DELETE /posts/:id/comments/:commentId/like` (unlikeComment) | Tested | Controller delegates, 1 test |
| 19 | C | `POST /posts/:id/report` (report) | Tested | Controller delegates, 1 test |
| 20 | C | `POST /posts/:id/dismiss` (dismiss) | Tested | Controller delegates, 1 test |
| 21 | H | `POST /posts/:id/archive` (archive) | **NO controller test** | Service tested, controller delegation untested |
| 22 | H | `DELETE /posts/:id/archive` (unarchive) | **NO controller test** | Service tested, controller delegation untested |
| 23 | H | `POST /posts/:id/comments/:commentId/pin` (pinComment) | **NO controller test** | Service tested, controller delegation untested |
| 24 | H | `DELETE /posts/:id/comments/:commentId/pin` (unpinComment) | **NO controller test** | Service tested, controller delegation untested |
| 25 | H | `POST /posts/:id/comments/:commentId/hide` (hideComment) | **NO controller test** | Service tested, controller delegation untested |
| 26 | H | `DELETE /posts/:id/comments/:commentId/hide` (unhideComment) | **NO controller test** | Service tested, controller delegation untested |
| 27 | H | `GET /posts/:id/share-link` (getShareLink) | **NO controller test** | Service tested, controller delegation untested |
| 28 | C | `POST /posts/:id/share-as-story` (shareAsStory) | Tested | Controller delegates, 1 test |
| 29 | C | `POST /posts/:id/cross-post` (crossPost) | **NO test at any level** | Service + controller both untested |
| 30 | C | `PATCH /posts/tags/:tagId/respond` (respondToTag) | **NO test at any level** | Service + controller both untested |
| 31 | C | `POST /posts/:id/save-to-collection` (saveToCollection) | **NO test at any level** | Service + controller both untested |
| 32 | H | `GET /posts/collections` (getCollections) | **NO test at any level** | Service + controller both untested (distinct from bookmarks module) |
| 33 | H | `GET /posts/collections/:name` (getCollection) | **NO test at any level** | Service + controller both untested |
| 34 | H | `GET /posts/:id/related` (getRelatedPosts) | **NO test at any level** | Service + controller both untested |
| 35 | H | `PATCH /posts/:id/pin` (pinPost) | **NO test at any level** | Service + controller both untested |
| 36 | H | `GET /posts/:id/analytics` (getPostAnalytics) | Tested | Service tested in posts.analytics.spec.ts (5 tests) |
| 37 | H | `GET /posts/:id/repurpose` (getRepurposeSuggestions) | **NO test at any level** | Service + controller both untested |
| 38 | H | `POST /posts/:id/impression` (trackImpression) | Tested | Service tested in posts.analytics.spec.ts (1 test) |
| 39 | H | `GET /posts/archived` (getArchived) | **NO controller test** | Service tested, controller delegation untested |
| 40 | H | `GET /posts/predict-engagement` (predictEngagement) | **NO test at any level** | Service + controller both untested |

### PostsService (2,004 lines, ~35 public methods + 1 cron + private helpers)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 41 | C | `create()` | Well tested | 30+ tests across publish-fields, schedule, edge, tag resolution specs |
| 42 | M | `create()` — content safety rejection | **NOT tested** | `contentSafety.moderateText` blocks unsafe content, but no test triggers the block path |
| 43 | M | `create()` — duplicate post detection | **NOT tested** | The ConflictException for duplicate content within 5 min is untested |
| 44 | C | `getFeed('following')` | Tested | 5 tests including blocked user exclusion, zero-follow fallback, blended feed |
| 45 | C | `getFeed('foryou')` | Tested | 2 tests covering scored feed cache |
| 46 | M | `getFeed('chronological')` | **NOT directly tested** | The chronological branch delegates to `getChronologicalFeed()` which is never exercised in tests |
| 47 | M | `getFeed('favorites')` | **NOT tested** | The favorites branch delegates to `getFavoritesFeed()` which is never exercised in tests |
| 48 | C | `getById()` | Well tested | 8 tests including removed, blocked user, isSaved, viewer scenarios |
| 49 | M | `getById()` — scheduled post hidden from non-owner | **NOT tested** | Line 757: `scheduledAt > now && user !== viewerId` path untested |
| 50 | C | `update()` | Well tested | 4 tests for ownership, not found, removed, edit window |
| 51 | M | `update()` — 15-minute edit window rejection | **NOT tested** | Line 813: `postAge > fifteenMinutesMs` path is asserted via BadRequestException test for "removed" but NOT for the edit window specifically |
| 52 | C | `delete()` | Well tested | 3 tests including ownership, not found |
| 53 | C | `react()` | Well tested | 7 tests including error paths, race condition, reaction update |
| 54 | M | `react()` — self-react rejection | **NOT tested** | Line 898: `post.userId === userId` throws BadRequestException, never tested |
| 55 | C | `unreact()` | Tested | 3 tests including idempotent null reaction |
| 56 | C | `save()` | Tested | 3 tests including not found, removed |
| 57 | C | `unsave()` | Tested | 2 tests |
| 58 | C | `share()` | Tested | 5 tests including duplicate, removed, blocked |
| 59 | M | `share()` — block check | Tested | In posts.service.abuse.spec.ts but ONLY the happy path; no blocked-user-share rejection test |
| 60 | C | `shareAsStory()` | Tested at controller level | Service-level NOT tested (no test for "no media" BadRequestException, block check) |
| 61 | C | `getComments()` | Tested | 3 tests including pagination, empty |
| 62 | C | `addComment()` | Well tested | 8 tests in comment-permission spec + main spec |
| 63 | C | `editComment()` | Tested | 4 tests |
| 64 | C | `deleteComment()` | Tested | 3 tests including post-owner deletion |
| 65 | M | `deleteComment()` — post owner deletes other's comment | Tested in auth spec | Only checks comment author permission; post-owner-deleting path covered |
| 66 | C | `likeComment()` | Tested | 3 tests |
| 67 | M | `likeComment()` — P2002 race condition | **NOT tested** | Lines 1410-1414: catch P2002 → ConflictException, untested |
| 68 | C | `unlikeComment()` | Tested | 2 tests |
| 69 | C | `report()` | Tested | 2 tests |
| 70 | M | `report()` — duplicate report idempotency | **NOT tested** | Line 1441: `findFirst` returns existing report → returns `{reported: true}` without creating, untested |
| 71 | C | `dismiss()` | Tested | 1 test |
| 72 | C | `archivePost()` | Tested | 4 tests |
| 73 | C | `unarchivePost()` | Tested | 3 tests |
| 74 | C | `getArchived()` | Tested | 1 test |
| 75 | C | `pinComment()` | Tested | 5 tests |
| 76 | C | `unpinComment()` | Tested | 2 tests |
| 77 | C | `hideComment()` | Tested | 3 tests |
| 78 | C | `unhideComment()` | Tested | 2 tests |
| 79 | C | `getHiddenComments()` | Tested | 3 tests |
| 80 | C | `getShareLink()` | Tested | 3 tests |
| 81 | C | `crossPost()` | **NO test** | Entire method (44 lines) untested — includes content moderation, space validation, post creation |
| 82 | C | `getRelatedPosts()` | **NO test** | 20-line method untested — hashtag-based related post lookup |
| 83 | C | `pinPost()` | **NO test** | 18-line method untested — includes unpin-previous logic, ownership check |
| 84 | C | `respondToTag()` | **NO test** | 25-line method untested — PostTaggedUser + ReelTaggedUser dual lookup |
| 85 | C | `saveToCollection()` | **NO test** | 23-line method untested — upsert-or-create logic |
| 86 | H | `getCollections()` (posts version) | **NO test** | 14-line method untested — groupBy query |
| 87 | H | `getCollection()` | **NO test** | 33-line method untested — cursor pagination |
| 88 | C | `predictEngagement()` | **NO test** | 96-line method untested — complex analytics with hour buckets, media vs text ratios |
| 89 | C | `getRepurposeSuggestions()` | **NO test** | 34-line method untested — conditional suggestion generation |
| 90 | M | `notifyScheduledPostsPublished()` (cron) | **NO test** | 37-line cron job untested — finds scheduled posts, dedup via Redis, sends notifications |
| 91 | M | `recordView()` | **NO test** | Fire-and-forget view counter, no test exercises this path |
| 92 | M | `moderatePostImage()` (private) | **NO test** | 55-line private method handling BLOCK/WARNING/error paths. Called from `create()` but image moderation mocked away |
| 93 | L | `getPostAnalytics()` controller test | **NO controller test** | Service tested, but controller delegation untested |
| 94 | L | `trackImpression()` controller test | **NO controller test** | Service tested (1 test), controller untested |
| 95 | L | `getImpressionCount()` | Tested | 1 test (service only), not exposed via controller |

### PostsController mock completeness

| # | Sev | Issue |
|---|-----|-------|
| 96 | L | Controller mock service missing 16 methods | `mockService` object in controller.spec.ts only mocks 20 of 36+ service methods. Missing: `archivePost`, `unarchivePost`, `getArchived`, `pinComment`, `unpinComment`, `hideComment`, `unhideComment`, `getHiddenComments`, `getShareLink`, `crossPost`, `respondToTag`, `saveToCollection`, `getCollections`, `getCollection`, `getRelatedPosts`, `pinPost`, `getPostAnalytics`, `getRepurposeSuggestions`, `trackImpression`, `predictEngagement` |

---

## BOOKMARKS MODULE

### BookmarksController (154 lines, 14 endpoints)

| # | Sev | Method/Endpoint | Test Status | Issue |
|---|-----|----------------|-------------|-------|
| 97 | C | `POST /bookmarks/posts` (savePost) | Tested | 1 controller test |
| 98 | C | `GET /bookmarks/posts` (getSavedPosts) | **NO controller test** | Service tested, controller untested |
| 99 | C | `GET /bookmarks/posts/:postId/status` (isPostSaved) | Tested | 1 controller test |
| 100 | H | `PATCH /bookmarks/posts/:postId/move` (moveToCollection) | **NO controller test** | Service tested, controller untested |
| 101 | C | `DELETE /bookmarks/posts/:postId` (unsavePost) | **NO controller test** | Service tested, controller untested |
| 102 | C | `GET /bookmarks/collections` (getCollections) | Tested | 1 controller test |
| 103 | H | `GET /bookmarks/threads` (getSavedThreads) | **NO controller test** | Service tested via auth spec |
| 104 | H | `GET /bookmarks/threads/:threadId/status` (isThreadSaved) | **NO controller test** | Service `isThreadSaved()` has **NO test** at any level |
| 105 | H | `POST /bookmarks/threads/:threadId` (saveThread) | **NO controller test** | Service tested |
| 106 | H | `DELETE /bookmarks/threads/:threadId` (unsaveThread) | **NO controller test** | Service tested |
| 107 | H | `GET /bookmarks/videos` (getSavedVideos) | **NO controller test** | Service tested via auth spec |
| 108 | H | `GET /bookmarks/videos/:videoId/status` (isVideoSaved) | **NO controller test** | Service `isVideoSaved()` has **NO test** at any level |
| 109 | H | `POST /bookmarks/videos/:videoId` (saveVideo) | **NO controller test** | Service tested |
| 110 | H | `DELETE /bookmarks/videos/:videoId` (unsaveVideo) | **NO controller test** | Service tested |

### BookmarksService (414 lines, 14 public methods)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 111 | C | `savePost()` | Well tested | 3 tests — happy path, not found, collection update |
| 112 | M | `savePost()` — P2002 race condition | **NOT tested** | Lines 54-59: concurrent duplicate handling untested |
| 113 | C | `unsavePost()` | Tested | 2 tests — happy path, P2025 |
| 114 | C | `saveThread()` | Tested | 2 tests |
| 115 | M | `saveThread()` — P2002 idempotency | **NOT tested** | Lines 116-123: concurrent duplicate handling untested |
| 116 | C | `unsaveThread()` | Tested | 1 test (happy path only) |
| 117 | M | `unsaveThread()` — P2025 error path | **NOT tested** | Lines 136-139: NotFoundException on not-found untested |
| 118 | C | `saveVideo()` | Tested | 2 tests |
| 119 | M | `saveVideo()` — P2002 idempotency | **NOT tested** | Lines 165-172: concurrent duplicate handling untested |
| 120 | C | `unsaveVideo()` | Tested | 1 test (happy path only) |
| 121 | M | `unsaveVideo()` — P2025 error path | **NOT tested** | Lines 193-196: NotFoundException on not-found untested |
| 122 | C | `getSavedPosts()` | Tested | 2 tests — basic + collection filter |
| 123 | M | `getSavedPosts()` — cursor pagination | **NOT tested** | Cursor branch (lines 234-240) never exercised |
| 124 | C | `getSavedThreads()` | Tested | 1 test (empty case) |
| 125 | M | `getSavedThreads()` — cursor pagination | **NOT tested** | Cursor branch never exercised |
| 126 | C | `getSavedVideos()` | Tested | 1 test (empty case via auth spec) |
| 127 | M | `getSavedVideos()` — cursor pagination | **NOT tested** | Cursor branch never exercised |
| 128 | C | `getCollections()` | Tested | 1 test |
| 129 | C | `moveToCollection()` | Tested | 2 tests |
| 130 | C | `isPostSaved()` | Tested | 2 tests |
| 131 | H | `isThreadSaved()` | **NO test** | 6-line method, never tested |
| 132 | H | `isVideoSaved()` | **NO test** | 6-line method, never tested |

---

## COLLABS MODULE

### CollabsController (61 lines, 7 endpoints)

| # | Sev | Method/Endpoint | Test Status | Issue |
|---|-----|----------------|-------------|-------|
| 133 | C | `POST /collabs/invite` | Tested | 1 test |
| 134 | C | `POST /collabs/:id/accept` | Tested | 1 test |
| 135 | C | `POST /collabs/:id/decline` | Tested | 1 test |
| 136 | C | `DELETE /collabs/:id` (remove) | Tested | 1 test |
| 137 | C | `GET /collabs/pending` | Tested | 1 test |
| 138 | C | `GET /collabs/accepted` | Tested | 1 test |
| 139 | C | `GET /collabs/post/:postId` | Tested | 1 test |

### CollabsService (110 lines, 7 public methods + 1 private)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 140 | C | `invite()` | Well tested | 4 tests — happy, self-invite, non-owner, duplicate |
| 141 | C | `accept()` | Tested | 2 tests — happy, wrong user |
| 142 | M | `accept()` — non-PENDING status | **NOT tested** | Line 36: `status !== PENDING` → BadRequestException path untested |
| 143 | C | `decline()` | Tested | 1 test |
| 144 | M | `decline()` — wrong user | **NOT tested** | ForbiddenException for wrong user in decline is not tested |
| 145 | C | `remove()` | Tested | 1 test (invited user removes) |
| 146 | M | `remove()` — post owner removes | **NOT tested** | Lines 57-61: post owner removal path untested |
| 147 | M | `remove()` — forbidden for third party | **NOT tested** | Neither invited user nor post owner → ForbiddenException untested |
| 148 | M | `remove()` — collab not found | **NOT tested** | `getCollab()` → NotFoundException path untested for remove |
| 149 | C | `getMyPending()` | **NO service test** | Only tested via controller delegation |
| 150 | C | `getPostCollabs()` | Tested | 2 tests |
| 151 | C | `getAcceptedCollabs()` | Tested | 1 test |
| 152 | M | `getAcceptedCollabs()` — cursor pagination | **NOT tested** | Cursor branch never exercised |
| 153 | L | `getAcceptedCollabs()` — empty result cursor | **Weak** | When `collabs` is empty, `collabs[collabs.length - 1]?.id` returns undefined, test doesn't verify meta.cursor |

---

## POLLS MODULE

### PollsController (90 lines, 4 endpoints)

| # | Sev | Method/Endpoint | Test Status | Issue |
|---|-----|----------------|-------------|-------|
| 154 | C | `GET /polls/:id` (getPoll) | Tested | 1 test |
| 155 | C | `POST /polls/:id/vote` (vote) | Tested | 1 test |
| 156 | C | `DELETE /polls/:id/vote` (retractVote) | Tested | 1 test |
| 157 | C | `GET /polls/:id/voters` (getVoters) | Tested | 2 tests (happy + missing optionId) |

### PollsService (247 lines, 4 public methods)

| # | Sev | Method | Test Status | Issue |
|---|-----|--------|-------------|-------|
| 158 | C | `getPoll()` | Well tested | 4 tests — with/without userId, not found, Arabic text |
| 159 | C | `vote()` | Tested | 4 tests — happy, not found, invalid option, already voted |
| 160 | M | `vote()` — expired poll rejection | Tested | In edge spec (1 test) |
| 161 | M | `vote()` — P2002 race condition | **NOT tested** | Lines 149-153: P2002 → ConflictException path untested |
| 162 | C | `vote()` — allowMultiple (multi-choice) | **NOT tested** | Lines 108-119: The entire multi-choice poll code path is untested. `allowMultiple=true` allows voting on different options; duplicate-option-check via `findUnique` never exercised |
| 163 | C | `retractVote()` | Tested | 3 tests — happy, not voted, expired |
| 164 | M | `retractVote()` — expired poll rejection | Tested | In edge spec |
| 165 | C | `getVoters()` | Tested | 4 tests — happy, not found, empty, cursor |
| 166 | L | `getPoll()` — `isExpired` calculation | **Weak assertion** | Tests check options and userVotedOptionId but no test verifies the `isExpired` boolean output |

---

## CROSS-CUTTING ISSUES

| # | Sev | Issue |
|---|-----|-------|
| 167 | I | All controller tests are pure delegation tests (call service, check args). No integration-level HTTP tests verify route registration, guards, decorators, or throttle rates. |
| 168 | I | No DTO validation test for `BookmarkDto`, `InviteCollabDto`, `CrossPostDto`, or `VoteDto`. Only `CreatePostDto` has validation tests. |
| 169 | I | Posts controller mock service is stale — 16+ service methods added since the mock was written, leading to potential runtime errors in tests if those controller methods are tested in future. |
| 170 | L | Bookmarks controller spec has only 4 tests for 14 endpoints (28% coverage of routes). |
| 171 | I | Collabs module has no edge-case spec file. No concurrency, no Prisma error handling, no pagination edge cases tested. |

---

## SEVERITY SUMMARY

| Severity | Count | Description |
|----------|-------|-------------|
| **C** (critical-path-untested) | **10** | `crossPost`, `respondToTag`, `saveToCollection`, `getCollections` (posts), `getCollection`, `pinPost`, `predictEngagement`, `getRepurposeSuggestions`, `shareAsStory` (service-level), multi-choice poll voting |
| **H** (public-endpoint-untested) | **17** | 11 controller-only gaps (archive, pin/unpin comment, hide/unhide, share-link, getArchived, predict-engagement, related, repurpose, analytics, impression) + `isThreadSaved`, `isVideoSaved`, 4 bookmarks controller gaps |
| **M** (error-path-untested) | **22** | P2002 race conditions (bookmarks 3, polls 1, comments 1), cursor pagination gaps (4), content safety block, duplicate detection, chronological/favorites feed, scheduled post visibility, self-react rejection, edit window, report dedup, cron job, recordView, moderatePostImage, collabs remove/decline error paths, accept non-pending |
| **L** (weak-assertion) | **5** | Stale controller mock, weak isExpired check, bookmarks controller coverage ratio, collabs cursor meta, analytics controller delegation |
| **I** (suggestion) | **4** | No HTTP integration tests, missing DTO validation for 4 DTOs, stale mock object, no collabs edge spec |

**C:10 H:17 M:22 L:5 I:4 Total: 58**
