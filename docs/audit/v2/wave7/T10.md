# T10 — Test Coverage Audit: Search, Hashtags, Embeddings, Recommendations, Feed, Promotions

## Module Summary

| Module | Source LOC | Test LOC | Ratio | Spec Files |
|--------|-----------|----------|-------|------------|
| search | 1,028 | 1,575 | 1.53 | 3 service specs + 1 controller spec |
| hashtags | 521 | 504 | 0.97 | 2 service specs + 1 controller spec |
| embeddings | 779 | 1,176 | 1.51 | 3 service specs + 1 controller spec + 1 pipeline spec |
| recommendations | 858 | 893 | 1.04 | 1 service spec + 1 controller spec |
| feed | 2,048 | 1,610 | 0.79 | 3 service specs + 1 controller spec |
| promotions | 307 | 234 | 0.76 | 1 service spec + 1 controller spec |
| **Total** | **5,541** | **5,992** | **1.08** | **18 spec files** |

---

## 1. SEARCH MODULE

### SearchService (search.service.ts — 697 lines, 9 public methods)

| Method | Tested? | Assertions | Notes |
|--------|---------|------------|-------|
| `search(query, type='people')` | Yes | where clause + result shape | Good |
| `search(query, type='threads')` | Yes | where + pagination | Good |
| `search(query, type='posts')` | Yes | where + pagination | Good |
| `search(query, type='tags')` | Yes | where + result shape | Good |
| `search(query, type='reels')` | Yes | where + pagination | Good |
| `search(query, type='videos')` | Yes | where + pagination | Good |
| `search(query, type='channels')` | Yes | where + pagination | Good |
| `search(query, no type)` | Yes | aggregate parallel queries | Good |
| `search` cursor pagination | Yes (posts, threads, videos, channels, reels) | hasMore + cursor value | Good |
| `search` limit capping | Yes | take=limit+1 | Good |
| `search` empty query | Yes | throws BadRequestException | Good |
| `search` whitespace query | Yes | throws BadRequestException | Good |
| `search` >200 char query | Yes | throws BadRequestException | Good |
| `search` boundary (200 chars) | Yes (edge spec) | no throw | Good |
| `search` Arabic query | Yes (edge spec) | no crash | Good |
| `search` regex special chars | Yes (edge spec) | no injection | Good |
| `search` Meilisearch fallback | Yes | falls through to Prisma | Good |
| `trending()` | Yes | SQL aggregation + velocity | Good |
| `getHashtagPosts(tag)` | Yes | pagination + hashtag lookup | Good |
| `getHashtagPosts` cursor | Yes | hasMore + cursor | Good |
| `getHashtagPosts` missing tag | Yes | null hashtag, empty data | Good |
| `suggestedUsers(userId)` | Yes | excludes followed + filters | Good |
| `suggestedUsers` with interests | Yes | interest-based filter | Good |
| `searchPosts(query)` | Yes | result shape | Good |
| `searchThreads(query)` | Yes | result shape | Good |
| `searchReels(query)` | Yes | result shape | Good |
| `getExploreFeed()` | Yes | 7-day window, trending | Good |
| `getSuggestions(query)` | Yes | combined users + hashtags | Good |
| `getSuggestions` empty query | Yes | returns empty arrays | Good |

### SearchController (search.controller.ts — 118 lines, 8 endpoints)

| Endpoint | Tested? | Notes |
|----------|---------|-------|
| `GET /search` | Yes | type validation + limit cap tested |
| `GET /search/trending` | Yes | delegation tested |
| `GET /search/hashtag/:tag` | Yes | delegation tested |
| `GET /search/suggested-users` | Yes | delegation tested |
| `GET /search/posts` | Partial | controller endpoint exists, not directly tested in controller spec |
| `GET /search/threads` | Partial | controller endpoint exists, not directly tested in controller spec |
| `GET /search/reels` | Partial | controller endpoint exists, not directly tested in controller spec |
| `GET /search/explore` | No | controller endpoint NOT tested in controller spec |
| `GET /search/suggestions` | Yes | delegation + limit cap tested |

### MeilisearchService (meilisearch.service.ts — 213 lines, 5 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `isAvailable()` | Yes | true when host set, false when empty |
| `onModuleInit()` | Yes | 6 indexes created + settings configured |
| `onModuleInit` error recovery | Yes | graceful on connection refused |
| `search(indexName, query, opts)` | Yes | URL, headers, body, options, error paths |
| `addDocuments(indexName, docs)` | Yes | skip when unavailable, skip empty, POST correct |
| `deleteDocument(indexName, id)` | Yes | skip when unavailable, URL encode, error handling |

---

## 2. HASHTAGS MODULE

### HashtagsService (hashtags.service.ts — 417 lines, 11 public methods)

| Method | Tested? | Assertions | Notes |
|--------|---------|------------|-------|
| `getTrendingRaw(limit)` | Yes | returns SQL aggregation result | Good |
| `search(query, limit)` | Yes | prefix match + ordering | Good |
| `getByName(name)` | Yes | found + not found | Good |
| `getPostsByHashtag(name, userId?, cursor?, limit?)` | Yes | where clause + pagination | Good |
| `getReelsByHashtag(name, userId?, cursor?, limit?)` | Yes | where clause + pagination | Good |
| `getThreadsByHashtag(name, userId?, cursor?, limit?)` | Yes | where clause + pagination | Good |
| `incrementCount(name, field)` | Yes | upsert call verified | Good |
| `decrementCount(name, field)` | Yes | update call + error swallowing | Good |
| `followHashtag(userId, hashtagId)` | Yes | upsert + NotFoundException | Good |
| `unfollowHashtag(userId, hashtagId)` | Yes | deleteMany call | Good |
| `getFollowedHashtags(userId, cursor?, limit?)` | Yes | pagination + empty case | Good |

#### Missing coverage:

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 1 | M | hashtags | `getPostsByHashtag` with userId (enrichment path) | NOT TESTED | When userId is provided, `enrichPosts` is called to add `userReaction`/`isSaved`. No test passes a userId to verify enrichment occurs or enriched fields are correct. |
| 2 | M | hashtags | `getReelsByHashtag` with userId (enrichment path) | NOT TESTED | Same issue — `enrichReels` path never exercised. |
| 3 | M | hashtags | `getThreadsByHashtag` with userId (enrichment path) | NOT TESTED | `enrichThreads` path never exercised. Tests always pass `undefined` for userId. |
| 4 | M | hashtags | `getPostsByHashtag` cursor pagination | NOT TESTED | Service test only tests without cursor. No test verifies hasMore=true or cursor calculation. |
| 5 | M | hashtags | `getReelsByHashtag` cursor pagination | NOT TESTED | Same as above. |
| 6 | M | hashtags | `getThreadsByHashtag` cursor pagination | NOT TESTED | Same as above. |
| 7 | L | hashtags | `getReelsByHashtag` NotFoundException | NOT TESTED | Missing test for when hashtag doesn't exist (unlike getPostsByHashtag which has it). |
| 8 | L | hashtags | `getThreadsByHashtag` NotFoundException | NOT TESTED | Same gap. |
| 9 | L | hashtags | `decrementCount` negative floor logic | NOT TESTED | After decrement, `updateMany` is called to floor at 0. Test verifies the decrement call but not the floor call. |

### HashtagsController (hashtags.controller.ts — 104 lines, 9 endpoints)

| Endpoint | Tested? | Notes |
|----------|---------|-------|
| `GET /hashtags/trending` | Yes | default limit + custom limit |
| `GET /hashtags/search` | Yes | empty query + custom limit |
| `GET /hashtags/following` | Yes | userId + cursor delegation |
| `GET /hashtags/:name` | Yes | delegation |
| `GET /hashtags/:name/posts` | Yes | delegation with userId + cursor |
| `GET /hashtags/:name/reels` | Yes | delegation |
| `GET /hashtags/:name/threads` | Yes | delegation |
| `POST /hashtags/:id/follow` | Yes | delegation |
| `DELETE /hashtags/:id/follow` | Yes | delegation |

---

## 3. EMBEDDINGS MODULE

### EmbeddingsService (embeddings.service.ts — 559 lines, 13 public/testable methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `generateEmbedding(text)` | Yes | valid, empty, whitespace, API error, network error, truncation, malformed JSON, 429, AbortError |
| `generateBatchEmbeddings(texts)` | Yes | valid, empty, API error, network failure |
| `buildContentText(content)` | Yes | all combinations + empty + whitespace |
| `storeEmbedding(contentId, type, vector)` | Yes (indirect via embedPost) | executeRaw called |
| `embedPost(postId)` | Yes | success, not found, empty text, embedding failure |
| `embedReel(reelId)` | No direct test | Only tested indirectly through pipeline |
| `embedThread(threadId)` | No direct test | Only tested indirectly through pipeline |
| `embedVideo(videoId)` | No direct test | Only tested indirectly through pipeline |
| `findSimilar(contentId, type, limit, filterTypes?)` | Yes | results, empty, SQL injection, all valid types, invalid types |
| `findSimilarByVector(vector, limit, filterTypes?, excludeIds?)` | Yes | results, empty, SQL injection, error handling |
| `getUserInterestVector(userId)` | Yes | no interactions, <5 vectors, >=5 vectors, null vectors, NaN, k calculation |
| `findSimilarByMultipleVectors(vectors, limit, ...)` | Yes | empty, single, merge/dedup, limit respect, sort, filter passthrough |
| `cosineDistance(a, b)` | Yes | identical, orthogonal, opposite, zero, mismatched, empty, similar |
| `averageVectors(vectors)` | Yes | empty, single, two, multiple, immutability |
| `kMeansClustering(vectors, k, maxIter)` | Yes | k=0, empty, k>=count, two clusters, convergence, k=1 |

#### Missing coverage:

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 10 | M | embeddings | `embedReel(reelId)` | NOT DIRECTLY TESTED | No test calls `embedReel` directly. The pipeline test calls it via mock. The actual Prisma lookup + text building + vector generation chain is untested for reels. |
| 11 | M | embeddings | `embedThread(threadId)` | NOT DIRECTLY TESTED | Same gap as embedReel. |
| 12 | M | embeddings | `embedVideo(videoId)` | NOT DIRECTLY TESTED | Same gap. Video builds text differently (`title + description`), untested path. |
| 13 | L | embeddings | `generateEmbedding` when API unavailable | NOT TESTED | Constructor sets `apiAvailable=false` when no API key, but no test creates service without API key and verifies `generateEmbedding` returns null. |
| 14 | L | embeddings | `storeEmbedding` error path | NOT TESTED | If `$executeRaw` throws (e.g., vector dimension mismatch), error propagates unhandled. No test covers this. |

### EmbeddingPipelineService (embedding-pipeline.service.ts — 187 lines, 2 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `backfillAll()` | Yes | batch processing, skip embedded, concurrent guard, error handling |
| `embedNewContent(id, type)` | Yes | all 4 types + unknown type + failure |

### EmbeddingsController (embeddings.controller.ts — 33 lines, 1 endpoint)

| Endpoint | Tested? | Notes |
|----------|---------|-------|
| `POST /embeddings/backfill` | Yes | admin check, non-admin rejection, missing user, pipeline error |

---

## 4. RECOMMENDATIONS MODULE

### RecommendationsService (recommendations.service.ts — 777 lines, 5 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `suggestedPeople(userId?, limit?)` | Yes | unauthenticated, FoF algorithm, block/mute exclusion, empty following |
| `suggestedPosts(userId?, limit?, offset?)` | Yes | engagement sort, block/mute, own post exclusion, pgvector attempt, fallback, exploration 15%, interleave, dedup |
| `suggestedReels(userId?, limit?, offset?)` | Yes | engagement sort, block/mute, 48h window, exploration 15% |
| `suggestedChannels(userId?, limit?, offset?)` | Yes | subscriber sort, block/mute, no isPrivate filter |
| `suggestedThreads(userId?, limit?, offset?)` | Yes | engagement fallback, pgvector attempt, exploration 15% |

#### Missing coverage:

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 15 | M | recommendations | `multiStageRank` — diversity author-dedup | PARTIALLY TESTED | The hashtag diversity test verifies deferred posts, but no test explicitly verifies that same-author back-to-back prevention works (e.g., author1, author1 sequence reordered). |
| 16 | M | recommendations | `multiStageRank` — error handling branch | NOT TESTED | The try/catch swallows non-SQL errors and re-throws SQL errors. No test triggers a non-SQL error (e.g., embeddings timeout) to verify the fallback returns `[]`. No test triggers a SQL error to verify it re-throws. |
| 17 | L | recommendations | `getEngagementScores` for THREAD type | NOT TESTED | Only POST type is tested indirectly through suggestedPosts. The THREAD and REEL paths use different field weightings but are never exercised. |
| 18 | L | recommendations | `suggestedPosts` with offset>0 | NOT TESTED | The offset parameter slicing logic (`results.slice(offset)`) in the fallback path is never tested with a non-zero offset. |
| 19 | I | recommendations | `suggestedPeople` — restrict exclusion | NOT CACHED | `getExcludedUserIds` calls a shared utility that includes restricts, but the mock setup only configures block+mute. Restrict is excluded via the utility, but the test's mock structure doesn't verify restrict works in this context. |

### RecommendationsController (recommendations.controller.ts — 81 lines, 5 endpoints)

| Endpoint | Tested? | Notes |
|----------|---------|-------|
| `GET /recommendations/people` | Yes | limit cap at 50, default 20 |
| `GET /recommendations/posts` | Yes | limit + offset |
| `GET /recommendations/reels` | Yes | limit cap |
| `GET /recommendations/channels` | Yes | limit cap |
| `GET /recommendations/threads` | Yes | limit cap, offset passthrough, negative offset |

---

## 5. FEED MODULE

### FeedService (feed.service.ts — 644 lines, 14 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `logInteraction(userId, data)` | Yes | create new + update existing |
| `dismiss(userId, contentId, contentType)` | Yes | upsert verified |
| `getDismissedIds(userId, contentType)` | Yes | returns IDs |
| `getOnThisDay(userId)` | **NOT TESTED** | Zero tests. Date filtering logic untested. |
| `getCommunityTrending(userId, limit?)` | **NOT TESTED** | Zero tests. Time-decayed scoring logic untested. |
| `getUserInterests(userId)` | Yes | space + hashtag scores, empty case |
| `resetAlgorithm(userId)` | **NOT TESTED** | Zero tests. Transaction + Redis SCAN cleanup untested. |
| `undismiss(userId, contentId, contentType)` | Yes | basic path only |
| `getContentFilter(userId)` | Yes | found + null |
| `buildContentFilterWhere(userId)` | **NOT TESTED** | No test verifies hideMusic or strictnessLevel filter clauses. |
| `getTrendingFeed(cursor?, limit?, userId?)` | Yes | cursor pagination, Redis cache, block/mute filter, corrupted cache, multi-tier window |
| `getFeaturedFeed(cursor?, limit?, userId?)` | Yes | block filter + scheduledAt |
| `featurePost(postId, featured, userId?)` | Yes | admin guard, feature/unfeature |
| `getSuggestedUsers(userId?, limit?)` | Yes | block/mute exclusion |
| `getUserFollowingCount(userId)` | Yes | returns count |
| `getNearbyContent(lat, lng, radius, cursor?, userId?)` | Yes | results, pagination, empty |
| `getFrequentCreatorIds(userId)` | Indirect | SQL aggregation not directly tested |
| `getFrequentCreators(userId)` | Yes | block exclusion |

#### Missing coverage:

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 20 | C | feed | `getOnThisDay(userId)` | ZERO TESTS | Critical user-facing feature ("On This Day" memories). Date matching logic (month+day across years) is completely untested. Could return wrong-day posts. |
| 21 | C | feed | `getCommunityTrending(userId, limit?)` | ZERO TESTS | Critical user-facing endpoint. Time-decayed scoring formula `engagement / ageHours^1.2` is untested. HashtagFollow lookup chain untested. |
| 22 | C | feed | `resetAlgorithm(userId)` | ZERO TESTS | Destructive operation (deletes all interactions + dismissals + Redis keys). Transaction correctness untested. Redis SCAN pattern cleanup untested. Could wipe wrong keys. |
| 23 | M | feed | `buildContentFilterWhere(userId)` | NOT TESTED | `hideMusic` and `strictnessLevel` filter logic not verified. Content filter could silently fail to filter. |
| 24 | M | feed | `undismiss` P2025 error path | NOT TESTED | The Prisma `P2025` (record not found) catch branch is untested. Only the happy path is tested. |
| 25 | M | feed | `getTrendingFeed` multi-tier window expansion | NOT TESTED | The adaptive 24h->48h->7d window expansion when <100 candidates is not directly tested. Tests only use empty or small result sets. |
| 26 | M | feed | `getTrendingFeed` content filter integration | NOT TESTED | When authenticated user has content filter settings, the `buildContentFilterWhere` result is spread into the where clause. Never tested. |
| 27 | L | feed | `getFrequentCreatorIds` empty result | NOT DIRECTLY TESTED | The SQL aggregation is tested indirectly via getFrequentCreators, but the direct method and its HAVING COUNT >= 10 threshold are not verified. |

### FeedTransparencyService (feed-transparency.service.ts — 232 lines, 3 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `explainPost(userId, postId)` | Yes | follows, engagement, Islamic boost, hashtags, recency, interests, fallback |
| `explainThread(userId, threadId)` | Yes | follows, engagement, hashtags, not found, fallback |
| `enhancedSearch(query, cursor?, limit?, userId?)` | Yes | empty keywords, content match, pagination, block/mute exclusion |

### PersonalizedFeedService (personalized-feed.service.ts — 909 lines, 2 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `trackSessionSignal(userId, signal)` | Yes | new session, categories, scroll, cap viewedIds, dedup, TTL, inactivity reset |
| `getPersonalizedFeed(userId?, space, cursor?, limit?, lat?, lng?)` | Yes | unauthenticated, space routing (saf/bakra/majlis/minbar), cold start, fallback, pagination, block/mute, Redis session |

Internal helpers tested directly (exposed for testability):

| Helper | Tested? |
|--------|---------|
| `getIslamicBoost(hashtags, lat?, lng?)` | Yes (7 tests) |
| `isRamadanPeriod(date?)` | Yes (4 tests) |
| `getSessionBoostFromData(session, hashtags)` | Yes (5 tests) |
| `calculateEngagementScore(post)` | Yes (5 tests) |
| `parseTimeToHours(timeStr)` | Yes (4 tests) |
| `applyTrendingDecay(post)` | Yes (5 tests) |
| `getTrendingFeed` (private) | Yes (2 tests) |
| `getIslamicEditorialPicks` (private) | Yes (1 test) |

### FeedController (feed.controller.ts — 263 lines, 16 endpoints)

| Endpoint | Tested? | Notes |
|----------|---------|-------|
| `POST /feed/interaction` | Yes | delegation |
| `POST /feed/dismiss/:contentType/:contentId` | Yes | delegation + contentType validation |
| `DELETE /feed/dismiss/:contentType/:contentId` | Yes | delegation |
| `GET /feed/community-trending` | **NOT TESTED** | Controller endpoint not in spec |
| `GET /feed/on-this-day` | **NOT TESTED** | Controller endpoint not in spec |
| `DELETE /feed/reset-algorithm` | **NOT TESTED** | Controller endpoint not in spec |
| `GET /feed/explain/post/:postId` | Yes | delegation |
| `GET /feed/explain/thread/:threadId` | **NOT TESTED** | Controller endpoint not in spec (service tested) |
| `GET /feed/search/enhanced` | **NOT TESTED** | Controller endpoint not in spec (service tested) |
| `GET /feed/personalized` | Yes | space validation + delegation |
| `POST /feed/session-signal` | Yes | delegation |
| `GET /feed/trending` | Yes | limit cap + userId passthrough |
| `GET /feed/featured` | Yes | limit cap |
| `GET /feed/suggested-users` | **NOT TESTED** | Controller endpoint not in spec |
| `GET /feed/frequent-creators` | **NOT TESTED** | Controller endpoint not in spec |
| `PUT /feed/admin/posts/:id/feature` | Yes | delegation |
| `GET /feed/nearby` | Yes | lat/lng clamping |

#### Missing coverage:

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 28 | H | feed | `GET /feed/community-trending` | NOT TESTED | Public authenticated endpoint with no controller test. |
| 29 | H | feed | `GET /feed/on-this-day` | NOT TESTED | Public authenticated endpoint with no controller test. |
| 30 | H | feed | `DELETE /feed/reset-algorithm` | NOT TESTED | Destructive endpoint with no controller test. |
| 31 | H | feed | `GET /feed/suggested-users` | NOT TESTED | Public endpoint with no controller test. |
| 32 | H | feed | `GET /feed/frequent-creators` | NOT TESTED | Public authenticated endpoint with no controller test. |
| 33 | M | feed | `GET /feed/explain/thread/:threadId` | NOT TESTED | Controller delegation not tested (service IS tested). |
| 34 | M | feed | `GET /feed/search/enhanced` | NOT TESTED | Controller delegation + limit parsing not tested (service IS tested). |

---

## 6. PROMOTIONS MODULE

### PromotionsService (promotions.service.ts — 201 lines, 5 public methods)

| Method | Tested? | Notes |
|--------|---------|-------|
| `boostPost(userId, data)` | Yes | success, invalid budget (0 and >10000), invalid duration (0 and >30), non-own post, existing promotion |
| `getMyPromotions(userId)` | **NOT TESTED** | Service method not directly tested (only controller delegation) |
| `cancelPromotion(id, userId)` | Yes | success, non-active, non-own |
| `setReminder(userId, postId, remindAt)` | Yes | success, past date, invalid date |
| `removeReminder(userId, postId)` | Yes | success, not found |
| `markBranded(userId, postId, partnerName)` | Yes | success, non-own, empty partner |

#### Missing coverage:

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 35 | H | promotions | `getMyPromotions(userId)` | NOT TESTED | Service method has zero direct tests. Only the controller delegation is tested. The Prisma query (orderBy, take:50) is never verified. |
| 36 | M | promotions | `boostPost` — post not found | NOT TESTED | When `post.findUnique` returns null, `NotFoundException` should be thrown. This specific error path is not tested. |
| 37 | M | promotions | `setReminder` — post not found | NOT TESTED | When post doesn't exist, NotFoundException path is untested. The test only verifies date validation. |
| 38 | M | promotions | `markBranded` — post not found | NOT TESTED | When post doesn't exist, NotFoundException is untested. |
| 39 | M | promotions | `markBranded` — existing branded tag replacement | NOT TESTED | The regex `\[Paid partnership with [^\]]*\]\s*` replacement logic is untested. Could corrupt content if regex fails. |
| 40 | L | promotions | `cancelPromotion` — not found | NOT TESTED | When promotion doesn't exist (findUnique returns null), NotFoundException is untested. |
| 41 | L | promotions | `boostPost` — targetReach calculation | WEAK ASSERTION | Test checks `targetReach: 5000` but the calculation `budget * REACH_MULTIPLIER` (50*100) is validated by coincidence, not by checking the formula. |

### PromotionsController (promotions.controller.ts — 106 lines, 6 endpoints)

| Endpoint | Tested? | Notes |
|----------|---------|-------|
| `POST /promotions/boost` | Yes | delegation |
| `GET /promotions/mine` | Yes | delegation |
| `POST /promotions/:id/cancel` | Yes | delegation |
| `POST /promotions/reminder` | Yes | delegation |
| `DELETE /promotions/reminder/:postId` | Yes | delegation |
| `POST /promotions/branded` | Yes | delegation |

---

## Findings Table

| # | Sev | Module | Method/Endpoint | Test Status | Issue |
|---|-----|--------|-----------------|-------------|-------|
| 1 | M | hashtags | `getPostsByHashtag` with userId (enrichment) | NOT TESTED | `enrichPosts` path adding `userReaction`/`isSaved` never exercised. |
| 2 | M | hashtags | `getReelsByHashtag` with userId (enrichment) | NOT TESTED | `enrichReels` path never exercised. |
| 3 | M | hashtags | `getThreadsByHashtag` with userId (enrichment) | NOT TESTED | `enrichThreads` path never exercised. |
| 4 | M | hashtags | `getPostsByHashtag` cursor pagination | NOT TESTED | hasMore=true/cursor calculation not verified. |
| 5 | M | hashtags | `getReelsByHashtag` cursor pagination | NOT TESTED | Same gap. |
| 6 | M | hashtags | `getThreadsByHashtag` cursor pagination | NOT TESTED | Same gap. |
| 7 | L | hashtags | `getReelsByHashtag` NotFoundException | NOT TESTED | Missing hashtag-not-found test for reels. |
| 8 | L | hashtags | `getThreadsByHashtag` NotFoundException | NOT TESTED | Missing hashtag-not-found test for threads. |
| 9 | L | hashtags | `decrementCount` negative floor logic | NOT TESTED | `updateMany` floor-at-zero call not verified. |
| 10 | M | embeddings | `embedReel(reelId)` direct test | NOT TESTED | Prisma lookup + text building + vector chain untested for reels. |
| 11 | M | embeddings | `embedThread(threadId)` direct test | NOT TESTED | Same gap for threads. |
| 12 | M | embeddings | `embedVideo(videoId)` direct test | NOT TESTED | Same gap. Video builds text differently (title+description). |
| 13 | L | embeddings | `generateEmbedding` API unavailable | NOT TESTED | No test creates service without API key to verify null return. |
| 14 | L | embeddings | `storeEmbedding` error path | NOT TESTED | `$executeRaw` failure unhandled/untested. |
| 15 | M | recommendations | `multiStageRank` author-dedup | PARTIALLY TESTED | No test with same-author consecutive items. |
| 16 | M | recommendations | `multiStageRank` error handling | NOT TESTED | Non-SQL error fallback and SQL error re-throw untested. |
| 17 | L | recommendations | `getEngagementScores` THREAD/REEL paths | NOT TESTED | Different field weightings untested. |
| 18 | L | recommendations | `suggestedPosts` offset>0 slicing | NOT TESTED | Offset logic in fallback path not exercised. |
| 19 | I | recommendations | `suggestedPeople` restrict mock | INCOMPLETE | Restrict utility not verified in test mock. |
| 20 | C | feed | `getOnThisDay(userId)` | ZERO TESTS | Date-matching logic completely untested. |
| 21 | C | feed | `getCommunityTrending(userId)` | ZERO TESTS | Scoring formula + hashtag follow chain untested. |
| 22 | C | feed | `resetAlgorithm(userId)` | ZERO TESTS | Destructive operation: transaction + Redis SCAN untested. |
| 23 | M | feed | `buildContentFilterWhere(userId)` | NOT TESTED | hideMusic/strictnessLevel filter logic unverified. |
| 24 | M | feed | `undismiss` P2025 error path | NOT TESTED | Prisma record-not-found catch untested. |
| 25 | M | feed | `getTrendingFeed` multi-tier expansion | NOT TESTED | 24h->48h->7d adaptive window not tested. |
| 26 | M | feed | `getTrendingFeed` content filter | NOT TESTED | Content filter integration untested. |
| 27 | L | feed | `getFrequentCreatorIds` direct test | NOT TESTED | SQL HAVING threshold not verified directly. |
| 28 | H | feed | `GET /feed/community-trending` | NOT TESTED | Public endpoint missing controller test. |
| 29 | H | feed | `GET /feed/on-this-day` | NOT TESTED | Public endpoint missing controller test. |
| 30 | H | feed | `DELETE /feed/reset-algorithm` | NOT TESTED | Destructive endpoint missing controller test. |
| 31 | H | feed | `GET /feed/suggested-users` | NOT TESTED | Public endpoint missing controller test. |
| 32 | H | feed | `GET /feed/frequent-creators` | NOT TESTED | Public endpoint missing controller test. |
| 33 | M | feed | `GET /feed/explain/thread/:threadId` | NOT TESTED | Controller delegation untested. |
| 34 | M | feed | `GET /feed/search/enhanced` | NOT TESTED | Controller delegation untested. |
| 35 | H | promotions | `getMyPromotions(userId)` | NOT TESTED | Service query (orderBy, take:50) not verified. |
| 36 | M | promotions | `boostPost` post not found | NOT TESTED | NotFoundException path untested. |
| 37 | M | promotions | `setReminder` post not found | NOT TESTED | NotFoundException path untested. |
| 38 | M | promotions | `markBranded` post not found | NOT TESTED | NotFoundException path untested. |
| 39 | M | promotions | `markBranded` existing tag replacement | NOT TESTED | Regex replacement logic untested. |
| 40 | L | promotions | `cancelPromotion` not found | NOT TESTED | NotFoundException untested. |
| 41 | L | promotions | `boostPost` targetReach formula | WEAK ASSERTION | Formula validated by coincidence not by testing the multiplier. |
| 42 | H | search | `GET /search/explore` controller | NOT TESTED | Public endpoint with limit parsing, no controller spec test. |
| 43 | M | search | `GET /search/posts` controller | NOT TESTED | Controller limit parsing not tested in controller spec. |
| 44 | M | search | `GET /search/threads` controller | NOT TESTED | Same gap. |
| 45 | M | search | `GET /search/reels` controller | NOT TESTED | Same gap. |
| 46 | M | search | Meilisearch-first search path | NOT TESTED | When `isAvailable()=true` and Meilisearch returns hits, the early-return path is never exercised in SearchService spec (mock always returns false/null). |
| 47 | L | search | `getExploreFeed` with userId (block/mute) | NOT TESTED | Block/mute exclusion in explore feed with authenticated user untested. |

---

## Severity Counts

| Severity | Count | Description |
|----------|-------|-------------|
| C (critical-path-untested) | 3 | `getOnThisDay`, `getCommunityTrending`, `resetAlgorithm` — user-facing features with zero tests |
| H (public-endpoint-untested) | 7 | 5 feed controller endpoints + 1 promotions service method + 1 search controller endpoint |
| M (error-path-untested) | 23 | Enrichment paths, cursor pagination, error branches, content filters, Meilisearch path |
| L (weak-assertion) | 10 | Missing NotFoundException tests, formula validation, floor logic, API-unavailable path |
| I (suggestion) | 1 | Restrict mock completeness |

**C:3 H:7 M:23 L:10 I:1 Total: 44**
