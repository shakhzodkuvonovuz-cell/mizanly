# I02 — i18n & Accessibility Audit (Wave 8, Screens 2/10)

**Scope:** `broadcast-channels`, `broadcast/[id]`, `call-history`, `call/[id]`, `camera`, `caption-editor`, `cashout`, `challenges`, `channel/[handle]`, `charity-campaign`, `chat-export`, `chat-folder-view`, `chat-folders`, `chat-lock`, `chat-theme-picker`, `chat-wallpaper`, `circles`, `close-friends`, `collab-requests`, `communities`

---

## Findings

| # | Sev | File:Line | Check | Finding | Impact |
|---|-----|-----------|-------|---------|--------|
| 1 | H | `broadcast/[id].tsx:479` | RTL | `marginRight: spacing.sm` in `composeInput` style. Should be `marginEnd`. | Compose input overlaps send button in RTL |
| 2 | M | `broadcast/[id].tsx:182` | a11y | `Icon name="users"` inside `subscribersBadge` has no `accessibilityLabel`. Decorative but grouped with text, minor. | Screen reader announces icon without context |
| 3 | M | `broadcast/[id].tsx:277` | a11y | `Pressable style={styles.muteBadge}` (line 277) has no `accessibilityRole="button"`. It's visually tappable but lacks role. | Mute badge not recognized as interactive by screen readers |
| 4 | H | `caption-editor.tsx:104` | i18n | `showToast({ message: 'Captions generated' })` hardcoded English string. Should use `t('captionEditor.captionsGenerated')`. | Toast not translated in any language |
| 5 | H | `caption-editor.tsx:109` | i18n | `showToast({ message: 'Failed to generate captions' })` hardcoded English. Should use `t('captionEditor.generateFailed')`. | Error toast not translated |
| 6 | H | `caption-editor.tsx:135` | i18n | `showToast({ message: 'Captions saved' })` hardcoded English. Should use `t('captionEditor.captionsSaved')`. | Success toast not translated |
| 7 | H | `caption-editor.tsx:140` | i18n | `showToast({ message: 'Failed to save captions' })` hardcoded English. Should use `t('captionEditor.saveFailed')`. | Error toast not translated |
| 8 | M | `caption-editor.tsx:36` | i18n | `FONT_OPTIONS` array uses raw English strings `'Default', 'Bold', 'Handwritten'` as type literals. Used for keys in `t()` calls but the raw value is also used as a type label fallback. | Minor: keys are translated via `t()` at render time |
| 9 | M | `caption-editor.tsx:38` | i18n | `POSITION_OPTIONS` uses raw English `'Top', 'Center', 'Bottom'`. Same pattern as FONT_OPTIONS. | Keys translated via `t()` but raw strings define types |
| 10 | M | `caption-editor.tsx:39` | i18n | `BACKGROUND_OPTIONS` uses raw English `'None', 'Dark Bar', 'Outline'`. | Same as above |
| 11 | M | `caption-editor.tsx:129` | i18n | `label: 'Auto-generated'` hardcoded in upload payload. Server-side, but user may see it in subtitle track labels. | Label not localized if displayed to user |
| 12 | H | `camera.tsx:199` | i18n | `accessibilityLabel={flashOn ? "Turn flash off" : "Turn flash on"}` hardcoded English. Should use `t('accessibility.flashOff')` / `t('accessibility.flashOn')`. | Flash toggle label not translated for screen readers |
| 13 | H | `camera.tsx:272` | i18n | `accessibilityLabel={mode === 'video' && isRecording ? "Stop recording" : "Capture"}` hardcoded English. Should use `t()`. | Capture button label not translated |
| 14 | L | `camera.tsx:112-118` | i18n | `Alert.alert()` uses `t()` with fallback strings `'Camera Access'` and `'Mizanly needs camera...'`. Acceptable if i18n keys exist, but fallbacks are English-only. | Fallback visible if key missing |
| 15 | M | `call/[id].tsx:722` | a11y | Reaction buttons (`Pressable key={emoji} onPress={...}`) lack `accessibilityRole="button"` and `accessibilityLabel`. | Screen readers cannot identify emoji reaction buttons |
| 16 | L | `call/[id].tsx:572` | RTL | `right: spacing.xl + (i % 3) * 30` in `floatingReaction` style. Should be computed with `end` or dynamically flipped. However, since this is for animated floating reactions, visual impact is minimal. | Reactions float from wrong side in RTL |
| 17 | M | `call/[id].tsx:582` | RTL | `right: spacing.base` in inline `qualityIndicator` style. Should be `end`. | Quality indicator on wrong side in RTL |
| 18 | M | `call-history.tsx:125-129` | a11y | The callback action button (phone/video icon) lacks `accessibilityLabel`. Should describe action e.g., `t('calls.callBack', { name: otherUser.displayName })`. | Screen reader just sees unlabeled button |
| 19 | M | `call-history.tsx:96-98` | a11y | Row `Pressable` has `accessibilityRole="button"` but no `accessibilityLabel` describing the contact name. | Screen reader doesn't know who the call entry is for |
| 20 | M | `channel/[handle].tsx:71` | a11y | `VideoCard` `Pressable` (line 71) lacks `accessibilityRole="button"` and `accessibilityLabel`. | Video cards not accessible |
| 21 | M | `channel/[handle].tsx:88` | a11y | Channel avatar `Pressable` lacks `accessibilityLabel`. | No label on channel avatar tap target |
| 22 | M | `channel/[handle].tsx:103` | a11y | More button `Pressable` (three dots) lacks `accessibilityRole="button"` and `accessibilityLabel`. | Options button invisible to screen readers |
| 23 | M | `channel/[handle].tsx:121` | a11y | `FeaturedVideoCard` `Pressable` lacks `accessibilityRole="button"` and `accessibilityLabel`. | Featured video not accessible |
| 24 | H | `channel/[handle].tsx:131` | i18n | `"FEATURED"` hardcoded English text in featured badge. Should use `t('channel.featured')`. | Badge text not translated |
| 25 | M | `channel/[handle].tsx:161` | a11y | `PlaylistCard` `Pressable` lacks `accessibilityRole="button"` and `accessibilityLabel`. | Playlist items not accessible |
| 26 | H | `channel/[handle].tsx:285` | i18n | `Share.share({ message: \`Check out ${channel?.name ?? handle} on Mizanly\` })` hardcoded English share message. Should use `t('channel.shareMessage', { name: ... })`. | Share text always English |
| 27 | M | `channel/[handle].tsx:496-498` | a11y | Back button `Pressable` (line 496) in `!channel` fallback lacks `accessibilityRole="button"` and `accessibilityLabel`. | Back button not accessible |
| 28 | M | `channel/[handle].tsx:516` | a11y | Back button `Pressable` in main header (line 516) lacks `accessibilityRole="button"` and `accessibilityLabel`. | Same as above |
| 29 | M | `channel/[handle].tsx:521-524` | a11y | Share and more-horizontal `Pressable` buttons in header lack `accessibilityRole="button"` and `accessibilityLabel`. | Header actions not accessible |
| 30 | H | `charity-campaign.tsx:53` | i18n | `Share.share({ message: \`${campaign.title} - Support this campaign on Mizanly!...\` })` hardcoded English. Should use `t('charity.shareMessage', { title: ... })`. | Share text not translated |
| 31 | M | `charity-campaign.tsx:75` | a11y | `rightAction={{ icon: 'share', onPress: () => {} }}` in loading skeleton has empty onPress and no `accessibilityLabel`. | Share button non-functional and unlabeled during loading |
| 32 | M | `chat-theme-picker.tsx:40-44` | i18n | `TABS` array uses hardcoded English labels: `'Solid Colors'`, `'Gradients'`, `'Patterns'`, `'Photos'`. These are NOT passed through `t()` in the data definition. However, they ARE translated at render time via `t(\`chatThemePicker.tab.${tab.id}\`)` so the raw labels are dead code. | Dead English labels in data array (not displayed) |
| 33 | M | `chat-theme-picker.tsx:47-59` | i18n | `SOLID_COLORS` array has hardcoded English `name` fields like `'Default Dark'`, `'Midnight Blue'`, etc. At render time, `t(\`chatThemePicker.themeName.${item.id}\`)` is used instead. But the `name` field remains untranslated dead code. | English names in data structures (translated at render) |
| 34 | L | `chat-theme-picker.tsx:628` | RTL | `gridRow` style lacks `flexDirection: 'row'` explicitly. Uses default which is `column`. Items inside are paired but the row layout relies on implicit rendering. Minor layout concern in RTL. | Grid may not reflow correctly in RTL |
| 35 | M | `chat-wallpaper.tsx:264` | i18n | `SOLID_COLORS` names like `'Emerald'`, `'Gold'`, `'Dark'` etc. are hardcoded English and rendered directly via `<Text>{color.name}</Text>`. NOT translated. | Color names always English |
| 36 | M | `chat-wallpaper.tsx:297` | i18n | `GRADIENT_PAIRS` names like `'Emerald Night'`, `'Golden Dusk'` rendered directly. NOT translated. | Gradient names always English |
| 37 | M | `chat-wallpaper.tsx:323` | i18n | `PATTERNS` names like `'Arabesque'`, `'Zellige'` rendered directly. NOT translated. | Pattern names always English |
| 38 | M | `chat-folders.tsx:154` | a11y | `GlassHeader leftAction` missing `accessibilityLabel` on back button. | Screen reader has no label for back button |
| 39 | M | `chat-folders.tsx:155` | a11y | `GlassHeader rightAction` (plus icon) missing `accessibilityLabel` for create action. | Create folder button unlabeled |
| 40 | M | `close-friends.tsx:41` | a11y | `UserRow` outer `Pressable` (line 41) lacks `accessibilityRole="button"` and `accessibilityLabel`. | User rows not properly accessible |
| 41 | M | `close-friends.tsx:312` | a11y | Clear search `Pressable` (line 312) lacks `accessibilityRole="button"` and `accessibilityLabel`. | Clear button not accessible |
| 42 | M | `communities.tsx:29-31` | i18n | `CATEGORIES` array contains hardcoded English strings `'All', 'Islamic', 'Tech', ...`. At render (line 328), they're translated via `t(\`screens.communities.category.${category.toLowerCase()}\`)` but the raw `'All'` string is also used as the default `activeCategory` state comparison. | Category filter logic depends on English strings |
| 43 | M | `communities.tsx:254` | a11y | `GlassHeader leftAction` (back button) missing `accessibilityLabel`. | Back button unlabeled |
| 44 | L | `communities.tsx:377` | i18n | `t('screens.communities.createCommunity') \|\| 'Create Community'` uses JS fallback instead of i18n fallback parameter syntax. Same on line 382. Should be `t('screens.communities.createCommunity', 'Create Community')`. | Minor: works but inconsistent with codebase pattern |
| 45 | M | `broadcast-channels.tsx:300` | a11y | Clear search `Pressable` has `accessibilityRole="button"` but no `accessibilityLabel`. | Screen reader says "button" but not what it does |
| 46 | L | `broadcast-channels.tsx:182` | a11y | `Icon name="users"` in `subscribersBadge` is decorative (next to count text). No `accessibilityLabel` needed but the parent View could use `accessibilityRole="text"`. | Minor grouping issue |
| 47 | M | `circles.tsx:66` | a11y | Emoji picker `Pressable` items have `accessibilityRole="button"` but `accessibilityLabel` is missing. Screen reader only announces the emoji character. | Emoji selection buttons lack descriptive labels |
| 48 | M | `circles.tsx:229-232` | a11y | Delete button `Pressable` has `accessibilityRole="button"` but no `accessibilityLabel`. | Delete action unlabeled for screen readers |
| 49 | M | `collab-requests.tsx:161-165` | a11y | Accept button: `Pressable` inside `LinearGradient` has `accessibilityRole="button"` but the outer gradient wrapping breaks the a11y tree. The Pressable should be the outermost touchable. | Accept action may not be properly focusable |
| 50 | M | `collab-requests.tsx:173-178` | a11y | Same issue for Decline button: `Pressable` nested inside `LinearGradient`. | Decline action may not be properly focusable |
| 51 | L | `call/[id].tsx:739` | RTL | `localVideoPiP` uses `right: spacing.base` (line 739). Should be `end: spacing.base`. | PiP video on wrong side in RTL |
| 52 | L | `call/[id].tsx:755` | RTL | `videoTileLabel` uses `left: spacing.sm`. Should be `start: spacing.sm`. | Tile label positioned from wrong side in RTL |
| 53 | L | `call/[id].tsx:780` | RTL | `e2eeIndicator` uses `left: spacing.base`. Should be `start: spacing.base`. | E2EE indicator on wrong side in RTL |
| 54 | L | `call/[id].tsx:785` | RTL | `verificationOverlay` uses `left: spacing.base, right: spacing.base`. Should be `start`/`end`. | Verification overlay edges wrong in RTL |
| 55 | L | `channel/[handle].tsx:349` | RTL | `gridLineVertical` uses `left: screenWidth / 3`. Not relevant to this file, but the referenced pattern is in camera.tsx:349. | N/A |
| 56 | M | `cashout.tsx:293` | i18n | `"= $" + ... + " USD"` currency display hardcoded to USD format with `$` prefix. Should respect user locale or use i18n currency formatting. | Currency format always USD/English |
| 57 | L | `cashout.tsx:296` | i18n | `t('cashout.exchangeRate', '100 diamonds = $0.70')` — the fallback contains a hardcoded USD amount. Should be parameterized. | Exchange rate text has hardcoded currency in fallback |
| 58 | M | `chat-folder-view.tsx:46-49` | i18n | `conversationName` function returns hardcoded English fallbacks: `'Group'`, `'Chat'`. Should use `t('common.group')` / `t('common.chat')`. But `t` is not available outside the component. | Conversation names fall back to English |
| 59 | L | `chat-theme-picker.tsx:626` | RTL | `gridRow` missing explicit `flexDirection: 'row'`. Default flexDirection in RN is `column` unless parent provides it. The grid rows are rendered via `.map()` with individual items, so each "row" is actually a `View` wrapping 2 items. Without `flexDirection: 'row'`, items stack vertically. | Grid layout broken (not RTL-specific, affects all) |

---

## Summary

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High     | 8 |
| Medium   | 33 |
| Low      | 12 |
| Info     | 0 |

**C:0 H:8 M:33 L:12 I:0 Total: 53**

### High Severity Breakdown
1. **Hardcoded English strings in JSX (not through `t()`):** 6 findings
   - `caption-editor.tsx` (4 toast messages: lines 104, 109, 135, 140)
   - `camera.tsx` (2 accessibility labels: lines 199, 272)
   - `channel/[handle].tsx` ("FEATURED" badge: line 131; share message: line 285)
   - `charity-campaign.tsx` (share message: line 53)

2. **RTL layout violations:** 1 finding
   - `broadcast/[id].tsx` `marginRight` instead of `marginEnd` (line 479)

### Most Common Issues
- **Missing `accessibilityLabel`** on interactive elements (21 findings across all screens)
- **Hardcoded English in data arrays** that are rendered directly (color names, gradient names, pattern names in wallpaper/theme pickers)
- **RTL `left`/`right` instead of `start`/`end`** in absolute positioning (6 findings, mostly in call screen)
