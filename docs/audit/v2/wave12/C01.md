# C01 — UI Components Audit (49 files, ~10K lines)

**Scope:** `apps/mobile/src/components/ui/` — all 49 component files
**Auditor:** Claude Opus 4.6
**Date:** 2026-03-30

---

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | C | VideoPlayer.tsx:107 | `seekIndicatorTimeoutRef` timeout not cleared on unmount. Only `controlsTimeoutRef` is cleared in the cleanup effect (line 130). If the component unmounts while the seek indicator is visible, `setTimeout` fires on an unmounted component, calling `setSeekIndicator` causing a state update on unmounted component. | Memory leak, potential crash in strict mode | Small |
| 2 | C | Toast.tsx:111 | `dismiss` callback uses bare `setTimeout` (line 111) inside the `dismiss` function to remove from store after animation. If `ToastItem` unmounts before that timer fires (e.g. parent unmounts all toasts), `dismissToast` is called on a stale reference. No cleanup exists for this timer. | State update on unmounted component | Small |
| 3 | C | LinkPreview.tsx:10 | Uses raw RN `Image` (`import { Image } from 'react-native'`) instead of `ProgressiveImage` or `expo-image` for the preview image (line 139). Violates project rule: "NEVER raw `<Image>` from expo-image for content". The RN Image lacks blurhash, transition, and caching. | Slower loading, no progressive display, inconsistent with rest of app | Small |
| 4 | C | ImageGallery.tsx:74-86 | `StatusBar.setHidden(true)` called when `visible=true` but if the component re-renders with `visible=false` due to parent state change (not through swipe dismiss), the cleanup at line 84 fires immediately. However, if the parent simply stops rendering `ImageGallery` (conditional render), the `useEffect` cleanup runs — but `entranceProgress` is set to 0 without `StatusBar.setHidden(false)` in the cleanup function. The StatusBar restore only happens in the `else` branch (line 84), not in `useEffect` return. | StatusBar stuck hidden if parent unmounts the gallery without setting visible=false first | Medium |
| 5 | H | VideoPlayer.tsx:74 | `selectedQuality` state is maintained but never applied to the actual video source. Changing quality sets `selectedQuality` state (line 484) but doesn't change `effectiveUri` (line 275, which only switches between `hlsUrl` and `uri`). Quality selection UI is entirely non-functional. | Users select quality but video doesn't change — broken feature | Medium |
| 6 | H | EmojiPicker.tsx:39 | `CELL_SIZE` uses `Dimensions.get('window').width` at module load (line 39), not in the component. On tablets/iPads with split view or rotation, this value is stale. FlatList `getItemLayout` (line 266) returns wrong offsets, causing layout bugs and scroll position miscalculations. | Broken emoji grid layout on rotation or split view | Medium |
| 7 | H | RichCaptionInput.tsx:119 | Autocomplete cursor position approximated as `text.length` (line 119) instead of actual selection position. When user edits in the middle of text, autocomplete triggers incorrectly scan from the end of the string, not the cursor. | Autocomplete triggers in wrong position when editing mid-text | Medium |
| 8 | H | MiniPlayer.tsx:117 | `handleTapExpand` has `router` in dependency array (line 117) but `router` is not used in the callback body (navigation uses `navigate()` utility instead). Stale dependency — but more critically, `closeMiniPlayer()` immediately followed by `navigate()` could cause a race: video `stopAsync` in `handleClose` isn't called because `handleTapExpand` calls `closeMiniPlayer` directly. | MiniPlayer video keeps playing in background after expanding | Small |
| 9 | H | VideoControls.tsx:82 | `runOnJS(setShowControls)(false)` at line 82 is called inside `withTiming` callback on the UI thread. However, `setShowControls(false)` hides the entire control overlay from the tree (line 307: `{showControls && renderControls()}`). This means controls are removed from the tree while the opacity animation is in mid-fade, causing a visual pop instead of smooth fadeout. | Controls disappear abruptly before fade animation completes | Small |
| 10 | H | UploadProgressBar.tsx:38 | `fillWidth.value = withSpring(...)` is called directly in the render body (line 38), not inside a `useEffect`. This triggers a new spring animation on every render, potentially causing jitter on frequent progress updates. React re-renders can restart the spring mid-animation. | Jittery progress bar animation | Small |
| 11 | M | VideoPlayer.tsx:493-500 | StyleSheet uses hardcoded `colors.dark.bgCard` (line 498) for container background instead of theme-aware color. All other hardcoded `colors.dark.*` in stylesheet are also not theme-aware (lines 515, 521, 531, 601, 638). | Wrong colors in light mode | Small |
| 12 | M | LocationPicker.tsx:161-333 | Multiple StyleSheet values use hardcoded `colors.dark.*` (lines 268-269 `searchInput`, line 289 `currentLocation`, line 209 `reminderBtn`). These are overridden inline for some elements (line 164) but not all — `input` text color at line 279 uses `colors.text.primary` (dark-only). | Wrong colors in light mode for search input text, borders | Small |
| 13 | M | Autocomplete.tsx:207-290 | All StyleSheet text colors hardcode dark theme: `headerTitle` line 229, `itemTitle` line 280, `itemSubtitle` line 285. Container bg/border are overridden inline (line 148) but list items use hardcoded `colors.dark.*`. | Wrong colors in light mode for autocomplete items | Small |
| 14 | M | MentionAutocomplete.tsx:197 | Uses `ActivityIndicator` for loading state (line 197). Project rules state: "use `<Skeleton>` for content loading, NOT `<ActivityIndicator>` (buttons OK)". This is content loading, not a button. | Violates component rules | Small |
| 15 | M | Toast.tsx:145 | `useEffect` at line 145 has an empty dependency array with eslint-disable comment, but references `translateY`, `opacity`, `progressWidth`, `duration`, and `dismiss` — all of which are stable refs/callbacks BUT the `duration` comes from `toast.duration` prop which could theoretically change if the store mutates the toast. More critically, `runOnJS(dismiss)()` is called inside `setTimeout` but `dismiss` is captured at initial render. | Stale closure in timer if toast data changes | Small |
| 16 | M | CallActiveBar.tsx:35 | Hardcoded English strings: "Call in progress" (line 35) and "Tap to return" (line 36). Component does not use `useTranslation()` hook. App supports 8 languages. | i18n violation — hardcoded English for 7 non-English languages | Small |
| 17 | M | CallActiveBar.tsx:28 | `accessibilityLabel` contains interpolated string `Return to call with ${activeCallName || 'user'}` (line 28). Not internationalized. | Accessibility label is English-only | Small |
| 18 | M | SocialProof.tsx:37 | `label` prop defaults to hardcoded `'Liked by'` (line 37). Not i18n'd. Also, "and", "other", "others" at lines 84-88 are hardcoded English. | i18n violation — hardcoded English | Small |
| 19 | M | EmojiPicker.tsx:252-255 | Empty state text "No recent emojis yet" and "No emojis found" at lines 253-254 are hardcoded English. Component has `useTranslation` imported but doesn't use `t()` for these strings. | i18n violation | Small |
| 20 | M | EmojiPicker.tsx:200 | Search placeholder "Search emojis..." at line 200 is hardcoded English instead of using `t()`. | i18n violation | Small |
| 21 | M | VideoPlayer.tsx:342-363 | Multiple interactive Pressables in the controls overlay lack `accessibilityLabel` and `accessibilityRole`. Loop toggle (line 342), quality button (line 347), fullscreen (line 357), speed (line 360) — none have a11y attributes. Only PiP button has them. | Screen reader users can't identify control buttons | Small |
| 22 | M | VideoPlayer.tsx:368-376 | Skip backward (line 368), play/pause (line 371), skip forward (line 374) lack `accessibilityLabel` and `accessibilityRole`. Compare with VideoControls.tsx which has full a11y on every button. | Screen reader inaccessible | Small |
| 23 | M | VideoPlayer.tsx:381 | Mute toggle (line 381) lacks `accessibilityLabel` and `accessibilityRole`. | Screen reader inaccessible | Small |
| 24 | M | VideoPlayer.tsx:388 | Time display text (line 388) lacks `accessibilityLabel`. Position time is unreadable by screen readers without context. | Screen reader gets raw time string without context | Small |
| 25 | M | VideoPlayer.tsx:405-413 | Seek bar (lines 405-413) lacks `accessibilityRole="adjustable"`, `accessibilityValue`, and `accessibilityActions`. Compare with VideoControls.tsx:264-277 which has full adjustable role with increment/decrement actions. | Seek bar completely inaccessible | Small |
| 26 | M | ImageGallery.tsx:245-257 | Close button Pressable (line 249) and share button (line 262) lack `accessibilityLabel` and `accessibilityRole`. | Gallery controls inaccessible to screen readers | Small |
| 27 | M | MiniPlayer.tsx:329 | Title text style (line 329) uses hardcoded `colors.text.primary` in StyleSheet. Not theme-aware. | Wrong color in light mode | Small |
| 28 | M | MiniPlayer.tsx:333 | Channel text style (line 333) uses hardcoded `colors.text.secondary`. Not theme-aware. | Wrong color in light mode | Small |
| 29 | M | PremiereCountdown.tsx:65 | `useEffect` for `pulseScale` animation (line 65) has `[time.expired]` dependency but is missing `pulseScale` in the dependency array. | ESLint exhaustive-deps violation, animation may not trigger | Small |
| 30 | M | RichText.tsx:28 | `TOKEN_RE` regex matches `\d{1,3}:\d{1,3}` which matches common time formats like "10:30" or "3:45" as Quran references. Text like "Meet at 3:45" would produce a clickable "Quran 3:45" link. | False positive Quran citation detection in natural text | Medium |
| 31 | M | RichText.tsx:70-71 | `e.stopPropagation?.()` — `stopPropagation` is not a standard method on React Native's `GestureResponderEvent`. It exists on web but not native. This call is a no-op on mobile, meaning link taps also trigger the parent `onPostPress`. | Tapping links also triggers post navigation | Small |
| 32 | M | GradientButton.tsx:38-39 | `accessibilityRole` prop typed as `string` instead of RN's `AccessibilityRole` type. The prop is declared but never forwarded to the underlying Pressable — all three variants hardcode `accessibilityRole="button"` (lines 176, 225, 249). Dead prop. | Type inaccuracy, unused prop | Small |
| 33 | M | GlassHeader.tsx:27 | `HeaderAction.icon` typed as `string | React.ReactNode`. When it's a string, it's cast to `IconName` at line 68 (`as IconName`). If a non-IconName string is passed, the Icon component silently returns null (dev-only console.warn). No runtime validation in production. | Silent icon rendering failure in production | Small |
| 34 | M | SchedulePostSheet.tsx:58 | `TIME_SLOTS.findIndex(...)` can return -1 if `nextHour >= 24` (rare edge: near midnight). `Math.min(-1, TIME_SLOTS.length - 1)` returns -1. Accessing `TIME_SLOTS[-1]` is `undefined` causing a crash at line 73 when `time.hours` is accessed. | Potential crash near midnight when opening schedule sheet | Small |
| 35 | M | AnimatedAccordion.tsx:79 | `contentHeight.current` defaults to 0 and falls back to 300 (line 79: `contentHeight.current || 300`). If content is exactly 0 height (empty children), it animates to 300px. More importantly, initial render measures with 0 before `onLayout` fires, causing a flicker from 300px to actual height. | Visual flicker on first expansion | Small |
| 36 | M | TabBarIndicator.tsx:33 | `indicatorWidth` is `containerWidth / tabCount`. If `tabCount` is 0, this produces `Infinity`, and `translateX` becomes `NaN * Infinity`. | Division by zero with no tabs | Small |
| 37 | M | Icon.tsx:168-176 | `iconProps` typed as `Record<string, unknown>` then cast `as LucideProps` (line 176). This bypasses type safety. If LucideProps changes, no compile error. | Type safety bypass | Small |
| 38 | L | VideoPlayer.tsx:262-268 | `formatTime` is a non-memoized function recreated every render. It's called in JSX (lines 388, 415). Should be extracted outside the component or wrapped in `useCallback`. | Micro performance — function recreated per render | Tiny |
| 39 | L | CreateSheet.tsx:360 | `gridCard` uses `shadowColor: '#000'` hardcoded (line 355). Shadow color should potentially vary with theme (lighter shadow in light mode). | Minor theme inconsistency | Tiny |
| 40 | L | CreateSheet.tsx:21 | `AnimatedPressable` created at module scope — this is fine and correct for Reanimated. No issue. But `GridCard` and `SecondaryRow` are not `memo`'d despite being rendered in a list. They recreate animated shared values on every render. | Unnecessary re-renders of grid cards | Small |
| 41 | L | ImageGallery.tsx:90-96 | `onViewableItemsChanged` is stored as a ref with `.current` at declaration time. This is a common pattern but the callback captures `resetImageState` from the first render. If `resetImageState` changes (it won't because it's wrapped in useCallback with stable deps), this would be stale. Minor fragility. | Fragile ref pattern | Tiny |
| 42 | L | Toast.tsx:59 | `toastId` is a module-level variable (line 59). In React Fast Refresh (hot reload), module-level state persists across refreshes. IDs keep incrementing but the store resets, leading to mismatched IDs during development. | Dev-only: toast ID mismatch on hot reload | Tiny |
| 43 | L | MiniPlayer.tsx:48-52 | Six separate `useStore` selectors (lines 47-52) — each creates a separate subscription. These should be combined into a single selector returning an object, or use `useShallow` to avoid unnecessary re-renders when unrelated store fields change. | Up to 6x unnecessary re-renders from store updates | Small |
| 44 | L | EmojiPicker.tsx:117 | `EmojiPicker` is NOT wrapped in `memo`. It's rendered inside a `BottomSheet` which itself re-renders on gesture updates. The entire emoji grid (FlatList + 200 items) re-renders on parent gesture. | Performance: emoji grid re-renders on sheet gesture | Small |
| 45 | L | ReactionPicker.tsx:94 | `ReactionButton` has no `accessibilityLabel`. The button renders an icon + optional count, but the reaction name (LOVE, FIRE, etc.) is never read to screen readers. The `labelKey` from `REACTIONS` config is never used for accessibility. | Reaction buttons unnamed for screen readers | Small |
| 46 | L | BottomSheet.tsx:93 | `close` function calls `haptic.navigate()` on every close. If user taps backdrop to dismiss (line 169), haptic fires. If Android back button dismisses (line 112), haptic fires. But the opening animation at line 86 has no haptic. Inconsistent haptic pattern. | UX: haptic on close but not open | Tiny |
| 47 | L | CharCountRing.tsx:97 | SVG `Circle` at line 99 uses hardcoded `stroke="rgba(255,255,255,0.1)"`. In light mode, this is invisible against a white background. | Track ring invisible in light mode | Small |
| 48 | L | EndScreenOverlay.tsx:26 | `POSITION_STYLES` values use `left`/`right` (lines 25-30) instead of `start`/`end`. These don't flip in RTL layouts. | Incorrect positioning in RTL languages | Small |
| 49 | L | RichCaptionInput.tsx:220-221 | Overlay Text uses `left: 0, right: 0` (lines 220-221) instead of `start: 0, end: 0`. Won't align correctly in RTL. | Overlay misaligned in RTL | Small |
| 50 | L | CaughtUpCard.tsx:79 | `setTimeout(() => haptic.success(), 300)` (line 79) — bare setTimeout not cleaned up in useEffect return. If component unmounts within 300ms, haptic fires on unmounted component. | Haptic fires after unmount | Tiny |
| 51 | L | FloatingHearts.tsx:86 | `useEffect` at line 86 has empty dependency array `[]` but uses `particle` from outer scope. Missing deps warning suppressed. All values are captured at mount time only, so this is safe but fragile. | Missing useEffect deps | Tiny |
| 52 | L | Avatar.tsx:283 | When `onPress` is falsy, the avatar renders without `accessibilityRole`. Non-pressable avatars should have `accessibilityRole="image"` for screen readers to identify them. | Non-pressable avatar has no semantic role | Tiny |
| 53 | L | Badge.tsx:94 | `text.color` hardcoded to `colors.text.primary` (white in dark theme). On a red badge, white text is fine, but if `color` prop is set to a light color like yellow, white text becomes unreadable. No contrast checking. | Potential low contrast with custom badge colors | Tiny |
| 54 | I | VideoPlayer.tsx:440-488 | Speed and quality bottom sheets render 6 and N static items respectively. These could use `FlatList` or at minimum be extracted as separate components to reduce the `VideoPlayer` render tree size. | Code organization | Small |
| 55 | I | VideoPlayer.tsx:45 | `VideoPlayer` is already `memo`'d (good). But it accepts `onProgress` and `onComplete` callbacks that will likely be new references each parent render, defeating memoization. Consider documenting that callers should wrap these in `useCallback`. | Memoization easily defeated by unstable callback props | Small |
| 56 | I | MiniPlayer.tsx:39 | `MiniPlayer` is NOT wrapped in `memo`. It's rendered in the root layout and subscribes to 6 store values. Any store update re-renders it. Should be `memo`'d with a custom comparator or use `useShallow`. | Unnecessary re-renders | Small |
| 57 | I | LocationPicker.tsx:31-42 | `POPULAR_LOCATIONS` hardcoded in component file. Should come from an API or at minimum a separate data file. 10 Islamic locations is a good start but limits discovery. | Static data in component | N/A |
| 58 | I | RichText.tsx:63 | `writingDirection` and `textAlign` are computed from text content. But a bilingual post (mixed Arabic + English) gets one direction for the whole block. Consider per-paragraph or per-line direction detection. | Mixed-direction text displays suboptimally | Medium |
| 59 | I | VideoControls.tsx:46 | `VideoControls` is not wrapped in `memo`. It's likely rendered as a child of VideoPlayer and re-renders on every playback status update (multiple times per second due to progress tracking). | Performance: frequent unnecessary re-renders | Small |
| 60 | I | Autocomplete.tsx:89 | `fetchResults` depends on `type` but not `t` (translation function). The `AccessibilityInfo.announceForAccessibility` message at line 70 uses `t()` which is captured in the callback but not in deps. If language changes while autocomplete is open, the announcement uses stale language. | Stale translation in announcement | Tiny |
| 61 | I | ScreenErrorBoundary.tsx:34 | Sentry imported via `require()` at runtime (line 34). This is intentional for optional dependency, but the `try/catch` around `require` means if Sentry IS installed but throws during `captureException`, the error is silently swallowed. | Error reporting failure silently ignored | Small |
| 62 | I | UploadProgressBar.tsx:114 | `uploadWithProgress` utility function is defined in a UI component file. It should be in a service/utils file. UI component files should only contain React components. | Code organization — utility in component file | Small |
| 63 | I | TabSelector.tsx:82 | `accessibilityRole="button"` on tab items. Should be `accessibilityRole="tab"` with the container having `accessibilityRole="tablist"` for proper tab semantics. (Compare: EmojiPicker.tsx correctly uses `"tab"` at line 235.) | Wrong accessibility semantics for tabs | Small |
| 64 | I | ForceUpdateModal.tsx:11 | `IOS_APP_STORE_URL` contains placeholder ID `id0000000000`. This will 404 when used. | Placeholder URL will fail in production | Small |

---

## Severity Counts

| Severity | Count | Description |
|----------|-------|-------------|
| **C** (crash/security/data-loss) | **4** | Unmount timer leaks (#1, #2), raw RN Image (#3), StatusBar stuck hidden (#4) |
| **H** (broken feature) | **6** | Non-functional quality selector (#5), stale layout (#6), wrong cursor pos (#7), video leak (#8), controls pop (#9), jittery progress (#10) |
| **M** (error-handling/perf) | **27** | Theme violations (#11-13, #27-28), rule violations (#14), i18n violations (#16-20), a11y violations (#21-26), logic bugs (#29-31, #33-37), stale closures (#15, #32) |
| **L** (smell) | **16** | Performance (#38, #40, #43-44), fragile patterns (#41-42), RTL (#48-49), misc (#45-47, #50-53) |
| **I** (suggestion) | **11** | Organization (#54, #56-57, #62), memoization (#55, #59), semantics (#58, #60-61, #63-64) |

**C:4 H:6 M:27 L:16 I:11 Total: 64**

---

## Top Priority Fixes

1. **#1, #2** — Timer cleanup on unmount (crash prevention)
2. **#5** — Quality selector does nothing (broken feature)
3. **#16-20** — i18n violations in CallActiveBar, SocialProof, EmojiPicker (8 languages affected)
4. **#21-26** — Accessibility gaps in VideoPlayer and ImageGallery (screen reader users locked out)
5. **#30** — False Quran citation detection (embarrassing UX bug for an Islamic app)
6. **#34** — SchedulePostSheet crash near midnight
7. **#3** — Raw RN Image in LinkPreview (violates project rules)
