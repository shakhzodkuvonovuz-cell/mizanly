# A01 — Auth, Users, Two-Factor, Devices

**Files audited:**
- `apps/api/src/modules/auth/auth.controller.ts` (72 lines) — READ IN FULL
- `apps/api/src/modules/auth/auth.service.ts` (434 lines) — READ IN FULL
- `apps/api/src/modules/auth/auth.module.ts` (11 lines) — READ IN FULL
- `apps/api/src/modules/auth/webhooks.controller.ts` (166 lines) — READ IN FULL
- `apps/api/src/modules/auth/dto/register.dto.ts` (47 lines) — READ IN FULL
- `apps/api/src/modules/auth/dto/set-interests.dto.ts` (17 lines) — READ IN FULL
- `apps/api/src/modules/users/users.controller.ts` (395 lines) — READ IN FULL
- `apps/api/src/modules/users/users.service.ts` (1309 lines) — READ IN FULL
- `apps/api/src/modules/users/users.module.ts` (13 lines) — READ IN FULL
- `apps/api/src/modules/users/dto/update-profile.dto.ts` (92 lines) — READ IN FULL
- `apps/api/src/modules/users/dto/report.dto.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/users/dto/contact-sync.dto.ts` (26 lines) — READ IN FULL
- `apps/api/src/modules/users/dto/nasheed-mode.dto.ts` (8 lines) — READ IN FULL
- `apps/api/src/modules/two-factor/two-factor.controller.ts` (140 lines) — READ IN FULL
- `apps/api/src/modules/two-factor/two-factor.service.ts` (638 lines) — READ IN FULL
- `apps/api/src/modules/two-factor/two-factor.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/devices/devices.controller.ts` (68 lines) — READ IN FULL
- `apps/api/src/modules/devices/devices.service.ts` (140 lines) — READ IN FULL
- `apps/api/src/modules/devices/devices.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/common/guards/clerk-auth.guard.ts` (114 lines) — READ IN FULL
- `apps/api/src/common/guards/optional-clerk-auth.guard.ts` (86 lines) — READ IN FULL
- `apps/api/src/common/decorators/current-user.decorator.ts` (25 lines) — READ IN FULL
- `apps/api/src/common/utils/sanitize.ts` (17 lines) — READ IN FULL
- `apps/api/prisma/schema.prisma` (User model, lines 674-754) — READ relevant section

**Files in scope but SKIPPED:** None. All `.controller.ts`, `.service.ts`, `.module.ts`, and `dto/` files in auth, users, two-factor, and devices were read in full.

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | H | apps/api/src/modules/auth/auth.service.ts:185 | **`register()` returns full Prisma User object without `select`**. The `upsert` at line 128 has no `select` clause, so `return user` at line 185 sends the entire User model to the client — including `email`, `phone`, `clerkId`, `isChildAccount`, `stripeConnectAccountId`, `banReason`, `warningsCount`, `reputationScore`, `tosAcceptedAt`, `tosVersion`, `referralCode`, `scheduledDeletionAt`, and all other internal fields. | Leaks internal/sensitive fields to the mobile client. `clerkId` and `email` are PII; `reputationScore`/`warningsCount` are moderation internals; `stripeConnectAccountId` is a payment secret. | 10 min — add `select` to the upsert |
| 2 | H | apps/api/src/modules/auth/auth.service.ts:400 | **`syncClerkUser()` returns full Prisma User object without `select`**. Both the `create` (line 371) and `update` (line 333) paths return the complete User record. This is returned to the webhook caller, but if the webhook response is ever logged or if the response interceptor serializes it, all internal fields are exposed. More critically, the function is exported from AuthService and could be called from other services that forward the result. | Same field leakage as #1. While webhook responses are less visible than API responses, returning unbounded data is a defense-in-depth violation. | 10 min — add `select` to create and update |
| 3 | H | apps/api/src/modules/users/users.controller.ts:297 | **`requestVerification` uses inline type `{ category: string; reason: string; proofUrl?: string }` instead of a DTO with class-validator decorators.** The `@Body()` parameter has no validation — class-validator's `ValidationPipe` only works with decorated DTO classes. Any string (including unbounded length, XSS payloads, or SQL-like content) can be injected into the `category`, `reason`, and `proofUrl` fields. | No length limits on `reason` (stored in DB), no format validation on `proofUrl` (potential SSRF if ever fetched), no enum validation on `category`. Attacker can store arbitrarily large strings in the `report.description` column. | 15 min — create `RequestVerificationDto` with proper decorators |
| 4 | M | apps/api/src/modules/auth/auth.controller.ts:54 | **`setInterests` endpoint is missing `@Throttle()` decorator.** While the controller-level default applies if set, this endpoint has no explicit rate limit. An attacker can call this repeatedly to burn DB resources (each call does `deleteMany` + `createMany`). | Repeated calls could cause DB write amplification. The `deleteMany`+`createMany` pattern is not idempotent abuse-wise — each call generates write load. | 5 min — add `@Throttle()` |
| 5 | M | apps/api/src/modules/auth/auth.controller.ts:29 | **`register()` uses `@CurrentUser('clerkId')` while all other endpoints use `@CurrentUser('id')`.** This is correct for the registration flow (user doesn't have a DB `id` yet at first call), but creates an inconsistency. The `auth.service.register()` then does a `findUnique({ where: { clerkId } })` which may fail if the Clerk webhook hasn't created the user yet. The retry logic in the guard (line 54 of clerk-auth.guard.ts) only solves the race for `findUnique by clerkId`, but `register` bypasses the guard's user lookup entirely since it uses `clerkId` directly. | If a user calls `/auth/register` before the Clerk `user.created` webhook fires, registration will create a new user via `upsert`. If the webhook then fires, `syncClerkUser` creates a second user with the same `clerkId` — but `clerkId` has a unique constraint, so it would fail silently. The actual risk is low because the upsert uses `where: { clerkId }`, but the flow is fragile and depends on upsert atomicity. | 30 min — document or add explicit lock |
| 6 | M | apps/api/src/modules/auth/auth.service.ts:104 | **`register()` leaks Clerk API error details in the exception message.** Line 104: `throw new BadRequestException('Failed to verify account: ${msg}')` includes the raw Clerk error message which may contain internal API details, rate limit info, or Clerk-specific error codes. | Information disclosure — helps attackers understand the backend's Clerk integration. | 5 min — use generic message |
| 7 | M | apps/api/src/modules/users/users.service.ts:39 | **`PUBLIC_USER_FIELDS` includes `isDeleted`, `isBanned`, `isDeactivated`, `lastSeenAt`**. These are returned by `getProfile()` and `updateProfile()` even though `getProfile` later checks for these states and throws. Including moderation fields like `isBanned` in the select for public-facing responses leaks internal state. `lastSeenAt` is a privacy concern — shows exact last activity time of any user. | Privacy leak: any authenticated user can see another user's exact `lastSeenAt` timestamp and moderation status fields (if they bypass the throw by being the target user themselves or via cached data). | 10 min — remove sensitive fields from `PUBLIC_USER_FIELDS`, add them only in `getMe()` |
| 8 | M | apps/api/src/modules/auth/auth.service.ts:379 | **`syncClerkUser()` creates user with implicit ToS acceptance (`tosAcceptedAt: new Date()`) for webhook-created users.** Line 379 sets `tosAcceptedAt` for users created via Clerk webhook without explicit consent. Under GDPR Art 7, consent must be "freely given, specific, informed and unambiguous." Auto-setting ToS acceptance for webhook-created users may not meet this standard. | Legal/GDPR risk — ToS consent recorded without actual user action. If challenged, the consent record is fabricated. | 30 min — remove auto-accept, require explicit acceptance on first app launch |
| 9 | M | apps/api/src/modules/devices/devices.controller.ts:60 | **`logoutAllOtherSessions` uses `@Delete('sessions')` with `@Body()`.** HTTP DELETE requests with a body are discouraged by RFC 7231 and some HTTP clients/proxies strip the body from DELETE requests. If the `currentSessionId` body field is lost in transit, the service call `logoutAllOtherSessions(userId, undefined)` would use `undefined` as `currentSessionId`, and the `id: { not: undefined }` Prisma filter would match ALL sessions including the current one — logging the user out of their own device. | User could be logged out of all devices including current one if a proxy strips the DELETE body. | 15 min — change to POST route or use query param for currentSessionId |
| 10 | M | apps/api/src/modules/two-factor/two-factor.service.ts:328 | **`validate()` returns `true` when 2FA is not enabled (line 333).** The TODO at line 322 acknowledges this is disconnected from Clerk's login flow, but the consequence is that calling `POST /two-factor/validate` with any code returns `{ valid: true }` for users without 2FA. This endpoint could be misinterpreted by mobile code as "2FA check passed" when it really means "no 2FA configured." | If mobile code uses `validate` to gate sensitive operations (thinking it verifies identity), users without 2FA bypass the check entirely. The `validateStrict()` method exists but is not exposed via any endpoint. | 20 min — expose `validateStrict` or change validate behavior |
| 11 | M | apps/api/src/modules/users/users.service.ts:1101 | **`findByPhoneNumbers()` fetches up to 10,000 users with phone numbers into memory.** Line 1101 loads all users with non-null phones (`take: 10000`) into Node.js memory, then iterates and hashes each one. At scale, this is an O(N) server-side operation per contact sync request. | Memory pressure and CPU cost proportional to total users with phone numbers. At 100K+ users with phones, this becomes a DoS vector since each request forces hashing 10K+ phone numbers. | 2 hours — use DB-side hashing or pre-computed hash column |
| 12 | M | apps/api/src/modules/auth/auth.service.ts:144 | **`register()` generates referral code using `randomBytes(6).toString('base64url').slice(0, 8)`.** The dynamic import `(await import('crypto'))` inside the `create` data object is unusual but functional. However, the 8-character base64url code has only ~48 bits of entropy. While sufficient for referral codes, collisions become likely at ~2^24 (~16M) users due to birthday paradox. More importantly, there's no retry on unique constraint violation. | Referral code collision at scale would cause registration failure with an unhandled Prisma unique constraint error (P2002). | 15 min — add retry loop or use CUID/UUID for referral codes |
| 13 | L | apps/api/src/modules/auth/auth.controller.ts:44 | **`checkUsername` endpoint is unauthenticated (no `@UseGuards`).** This is intentional (checking username availability during signup before auth), but combined with the rate limit of 10/60s, an attacker can enumerate existing usernames at 10 checks per minute. | Username enumeration — attacker can check ~14,400 usernames per day per IP. Mitigated partially by the throttle, but the endpoint confirms or denies specific username existence. | 30 min — add proof-of-work or CAPTCHA, or increase throttle strictness |
| 14 | L | apps/api/src/modules/users/users.service.ts:96 | **`updateProfile()` uses `Record<string, unknown>` cast and spreads DTO directly into Prisma update.** Line 96: `const sanitizedData: Record<string, unknown> = { ...dto }`. If the DTO ever gains a field that maps to a Prisma column the code doesn't intend to update (e.g., `role`, `isVerified`, `isBanned`), it would be passed through. Class-validator prevents unexpected fields if `whitelist: true` is set globally, but this is a defense-in-depth gap. | If `ValidationPipe` whitelist mode is not enabled globally, extra fields in the request body could modify unintended DB columns. | 15 min — explicitly destructure allowed fields instead of spreading |
| 15 | L | apps/api/src/modules/devices/devices.service.ts:99 | **`touchSession()` takes `deviceId` as a plain string parameter with no ownership check.** Any code calling `touchSession('some-id', ip)` can update any device's `lastActiveAt` and `ipAddress`. There's no `userId` filter. | If this method is ever exposed via an endpoint or called with user-controlled input, it enables updating metadata of devices belonging to other users. Currently only called internally so risk is low. | 5 min — add userId parameter and filter |
| 16 | L | apps/api/src/modules/two-factor/two-factor.controller.ts:19 | **DTOs (`SetupResponseDto`, `VerifyDto`, `DisableDto`, `VerifyBackupDto`) are defined inline in the controller file rather than in a `dto/` directory.** This is inconsistent with the pattern used by auth, users, and devices modules. | Code organization inconsistency. Not a functional issue, but makes the codebase harder to navigate and maintain. | 10 min — move to dto/ files |
| 17 | L | apps/api/src/modules/users/users.controller.ts:286 | **`getReferralCode` uses `as Record<string, unknown>` cast to access `referralCode`.** The `getMe()` return type doesn't include `referralCode` in its `select`, so the cast is hiding a bug — `referralCode` will always be `undefined` because it's not selected in the `getMe()` query (line 78-92 of users.service.ts). | The referral code endpoint always returns `{ referralCode: undefined, shareUrl: 'https://mizanly.app/join?ref=undefined' }`. Broken feature. | 10 min — add `referralCode` to the `getMe()` select clause |
| 18 | L | apps/api/src/modules/auth/auth.service.ts:355 | **`queueService.addSearchIndexJob(...).catch(() => {})` silently swallows errors.** Lines 355 and 398 catch and discard search indexing failures. If search indexing consistently fails, users will be invisible in search with no indication to ops. | Silent data loss — users might not appear in search results, with no error trail. | 10 min — log the error in the catch handler |
| 19 | I | apps/api/src/modules/auth/auth.service.ts:163 | **Minor's age is logged in plaintext.** Line 163: `this.logger.log('Minor registered (age ${age}): user ${user.id}')`. Under COPPA, recording a minor's exact age in logs could be a compliance concern depending on log retention and access policies. | Minor compliance risk — logs with age data may need special retention/access rules. | 5 min — log `isMinor: true` without exact age |
| 20 | I | apps/api/src/modules/users/users.service.ts:192 | **`getProfile()` caches the full user object in Redis for 5 minutes (line 227) but the cached version may include stale `isDeleted`/`isBanned`/`isDeactivated` values.** If a user is banned, their profile remains accessible via cache for up to 5 minutes. | Banned/deleted users remain visible for up to 5 minutes after ban. Low real-world impact but worth noting for compliance. | 15 min — invalidate cache on ban/deactivate, or check flags after cache hit (already done at line 231) |
| 21 | I | apps/api/src/modules/two-factor/two-factor.service.ts:69 | **`verifyTotp()` uses constant-time comparison via the TOTP algorithm's integer comparison, but the string comparison at line 80 (`token === code...`) is NOT timing-safe.** While the timing difference for a 6-digit string comparison is negligible (nanoseconds) and TOTP codes rotate every 30 seconds making timing attacks impractical, this is technically a deviation from best practices. | Theoretical timing side-channel on TOTP verification. Practically unexploitable given 30-second code rotation and network jitter. | 10 min — use `timingSafeEqual` for the string comparison |

**Severity counts:** C:0 H:3 M:7 L:5 I:3
**Total findings:** 21
