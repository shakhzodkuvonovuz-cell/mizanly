# A05 — Videos, Video Editor, Video Replies, Subtitles, Thumbnails

**Files audited:**
- `apps/api/src/modules/videos/videos.controller.ts` (357 lines) — READ IN FULL
- `apps/api/src/modules/videos/videos.service.ts` (1117 lines) — READ IN FULL
- `apps/api/src/modules/videos/videos.module.ts` (16 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/create-video.dto.ts` (64 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/update-video.dto.ts` (36 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/create-video-comment.dto.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/report.dto.ts` (9 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/video-progress.dto.ts` (9 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/end-screen.dto.ts` (17 lines) — READ IN FULL
- `apps/api/src/modules/videos/dto/premiere.dto.ts` (8 lines) — READ IN FULL
- `apps/api/src/modules/video-replies/video-replies.controller.ts` (68 lines) — READ IN FULL
- `apps/api/src/modules/video-replies/video-replies.service.ts` (175 lines) — READ IN FULL
- `apps/api/src/modules/video-replies/video-replies.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/subtitles/subtitles.controller.ts` (73 lines) — READ IN FULL
- `apps/api/src/modules/subtitles/subtitles.service.ts` (136 lines) — READ IN FULL
- `apps/api/src/modules/subtitles/subtitles.module.ts` (9 lines) — READ IN FULL
- `apps/api/src/modules/thumbnails/thumbnails.controller.ts` (88 lines) — READ IN FULL
- `apps/api/src/modules/thumbnails/thumbnails.service.ts` (199 lines) — READ IN FULL
- `apps/api/src/modules/thumbnails/thumbnails.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/video-editor/ffmpeg-engine.spec.ts` (1130 lines) — SKIMMED (test-only file, no source code)

**Files in scope but SKIPPED:**
- `video-editor` module has no controller, service, or module file — only a test spec file (`ffmpeg-engine.spec.ts`). The video editor logic lives on the mobile side (`apps/mobile`), not in the API. No API source code to audit.

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | H | `thumbnails.controller.ts:17` | **contentType case mismatch — all thumbnail operations silently broken.** `CreateVariantsDto` validates `@IsIn(['post', 'reel', 'video'])` (lowercase), but the controller casts to uppercase `as 'POST' \| 'REEL' \| 'VIDEO'` at line 43. TypeScript `as` is a type assertion, not a runtime transform. The lowercase string `'post'` reaches Prisma which expects the `ThumbnailContentType` enum (`POST`, `REEL`, `VIDEO`). Prisma will reject the value or store the wrong case, causing lookups to fail. Same issue at lines 59 and 69. | All thumbnail A/B testing operations fail with Prisma validation errors. Feature is completely non-functional. | 5 min — change `@IsIn` to `['POST', 'REEL', 'VIDEO']` |
| 2 | H | `end-screen.dto.ts:5` | **EndScreen type case mismatch — end screens silently broken.** `EndScreenItemDto` validates `@IsIn(['subscribe', 'watch_next', 'playlist', 'link'])` (lowercase), but `videos.service.ts:1044` casts `item.type as EndScreenType`. The Prisma enum `EndScreenType` has values `SUBSCRIBE`, `WATCH_NEXT`, `PLAYLIST`, `LINK` (uppercase). Runtime value is lowercase, cast doesn't transform it. Prisma rejects the insert. | All end screen creation fails. Feature non-functional. | 5 min — change `@IsIn` to uppercase values |
| 3 | H | `videos.service.ts:360` | **`getById` does not filter banned/deactivated/deleted users.** The `getFeed` method at line 278 correctly filters `user: { isPrivate: false, isDeactivated: false, isBanned: false, isDeleted: false }`, but `getById` at line 356-360 only checks `status` and `isRemoved`. A banned user's videos remain individually accessible via direct URL. | Banned/deactivated users' content continues to be served when accessed by direct link. Privacy and moderation bypass. | 10 min — add user state filter to `getById` where clause |
| 4 | H | `videos.service.ts:858-870` | **`getRecommended` does not filter banned/deactivated/deleted users.** Same omission as `getById` — the where clause filters `status` and `isRemoved` but not `user.isBanned`, `user.isDeactivated`, `user.isDeleted`, or `user.isPrivate`. Banned users' content appears in recommendations. | Banned/deactivated users' videos served in recommendation feeds. | 10 min — add user state filter |
| 5 | H | `video-replies/video-replies.controller.ts:30-37` | **No DTO class for video-reply creation — inline object type instead of class-validator DTO.** The `@Body()` parameter is typed as a raw inline object `{ commentId: string; commentType: 'POST' \| 'REEL'; ... }`. Without a DTO class with class-validator decorators, the NestJS validation pipe does NOT validate this input. Any shape of JSON body is accepted — missing fields, wrong types, extra fields all pass through. The service does manual validation (lines 40-56), but this bypasses the global validation pipeline pattern and is incomplete (no `@IsUUID` on `commentId`, no `@MaxLength` on `mediaUrl`). | Untrusted input reaches the service layer without standard validation. Possible junk data in DB (e.g., extremely long URLs, non-UUID commentId). | 20 min — extract to a proper DTO class with decorators |
| 6 | H | `subtitles/subtitles.controller.ts:64` | **Open redirect via `@Redirect()` on SRT endpoint.** The `getSrtRedirect` endpoint uses NestJS `@Redirect()` to send users to `track.url`, which is a user-provided URL stored via `createTrack`. An attacker creates a subtitle track with `srtUrl: "https://evil.com/phishing"`, then shares `GET /videos/:videoId/subtitles/:id/srt`. The server responds with a 302 redirect to the attacker's URL. This is a classic open redirect. | Phishing attacks where mizanly.app domain redirects to malicious sites. Trust exploitation. | 30 min — validate URL domain against allowlist (R2/CDN domains only), or serve content directly instead of redirecting |
| 7 | M | `videos.service.ts:634-644` | **No content moderation on video comments.** Video creation (line 128) and updates (line 421) run `contentSafety.moderateText()`, but the `comment()` method at line 634 does not. Comments are persisted with only `sanitizeText()` (XSS prevention), not content safety moderation. | Hate speech, harassment, and other harmful content can be posted in comments without any automated moderation check. | 15 min — add `contentSafety.moderateText(content)` call before persisting |
| 8 | M | `videos.service.ts:644-645` | **Comment `parentId` not validated to belong to same video.** The `parentId` is passed directly from user input to `videoComment.create()` without verifying that the parent comment belongs to the same `videoId`. A user can create a reply on video A that references a comment from video B, creating cross-video reply trees. | Data integrity issue — comment reply counts and threading are corrupted across videos. | 10 min — fetch parent comment and verify `parentId.videoId === videoId` before creating |
| 9 | M | `videos.controller.ts:78-88` | **Missing `@Throttle()` on video update endpoint.** The `PATCH :id` (update) endpoint has `@UseGuards(ClerkAuthGuard)` but no `@Throttle()`. An attacker can spam update requests to trigger content moderation API calls (line 421-426), which are likely expensive (external API). | Denial-of-wallet via content moderation API spam. Rate-limited endpoints like create (5/min) are protected, but update is not. | 2 min — add `@Throttle()` decorator |
| 10 | M | `videos.controller.ts:90-99` | **Missing `@Throttle()` on video delete endpoint.** `DELETE :id` has no rate limit. While each delete requires ownership, a compromised account could rapid-fire delete all their videos. | Rapid mass deletion without rate limiting. | 2 min — add `@Throttle()` |
| 11 | M | `videos.controller.ts:245-254` | **Missing `@Throttle()` on premiere creation.** `POST :id/premiere` has no rate limit. | Premiere creation spam. | 2 min |
| 12 | M | `videos.controller.ts:263-268` | **Missing `@Throttle()` on premiere reminder set/remove.** `POST :id/premiere/reminder` and `DELETE :id/premiere/reminder` have no rate limit. | Reminder toggle spam causing DB write amplification. | 2 min |
| 13 | M | `videos.controller.ts:277-282` | **Missing `@Throttle()` on premiere start.** `POST :id/premiere/start` has no rate limit. | Repeated start requests. Idempotent in DB but still hits DB each time. | 2 min |
| 14 | M | `videos.controller.ts:302-312` | **Missing `@Throttle()` on end screen set/delete.** `PUT :id/end-screens` and `DELETE :id/end-screens` have no rate limit. The `setEndScreens` method at line 1037 does a `deleteMany` + N `create` calls per request — amplified DB load. | DB write amplification via spammed end screen replacements. | 2 min |
| 15 | M | `videos.service.ts:1037-1053` | **`setEndScreens` delete + create is not atomic.** `deleteMany` runs at line 1037, then individual `create` calls run via `Promise.all` at line 1039-1053. If any create fails mid-way, old end screens are deleted but new ones are partially created. Should be a single `$transaction`. | Partial data state on failure — some end screens lost, some half-created. | 10 min — wrap in `$transaction` |
| 16 | M | `videos.controller.ts:64` + `videos.controller.ts:232` | **Unbounded `limit` query parameter.** `getCommentReplies` (line 64) and `getRecommended` (line 232) parse `limit` via `parseInt` with no upper bound. A client can pass `?limit=999999` and the service passes it directly to Prisma `take`. | Memory and performance DoS — single request loads millions of rows. | 5 min — cap limit with `Math.min(limitNum, 100)` |
| 17 | M | `videos.service.ts:822-848` | **Report `reason` field semantics mismatch.** `ReportDto` accepts free-text (3-500 chars, line report.dto.ts:6-9), but `videos.service.ts:831-845` uses it as a lookup key in `reasonMap`. Any free-text that isn't an exact enum key (e.g., "This video is spam and hateful") falls through to `'OTHER'`. The actual user-provided reason text is never stored — `description` field stores `video:${videoId}` at line 844, losing the human-readable reason. | All user report reasons are lost. Moderators see only "OTHER" with no context for most reports. Feature broken by design. | 15 min — either (a) change `ReportDto` to use `@IsEnum(ReportReason)` for structured reasons + optional freetext field, or (b) store reason text in `description` alongside videoId |
| 18 | M | `thumbnails/thumbnails.controller.ts:24-29` | **No URL validation on `thumbnailUrls` array.** `CreateVariantsDto.thumbnailUrls` has `@IsString({ each: true })` but no `@IsUrl({ each: true })`. Any string is accepted, including non-URL garbage, XSS payloads stored in DB, or internal network addresses (SSRF if URLs are fetched server-side later). | Invalid/malicious URLs stored as thumbnail URLs. Potential stored XSS if rendered in admin panels. | 5 min — add `@IsUrl({}, { each: true })` |
| 19 | M | `videos.service.ts:681-721` | **Comments query does not filter banned/deactivated user comments.** `getComments` fetches all comments for a video with no filter on user state. Comments from banned or deactivated users remain visible. Same issue in `getCommentReplies` (line 883-916). | Banned users' comments remain visible in video comment sections. | 10 min — add `user: { isBanned: false, isDeactivated: false, isDeleted: false }` to where clause |
| 20 | L | `subtitles/subtitles.service.ts:10-16` | **DTO class defined inside service file instead of dedicated dto/ directory.** `CreateSubtitleTrackDto` is defined in `subtitles.service.ts`. This breaks the module convention used by every other module (separate `dto/` folder). Makes it harder to find and maintain DTOs. | Inconsistent code organization. No functional impact. | 5 min — move to `subtitles/dto/create-subtitle-track.dto.ts` |
| 21 | L | `thumbnails/thumbnails.controller.ts:10-30` | **DTO classes defined in controller file instead of dto/ directory.** `TrackVariantDto` and `CreateVariantsDto` are defined inline in the controller file. Same organizational issue as finding 20. | Inconsistent code organization. | 5 min — move to `thumbnails/dto/` |
| 22 | L | `videos.service.ts:190` | **Unsafe type assertion to check for `scheduledAt`.** Line 190 uses `(dto as unknown as Record<string, unknown>).scheduledAt` to check if `CreateVideoDto` has a `scheduledAt` field. But `CreateVideoDto` has no `scheduledAt` property — this check is always false. The double cast `as unknown as Record<string, unknown>` bypasses type safety entirely. Same pattern at line 210. | Dead code path — `scheduledAt` check never triggers since the DTO doesn't have the field. Publish workflow and gamification always fire, even for scheduled videos (if the feature is ever added). | 10 min — either add `scheduledAt` to the DTO or remove the dead check |
| 23 | L | `videos.service.ts:97-105` | **Misaligned `take` clauses in `enhanceVideos`.** Lines 99 and 104 have `take: 50` but they're indented to appear inside the `findMany` options but are actually at the wrong nesting level. The `take: 50` at line 99 is outside the `where` block but inside the `findMany` call — this works in Prisma but is confusing. If the video list has more than 50 items, some reactions/bookmarks are silently not loaded. | For lists >50 videos, some isLiked/isBookmarked flags are incorrectly false. Feed is typically 20 items so no immediate impact, but breaks for batch operations. | 5 min — remove the arbitrary `take: 50` or set it to match the actual batch size |
| 24 | L | `video-replies/video-replies.service.ts:78-91` | **No content moderation on video reply creation.** Unlike video creation which runs `contentSafety.moderateText()`, video reply creation stores `mediaUrl` without any check. No image/video moderation on the media URL. | Harmful video content can be posted as replies without automated moderation. | 15 min — add media moderation |
| 25 | L | `videos.service.ts:1018-1023` | **`incrementPremiereViewerCount` has no deduplication.** Any authenticated user can spam `POST :id/premiere/viewer` and inflate the viewer count indefinitely (rate limit is 5/min but still allows 5 fake views per minute). No check for unique viewer. | Inflated premiere viewer counts. Minor vanity metric manipulation. | 15 min — deduplicate by user (upsert pattern or Redis set) |
| 26 | I | `videos.controller.ts:329-335` | **Stub endpoint `POST :id/cross-publish` returns hardcoded response.** Returns `{ crossPublished: false, message: 'Coming soon.' }`. This is dead code exposed as a live API endpoint. | Unused API surface area. Clients may try to use it and get confused. | 2 min — remove or add `@ApiExcludeEndpoint()` |
| 27 | I | `videos.service.ts:533-537` + `587-592` | **Raw SQL uses `"videos"` (lowercase plural) as table name.** Prisma model is `Video`, mapped table name may differ. If the table is actually `"Video"` (Prisma default), these raw SQL queries fail. The `$executeRaw` at lines 533, 543, 587, 597, 619, 624 all reference `"videos"`. Other raw queries at lines 487, 736, 773 reference `"Channel"` and `"videos"` inconsistently (Channel uppercase, videos lowercase). | If table mapping doesn't match, these queries silently fail or error. Needs verification against actual Prisma migration output. | 10 min — verify table name in migrations and make consistent |
| 28 | I | `thumbnails/thumbnails.service.ts:108` | **`Math.random()` used for A/B test variant selection.** While not a crypto context (so not a security issue per crypto rules), `Math.random()` has poor distribution properties. For statistically significant A/B testing, a more uniform distribution is preferred. | Slightly biased A/B test results over large sample sizes. Not a security issue. | 5 min — use crypto.randomInt or similar for better uniformity |

**Severity counts:** C:0 H:6 M:11 L:6 I:3 (plus 2 L that are organizational)
**Total findings:** 28
