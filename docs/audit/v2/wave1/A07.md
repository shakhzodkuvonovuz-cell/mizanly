# A07 — Stories, Story Chains

**Files audited:**
- `apps/api/src/modules/stories/stories.controller.ts` (209 lines) — READ IN FULL
- `apps/api/src/modules/stories/stories.service.ts` (604 lines) — READ IN FULL
- `apps/api/src/modules/stories/stories.module.ts` (7 lines) — READ IN FULL
- `apps/api/src/modules/stories/dto/create-story.dto.ts` (72 lines) — READ IN FULL
- `apps/api/src/modules/stories/dto/create-highlight.dto.ts` (14 lines) — READ IN FULL
- `apps/api/src/modules/stories/dto/update-highlight.dto.ts` (15 lines) — READ IN FULL
- `apps/api/src/modules/story-chains/story-chains.controller.ts` (79 lines) — READ IN FULL
- `apps/api/src/modules/story-chains/story-chains.service.ts` (199 lines) — READ IN FULL
- `apps/api/src/modules/story-chains/story-chains.module.ts` (10 lines) — READ IN FULL
- `apps/api/prisma/schema.prisma` (Story model lines 1101-1134, StoryChain 2907-2921, StoryChainEntry 2923-2937, StoryHighlightAlbum 2318-2330, StoryStickerResponse 2332-2345) — READ RELEVANT SECTIONS
- `apps/api/src/common/guards/optional-clerk-auth.guard.ts` (lines 13-28) — READ FOR CROSS-MODULE VERIFICATION

**Files in scope but SKIPPED:** None. All `.controller.ts`, `.service.ts`, `.module.ts`, and `dto/` files were read in full.

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | H | stories.service.ts:252-288 | `getById` returns raw `story` object with no `select` clause. Returns full Prisma model including internal fields (`isSensitive`, `musicId`, all DB columns). Also does NOT check `closeFriendsOnly` or `subscribersOnly` flags — anyone with a story ID can view a close-friends-only or subscribers-only story directly, bypassing feed-level filtering. | Privacy bypass: close-friends/subscribers-only stories accessible by direct ID. Information leak: internal fields exposed. | 30min |
| 2 | H | stories.service.ts:568-576 | `submitStickerResponse` has no block check, no expired/archived check, no `closeFriendsOnly`/`subscribersOnly` visibility check, and no banned-user check. Anyone with a story ID can submit sticker responses to stories they shouldn't be able to see (blocked users, expired stories, close-friends-only stories). | Authorization bypass: blocked users can interact with stories. Data integrity: responses on expired stories. | 30min |
| 3 | H | stories.service.ts:315-343 | `markViewed` does not check if `viewerId === story.userId`. Story owners can mark their own story as viewed, inflating `viewsCount`. This is a view-count manipulation bug. | Data integrity: artificially inflated view counts. | 10min |
| 4 | H | story-chains.service.ts:30-55 | `getTrending` pagination is broken. `orderBy` is `[participantCount: desc, createdAt: desc]` but cursor uses `id: { lt: cursor }`. Since `id` (cuid) has no correlation with `participantCount` sort order, the cursor will skip and duplicate items across pages. Prisma cursor-based pagination requires the cursor field to match the orderBy field. | Broken pagination: users see duplicate chains or miss chains entirely when paging through trending results. | 30min |
| 5 | H | story-chains.service.ts:66-106 | `getChain` does not filter out banned/deactivated/deleted users from the user lookup (line 95-105), and does not filter archived or expired stories from the story lookup (line 83-93). Banned users' stories and profiles remain visible in chain entries. | Content from banned users shown to all users. Expired/archived stories still returned in chain entry data. | 20min |
| 6 | M | stories/dto/create-story.dto.ts:48-60 | `CreateStoryDto` contains fields `fontFamily`, `filter`, and `bgGradient` that do NOT exist in the Prisma `Story` model. These fields are validated by class-validator but silently discarded by the service's `create` method. The DTO also LACKS a `subscribersOnly` field even though the service accepts and persists it (service.ts:174,189). Users cannot set `subscribersOnly` via the API. | Feature gap: `subscribersOnly` flag cannot be set by clients. Dead code: 3 DTO fields validated but never persisted. | 15min |
| 7 | M | stories.controller.ts:141-186 | Highlight mutation endpoints (`createHighlight`, `updateHighlight`, `deleteHighlight`, `addToHighlight`) lack `@Throttle()` decorator. Other mutation endpoints (create story, reply, mark viewed, sticker response) all have rate limiting. An attacker can spam highlight creation (100 album limit exists but no rate limit) or deletion. | Abuse: rapid-fire highlight manipulation. Inconsistent rate limiting across the module. | 10min |
| 8 | M | stories.controller.ts:25-32 | `StoryReplyDto` and `StickerResponseDto` are defined inline in the controller file rather than in dedicated `dto/` files. `StickerResponseDto.responseData` uses `@IsObject()` with no size constraint — an attacker can submit a multi-MB JSON payload as `responseData`, which gets stored in the DB as a `Json` column. | DoS: large JSON payloads stored in DB. Code pattern inconsistency: DTOs should be in `dto/` directory. | 20min |
| 9 | M | stories.service.ts:184 | `textOverlay` is saved directly to the database without `sanitizeText()` call. The `replyToStory` method (line 431) correctly sanitizes content via `sanitizeText(content)`, but story creation does not sanitize `textOverlay`. If `sanitizeText` strips XSS vectors, this is an XSS vector when textOverlay is rendered. | Stored XSS if textOverlay is rendered in a web view or admin panel without escaping. | 5min |
| 10 | M | story-chains.service.ts:153-178 | `joinChain` uses a timing heuristic (`upserted.createdAt.getTime() > Date.now() - 1000`) to detect if the entry is new. This 1-second window is fragile: if the DB transaction takes >1 second (network latency, lock contention), a new entry will be falsely detected as existing and `participantCount` will NOT be incremented. Conversely, clock skew between app server and DB could cause an update to be falsely detected as new. | Data integrity: `participantCount` can become permanently incorrect (too low or too high). | 20min |
| 11 | M | stories.service.ts:467-474 | `replyToStory` notification body contains the actual reply content in plaintext (line 473: `body: content.length > 100 ? content.slice(0, 99) + '\u2026' : content`). Per project E2E crypto rules: "Push notifications: generic body for ALL messages (no encryption status leak)." This leaks message content through push notifications. | Privacy: reply content visible in push notifications (lock screen, notification center). Violates E2E encryption rules. | 5min |
| 12 | M | stories.service.ts:252-288 | `getById` with `OptionalClerkAuthGuard` does not check if the viewer is blocked by the story author. A blocked user can still view individual stories by ID. The feed correctly filters blocks (line 58-88), but direct access does not. | Authorization gap: blocked users can view stories via direct ID lookup. | 15min |
| 13 | L | story-chains.controller.ts:18-25 | `CreateChainDto` and `JoinChainDto` are defined inline in the controller file instead of in a `dto/` directory. Inconsistent with the stories module pattern which has a dedicated `dto/` folder. | Code organization inconsistency. | 10min |
| 14 | L | stories.service.ts:280-284 | `getById` fires a `storyView.upsert` as a fire-and-forget `.catch()` side effect (line 280-284), AND `markViewed` (line 315-343) is a separate dedicated endpoint for the same purpose. This means viewing a story records the view twice via two different code paths with different semantics (upsert vs. check-then-create-in-transaction). The `getById` path does NOT increment `viewsCount`, creating a data inconsistency. | Data inconsistency: `storyView` record may exist without corresponding `viewsCount` increment. Confusing dual code paths for the same operation. | 15min |
| 15 | L | story-chains.service.ts:12-28 | `createChain` does not sanitize the `prompt` text. Only `.trim()` and length check are applied. If the prompt is rendered in any web context, this could be an XSS vector. Also no rate limit per-user on chain creation (only global 60/min from controller-level `@Throttle`). | Potential stored XSS. No per-user creation limit means one user can create many chains. | 10min |
| 16 | L | stories.service.ts:57-79 | `getFeedStories` loads up to 10,000 follows, 10,000 blocks, 10,000 mutes, and 10,000 restricts into memory for every feed request. For users with many relationships, this is 40,000 records loaded per request. | Performance: excessive memory allocation for feed requests from power users. | 30min |
| 17 | I | stories.controller.ts:20 | Unused import: `IsString`, `IsObject`, `MaxLength` from `class-validator` are imported at line 20, but they are used for inline DTO classes. This is not technically unused, but the inline DTOs should be moved to `dto/` files per project conventions. | Code hygiene. | 5min |
| 18 | I | stories.service.ts:110-115 | Close friends filtering always returns `false` for non-owners (line 112: `if (story.closeFriendsOnly) return false`). The comment says "Currently rejects all close-friends stories for non-owners (safe default)" but this means close-friends-only stories are NEVER shown to close friends. The feature is incomplete. | Feature incomplete: close friends can never see close-friends-only stories. | 2h |

**Severity counts:** C:0 H:5 M:5 L:4 I:2
**Total findings:** 18
