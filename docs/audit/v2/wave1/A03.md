# A03 — Reels, Reel Templates, Clips

**Files audited:**
- `apps/api/src/modules/reels/reels.controller.ts` (341 lines) — READ IN FULL
- `apps/api/src/modules/reels/reels.service.ts` (1352 lines) — READ IN FULL
- `apps/api/src/modules/reels/reels.module.ts` (16 lines) — READ IN FULL
- `apps/api/src/modules/reels/dto/create-reel.dto.ts` (168 lines) — READ IN FULL
- `apps/api/src/modules/reels/dto/create-comment.dto.ts` (14 lines) — READ IN FULL
- `apps/api/src/modules/reels/dto/report.dto.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/reel-templates/reel-templates.controller.ts` (95 lines) — READ IN FULL
- `apps/api/src/modules/reel-templates/reel-templates.service.ts` (116 lines) — READ IN FULL
- `apps/api/src/modules/reel-templates/reel-templates.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/clips/clips.controller.ts` (74 lines) — READ IN FULL
- `apps/api/src/modules/clips/clips.service.ts` (99 lines) — READ IN FULL
- `apps/api/src/modules/clips/clips.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/clips/dto/create-clip.dto.ts` (7 lines) — READ IN FULL

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | H | reels.controller.ts:106 | `updateReel` uses inline type `{ caption?: string; hashtags?: string[] }` for `@Body()` instead of a DTO class with class-validator decorators. This means **no validation runs** on the PATCH body — any JSON payload is accepted. An attacker could send `caption` with 10MB of text or `hashtags` with 100K entries. | Unbounded input, potential DoS, bypasses all validation | 15 min |
| 2 | H | reels.service.ts:480-511 | `updateReel` does not run content moderation on the updated caption. `create` runs `contentSafety.moderateText()` on the caption (line 99), but `updateReel` skips moderation entirely. A user can create a clean reel, then immediately PATCH it with policy-violating content. | Content moderation bypass | 20 min |
| 3 | H | reels.service.ts:990-1004 | `getUserReels` does not check if the target user's profile is private. It only checks `isOwn` to decide whether to show trial/scheduled reels. A non-follower can fetch all published reels from a private account via `GET /reels/user/:username`. The feed query at line 359 correctly filters `isPrivate: false`, but `getUserReels` does not. | Privacy violation — private account reels exposed to anyone | 20 min |
| 4 | H | reels.service.ts:1065-1108 | `getByAudioTrack` does not filter out reels from private accounts (`user: { isPrivate: false }`). The feed and trending queries both include this filter, but this endpoint omits it. Reels from private accounts are leaked through audio track browsing. | Privacy violation — private account content leaked via audio track | 10 min |
| 5 | H | reels.service.ts:1110-1158 | `getDuets` does not filter out reels from private accounts. Same issue as #4 — missing `user: { isPrivate: false, isDeactivated: false, isBanned: false, isDeleted: false }` in the where clause. | Privacy violation — private account content leaked via duets | 10 min |
| 6 | H | reels.service.ts:1160-1208 | `getStitches` does not filter out reels from private accounts. Same issue as #4 and #5. | Privacy violation — private account content leaked via stitches | 10 min |
| 7 | M | reels.service.ts:557-562 | `recordView` silently swallows ALL errors including database connectivity failures (`}).catch(() => {})`). This means view counts can silently drift. Additionally, controller line 118 calls `recordView(id)` without `await`, making it fire-and-forget, but the method itself is async with an internal await — the promise error is silently swallowed. | Silent data loss for view counts, violates "never silently swallow errors" rule | 10 min |
| 8 | M | reels.service.ts:564-569 | `recordLoop` silently swallows ALL errors with the same `}).catch(() => {})` pattern as `recordView`. | Silent data loss for loop counts | 5 min |
| 9 | M | reels.controller.ts:118 | `getById` calls `this.reelsService.recordView(id)` as a side effect without awaiting. This is a fire-and-forget async call on a GET endpoint. If the reel doesn't exist, `recordView` will attempt to update a nonexistent row (swallowed by catch). The actual 404 is thrown by the next line's `getById`. But the side-effect means every GET request, including those that return 404, attempts a DB write. | Wasted DB writes on invalid reel IDs, also double-increments view count (once from `getById` GET and once from explicit `POST :id/view`) | 15 min |
| 10 | M | clips.service.ts:44-63 | `getByVideo` and `getByUser` accept `limit` parameter with no upper bound. The default is 20, but a caller can pass `limit=999999` via query string. The reel-templates service correctly bounds with `Math.min(Math.max(limit, 1), 50)`, but the clips service does not. | Potential DoS — attacker can request unbounded result sets | 10 min |
| 11 | M | clips.controller.ts:45,57 | `parseInt(limit)` without radix and without NaN check. If `limit=abc`, `parseInt` returns `NaN`, which flows into `findMany({ take: NaN + 1 })`. Prisma may reject or return unexpected results. | Unhandled NaN in query parameter | 10 min |
| 12 | M | reels.service.ts:1042-1063 | `report` uses a `description` field to store `reel:${reelId}` and checks for duplicates using `findFirst({ where: { reporterId, description } })`. This is fragile — it uses `description` as a composite key instead of a proper `targetType`/`targetId` pattern. If the Report model has dedicated fields for target type/ID, these aren't being used. Also, the `reasonMap` silently maps unknown reasons to `'OTHER'` without notifying the caller. | Fragile duplicate detection, potential report model mismatch | 30 min |
| 13 | M | reel-templates.service.ts:88-99 | `markUsed` accepts `_userId` but never uses it. There's no rate limit on how many times a user can increment `useCount`. A single user could call `POST :id/use` 60 times/minute (the controller throttle limit) to artificially inflate template popularity in trending sort. | Inflatable popularity metric, unused parameter | 20 min |
| 14 | M | reels.service.ts:1277-1293 | `saveDraft` accepts a `CreateReelDto` but allows `videoUrl` to be empty string (line 1282: `dto.videoUrl || ''`). The DTO has `@IsUrl()` on `videoUrl` which requires a valid URL — so either validation runs and rejects empty (breaking draft save for users who haven't uploaded yet) or the `||` fallback is dead code. If the intent is to allow drafts without a video, the DTO needs a separate `CreateDraftDto` where `videoUrl` is optional. | DTO/service impedance mismatch — either drafts without video break or the fallback is dead code | 20 min |
| 15 | M | reels.service.ts:832-878 | `getComments` filters out comments from blocked/muted users but only checks `blockerId: userId` (line 837) — it does NOT check reverse blocks (`blockedId: userId`). The feed query at line 342-344 correctly checks both directions. This means if User A blocks User B, User B's comments are hidden from User A, but User A's comments remain visible to User B. | Inconsistent block enforcement on comments vs feed | 15 min |
| 16 | L | reels.controller.ts:129,138 | `like` has `@Throttle` but `unlike` at line 137-142 does not. An attacker could rapidly toggle unlike to generate DB load. Similarly, `unbookmark` at line 217-222 lacks `@Throttle` while `bookmark` at line 209-215 has it. | Unthrottled mutation endpoints | 10 min |
| 17 | L | clips.service.ts:34 | `clipUrl` is generated by appending `?start=&end=` query params to `video.hlsUrl`. HLS URLs typically don't support range params this way — this is likely non-functional and would produce a broken URL. The clip is essentially a metadata record with no actual clipped video. | Non-functional clip URL generation | 30 min |
| 18 | L | reel-templates.controller.ts:22-31 | DTOs `TemplateSegmentDto` and `CreateReelTemplateDto` are defined inline in the controller file rather than in a `dto/` directory. This breaks the module's file organization pattern. | Inconsistent file organization | 10 min |
| 19 | L | reels.service.ts:1210-1232 | `archive` and `unarchive` don't check if the reel is already in the desired state. Archiving an already-archived reel or unarchiving a non-archived reel silently succeeds. Also, archived reels are not filtered out in `getUserReels`, `getByAudioTrack`, `getDuets`, or `getStitches` queries — archived reels are still publicly visible. | Archive feature is non-functional — archived reels still appear in all listings | 20 min |
| 20 | L | reels.service.ts:1318-1335 | `getDownloadUrl` returns the original `videoUrl` directly. This exposes the raw R2/CDN URL to the client with no auth, no signed URL, no expiry. Anyone who obtains this URL can download the video forever without authentication. | No access control on download URL | 30 min |
| 21 | L | reel-templates.service.ts:49-50 | `segments` field is stored via `JSON.parse(JSON.stringify(data.segments))`. This double-serialization is unnecessary — Prisma handles JSON fields natively. If segments contained `undefined` values, this would silently drop them (which might be intentional but is undocumented). | Unnecessary serialization round-trip | 5 min |
| 22 | I | reels.service.ts:1338-1351 | `getAccessibilityReport` is defined as a public method but is never called from any controller endpoint. It's dead code. | Dead code | 5 min |
| 23 | I | reel-templates.service.ts:25-52 | `create` method validates name and segments manually (lines 26-43) even though the `CreateReelTemplateDto` already has class-validator decorators (`@IsString()`, `@MaxLength()`, `@ArrayMinSize()`, `@ArrayMaxSize()`, `@Min()`, `@Max()`). The manual validation is redundant if the global validation pipe is active. If the pipe is NOT active, the DTO decorators are dead code. | Redundant double validation | 10 min |
| 24 | I | clips.service.ts:89 | `delete` uses `findFirst({ where: { id: clipId, userId } })` which correctly checks ownership. However, the error message is empty: `throw new NotFoundException()` with no message. Other methods in the same file and across modules consistently provide error messages. | Missing error message in NotFoundException | 2 min |

**Severity counts:** C:0 H:6 M:9 L:5 I:4
**Total findings:** 24
