# A13 — Channels, Channel Posts, Communities, Community Notes

**Files audited:**
- `apps/api/src/modules/channels/channels.controller.ts` (173 lines) — READ IN FULL
- `apps/api/src/modules/channels/channels.service.ts` (533 lines) — READ IN FULL
- `apps/api/src/modules/channels/channels.module.ts` (13 lines) — READ IN FULL
- `apps/api/src/modules/channels/dto/create-channel.dto.ts` (20 lines) — READ IN FULL
- `apps/api/src/modules/channels/dto/update-channel.dto.ts` (25 lines) — READ IN FULL
- `apps/api/src/modules/channels/dto/set-trailer.dto.ts` (8 lines) — READ IN FULL
- `apps/api/src/modules/channel-posts/channel-posts.controller.ts` (81 lines) — READ IN FULL
- `apps/api/src/modules/channel-posts/channel-posts.service.ts` (100 lines) — READ IN FULL
- `apps/api/src/modules/channel-posts/channel-posts.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/channel-posts/dto/create-channel-post.dto.ts` (14 lines) — READ IN FULL
- `apps/api/src/modules/communities/communities.controller.ts` (120 lines) — READ IN FULL
- `apps/api/src/modules/communities/communities.service.ts` (461 lines) — READ IN FULL
- `apps/api/src/modules/communities/communities.module.ts` (19 lines) — READ IN FULL
- `apps/api/src/modules/communities/dto/create-community.dto.ts` (31 lines) — READ IN FULL
- `apps/api/src/modules/communities/dto/update-community.dto.ts` (32 lines) — READ IN FULL
- `apps/api/src/modules/community/community.controller.ts` (225 lines) — READ IN FULL
- `apps/api/src/modules/community/community.service.ts` (369 lines) — READ IN FULL
- `apps/api/src/modules/community/community.module.ts` (12 lines) — READ IN FULL
- `apps/api/src/modules/community/dto/community.dto.ts` (87 lines) — READ IN FULL
- `apps/api/src/modules/community-notes/community-notes.controller.ts` (67 lines) — READ IN FULL
- `apps/api/src/modules/community-notes/community-notes.service.ts` (95 lines) — READ IN FULL
- `apps/api/src/modules/community-notes/community-notes.module.ts` (9 lines) — READ IN FULL

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | C | community.service.ts:147 | `answerId: answer` stores the answer **text string** into a self-referential FK column (`FatwaQuestion.answerId` is a relation to another `FatwaQuestion`). The schema defines `answerId String?` with `@relation("FatwaAnswer", fields: [answerId], references: [id])`. Passing the full answer text as a CUID-typed FK will either crash with a Prisma validation error or a PostgreSQL FK constraint violation on every fatwa answer attempt. | Fatwa answering is completely broken; no scholar can ever answer a question. | 30 min -- add an `answerText` field to the model or store the answer in a separate table and reference its ID. |
| 2 | C | community.service.ts:339-368 | `getDataExport` fetches the full `User` object with `findUnique` and NO `select` clause. The raw User model includes `email`, `phone`, `clerkId`, `stripeConnectAccountId`, `banReason`, `tosVersion`, `referralCode`, and every other sensitive field. This is returned directly in the API response. While it is the user's own data, the response also travels over the wire and could be cached by intermediaries. More critically, there is no select clause so if new sensitive fields are added to User, they auto-leak. | Exposes PII and internal identifiers (Clerk ID, Stripe connect ID) in API responses. A proxy/CDN cache or logging middleware could capture this. | 30 min -- add explicit `select` on User query. |
| 3 | H | communities.service.ts:72 | `generateSlug` uses `Math.random()` for fallback slug generation (line 72). The CLAUDE.md standing rules explicitly forbid `Math.random()` for any purpose that could be security-relevant. While slug generation is not crypto, the rule is absolute, and the generated slug is predictable/guessable. | Violates standing rule. Slug collisions are probabilistically possible with `Math.random().toString(36).slice(2, 9)` (only ~36^7 = 78B combinations, birthday problem applies at scale). | 10 min -- replace with `crypto.randomUUID().slice(0,8)` or similar CSPRNG. |
| 4 | H | communities.service.ts:389-461 | Role management methods (`createRole`, `updateRole`, `deleteRole`, `listRoles`) exist in the service but have **no controller endpoints**. The `CommunitiesController` has no routes that call these methods. These are dead code that will never execute via the API. | Community role management is entirely unreachable from the API. Admins cannot create, edit, or delete roles. | 1 hr -- add controller endpoints with proper guards, DTOs, and rate limiting. |
| 5 | H | channel-posts.service.ts:14 | `create` method does not call `sanitizeText()` on `data.content` before persisting. The channels service sanitizes text for channel names/descriptions, but channel posts go straight to DB unsanitized. | Stored XSS if content is rendered in a web view or admin panel. At minimum, inconsistent with the pattern established in `channels.service.ts`. | 15 min -- import and apply `sanitizeText`. |
| 6 | H | channel-posts.service.ts:9 | `create` method does not call `contentSafety.moderateText()` before persisting. The channels service has pre-save content moderation (lines 84-93), but channel posts skip moderation entirely. | Harmful/abusive content can be posted to channels with zero moderation, bypassing the safety system. | 30 min -- add ContentSafetyService dependency and pre-save check. |
| 7 | H | community.service.ts:19 | `createBoard` spreads `dto` directly into Prisma `data` without sanitization or content moderation. Same pattern in `createStudyCircle` (line 95), `askFatwa` (line 115), `createOpportunity` (line 168), `createEvent` (line 194), `createVoicePost` (line 250), `createWatchParty` (line 277), `createCollection` (line 294), `createWaqf` (line 310). None of these 9 create methods sanitize text or moderate content. | All community features bypass content safety entirely. Abusive/harmful text in board names, fatwa questions, event descriptions, etc. is stored raw. | 2 hr -- add sanitization and moderation to all create paths. |
| 8 | H | channels.controller.ts:71-81 | `update` endpoint has no `@Throttle()` decorator. All other mutation endpoints in channels have rate limiting, but update is unprotected. An attacker can spam channel updates. | No rate limiting on channel updates allows DoS on the database and potential abuse (rapid name changes to evade moderation). | 5 min -- add `@Throttle()`. |
| 9 | H | channels.controller.ts:83-92 | `delete` endpoint has no `@Throttle()` decorator. Although deleting one's own channel is idempotent after the first call, the endpoint still hits the database for every request. | No rate limiting on channel deletion allows unnecessary DB load from repeated requests. | 5 min -- add `@Throttle()`. |
| 10 | M | channel-posts.service.ts:21 | `getFeed` uses `id: { lt: cursor }` for cursor-based pagination instead of the standard `cursor: { id: cursor }, skip: 1` pattern used everywhere else in the codebase. CUIDs are not lexicographically ordered by creation time -- they have a random component. Using `id < cursor` produces **non-deterministic page ordering** that can skip or duplicate posts. | Channel post feeds may show duplicate posts or skip posts across pages. Pagination is unreliable. | 30 min -- switch to `cursor: { id: cursor }, skip: 1` with `orderBy: { createdAt: 'desc' }`. |
| 11 | M | communities.controller.ts:43 | `limit` parameter has type `= 20` (default value) but no validation. It arrives as a `string` from the query string, and NestJS does not auto-parse it to `number` without `@Type(() => Number)` from class-transformer or `parseInt`. Passing `limit=99999` would be sent as-is to the service, and Prisma's `take` would accept the string coercion. | Unbounded pagination -- a client can request `limit=100000` and dump the entire communities table in one request. DoS vector. | 15 min -- add `ParseIntPipe` or a DTO with `@IsInt() @Min(1) @Max(100)` validation. |
| 12 | M | communities.controller.ts:117 | Same issue as #11 -- `listMembers` `limit` param defaults to `50` but has no type validation or max bound. | Unbounded member list query. | 10 min -- add validation. |
| 13 | M | communities.service.ts:232-233 | When `name` is updated, the slug is regenerated from the new name. But there is no uniqueness check on the new slug before the update. If two communities have similar names producing the same slug, the second update will crash with a Prisma unique constraint error (P2002) that is not caught. | Unhandled P2002 error results in a 500 Internal Server Error instead of a friendly 409 Conflict. | 15 min -- wrap in try/catch like the `create` method does. |
| 14 | M | channel-posts.controller.ts:27 | `getFeed` does not pass `userId` to the service even though `OptionalClerkAuthGuard` is used. The service method ignores blocked/muted users because it has no userId context. Compare with `channels.service.ts:getVideos` which properly filters blocks/mutes. | Channel post feeds show content from users that the viewer has blocked or muted. | 30 min -- thread userId through to service and add block/mute filtering. |
| 15 | M | community-notes.controller.ts:10-14 | `CreateNoteDto` and `RateNoteDto` are defined inline in the controller file rather than in a `dto/` subdirectory. This breaks the codebase pattern where every module has DTOs in a `dto/` folder. | Inconsistent code organization. If DTO validation logic grows, the controller file becomes bloated. | 15 min -- extract to `dto/create-note.dto.ts` and `dto/rate-note.dto.ts`. |
| 16 | M | channel-posts.dto/create-channel-post.dto.ts:11-13 | `mediaUrls` array accepts any string -- no `@IsUrl({ each: true })` validation. Arbitrary strings (including script tags, SQL, or non-URL data) can be stored in the mediaUrls array. | Allows storing non-URL data in what should be a URL-only array field. Potential XSS vector if rendered unescaped. | 10 min -- add `@IsUrl({}, { each: true })`. |
| 17 | M | channels.service.ts:149-168 | `update` method skips content moderation that is present in `create`. A channel owner can create a channel with a clean name, then update it to something abusive/harmful. | Circumvents content moderation by updating rather than creating. | 20 min -- add moderation check in update path. |
| 18 | M | community-notes.service.ts:31-36 | `getNotesForContent` does not validate `contentType` parameter. Any string is cast to `EmbeddingContentType`. Unlike `createNote` which validates against `['post', 'thread', 'reel']`, the GET endpoint accepts arbitrary values. | Invalid contentType values are passed to Prisma which either silently returns empty results or throws an unhandled enum error depending on DB configuration. | 10 min -- add validation. |
| 19 | M | community-notes.service.ts:87-93 | Same validation gap as #18 -- `getHelpfulNotes` does not validate `contentType`. | Same as #18. | 5 min. |
| 20 | L | channels.service.ts:254-260 | `getVideos` fetches up to 10,000 blocks and 10,000 mutes per request (`take: 10000`). For users with many blocks/mutes, this is a significant memory and DB cost on every video feed request. | Performance degradation for heavy block/mute users. O(n) set construction on every request. | 1 hr -- cache block/mute sets in Redis, or use a subquery/JOIN instead of client-side filtering. |
| 21 | L | communities.service.ts:144-149 | `list` method limits private community membership lookup to `take: 50`. If a user is a member of more than 50 private communities, only the first 50 are considered, and the rest silently disappear from their community list. | Users in 50+ private communities will not see all their communities in the list. | 15 min -- paginate or remove the limit. |
| 22 | L | community.service.ts:144 | `answerFatwa` checks `q.status === 'FATWA_ANSWERED'` using a string literal instead of the imported `FatwaStatus` enum. If the enum value changes, this comparison silently breaks. | Fragile code -- enum string could diverge from the Prisma enum definition. | 5 min -- use `FatwaStatus.FATWA_ANSWERED`. |
| 23 | L | community.service.ts:49 | `requestMentorship` casts `dto.topic as FatwaTopicType` but the topic comes from a DTO with `@IsIn(['new_muslim', 'quran', 'arabic', 'fiqh', 'general'])`. These string values may not match the `FatwaTopicType` enum values in Prisma (which typically use UPPER_CASE). If they don't match, Prisma will throw an unhandled enum validation error. | Mentorship creation may fail with a cryptic Prisma error for valid-looking topic values. | 15 min -- verify enum values match or add a mapping. |
| 24 | L | community.service.ts:95 | Same enum casting issue: `dto.topic as ScholarTopicType` with `@IsIn(['quran', 'hadith', 'fiqh', 'seerah', 'arabic', 'tafsir'])` values that may not match the Prisma enum. | Study circle creation may fail. | 10 min. |
| 25 | L | community.service.ts:169 | Same enum casting issue: `dto.category as VolunteerCategory` from a DTO that only validates `@IsString() @MaxLength(50)` with no `@IsIn()` check at all. Any string is accepted and cast to VolunteerCategory. | Invalid category values cause Prisma enum errors at runtime instead of a clean 400 Bad Request. | 10 min -- add `@IsIn()` to DTO. |
| 26 | L | community.service.ts:195 | Same enum casting issue: `dto.eventType as IslamicEventType` from a DTO with `@IsString() @MaxLength(30)` and no `@IsIn()` validation. | Invalid event types cause Prisma errors. | 10 min. |
| 27 | I | communities.service.ts:98-101 | Slug generation + uniqueness check is not atomic. Between the `findUnique` check and the `create`, another request could create the same slug (TOCTOU race). The P2002 catch block at line 127 handles this, so it's not a bug, but the initial check at line 99 is redundant. | Minor -- the initial check provides a slightly better error message but is not necessary given the catch block. | N/A |
| 28 | I | channels.service.ts:101 | Ternary `dto.description ? sanitizeText(dto.description) : dto.description` is a no-op when description is undefined -- could be simplified to `dto.description && sanitizeText(dto.description)`. Same pattern at line 159. | Code clarity. | 2 min. |

**Severity counts:** C:2 H:6 M:8 L:6 I:2
**Total findings:** 24
