# A11 — Search, Hashtags, Embeddings, Recommendations

**Files audited:**
- `apps/api/src/modules/search/search.module.ts` (12 lines) — READ IN FULL
- `apps/api/src/modules/search/search.controller.ts` (119 lines) — READ IN FULL
- `apps/api/src/modules/search/search.service.ts` (698 lines) — READ IN FULL
- `apps/api/src/modules/search/meilisearch.service.ts` (214 lines) — READ IN FULL
- `apps/api/src/modules/hashtags/hashtags.module.ts` (10 lines) — READ IN FULL
- `apps/api/src/modules/hashtags/hashtags.controller.ts` (105 lines) — READ IN FULL
- `apps/api/src/modules/hashtags/hashtags.service.ts` (418 lines) — READ IN FULL
- `apps/api/src/modules/hashtags/dto/hashtag-query.dto.ts` (15 lines) — READ IN FULL
- `apps/api/src/modules/embeddings/embeddings.module.ts` (12 lines) — READ IN FULL
- `apps/api/src/modules/embeddings/embeddings.controller.ts` (34 lines) — READ IN FULL
- `apps/api/src/modules/embeddings/embeddings.service.ts` (559 lines) — READ IN FULL
- `apps/api/src/modules/embeddings/embedding-pipeline.service.ts` (188 lines) — READ IN FULL
- `apps/api/src/modules/recommendations/recommendations.module.ts` (12 lines) — READ IN FULL
- `apps/api/src/modules/recommendations/recommendations.controller.ts` (82 lines) — READ IN FULL
- `apps/api/src/modules/recommendations/recommendations.service.ts` (778 lines) — READ IN FULL
- `apps/api/src/common/guards/optional-clerk-auth.guard.ts` (87 lines) — READ IN FULL (cross-module dependency)
- `apps/api/src/common/utils/excluded-users.ts` (73 lines) — READ IN FULL (cross-module dependency)
- `apps/api/src/common/constants/feed-scoring.ts` (187 lines) — READ IN FULL (cross-module dependency)

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | H | search.controller.ts:21-29 | **Search endpoints do not pass userId to service, blocked/muted users not filtered.** The main `search()` endpoint uses `OptionalClerkAuthGuard` but never extracts `@CurrentUser('id')` and never passes it to `searchService.search()`. The service's aggregate search (lines 312-428 in search.service.ts) has zero block/mute filtering. An authenticated user searching "people" will see users they've blocked and vice versa. Same issue applies to `searchThreads()` at line 72 and `searchReels()` at line 85 — neither passes userId. | Blocked users appear in search results, privacy violation. Users who blocked each other can discover each other via search. | 1h |
| 2 | H | search.service.ts:169-183 | **Meilisearch results bypass all visibility/safety filters.** When Meilisearch is available and returns hits (lines 177-179), the service returns raw Meilisearch hits directly without checking `isRemoved`, `isBanned`, `isDeactivated`, `visibility`, `scheduledAt`, or block/mute status. The Meilisearch index may contain stale data for deleted/banned/removed content that hasn't been de-indexed yet. The Prisma fallback path does filter correctly, but the Meilisearch fast path returns unfiltered data. | Removed, banned, or scheduled-not-yet-due content appears in search results when Meilisearch is active. | 2h |
| 3 | H | embedding-pipeline.service.ts:63-83 | **Backfill infinite loop when embedding API fails.** `backfillPosts()` (and identically `backfillReels`, `backfillThreads`, `backfillVideos`) runs `while(true)` fetching posts that have no embedding via `NOT EXISTS`. If `embedPost()` returns `false` for every post in a batch (API key invalid, Gemini outage, rate limited), no embeddings are inserted, the same posts match next iteration, and the loop never terminates. The `cursor` variable declared on line 61 is never used — confirms the pagination was abandoned. The `isRunning` guard prevents concurrent runs but not self-starvation. | Admin-triggered backfill hangs forever, consuming DB connections and Gemini API quota in an infinite retry loop. Server process never recovers without restart. | 30m |
| 4 | M | search.controller.ts:54-66 | **`searchPosts` has no DTO validation on query parameter `q`.** Unlike the main `search()` which validates query length (search.service.ts:159-164), `searchPosts()` passes `query` directly with no length check. The service method `searchPosts()` at line 554 also does no validation. A user could send a multi-KB query string causing expensive `ILIKE '%...%'` full-table scans. Same issue for `searchThreads()` (line 72) and `searchReels()` (line 85). | DoS via expensive database queries with very long search strings. | 20m |
| 5 | M | search.service.ts:554-572 | **`searchPosts` and `searchThreads` compare `hasMore` against raw `limit` parameter (possibly string), not `safeLim`.** Line 570: `const hasMore = posts.length > limit` — but `limit` is the function parameter which may still be a string at runtime (NestJS query params are strings). The `safeLim` is correctly computed on line 555 but `hasMore` compares against `limit` not `safeLim`. When `limit` is a string, `posts.length > limit` does string comparison which always produces wrong results. Same pattern in `searchThreads()` line 592. | Pagination breaks: `hasMore` flag is incorrect, clients may see infinite "load more" or premature end of results. | 10m |
| 6 | M | recommendations.controller.ts:9-10 | **`@ApiBearerAuth()` at class level but all endpoints use `OptionalClerkAuthGuard`.** The `@ApiBearerAuth()` on line 9 tells Swagger clients that auth is required for all endpoints, but every endpoint uses `OptionalClerkAuthGuard` (not `ClerkAuthGuard`). This is misleading API documentation — clients may think auth is required when it's optional. Additionally, the `suggestedPeople` endpoint at line 14 works without auth but returns popular users without block/mute filtering for unauthenticated users (which is correct, but the Swagger docs are wrong). | Swagger/OpenAPI documentation incorrectly marks all endpoints as requiring auth, confusing API consumers. | 5m |
| 7 | M | meilisearch.service.ts:118 | **`indexName` not URI-encoded in `search()` and `addDocuments()` methods, but IS encoded in `deleteDocument()`.** Line 118: `` `${this.host}/indexes/${indexName}/search` `` — no encoding. Line 152: `` `${this.host}/indexes/${indexName}/documents` `` — no encoding. But line 174: `` `${this.host}/indexes/${encodeURIComponent(indexName)}/documents/...` `` — properly encoded. Inconsistent encoding across methods. Currently safe because all index names are hardcoded constants, but any future dynamic index name would break or cause path traversal. | Inconsistent URI encoding; future index names with special characters would break `search`/`addDocuments` but work for `deleteDocument`. | 10m |
| 8 | M | search.service.ts:170-173 | **Meilisearch index map missing `channels` type.** The `indexMap` on lines 171-174 maps search types to Meilisearch indexes but omits `channels`. When a user searches with `type=channels`, Meilisearch is skipped entirely and falls through to the Prisma ILIKE scan even when Meilisearch is configured and has a channels index (configured in meilisearch.service.ts:55). | Channel searches never use Meilisearch, always hit the slower Prisma path. | 5m |
| 9 | M | embeddings.service.ts:57 | **Gemini API key passed as URL query parameter.** Line 57: `` `...?key=${this.apiKey}` ``. While the code logs a warning about this (lines 38-41) and avoids logging full URLs, the key is still exposed in: (a) any HTTP proxy/load-balancer access logs that capture full URLs, (b) Node.js `fetch` error stack traces that may include the URL, (c) Sentry breadcrumbs if fetch requests are auto-instrumented. The Gemini API does support `x-goog-api-key` header as an alternative. | API key exposure risk through infrastructure logs, proxy logs, or error tracking systems. | 15m |
| 10 | M | hashtags.service.ts:155-188 | **Trending hashtags query runs 8 correlated subqueries per row.** The SQL in `fetchTrendingHashtags()` runs 4 `COUNT(*)` subqueries in the SELECT and the same 4 in the WHERE clause for every hashtag row. With N hashtags, this is O(8N) correlated subqueries, each doing a full array containment scan (`@>`) on posts/reels/threads/videos. At scale (100K+ content items), this will timeout. The 5-minute Redis cache mitigates but doesn't fix. | Query performance degrades quadratically with content volume. Will timeout at production scale. | 2h |
| 11 | M | embedding-pipeline.service.ts:51-57 | **`getEmbeddedIds` method is dead code.** The method loads ALL embedded IDs into memory for a given content type. The backfill methods were refactored to use `NOT EXISTS` subqueries instead (lines 68, 95, etc.), making this method unused. It would also be a memory bomb at scale (loading millions of IDs into a Set). | Dead code. If accidentally called at scale, would OOM the server loading millions of IDs. | 5m |
| 12 | L | search.service.ts:493 | **`getHashtagPosts` limit parameter not capped.** The method signature `getHashtagPosts(tag: string, cursor?: string, limit = 20)` accepts `limit` from the caller, but the controller at search.controller.ts:42-47 never passes a limit parameter. The default of 20 is fine, but the method is exported and could be called by other modules with an arbitrary limit. Unlike other methods which cap at 50, this one has no upper bound. | No immediate issue (default 20), but API inconsistency — caller could request unlimited rows. | 5m |
| 13 | L | recommendations.controller.ts:19-24 | **`limit` and `offset` query params are typed as `number` but arrive as strings.** NestJS `@Query()` params are always strings unless using `ParseIntPipe` or a DTO with `@Type(() => Number)`. Lines 19, 33-34, 47-48, 61-62, 76-77 all declare `limit?: number` and `offset?: number`. The `Math.min(Math.max(1, limit || 20), 50)` fallback works because JS coerces strings, but `typeof limit` would be `'string'` not `'number'`. Not currently breaking due to JS coercion, but misleading types. | Misleading TypeScript types; works at runtime only due to JS type coercion. Fragile if strict comparison is ever added. | 15m |
| 14 | L | search.controller.ts:17-29 | **No DTO classes with class-validator decorators on search controller query params.** The search controller uses raw `@Query('q')`, `@Query('type')`, `@Query('limit')` with no DTO class. The hashtags controller correctly uses `TrendingQueryDto` and `SearchQueryDto` with class-validator decorators. Missing DTOs means the NestJS validation pipe does not validate/sanitize input for search endpoints. | No automatic input validation via NestJS pipe. Manual validation exists in the service but is inconsistent (some endpoints validate, some don't). | 30m |
| 15 | L | hashtags.service.ts:191-203 | **`search()` returns raw array instead of `{ data, meta }` wrapper.** `HashtagsService.search()` returns `hashtags` directly (line 202), not wrapped in `{ data: hashtags, meta: { total, hasMore } }`. This is inconsistent with every other method in the same service and violates the API pattern documented in `api-modules.md`: "All responses: `{ data: T, success: true, timestamp }` via TransformInterceptor." | Inconsistent response shape for hashtag search endpoint. | 10m |
| 16 | L | search.service.ts:407-428 | **Paginated `people` and `tags` search types return `SearchResults` object without cursor/hasMore meta.** When `type=people` or `type=tags`, the response is `{ people: [...] }` or `{ hashtags: [...] }` with no pagination meta. But other types (posts, threads, reels, videos, channels) return `{ data, meta: { cursor, hasMore } }`. Client code expecting consistent pagination will break for people/tag searches. | Inconsistent API response shape prevents client-side pagination for people and tag search. | 20m |
| 17 | I | embedding-pipeline.service.ts:61 | **Unused variable `cursor` in `backfillPosts`.** `let cursor: string | undefined` is declared but never read or assigned. Leftover from a previous cursor-based pagination approach that was replaced with `NOT EXISTS`. | Dead code, no impact. | 1m |
| 18 | I | embeddings.service.ts:86-118 | **`generateBatchEmbeddings` is never called.** The batch embedding method exists but no code path calls it. The pipeline processes items one at a time via `embedPost/embedReel/embedThread/embedVideo`. Using batch would reduce API calls by up to 20x during backfill (BATCH_SIZE is 20). | Missed optimization. Backfill makes 20 individual API calls instead of 1 batch call per batch. | 30m |

**Severity counts:** C:0 H:3 M:7 L:5 I:2
**Total findings:** 17
