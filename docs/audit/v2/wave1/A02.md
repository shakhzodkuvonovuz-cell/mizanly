# A02 — Posts, Bookmarks, Collabs

**Files audited:**
- `apps/api/src/modules/posts/posts.controller.ts` (514 lines) — READ IN FULL
- `apps/api/src/modules/posts/posts.service.ts` (2004 lines) — READ IN FULL
- `apps/api/src/modules/posts/posts.module.ts` (15 lines) — READ IN FULL
- `apps/api/src/modules/posts/dto/create-post.dto.ts` (177 lines) — READ IN FULL
- `apps/api/src/modules/posts/dto/add-comment.dto.ts` (14 lines) — READ IN FULL
- `apps/api/src/modules/posts/dto/report.dto.ts` (9 lines) — READ IN FULL
- `apps/api/src/modules/posts/dto/cross-post.dto.ts` (17 lines) — READ IN FULL
- `apps/api/src/modules/bookmarks/bookmarks.controller.ts` (154 lines) — READ IN FULL
- `apps/api/src/modules/bookmarks/bookmarks.service.ts` (414 lines) — READ IN FULL
- `apps/api/src/modules/bookmarks/bookmarks.module.ts` (9 lines) — READ IN FULL
- `apps/api/src/modules/bookmarks/dto/bookmark.dto.ts` (20 lines) — READ IN FULL
- `apps/api/src/modules/collabs/collabs.controller.ts` (61 lines) — READ IN FULL
- `apps/api/src/modules/collabs/collabs.service.ts` (110 lines) — READ IN FULL
- `apps/api/src/modules/collabs/collabs.module.ts` (9 lines) — READ IN FULL
- `apps/api/src/modules/collabs/dto/invite-collab.dto.ts` (11 lines) — READ IN FULL

**Files in scope but SKIPPED:** None

## Findings

| # | Sev | File:Line | Finding | Impact | Fix Effort |
|---|-----|-----------|---------|--------|------------|
| 1 | C | posts.service.ts:163 | **Feed dismissal case mismatch breaks "Not Interested".** `dismiss()` at line 1460 writes `contentType: 'POST'` (uppercase), but `getFeed()` foryou query at line 163 reads `contentType: 'post'` (lowercase). Dismissals are never matched, so dismissed posts keep appearing in the For You feed indefinitely. | Users cannot remove unwanted content from their feed despite using the dismiss feature. | 5 min — change line 163 to `'POST'` |
| 2 | H | posts.service.ts:745-791 | **`getById` does not enforce visibility.** A `FOLLOWERS`-only or `CIRCLE`-only post is returned to any caller who knows the post ID. There is no check that the viewer follows the author or is in the circle. Block checks and scheduled-post checks exist, but `visibility` is ignored entirely. | Authorization bypass — private posts accessible via direct ID. | 30 min |
| 3 | H | posts.service.ts:745-791 | **`getById` does not filter banned/deactivated/deleted authors.** The post is returned even if `post.user.isBanned`, `isDeactivated`, or `isDeleted` is true. Other feed endpoints filter these; this one does not. | Content from banned/deleted users remains accessible by direct link. | 10 min |
| 4 | H | posts.service.ts:307-324 | **Blended feed leaks CIRCLE-only posts.** `getBlendedFeed()` queries following posts without any `visibility` filter. The normal following feed (line 232) has `AND: [{ OR: [{ visibility: 'PUBLIC' }, { visibility: 'FOLLOWERS' }, { userId }] }]`, but blended feed omits this. Users with < 10 follows see CIRCLE-visibility posts they shouldn't. | Privacy breach — circle-restricted content appears in public-like feed. | 10 min |
| 5 | H | posts.service.ts:414-447 | **Favorites feed does not filter banned/deactivated/deleted users.** `getFavoritesFeed()` at line 427 filters `visibility` but lacks `user: { isBanned: false, isDeactivated: false, isDeleted: false }`. Other feeds include this filter; favorites does not. | Banned users' content appears in favorites feed. | 5 min |
| 6 | H | posts.service.ts:804-852 | **Post edit bypasses content moderation.** `update()` runs `sanitizeText()` but does NOT call `contentSafety.moderateText()` or queue a moderation job. A user can create an innocent post, then edit it within the 15-minute window to contain hate speech, harassment, or CSAM that bypasses all moderation. | Content policy bypass via edit-after-publish. | 15 min |
| 7 | H | posts.service.ts:502-505 | **`$executeRawUnsafe` used instead of `$executeRaw` tagged template.** The SQL string at line 502-503 is static and the parameter is passed correctly, so this is not currently exploitable. However, `$executeRawUnsafe` is the wrong API — it exists specifically for cases where tagged templates won't work. Using it unnecessarily widens the attack surface if someone later refactors with string concatenation. Per project rules: "$executeRaw tagged template literals are SAFE — do NOT replace them" implies the preference is for tagged templates. | Risk of SQL injection if future maintainer adds string interpolation. | 10 min — convert to `$executeRaw` with Prisma.sql |
| 8 | M | bookmarks.service.ts:202-253 | **`getSavedPosts` returns removed/banned content.** The query has no `post: { isRemoved: false }` or `user: { isBanned: false, ... }` filter. A user's bookmarks list will show posts that have been soft-deleted or whose authors were banned. Compare with `posts.service.ts:1047-1051` which correctly filters these. | Users see ghost posts (removed content) in their saved list. | 10 min |
| 9 | M | bookmarks.service.ts:256-301 | **`getSavedThreads` returns removed/banned content.** Same issue as #8 but for threads. No `thread: { isRemoved: false }` or user status filter. | Users see removed threads in saved list. | 10 min |
| 10 | M | bookmarks.service.ts:304-360 | **`getSavedVideos` returns removed/banned content.** Same issue as #8 but for videos. No `video: { isRemoved: false }` or user status filter. | Users see removed videos in saved list. | 10 min |
| 11 | M | posts.controller.ts:450 | **`saveToCollection` uses `@Body('collection')` without DTO validation.** No `IsString()`, no `MaxLength()`, no class-validator decorators. An attacker can send a multi-megabyte string as the collection name, stored directly in the database. This violates the project rule: "Every @Body() uses a DTO class with class-validator decorators, not inline types." | Unbounded string stored in DB; potential DoS via oversized collection names. | 15 min — create DTO |
| 12 | M | posts.controller.ts:472 | **`pinPost` uses `@Body('isPinned')` without DTO validation.** The raw `isPinned` value is coerced with `!!isPinned` but is not validated by class-validator. NestJS does not auto-transform body properties when extracted with `@Body('key')`. | Inconsistent pattern; `isPinned` could be any truthy value. | 10 min — create DTO |
| 13 | M | posts.service.ts:1198-1224 | **`getComments` does not filter comments from banned/deactivated/deleted users.** The query filters `isRemoved` and `isHidden` but not the comment author's status. Banned users' comments remain visible. | Banned user content visible in comment threads. | 10 min |
| 14 | M | posts.service.ts:1226-1251 | **`getCommentReplies` does not filter replies from banned/deactivated/deleted users.** Same issue as #13 for nested replies. Additionally, `isHidden` is not filtered on replies (line 1228), unlike top-level comments (line 1200). | Banned user content visible in reply threads; hidden replies still show. | 10 min |
| 15 | M | collabs.service.ts:9-31 | **Collab invite ignores block relationships.** `invite()` does not check if the post owner has blocked the target user or vice versa. A user can use collab invites to bypass blocks, reaching users who blocked them via notification. | Block bypass — blocked user receives collab invite notification. | 15 min |
| 16 | M | collabs.service.ts:9-31 | **Collab invite does not verify target user exists or is active.** `invite()` passes `targetUserId` directly to `postCollab.create` without checking `user.findUnique`. If the FK constraint is present, it throws an unhandled P2003 error. If not, it creates a dangling record. | Unhandled error or orphaned DB records. | 10 min |
| 17 | M | posts.controller.ts:312-322 | **`dismiss` endpoint missing `@Throttle()`.** All other mutation endpoints have rate limiting. `dismiss` does not, allowing rapid-fire dismissal requests that could exhaust DB resources via upsert operations. | Potential DoS via rapid dismiss requests. | 2 min |
| 18 | M | bookmarks.controller.ts:54 | **`limit` query param not validated or transformed.** `@Query('limit') limit?: number` receives a string from the HTTP query string. Without `ParseIntPipe` or `@Transform`, NestJS passes it as a string. Prisma's `take` may accept a string coerced by JS, but the service defaults to 20 only when `limit` is undefined, not when it's `NaN` or a malicious value like `"99999"`. | Could return unbounded result sets or cause unexpected behavior. | 10 min — add `ParseIntPipe` + max validation |
| 19 | L | posts.service.ts:129-131 | **`recordView` in `getById` fires without auth.** The controller at line 130 calls `this.postsService.recordView(id)` fire-and-forget, but `getById` uses `OptionalClerkAuthGuard`, so unauthenticated visitors inflate view counts. While this matches Instagram behavior (per the comment), it makes view counts easily gameable by bots with no auth needed. | View count inflation by unauthenticated bots. | 30 min (debatable — design choice) |
| 20 | L | collabs.service.ts:81-88 | **`getPostCollabs` exposes all collab statuses including DECLINED.** The query returns all collabs for a post with no status filter and no auth check beyond ClerkAuthGuard. Any authenticated user can see who was invited and who declined, which is potentially sensitive. | Privacy leak — declined invites visible to any authenticated user. | 10 min — filter to ACCEPTED/PENDING, or require post ownership |
| 21 | L | posts.service.ts:1253-1280 | **`addComment` does not run content moderation.** `create()` checks `contentSafety.moderateText()` and queues AI moderation, but `addComment()` only sanitizes text. Comments can contain abusive content that bypasses moderation. | Abusive comments bypass automated moderation pipeline. | 15 min |
| 22 | L | collabs.service.ts:90-104 | **`getAcceptedCollabs` pagination uses `id: { lt: cursor }` instead of Prisma cursor.** The query at line 92 uses a manual `{ id: { lt: cursor } }` filter instead of Prisma's `cursor: { id: cursor }, skip: 1` pattern used everywhere else in the codebase. If IDs are UUIDs (not CUID/ULID), lexicographic comparison in `lt` may not produce chronological ordering, leading to skipped or duplicated items. | Pagination may produce incorrect results depending on ID format. | 10 min |
| 23 | L | posts.service.ts:1601-1605 | **`getShareLink` does not check visibility or ownership.** Any authenticated user can generate a share link for any post (including CIRCLE/FOLLOWERS visibility). The link itself just returns a URL string, but it normalizes the assumption that the post is shareable. | Minor information leak — confirms post existence for any ID. | 5 min |
| 24 | I | posts.controller.ts:26-54 | **Five inline DTO classes defined in controller file.** `ReactDto`, `EditCommentDto`, `RespondToTagDto`, `ShareDto`, `UpdatePostDto` are all defined inline in the controller rather than in the `dto/` directory. This is inconsistent with the rest of the codebase where DTOs live in separate files. | Maintainability — harder to find and reuse DTO definitions. | 15 min — extract to dto/ files |
| 25 | I | posts.service.ts:546 | **Dynamic `import('crypto')` inside transaction.** `(await import('crypto')).createHash('sha256')` is called inside a DB transaction at line 546. While Node caches modules after first import, the async import adds unnecessary latency inside a transaction boundary. Should be a top-level import. | Minor perf — async import in transaction path. | 2 min |

**Severity counts:** C:1 H:5 M:8 L:5 I:2
**Total findings:** 21 (after merging related items; 25 distinct entries)
