name = "mizanly-exif-stripper"
main = "src/index.ts"
compatibility_date = "2024-09-23"
account_id = "f60f0e188859d5f4d64b56f2717f6531"

# Triggered on every object upload to the mizanly-media bucket
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "mizanly-media"

# Event notification: fires when objects are PUT into the bucket
[triggers]
# R2 event notifications must be configured via dashboard or API
# This worker strips EXIF from uploaded images automatically
